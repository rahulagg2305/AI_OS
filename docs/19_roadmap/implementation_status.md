# Implementation Status – AI_OS

**Project:** AI_OS (AI Operating System)
**Document:** Implementation Status
**Version:** 2.0 (restructured short form)
**Status:** Active
**Last Updated:** 2026-07-29 (Platform SDK v1.0.0: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, and 7 of 18 complete** — `pipeline.py` relocated out of the pack's shipped wheel into `tests/integration/_delivery_pipeline.py` (proven via a real wheel build with the file and its `sqlalchemy` import both confirmed absent, and both relocated integration tests — including the real Docker-backed one — passing unchanged); `ContextService` boundary models landed, narrowed against the real Kernel shape; the full entry-point contract (`PackContext`/`CapabilityPack`/`PackRegistration`/`HealthReport`) relocated from the Kernel into `ai_os_sdk.contracts.capability_pack`, additively, with the Kernel's own module now a real re-export. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, and 6b of 18 complete** — the `EntrypointLoader` zero-argument injection blocker is resolved without changing `EntrypointLoader` itself: a new SDK Protocol (`PackContextReceiver`) lets a zero-arg-constructible entrypoint receive a real, permission-gated `PackContext` via one standard `bind_pack_context()` call, proven end to end against a real test entrypoint (real construction → real injection → genuine Echo LLM completion, prompt render, and sandboxed command). Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, and 6a of 18 complete** — real Kernel-side adapters (`kernel/src/ai_os_kernel/sdk_adapters/`) now genuinely implement `LLMGateway`, `PromptRegistry`, and `ToolInvoker` over `DispatchingLLMGateway`, the Prompt Engine, and `LocalSubprocessSandbox`, proven against real underlying objects; the timeout-precedence nuance step 6 left open is resolved (`min()` of the two timeouts). Prior entry: **Steps 1, 2, 2a, 3, 4, 5, and 6 of 18 complete** — the `ToolInvoker` Protocol now exists, proven against the real, executed sandbox, which found and fixed a too-strict validator and a circular import. Prior entry: **Steps 1, 2, 2a, 3, and 4 of 18 complete** **Steps 1, 2, 2a, 3, and 4 of 18 complete** — the `LLMGateway` Protocol now exists and the real `DispatchingLLMGateway` provably satisfies it unmodified. Prior entry: **Steps 1, 2, 2a, and 3 of 18 complete** — the `Agent`/`Tool` Protocols now exist and every real agent and tool provably satisfies them unmodified. Prior entry: **Steps 1, 2, and 2a of 18 complete** — packaging scaffold, error taxonomy + shared boundary models, and the five Protocol/reality reconciliation decisions. Step 3 next, and smaller than originally scoped. Prior entry: Step 1 of 15 complete — `platform_sdk/` scaffolded as a real, installable `ai-os-sdk` distribution, packaging only, no Protocols/models/errors yet. See §2 and §6. Prior entries: the SDK build was scoped as its own plan document (`../03_architecture/platform/platform_sdk_v1_scope.md`); before that, a targeted fix step corrected stale test/mypy numbers in §2 below and closed 15 real mypy errors with a `py.typed` marker; before that, the product-owner decision recorded **the direct-Kernel-import exception is now a hard gate — no new agent or Capability Pack may be added until the Platform SDK exists**. See §4 and `history/026_platform_sdk_gate_and_audit_completion.md`.)

---

## 1. Purpose

A living record of what has actually been built, checked against the exit criteria in `implementation_roadmap.md`, so any session (human or AI) can resume work without rescanning the repository or relying on chat history.

This document is deliberately **short** — current stage, what exists, current blockers, and the next recommended step only. Full chronological detail, subsystem by subsystem, lives in `docs/19_roadmap/history/` (see `history/INDEX.md`). For the complete, granular, per-module completion tracker (every feature/module/phase with a percentage and status), see `docs/19_roadmap/feature_inventory.md` — that document, not this one, is the authority on "what % done is X." This document is descriptive, not authoritative — on any conflict, the Implementation Roadmap and the ADRs govern.

**Read this file first, every session, before anything else** (see `CLAUDE.md`). Read a `history/` file only when the current task specifically needs that subsystem's detail.

---

## 2. Current Stage

**Stage A – Platform Skeleton** is process-complete but not exit-criteria-complete (see §4). **Stage B – Minimum Viable Kernel** is underway and has reached a real, demonstrable, **now genuinely Docker-verified** milestone: a complete four-agent software-engineering delivery pipeline (Architecture → Build → Test → Documentation) runs end to end through the real Workflow Engine and **`DockerSandbox` as the genuine default Tier 1 sandbox backend**, proven for real against a live daemon — including a real proof that network isolation and filesystem containment hold for code the pipeline itself generated and ran, not just in an isolated guarantee check. A fifth agent, Requirements Analyst, is built and proven independently (not yet chained into the pipeline).

**Numbers current as of 2026-07-28 (re-verified fresh, not carried forward from an earlier step — see the lesson recorded in `docs/process/standing_rules.md` about exactly this failure mode):** `pytest -q` (full suite, unit + integration + pack, Docker available) → **803 passed, 11 skipped** (all 11 are the same opt-in-live-provider suite, no API key set — expected per ADR-0015), **0 failed**. `mypy --strict kernel/src kernel/alembic tests` (the exact scope CI runs) → **0 errors, 267 source files checked**, clean. `ruff check .` → clean. This is the first genuinely real-Docker-verified pass of the entire integration suite since `DockerSandbox` became the default.

A prior version of this paragraph claimed "849 tests passed" and "mypy --strict (283 source files) clean" — both were stale by the time a hard-evidence audit re-ran them fresh and found 803/803 passing but **15 real mypy errors** (root cause: `capability_packs/software-engineering` shipped with no `py.typed` marker, so mypy treated cross-boundary imports from `tests/` into the pack as untyped). Fixed this step by adding the marker; see `history/026_platform_sdk_gate_and_audit_completion.md`'s successor step for the full before/after. The "283 files" figure was real once, under a *different* mypy invocation that additionally passes `capability_packs/software-engineering/src` and its `tests/` directory on the command line (matching `[tool.mypy].files` in `pyproject.toml` exactly) — CI itself runs the narrower `kernel/src kernel/alembic tests` invocation quoted above, which is what these numbers now reflect.

Three prior steps (2026-07-28) were documentation/infrastructure-only. The step recorded above this paragraph closed the documentation audit and recorded a hard product-owner gate (see §4, first item) — no code changed. This paragraph itself was corrected by a small, targeted follow-up fix step (also 2026-07-28) that touched only: the `py.typed` marker, this paragraph's numbers, a new standing-rules lesson, and two documentation-formatting fixes.

**Platform SDK build: the sequence was revised from 15 to 18 steps after an independent architecture review (2026-07-29), and Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, and 7 are complete.** The review found 7 real problems in the original plan, three of them blocking — most importantly that no step made the Kernel able to *supply* the Protocols the pack would be migrated onto, and that the injection path (`EntrypointLoader` hardcodes `cls()`; `PackContext` is identity-only) was listed as a non-goal. All findings accepted; three new steps (**2a** Protocol/reality reconciliation, **6a** Kernel-side adapters, **6b** `PackContext` construction + injection) were inserted, `SecretResolver` was dropped, migration order reversed to `qa-test`-first, and `pipeline.py` relocation became step 7. Full findings and reasoning: `../03_architecture/platform/platform_sdk_v1_scope.md` §6a.

- **Step 1 (done):** `platform_sdk/` is a real, installable PEP 621 distribution (`ai-os-sdk`), a workspace member; `import ai_os_sdk` works.
- **Step 2 (done):** `ai_os_sdk.errors` holds the real `ErrorCategory` (all 6 documented categories), `StructuredError`, and the `AiOsError` hierarchy with its six subclasses, each mapping 1:1 onto a `StructuredError`. `ai_os_sdk.models` holds `ArtifactRef`, the canonical §4.1 `TraceContext`, `SecurityContext`, and `StepBudget`. **Consumed by nothing; no existing code changed.** Note: this creates the *third* `TraceContext` in the repository — `ai_os_kernel.observability.trace` and `ai_os_kernel.llm_gateway.models` each hold a narrower one, and both of their docstrings already name §4.1 as the canonical shape they reduce. Consolidating them is Kernel-side work — step 2a confirmed it belongs to the Kernel-adapter step (6a), not to the SDK.
- **Step 2a (done, docs only):** all five Protocol/reality reconciliation decisions made and recorded as dated blocks in `platform_sdk.md` §4.2/§4.3/§5.1/§5.2/§5.6. Net: **four narrowings** (`Agent` and `Tool` to their dict-based Kernel shapes; `LLMGateway` from 5 methods to the 2 that exist; `PromptRegistry`'s `version` to required), **two targeted extensions** where the real Kernel is richer than the spec (`ProviderCapabilities` 10 → 13 fields; `ToolResult` gains `timed_out`/`truncated`, two real outcomes the spec could not express), **one deliberate reversal in the spec's favour** (`PromptRegistry` keeps the documented keyword call style rather than the Kernel's request-object envelope — a pack-facing Protocol is shaped for its caller), and **one from-scratch design** (`ToolInvoker`, grounded in a `platform.sandbox.run_command` platform-provided tool id, which also fixes `SandboxedCommandTool.execute(inputs)` ignoring its own `inputs`). Summary table with evidence: `platform_sdk_v1_scope.md` §6b.
- **Step 3 (done):** `ai_os_sdk.contracts` holds the `Agent` and `Tool` Protocols at their narrowed shapes, plus the SDK's own `TrustTier` (defined rather than imported — the SDK is the dependency floor). **Structural compatibility is proven against real code, not mocks:** all five pack agents and `EchoAgent` satisfy `Agent`; `EchoTool` and the real `SandboxedCommandTool` satisfy `Tool` — 13 assertions in `tests/unit/platform_sdk/test_kernel_satisfies_sdk_contracts.py`, with **zero modification to any Kernel or pack source**. `AgentRequest`/`AgentResult`/`ToolRequest` remain deferred past v1.0.0 (no consumer under the narrowed shapes); `ToolResult` lands in step 6 with `ToolInvoker`.
- **Step 4 (done):** `ai_os_sdk.contracts.LLMGateway` holds the Protocol narrowed to `complete()`/`capabilities()` — the two methods the real `DispatchingLLMGateway` implements. `ai_os_sdk.models` gained `Message`/`MessageRole`/`LLMRequest`/`StopReason`/`UsageRecord`/`LLMResponse` (field-for-field mirrors of the real Kernel models) and `ProviderCapabilities` (extended to the real 13 fields — the real `StaticCapabilityNegotiator` already implements all 13, and its own docstring names `platform_sdk.md` as the discrepancy it "implements past"). **Structural compatibility proven against the real `DispatchingLLMGateway`**, constructed with an empty router and no providers (zero I/O) — 15 new assertions total. `EchoLLMGateway` correctly does *not* satisfy the SDK Protocol, since it implements only `complete()`, which is evidence the narrowing is exact rather than accidentally loose.
- **Step 5 (done):** `ai_os_sdk.contracts.PromptRegistry` holds the Protocol at the documented keyword call style (`render(prompt_id, variables, *, version)`) — the one deliberate reversal from step 2a, kept over the Kernel's own request-object envelope because a pack-facing Protocol should be shaped for its caller (ADR-0004). `version` is keyword-only and required. `RenderedPrompt` narrowed to `prompt_id`/`version`/`content`. **No real Kernel class satisfies this Protocol** — it is a from-scratch call convention, not a narrowing — so instead of an `isinstance` proof, the conversion to/from the real `PromptRenderRequest`/`PromptRenderResponse` is proven **lossless in both directions**, 7 assertions in `tests/unit/platform_sdk/test_prompt_registry_adapter_conversion.py`; the reference conversion functions are test-local and illustrative, not shipped SDK code.
- **Step 6 (done):** `ai_os_sdk.contracts.ToolInvoker` holds the Protocol at the from-scratch shape decided in 2a — `invoke(tool_id, inputs, *, timeout_seconds) -> ToolResult` + `available_tools() -> tuple[ToolDescriptor, ...]` — grounded in a new platform-provided tool, `PLATFORM_SANDBOX_RUN_COMMAND_DESCRIPTOR` (id `platform.sandbox.run_command`), so `available_tools()` has a genuine non-empty answer. `ToolResult` (moved here from step 3) narrows `stdout_ref`/`stderr_ref` to inline strings and drops `artifacts` (no `StorageService`), and **extends** the documented shape with `timed_out`/`truncated`. **Proven against the real, actually-executed `LocalSubprocessSandbox`** — a genuine clean run, timeout, and output-cap breach — not hand-built `SandboxResult` values, because the whole point of the extension is a distinction real execution produces. **This real execution found and fixed two genuine bugs**: `ToolResult`'s own validator wrongly required `exit_code` whenever `timed_out` was `False` — disproven when a real cap breach left `exit_code=None` with `timed_out=False` too (the sandbox kills the process on cap breach before it can exit); and a real circular import between `ai_os_sdk.errors` and `ai_os_sdk.models`, fixed by relocating `ErrorCategory`/`StructuredError` into a new `ai_os_sdk.models.error` module (both remain fully public via `ai_os_sdk.errors`). Full record: `platform_sdk_v1_scope.md` §6f.
- **Step 6a (done):** three real adapter classes in the new `kernel/src/ai_os_kernel/sdk_adapters/` package (deliberately outside `platform_sdk/`, since the SDK cannot depend on the Kernel) — `LLMGatewayAdapter`, `PromptRegistryAdapter`, `ToolInvokerAdapter` — genuinely implement the SDK's `LLMGateway`/`PromptRegistry`/`ToolInvoker` Protocols by wrapping real `DispatchingLLMGateway`, `PromptEngine`, and `SandboxExecutor` instances. `kernel/pyproject.toml` gained its first dependency change this whole initiative (`ai-os-sdk`), since the Kernel now implements SDK Protocols. Every field mapping was checked against real source, including one genuine, documented narrowing (SDK's 7-field `TraceContext` → Kernel's 2-field one) and one newly-surfaced gap (`ToolInvoker.invoke` carries no trace parameter at all, resolved with a fresh, uncorrelated, per-invocation `TraceContext`). **Resolves the timeout-precedence nuance step 6 left open**: the more restrictive of `invoke()`'s own `timeout_seconds` and the tool's own `inputs["timeout_seconds"]` always wins (`min()`), proven with 4 real-execution tests measuring genuine elapsed wall-clock time around real `time.sleep(5)` subprocess runs. **Proven against real underlying objects, not mocks** (23 new tests: 8 against a real Echo-backed `DispatchingLLMGateway`, 5 against a real `InMemoryPromptEngine`, 10 against a real `LocalSubprocessSandbox`). Not wired into `bootstrap.py`/`EntrypointLoader`/any pack yet — that is step 6b. Full record: `platform_sdk_v1_scope.md` §6g.
- **Step 6b (done):** the `EntrypointLoader` zero-argument injection blocker is resolved **without changing `EntrypointLoader` itself**. A new SDK Protocol, `ai_os_sdk.contracts.PackContextReceiver` (`bind_pack_context(context: Any) -> None`), lets any zero-arg-constructible entrypoint receive a real context once, before first use — the generalized form of the lazy-build workaround all five real pack agents already use, instead of a sixth hand-rolled copy. `ai_os_kernel.capability_manager.pack_contract.PackContext` is extended additively (exactly as its own docstring already anticipated) with `llm`/`prompts`/`tools`, backed by the step 6a adapters; a new `ai_os_kernel.sdk_adapters.pack_context.build_pack_context` constructs one from a set of declared permissions, granting `llm`+`prompts` only on `llm:invoke` and `tools` only on `sandbox:execute` — enforced per-entrypoint (never the pack's aggregate permissions), proven against the real manifest's own asymmetric declarations (`qa-test`: tools only; `architecture`: llm+prompts only; `build`: all three). Proven end to end against a real test entrypoint, not a real migrated agent (deliberately out of this step's scope): real `EntrypointLoader` construction, a real `isinstance(loaded, PackContextReceiver)` check, real `bind_pack_context()`, then a genuine Echo LLM completion, prompt render, and sandboxed command through the injected context. **No blocker found** — `EntrypointLoader`'s zero-argument constraint is resolved cleanly by a caller-side check, no new capability demanded of it. `PackContext` remains Kernel-side for now — formally relocating the whole entry-point contract into `ai_os_sdk` stays step 7's own job, deliberately not pulled forward. Full record: `platform_sdk_v1_scope.md` §6h.
- **Step 7 (done):** `pipeline.py` relocated from `capability_packs/software-engineering/src/ai_os_pack_software_engineering/pipeline.py` to `tests/integration/_delivery_pipeline.py` — test-harness workflow composition, never pack-facing capability, imported only by two Kernel-side tests. Verified by a real wheel build (`python -m uv build --package ai-os-pack-software-engineering`): the resulting `.whl`'s own file list no longer contains `pipeline.py`, and a full `src/` grep confirms zero remaining `sqlalchemy` imports in pack source. Both relocated tests re-run and pass unchanged from the new location, including the real, live Docker-backed one (this session's daemon was reachable). `ContextService` boundary models (`ai_os_sdk.models.context`) and Protocol (`ai_os_sdk.contracts.context_service`) landed, narrowed against the real, already-reduced Kernel `context_manager.models` shape — a genuine, dated reconciliation decision recorded even though none existed for this interface before this step — with structural compatibility proven against the real `DefaultContextManager` via `isinstance`. The full entry-point contract (`PackContext`/`CapabilityPack`/`PackRegistration`/`HealthReport`) relocated from `ai_os_kernel.capability_manager.pack_contract` into `ai_os_sdk.contracts.capability_pack`, additively; the Kernel's own module is now a real re-export (proven by identity, not just import success), so real pack source needed zero changes. **A real, discovered layering correction, recorded rather than silently applied:** `PackContext`/`PackRegistration` reference other Protocols by nature and so land in `contracts/`, not `models/` as an earlier docstring speculatively planned — corrected in the same step that found the conflict. **No blocker found.** Full record: `platform_sdk_v1_scope.md` §6i.

Verified fresh after step 2: `pytest -q` (root suite) → **803 passed, 11 skipped, 0 failed** (unchanged); `pytest -q capability_packs/software-engineering/tests/` → **46 passed** (unchanged); `pytest -q platform_sdk/tests/` → **65 passed**; `mypy --strict` over the full configured scope → **0 errors, 295 source files**; `ruff check .` and `ruff format --check .` → clean.

Verified fresh after step 6a: `pytest -q` (root suite) → **852 passed, 11 skipped, 0 failed** (829 → 852, the 23 new `sdk_adapters` tests, unchanged skips); `pytest -q capability_packs/software-engineering/tests/` → **46 passed** (unchanged); `mypy --strict` over the full configured scope → **0 errors, 323 source files**; `ruff check .` and `ruff format --check .` → clean; `git status` confirms only `kernel/pyproject.toml` and `uv.lock` modified among pre-existing tracked files.

Verified fresh after step 6b: `pytest -q` (root suite) → **864 passed, 11 skipped, 0 failed** (852 → 864, the 12 new `pack_context`/`pack_context_injection` tests, unchanged skips); `pytest -q capability_packs/software-engineering/tests/` → **46 passed** (unchanged); `mypy --strict` over the full configured scope → **0 errors, 328 source files**; `ruff check .` and `ruff format --check .` → clean; `git status` confirms only `pack_contract.py`, `sdk_adapters/__init__.py`, and `contracts/__init__.py` modified among pre-existing tracked files.

Verified fresh after step 7: `pytest -q` (root suite) → **869 passed, 11 skipped, 0 failed** (864 → 869, the 5 new `test_context_service_and_capability_pack.py` tests, unchanged skips — the relocated pipeline tests moved, not multiplied); `pytest -q capability_packs/software-engineering/tests/` → **46 passed** (unchanged); a real `python -m uv build --package ai-os-pack-software-engineering` followed by wheel inspection confirms `pipeline.py` absent from the shipped `.whl`; a full `src/` grep confirms zero remaining `sqlalchemy` imports in pack source; `mypy --strict` over the full configured scope → **0 errors, 332 source files**; `ruff check .` and `ruff format --check .` → clean; `git status` shows a clean rename (`pipeline.py` → `tests/integration/_delivery_pipeline.py`) plus the expected modified/new files, no stray build artifacts.

> **🛑 HARD GATE (product-owner decision, 2026-07-28) still in force: no new agent or Capability Pack until the Platform SDK exists *and* the Software Engineering pack is migrated onto it and verified compliant (`platform_sdk_v1_scope.md` step 15).** Step 1 (scaffolding) does not lift this gate on its own — see §4 for the full statement and §6 for what's next.

---

## 3. What Exists Now (compact)

- **Persistence & Workflow Engine core**: Postgres schema + Alembic migrations for `workflow_instances`/`workflow_events`/`workflow_leases`/`workflow_steps`/`approvals`; definition loading/validation; instance creation, state transitions, multi-step progression to completion; lease acquire/renew/release/reap; single-shot and run-to-completion composition; `list_steps`/`list_events` read accessors. → `history/002`, `003`.
- **Cross-cutting infra**: CI aligned with the real codebase; minimal Secrets Resolution (`EnvSecretProvider`, ADR-0024); real OpenTelemetry spans + one metric (console exporters). → `history/004`.
- **Catalog & evaluation schema**: all 6 `catalog` tables and all 6 `evaluation` tables exist, with FKs retrofitted per `data_model.md`. → `history/005`.
- **LLM Gateway**: `AnthropicAdapter` + `LocalAdapter`, `Router`/`DispatchingLLMGateway`, Retry & Fallback (Circuit Breaker, Backoff, Error Taxonomy), two independent budget ceilings (alias + workflow), `TraceContext`, Capability Negotiator matrix. Only `anthropic` registered by default. → `history/006`, `011`.
- **Prompt Engine**: Protocol + `InMemoryPromptEngine`/`SqlPromptCatalog`, `PromptedCompletionService`. No version resolution/Resolver/caching yet. → `history/006`.
- **Agent/Tool registries**: `AgentRegistry`/`ToolRegistry`, both in-memory and SQL-backed (`SqlAgentRegistry`/`SqlToolRegistry` resolve real catalog rows, gated on pack activation, via `EntrypointLoader`). → `history/007`.
- **Capability Manager**: minimal pack lifecycle (register/activate/deactivate), no discovery/upgrades/health/permissions yet. → `history/008`, `010`.
- **Security Manager**: bearer-token JWT auth, 4 permissions, fronting 9 HTTP routes — all narrower than their documented contracts. Not OIDC. → `history/009`.
- **Context Manager**: `WorkflowStateResolver` + `WorkflowStepOutputResolver`, Size & Token Budget Enforcer. 1 of 6 documented sources built. → `history/012`.
- **Retrieval**: `knowledge.documents`/`chunks` writer + keyword-search reader. No vector/hybrid search, no consumer yet. → `history/013`.
- **Sandbox & Tool execution**: `LocalSubprocessSandbox` (3/5 guarantees) and `DockerSandbox` (5/5, ADR-0016 Tier 1, proven live) both implement `SandboxExecutor`, each now declaring its own `python_command`; `SandboxedCommandTool` + `ToolStepExecutor` dispatch; `ai_os_kernel.sandbox.default_executor` resolves the real default from `AIOS_SANDBOX_BACKEND` (`"docker"` unless set to `"local"`). → `history/014`, `020`, `023`.
- **Software Engineering Capability Pack**: 5 real agents — Architecture, Build, Test, and Documentation chained into one declared workflow (`se.delivery_pipeline`) via `WorkflowStepOutputResolver`, defaulting to `DockerSandbox`; Requirements Analyst proven independently, not yet chained in. → `history/015`–`019`, `023`, `024`.
- **Documentation reconciliation**: agent-catalog naming fixed (`architecture`/`qa-test`/`build`), doc-drift closed, shared Postgres test fixture (`tests/integration/_postgres_fixture.py`) skips cleanly without Docker. → `history/021`.
- **Feature/module/phase inventory**: tracked, weighted completion table — see `docs/19_roadmap/feature_inventory.md`. Update that document's own table at the end of every future step.
- **Prior step**: `capability_packs/analytics/` (stale, undocumented) deleted; `DockerSandbox` wired in as the SE pack's real default sandbox. → `history/023`.
- **Prior step**: the full integration suite (incl. the Docker-gated pipeline test) ran for real and passed; two real bugs found and fixed (a `python_command` mismatch, a hardcoded `sys.executable` in a test); Requirements Analyst Agent built. → `history/024`.
- **This step**: documentation consolidation audit — the docs are now self-sufficient for a zero-context LLM. 82 documents carry honest Implementation Status sections; stage status corrected everywhere; all 25 ADRs annotated; every TBD resolved; the dead `capability_packs/software_engineering/` duplicate removed. → `history/025`.

**Reading the architecture docs correctly (important, new this step):** AI_OS is documentation-first, so most `docs/03_architecture/` documents were written before their code and many describe subsystems that are 0% built. **Every one now carries an `## Implementation Status` section near the top — read it before assuming the document describes callable code.** `docs/DOCUMENTATION_INDEX.md` §2a enumerates the entirely-unbuilt set. `docs/process/folder_structure.md` is definitive on which directories actually exist (git tracks no empty directories, so most "planned" folders are absent from a fresh clone).

---

## 4. Current Blockers

Full detail (every gap, every reasoning) is preserved verbatim in `history/022_gap_analysis_and_blockers_snapshot.md`. The load-bearing ones, distilled:

- **🛑 HARD GATE, recorded 2026-07-28 (product-owner decision): the direct-Kernel-import exception documented in `docs/03_architecture/capability_framework/capability_pack_contract.md`'s Platform Interaction Rules is now a hard blocker on further pack growth, not a soft "temporary" note.** No new agent may be added to any Capability Pack, and no new Capability Pack may be added, **until a real Platform SDK package (`ai-os-sdk`) exists.** The five existing Software Engineering pack agents (`requirements-analyst`, `architecture`, `build`, `qa-test`, `documentation`) are grandfathered as-is and need no change — this gate blocks only the *next* addition. Recorded in three places: `capability_pack_contract.md` (the gate itself), `feature_inventory.md` module 27 (tracked as the blocking item), and `docs/process/standing_rules.md` (the standing rule preventing a future step from accidentally violating it). **The Platform SDK build is not started by this step** — it is explicitly scoped as its own dedicated future step (see §6).
- **Requirements Analyst is proven but not chained into `se.delivery_pipeline`** — the pipeline still starts at Architecture; wiring Requirements Analyst in as its own first step is a distinct, later increment (would need `delivery_pipeline.yaml`/`pipeline.py`'s own `_STEP_SOURCES` updated, plus re-verification of the existing chained tests).
- **Only one LLM provider (`anthropic`) is registered by default**; `LocalAdapter`/cross-provider fallback exist but are commented out in checked-in config.
- **Security Manager is pre-shared-secret JWT, not real OIDC** — not production-credible as-is.
- **No pack discovery, upgrade path, health monitoring, or permissions enforcement** in the Capability Manager.
- **`approvals` table has no writer** — no Human Approval Point execution path exists yet.
- **No multi-instance worker loop** — `run_to_completion`/`reap_once` exist for one instance/one bounded pass; nothing yet schedules either across many instances.
- **Context Manager has only 1 of 6 documented sources** (Knowledge/Memory/AI Context Packs/Runtime Config resolvers don't exist); no Filter/Ranker, no Context Audit Logger persistence.
- **No GitHub remote configured** for this repository (see §5) — `gh` CLI unavailable in this environment; a manual step is needed.
- **Docker Desktop availability is intermittent across sessions** in this development environment — see `CLAUDE.md`.

---

## 5. Git / Repository Status

This is now a git repository (`git init` this step), branch `main`, with one baseline commit capturing all work through the naming-reconciliation step. **No GitHub remote exists** — the `gh` CLI is unavailable in this environment, so pushing to GitHub requires a manual step from the product owner (create a remote repository, then `git remote add origin <url> && git push -u origin main`). The repository is otherwise ready to push as-is.

Per standing rule (`docs/process/standing_rules.md`), every step from now on ends with a commit; the hash is reported at the end of each step's report.

---

## 6. Recommended Next Small Step

**Step 8 of 18 — `pack_contract_suite` check 7 (forbidden-import AST scanner) plus an explicit, documented, expiring waiver for the still-unmigrated Software Engineering pack.** Step 7 removed the one file (`pipeline.py`) that would have made check 7 fail forever on this pack; the pack's 5 real agents still import `ai_os_kernel` directly (the documented, dated exception), so check 7 needs a waiver that expires at step 14, when `pack.py`'s entry point migrates onto the real SDK types and every agent import is gone.

Full 18-step sequence, current position: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7 done.** Then 8 → 9–14 (migration, `qa-test` first) → 15 (compliance completion, and only then does the growth gate lift). See `../03_architecture/platform/platform_sdk_v1_scope.md`; each step is individually approved before it starts.

A separate, smaller, non-blocking follow-up: the prior step completed the full documentation audit (see §7 and `history/026`), so there is no remaining doc-completeness debt to schedule.

---

## 7. Maintenance

Update this document at the end of every implementation step: refresh §2 (current stage) and §4 (blockers), re-state §6 with the next recommended step, and commit. If a step's own detail is large enough to warrant its own history entry, add a new numbered file to `docs/19_roadmap/history/` and a line to `history/INDEX.md` — do not let this document grow long again (see the big-file convention in `CLAUDE.md`).

**Also update `docs/19_roadmap/feature_inventory.md`'s own completion table (Section 5) and overall weighted percentage (Section 6) at the end of every step** — standing rule recorded 2026-07-28 in `CLAUDE.md` / `docs/process/standing_rules.md`.
