# Implementation Status – AI_OS

**Project:** AI_OS (AI Operating System)
**Document:** Implementation Status
**Version:** 2.0 (restructured short form)
**Status:** Active
**Last Updated:** 2026-07-30 (**Fifteenth normal feature step since the Platform SDK initiative closed: general, error-category-driven step retry — widened beyond quality gates to any step-executor exception, reusing the mechanism the prior two steps already proved generic.** `WorkflowAdvanceRunner._maybe_retry_failed_step` no longer matches `QualityGateFailedError` by type at all — it retries any exception whose own `getattr(exc, "retriable", None) is True` (the identical self-declaration convention `ai_os_kernel.llm_gateway.errors.LLMProviderError` already carries for its own provider-retry logic) and whose `step_id` (now attached to *every* caught exception, uniformly, by `WorkflowInstanceService.advance` via `setattr(exc, "step_id", ...)  # noqa: B010` — mypy `--strict` rejects the direct-assignment form since `exc`'s static type inside `except Exception as exc:` is bare `Exception`) has a configured retry target. **The retriable-vs-not split, decided and justified, not defaulted blindly**: `QualityGateFailedError` self-declares `retriable = True` unconditionally (retrying re-runs the artifact-*producing* step — genuine corrective work, not a blind re-evaluation of an already-failed artifact); `LLMProviderError` carries this project's own real Error Taxonomy classification straight through (a `transient`/`CHAIN_EXHAUSTED`/`CIRCUIT_OPEN` failure is retriable, a `permanent`/`budget` one is not — the identical reasoning a Kubernetes Job retry applies on top of a container's own internal HTTP retries, not "retrying the same thing twice"); every other Workflow Engine exception (`AgentOutputValidationError`, `ToolOutputValidationError`, `ToolSandboxRequiredError`, `AgentNotRegisteredError`, `ToolNotRegisteredError`, `EntrypointLoadError`, `PackNotActivatedError`, `PromptedAgentInputError`) declares no `retriable` attribute at all, so `getattr(...)` defaults it to non-retriable — the exact prior "fail immediately" behavior, unchanged; `AgentRegistryError`/`ToolRegistryError` are deliberately left undecided (each conflates a genuinely transient and a genuinely permanent nature in one type with no structural way to tell them apart today — defaulting to non-retriable is the safe choice, splitting the type in two is named future work, not built here). **Zero new retry-budget concept**: `RetryPolicy.max_attempts`/`max_duration_seconds`, the per-step attempt/deadline tracking, and `reset_current_step`/`record_failed_attempt` are all reused unchanged — only the exception-matching and naming generalized (`gate_retry_targets`→`step_retry_targets`, `retry_after_gate_failure`→`retry_after_step_failure`, `_maybe_retry_gate_failure`→`_maybe_retry_failed_step`). `se.delivery_pipeline`'s own real `_STEP_RETRY_TARGETS` gained a genuine third entry, `{"build": "build"}` — `build` now retries from itself on a real, transient LLM-provider failure, on top of the two gates already retrying from it. Real proof, both directions, end to end against the real production pipeline, not only at the unit level: a new integration test where `build`'s own LLM call raises a real, retriable `LLMProviderError` once then succeeds — the pipeline retries `build` from itself and completes, with the genuinely failed first attempt persisted as its own real `workflow_steps` row; a new integration test where the identical call raises an `LLMProviderError(..., retriable=False)` — despite the real, configured `_STEP_RETRY_TARGETS["build"]` entry, it fails immediately, exactly once, proving the retriability check (not merely a configured target) genuinely gates the decision. Two matching unit-level tests added in `test_advance_runner.py` (a non-gate step retrying via `LLMProviderError`; a non-retriable `AgentOutputValidationError` failing immediately despite a configured target), plus the four pre-existing gate-retry unit tests' own fixture corrected to raise from the step *executor* (mirroring production) rather than from the repository's persistence write, which the new generic `step_id` attachment made a load-bearing distinction for the first time. Real evidence: `pytest -q` (root suite) → **1004 passed, 12 skipped, 0 failed** (1000 → 1004, exactly the 4 new tests — 2 in `test_advance_runner.py`, 2 in `test_delivery_pipeline.py`); `mypy --strict`/`ruff check`/`ruff format --check` clean repo-wide. Full detail: `advance_runner.py`'s own module docstring (the full category split), `workflow_engine.md`, `error_handling_retry.md` (both updated), `feature_inventory.md` modules 5/15. Prior entry, 2026-07-30: **Fourteenth normal feature step since the Platform SDK initiative closed: a genuinely failed step attempt is now genuinely recorded, closing the last named observability gap in this thread.** Confirmed the gap precisely before fixing: `WorkflowInstanceService.advance` called the step executor with no `try`/`except` at all — a raised exception propagated straight past `advance_workflow` (the only method that ever wrote a `workflow_steps` row), so a failed attempt left **zero** trace; only an eventual successful retry, or nothing at all if the bound was exhausted, was ever persisted. Fixed: the executor call is now wrapped in `try`/`except`; on any exception, a new `WorkflowInstanceRepository.record_failed_attempt` writes a real `workflow_steps` row (`status="failed"`, real `error` detail, an `attempt` number computed by the identical `MAX(attempt)+1` query the successful path already uses, so the two writers share one source of truth) and a real `step.failed` event, guarded by the same CAS pattern `advance_workflow`/`reset_current_step` already use — then the *original* exception is re-raised completely unchanged (bare `raise`), so `WorkflowAdvanceRunner`'s own retry/failure logic is provably unaffected; only the new persisted row is added. Applies to every step-executor exception, not only `QualityGateFailedError` — closes quality_gate_engine.md §9's own "every gate execution must record ... error details" requirement in full. Proof: the existing gate-retry integration test now shows two real, distinct `workflow_steps` rows for the same gate (attempt 1 `status="failed"` with real error detail, attempt 2 `status="completed"` with `passed=True`) instead of just the one it used to see; the exhausted-bound tests (both the Testing and Static Analysis gates) now show **every** failed attempt persisted, not none. Real evidence: `pytest -q` (root suite) → **1000 passed, 12 skipped, 0 failed** (998 → 1000, exactly the 2 new unit tests in `test_service.py`); `mypy --strict`/`ruff check`/`ruff format --check` clean repo-wide. Full detail: `workflow_engine.md`, `quality_gate_engine.md` (both updated), `feature_inventory.md` modules 5/15. Prior entry, 2026-07-30: **Thirteenth normal feature step since the Platform SDK initiative closed: a second, distinct quality gate category proves the mechanism genuinely generalizes.** A new Lint Agent (`ai_os_pack_software_engineering.agents.lint:LintAgentEntrypoint`, this pack's sixth agent — the first added since the Capability Pack growth gate lifted 2026-07-29) runs a real static-analysis check via the identical `platform.sandbox.run_command` tool `qa-test` already uses, reporting `passed`/`exitCode`/`output`. Wired into `se.delivery_pipeline` between `build` and `test` as a new `lint` agent step plus a second `quality-gate-lint-clean` gate step, reusing `QualityGateStepExecutor`/`DispatchingStepExecutor`/`WorkflowAdvanceRunner` **completely unchanged** — only two new dict entries (`_GATE_SOURCES`, `_GATE_RETRY_TARGETS`) plus the new agent. **The mechanism genuinely generalized via configuration alone; the one real thing that had to change was the choice of tool**: `ruff` was tried first, works against the deterministic tier, but genuinely fails against `DockerSandbox`'s own default image (`python:3.12-slim`, no `ruff` installed, no dependency-install step exists anywhere in this codebase) — discovered when the existing Docker-gated pipeline test broke. Switched to `python -m py_compile` (stdlib, works identically on every real backend), accepting a real, recorded trade-off: syntax errors only, not style/unused-import findings. Proof: a new integration test where Build's generated file is genuinely lint-clean — both gates pass, pipeline completes; a new test where it has a genuine, persistent syntax error — the lint gate halts, retries once (per the existing bounded mechanism), genuinely exhausts, and halts for good, with `test`/`quality-gate-tests-pass`/`documentation` never running at all (Lint sits before Test). Real evidence: `pytest -q` (root suite) → **998 passed, 12 skipped, 0 failed** (996 → 998 — 1 new pipeline test + 1 new parametrize case in the existing `test_kernel_satisfies_sdk_contracts.py` agent-coverage test); pack-local Lint Agent tests (new file, `capability_packs/software-engineering/tests/test_lint_agent.py`) → **14 passed**; `mypy --strict`/`ruff check`/`ruff format --check` clean repo-wide. Full detail: `ai_os_kernel.workflow_engine.delivery_pipeline`'s own docstring, `agents/lint.py`'s own docstring (the ruff/py_compile reasoning in full), `docs/06_capability_packs/software_engineering/agents.md`/`overview.md`, `capability_pack_contract.md`, `feature_inventory.md` modules 5/15/29/30. Prior entry, 2026-07-30: **Twelfth normal feature step since the Platform SDK initiative closed: a real, bounded retry for a failed quality gate.** `RetryPolicy` (declared on `WorkflowDefinition`, validated since Stage A) is read at runtime for the first time: `WorkflowAdvanceRunner.run_to_completion` catches `QualityGateFailedError` and, when the gate has a configured retry target (`se.delivery_pipeline` retries `quality-gate-tests-pass` from `build`, not the whole pipeline — Requirements Analyst/Architecture are unrelated to a build/test failure) and the definition declares a `retryPolicy`, calls a new `WorkflowInstanceService.retry_after_gate_failure` to reset `current_step_id` backward — genuinely bounded on both `maxAttempts` (2) and `maxDurationSeconds` (60.0), never unconditional. Required a real, computed `attempt` number in `SqlWorkflowInstanceRepository.advance_workflow` (previously hardcoded to 1) and a new `reset_current_step` repository method (CAS-guarded, appends a real `workflow.step_retry_scheduled` event) — zero behavior change for every step that still only runs once. Proof: a real test where Build's generated script fails once (a real marker-file check, not a coin flip) then succeeds on retry — pipeline completes, Documentation runs; a real test where it fails every attempt — exactly `maxAttempts` real attempts, then genuine halt, never a third. Real evidence: `pytest -q` (root suite) → **996 passed, 12 skipped, 0 failed** (986 → 996, exactly the 10 new tests); `mypy --strict`/`ruff check`/`ruff format --check` clean repo-wide. Full detail: `docs/03_architecture/workflow/error_handling_retry.md`, `workflow_engine.md`, `workflow_architecture.md`, `quality_gate_engine.md` (all updated), `feature_inventory.md` modules 5/15. Prior entry, 2026-07-30: **Eleventh normal feature step since the Platform SDK initiative closed: the Quality Gate Engine's smallest real slice.** `ai_os_kernel.workflow_engine.quality_gate.QualityGateStepExecutor` genuinely blocks `se.delivery_pipeline` progression — a new `quality-gate-tests-pass` step between `test` and `documentation` reads `test`'s own real `passed` field and raises `QualityGateFailedError` unless it is `True`, halting `WorkflowAdvanceRunner.run_to_completion` (Documentation never runs) via the same existing failure boundary `AgentOutputValidationError` already uses — no new orchestration logic, no Gate Registry, formalizing this codebase's own de facto Build→Test gate into a real, declared, blocking step, per this step's own approved framing. `DispatchingStepExecutor` gained an optional `quality_gate_executor` param, defaulted `None` (every other caller unaffected). Proof: `tests/integration/workflow_engine/test_delivery_pipeline.py`'s new test — a genuinely failing build (`sys.exit(1)`) halts the pipeline, Documentation never invoked; the existing pass-through test and the Docker-backed test both still pass unchanged. Real evidence: `pytest -q` (root suite) → **986 passed, 12 skipped, 0 failed** (974 → 986, exactly the 12 new tests); `mypy --strict`/`ruff check`/`ruff format --check` clean repo-wide. Full detail: `docs/03_architecture/kernel/quality_gate_engine.md`, `quality_gates_framework.md`, `workflow_engine.md`, `workflow_architecture.md` (all updated), and `feature_inventory.md` modules 5/15/30. Prior entry, 2026-07-30: **Process/hygiene step, no feature work, no module % change** — 5 new efficiency standing rules recorded in `docs/process/standing_rules.md` (concrete 500-line file-splitting threshold; scoped-by-default test/lint runs with a full-sweep checkpoint every 5 steps/before core-infra commits/on request; trimmed report evidence; no re-verifying subsystems a diff didn't touch; batched tracker updates — this entry is itself the first one following the new shape). `platform_sdk_v1_scope.md` archived per the new threshold: 719 → 71 lines, full record at `history/027_platform_sdk_v1_scope_plan.md`. See `docs/process/standing_rules.md` and `docs/process/reporting_format.md` for the rules themselves; no code changed, no test run required (docs-only). Prior entry, 2026-07-30: **Tenth normal feature step since the Platform SDK initiative closed: the Lease Reaper now genuinely runs continuously — applying the exact background-task pattern the previous step proved for the Pack Health Collector.** `ai_os_kernel.workflow_engine.lease_reaper.run_reap_loop()` — the second real, continuously-running background task this codebase has ever started — sleeps for `LEASE_REAP_INTERVAL_SECONDS`, calls `WorkflowLeaseReaper.reap_once()` completely unchanged, repeats, until cancelled; `reap_once()`/`reap_expired()` themselves are untouched — this step is scheduling only. **The interval decision, with real evidence, not an arbitrary number**: neither `workflow_engine.md` nor `kernel_architecture.md` names a specific reap cadence (§7.1 documents the mechanism, not a number) — every real lease duration this codebase currently issues is 30 seconds (`ai_os_kernel.bootstrap._DEMO_WORKFLOW_LEASE_DURATION_SECONDS` and `ai_os_kernel.workflow_engine.delivery_pipeline._LEASE_DURATION_SECONDS`, two real, independently-arrived-at values), so `LEASE_REAP_INTERVAL_SECONDS` (15.0) is set to half that — a crashed worker's lease is reclaimed within, on average, half a lease-duration of its own expiry, not left to however long another worker's own `acquire()` call happens to take to trigger a reactive reclaim. `LEASE_REAP_BATCH_LIMIT` (100) matches `ai_os_kernel.routes.workflows._MAX_LIST_LIMIT`, this codebase's own already-established "reasonable batch size" convention, rather than inventing a new one. Wired into `_lifespan` identically to the health-polling loop: started right after `_build_workflow_trigger`, backed by a dedicated `WorkflowLeaseReaper(WorkflowLeaseService(SqlWorkflowLeaseRepository(engine)))` pair over the same shared engine, the returned `asyncio.Task` stored at `app.state.lease_reap_task`, and cancelled + awaited in `_lifespan`'s own `finally` block — now cancelling *both* background tasks — before `engine.dispose()` runs. A new, optional `PlatformConfig.lease_reap_interval_seconds` field (`None` by default, every real deployment unaffected) mirrors `pack_health_poll_interval_seconds`'s own exact shape for test-only short intervals. **Proven end to end against real Postgres containers, no mocking**, in a new file, `tests/integration/test_lease_reap_loop.py` (2 tests), with real, captured evidence: a real running workflow instance, leased by a real worker, then forced into the past directly (the identical "simulate the crashed worker" technique `test_lease_acquisition.py`'s own `test_an_expired_lease_can_be_reclaimed` already established) — confirmed present before Kernel startup, then genuinely gone (reclaimed by the loop itself, with **no manual `reap_once()` call anywhere in the test**) after real waits spanning multiple real intervals; the background task confirmed `not done()` while running, then genuinely `done()`/`cancelled()` immediately after a real `TestClient` shutdown. **Proven: zero regression** — the full workflow-engine/bootstrap/delivery-pipeline regression suite (88 passed, 7 correctly skipped) re-verified unaffected; `pytest -q` (root suite) → **974 passed, 12 skipped, 0 failed** (972 → 974, exactly the 2 new tests, nothing else moved); `mypy`/`ruff check`/`ruff format --check` clean repo-wide. `workflow_engine.md`'s own Implementation Status updated: the Lease Manager's proactive reclaim is now genuinely, continuously scheduled — `lease_reaper.py`'s own module docstring, which used to defer scheduling to "a future worker process framework," now names both of that framework's first two real instances. Prior entry, 2026-07-30: **Ninth normal feature step since the Platform SDK initiative closed: the Pack Health Collector now genuinely re-polls continuously — `POLL_INTERVAL_SECONDS` is enforced by a real background scheduler, not just decided.** `ai_os_kernel.capability_manager.health_poller.run_health_polling_loop()` — the first real, continuously-running background task this codebase has ever started (the Workflow Engine's own closest analog, `WorkflowLeaseReaper`, deliberately stops at "reclaim once in a bounded pass" and defers scheduling to "a future worker process framework"; this step is that framework's first real instance, narrowly scoped to this one, already-decided policy value) — sleeps for the real interval, then re-polls every discovered pack, forever, until cancelled. Sleeps *before* each pass, not after, so the loop's own first real poll never duplicates the existing one-shot startup poll (`_poll_discovered_pack_health`, unchanged). Wired into `_lifespan`: started immediately after the existing one-shot poll, the returned `asyncio.Task` stored at `app.state.pack_health_polling_task`, and — in `_lifespan`'s own `finally` block — genuinely cancelled and awaited (`task.cancel()` + `await task` inside `contextlib.suppress(asyncio.CancelledError)`) *before* `engine.dispose()` runs, so no in-flight poll query can ever race a closing connection pool. **No hardcoded test-only interval** — a new, optional `PlatformConfig.pack_health_poll_interval_seconds` field (`None` by default, meaning "use the real, decided `POLL_INTERVAL_SECONDS` constant" — every real deployment is unaffected) lets a test run the loop against a short interval instead of waiting out the real 30-second cadence. **Proven end to end against real Postgres containers, no mocking**, in a new file, `tests/integration/test_health_polling_loop.py` (2 tests), with real, captured evidence: `catalog.packs.health.checked_at` genuinely, strictly increases across three independent real waits (`2026-07-30T10:56:57.36` → `10:56:58.22` → `10:56:58.79` → `10:56:59.36`), well after the one-shot startup poll — real proof of continuous re-polling, not a one-shot artifact; the background task is confirmed `not done()` while the Kernel is running, then genuinely `done()` and `cancelled()` immediately after a real `TestClient` shutdown, with no extra wait needed to observe it (`TestClient.__exit__` itself only returns once the real ASGI shutdown sequence, including the `await` on the task, has completed). **Proven: zero regression** — the full health/bootstrap/capability_manager regression suite (64 tests) and every pack-agent/delivery-pipeline test (68 passed, 7 skipped) re-verified unaffected, since `task.cancel()` on a task currently sleeping raises `CancelledError` immediately, not after the remaining sleep duration — no test's own shutdown got slower; `pytest -q` (root suite) → **972 passed, 12 skipped, 0 failed** (970 → 972, exactly the 2 new tests, nothing else moved); `mypy`/`ruff check`/`ruff format --check` clean repo-wide. `capability_manager.md` §9 and `health_lifecycle.md` §10 both updated: the Pack Health Collector's own three-value policy row marked **fully decided and enforced — nothing remains open in either document from this whole multi-step thread.** Prior entry, 2026-07-30: **Eighth normal feature step since the Platform SDK initiative closed: `/health/ready` now genuinely surfaces the Pack Health Collector's own real `catalog.packs.health` snapshot — not just activation state.** `manifest_loader_check` (`ai_os_kernel.bootstrap._build_health_service`) now reads each genuinely-activated, already-polled pack's own real `health` column (via the existing `get_pack()` accessor) and reports its full detail via a new small formatter, `_format_pack_health_summary`: real status word, real `consecutive_failures` count against the real `CONSECUTIVE_FAILURE_THRESHOLD` (imported from `health_poller`, never re-hardcoded), a real `checked_at` timestamp, and which specific agents are failing, if any. **The one genuinely new reporting rule this step adds**: an activated pack with real, non-zero `consecutive_failures` — genuinely degrading but not yet moved to `PackState.FAILED` — now makes the overall check (and therefore `/health/ready`'s own overall status) `"degraded"` too, a real, visible early warning before the pack actually goes down, not silence until it does; a pack still at `consecutive_failures=0` (or never yet polled) is unaffected. **Proven end to end against real Postgres containers, no mocking**, in a new file, `tests/integration/test_health_pack_health_detail.py` (2 tests), with real JSON captured directly from a live `TestClient` run: a genuinely successful startup (zero manual register/activate/poll calls, the prior two steps' own real composition) makes `/health/ready` report `{"status": "ready", ...: {"status": "ok", "detail": "...; health: template-pack [healthy, consecutive_failures=0/3, checked_at=...]; software-engineering [healthy, consecutive_failures=0/3, checked_at=...]"}}`; a synthetic pack with one genuinely unimportable agent entrypoint, started as two real, separate Kernel "restarts" against the same database (each incrementing a real, observed consecutive-failure count via `_lifespan`'s own existing poll wiring), makes `/health/ready` report `{"status": "degraded", ...: {"status": "degraded", "detail": "...; health: <pack-id> [unhealthy, consecutive_failures=2/3, checked_at=...] failed_agents=['<pack-id>/flaky-agent']"}}` — while the pack's own `catalog.packs.state` is still genuinely `ACTIVATED`, confirmed by the deliberate absence of the pre-existing `"[state=..."` annotation, the real proof this is an early warning, not a duplicate of the already-covered "pack failed" case. **Proven: zero regression** — every pre-existing health-check test (including both `test_health_pack_activation.py` scenarios and both `test_health_database_check.py` scenarios) re-verified passing unchanged, since none of their own packs ever accumulate non-zero consecutive failures while still activated; `pytest -q` (root suite) → **970 passed, 12 skipped, 0 failed** (968 → 970, exactly the 2 new tests, nothing else moved); `mypy`/`ruff check`/`ruff format --check` clean repo-wide. `health_lifecycle.md` updated: the "natural, small next increment" the prior entry itself named is now built. Prior entry, 2026-07-30: **Seventh normal feature step since the Platform SDK initiative closed: the Pack Health Collector — capability_manager.md §9's own last-named "health check protocols" gap and health_lifecycle.md §10's own last-remaining named gap — now has a real, smallest-slice implementation.** `catalog.packs.health` genuinely gets written now. **The three-value policy §9 named, decided here for the first time**: `POLL_INTERVAL_SECONDS`=30.0, `POLL_TIMEOUT_SECONDS`=5.0, `CONSECUTIVE_FAILURE_THRESHOLD`=3 (matching Kubernetes' own common `failureThreshold` default), all named constants in the new `ai_os_kernel.capability_manager.health_poller`. `poll_pack_health()` genuinely resolves every one of a discovered pack's own `catalog.agents` rows through a real `AgentRegistry` — "can the pack's agents still resolve/construct," a real, genuine check, not a placeholder — writes a real `catalog.packs.health` snapshot on every poll via a new `SqlPackLifecycleRepository.record_health()` (status/details/consecutive_failures/checked_at, healthy or not), and, once `consecutive_failures` crosses the threshold, calls a new, real `mark_failed()` — the one real `-> failed` transition `repository.py` gains beyond register/activate/deactivate, following the identical lock/validate/write/record audited shape. Wired into `_lifespan`: one real poll per discovered pack, immediately after registration/activation, per Kernel startup — **poll interval is decided but not yet enforced by a background scheduler**, the identical, already-accepted "no full worker scheduler" gap this codebase carries for the Workflow Engine, deliberately out of this step's own "smallest real slice" scope. **A real, discovered design flaw found and fixed during this step's own regression testing, not shipped as a first guess**: the poll was first wired to reuse `_build_se_delivery_pipeline_registry`'s own real, credential-gated registry — which degrades `llm_gateway` to `None` when no live Anthropic secret is configured, so all 4 `llm:invoke` agents would report "unhealthy" purely because no secret is configured (a completely ordinary dev/CI state, not a broken pack); after 3 such Kernel restarts, the pack would have been genuinely, permanently marked `FAILED`, with no recovery path (`_ACTIVATABLE_FROM_STATES` excludes `FAILED`) — caught by this step's own regression suite (`test_bootstrap_pack_discovery.py`'s existing tests started failing). Fixed by building a dedicated, always-answering `SqlAgentRegistry(engine, llm_gateway=EchoLLMGateway(), prompt_engine=InMemoryPromptEngine(templates={}))` for the health poll specifically, separate from the real pipeline-trigger registry — "can this agent's code resolve/construct" and "is a live credential currently configured" are genuinely different questions, and conflating them made the poller trigger-happy about permanently failing a pack for a routine configuration state. **Proven end to end against real Postgres containers, no mocking**, in a new file, `tests/integration/capability_manager/test_health_poller.py` (2 tests): the real Software Engineering pack, registered through the real manifest → catalog installer and polled with a real Echo-backed registry, has all 5 real agents genuinely resolve and `catalog.packs.health` genuinely populated (`status="healthy"`, `consecutive_failures=0`); a minimal synthetic pack with one agent whose own `entrypoint` is a real, genuinely unimportable dotted path (a real `EntrypointLoadError`, not a mock) is polled three times in a row, proving `consecutive_failures` genuinely increments (1, 2, 3) across separate, real poll calls and that the pack is genuinely moved to `PackState.FAILED` only once the real threshold is crossed, never before. **Proven: zero regression** — the design-flaw fix above was itself required to restore the full regression suite to green; `pytest -q` (root suite) → **968 passed, 12 skipped, 0 failed** (966 → 968, exactly the 2 new tests, nothing else moved); `mypy`/`ruff check`/`ruff format --check` clean repo-wide. `capability_manager.md` §9 and `health_lifecycle.md` §10 both updated: the Pack Health Collector's own three-value policy row marked **DECIDED and BUILT**, closing the last named gap either document still had open. Prior entry, 2026-07-30: **Sixth normal feature step since the Platform SDK initiative closed: `health_lifecycle.md` §10's long-named "hard vs. soft dependency" gap is resolved — `/health/ready` now genuinely returns HTTP 503, not just 200 `degraded`, when the database is unreachable.** **The decision, with real evidence, not a default:** the database is a hard dependency. Every functional route in this codebase (`POST /api/v1/workflows`, `.../se.delivery_pipeline`, `POST`/`GET /api/v1/packs*`) already independently returns 503 the instant its own database-backed `app.state` object is absent — confirmed by reading each route's own source. A Kernel reporting `200 degraded` while every functional route will 503 anyway would be actively misleading to a Kubernetes readiness probe, exactly the "worse than refusing traffic" failure mode `health_lifecycle.md` §7 already warned about. **Built, generically, not as a one-off if-statement**: `ComponentStatus` gained `critical: bool = False` (every pre-existing check defaults to `False`, unaffected); `ReadinessReport.status` gained a third value, `"not_ready"`, produced whenever any `critical` component's own status is not `"ok"`; `routes/health.py` maps `"not_ready"` to HTTP 503, `"ready"`/`"degraded"` still to 200. A new `database_check` (`ai_os_kernel.bootstrap._build_health_service`) is the one check marked `critical=True` — it issues a real `SELECT 1` against `app.state.database_engine` (a new, raw engine reference `_lifespan` now exposes for exactly this reason — every other `app.state` object wraps the engine inside a higher-level repository that doesn't expose it), bounded by a short, named `_DATABASE_CHECK_TIMEOUT_SECONDS` (2.0s) so a completely unreachable host fails fast rather than hanging on the OS's own default TCP connect timeout. **Genuinely tests reachability, not just "was a URL configured"**: `create_async_engine` is lazy, so the engine object existing on `app.state` proves nothing about whether the real host behind it is reachable *right now* — confirmed directly: `_lifespan`'s own `try/except` around `build_engine(...)` almost never fires for a well-formed-but-unreachable URL, only for a malformed one or a missing env var, since SQLAlchemy never connects at construction time. **Proven end to end against real Postgres containers**, both directions, in a new file, `tests/integration/test_health_database_check.py` (2 tests): a genuinely reachable database leaves `/health/ready` completely unaffected (`200`, `"ready"`, database component `"ok"`); a genuinely unreachable one (a well-formed URL with nothing listening on the given port) makes it report `503`, `"not_ready"`, with the database component's own real error detail, while every other, non-critical component's own report stays untouched. **Proven: zero regression to every scenario that DOES have a real, reachable database** (both existing `test_health_pack_activation.py` tests re-verified passing unchanged, since their own containers are genuinely reachable). **An honest, necessary correction to 2 pre-existing unit tests that never configured a real database at all** (`tests/unit/kernel/entrypoints/test_api.py`, whose own docstring already states "no real database"): these used to silently claim `"ready"` with zero database — the exact bug this whole step exists to close — so their assertions are updated to the new, correct `"not_ready"`/503, while the specific component-level behavior each test actually exists to prove (`manifest_loader`'s own detail/status) is asserted directly and remains unchanged. `pytest -q` (root suite) → **966 passed, 12 skipped, 0 failed** (961 → 966, exactly the 5 new test functions — 2 new integration tests in `test_health_database_check.py` + 3 new unit aggregation tests in `test_service.py`; the 2 pre-existing unit tests whose assertions changed kept their test-function count net zero); `mypy`/`ruff check`/`ruff format --check` clean repo-wide. `health_lifecycle.md` §10 updated: the "hard vs. soft dependency" gap marked **DECIDED and BUILT**, leaving only part (b), pack health polling, open. Prior entry, 2026-07-30: **Fifth normal feature step since the Platform SDK initiative closed: the Health Service's `manifest_loader_check` now reports a discovered pack's real activation status, not just filesystem-level schema validity.** `ai_os_kernel.bootstrap._build_health_service` now takes the `FastAPI` `app` itself (built earlier in `build_app()` than before, precisely so this function can be given it) and `manifest_loader_check` reads `app.state.pack_lifecycle_repository` lazily, at call time, via the same `getattr(app.state, ..., None)` idiom `routes/packs.py` already established — degrading to its exact prior, database-free behaviour when absent (no database configured, or a bare `TestClient(app)`, which never triggers `_lifespan`). When present, it looks up each discovered pack's real `catalog.packs.state` via the existing `get_pack()` accessor and now distinguishes three real states: (a) schema-valid and genuinely `ACTIVATED`; (b) schema-valid but stuck/failed during registration or activation — named explicitly in the response, e.g. `"my-pack [state=failed]"`, and makes the check (and the overall `/health/ready` report) genuinely `degraded`; (c) not schema-valid at all (`report.failed`, unchanged, existing behaviour). **`HealthService`/`ComponentCheck` evolved from strictly-synchronous to mixed sync/async** — `readiness()` is now `async def`, `await`ing any check that returns an `Awaitable[ComponentStatus]` (`inspect.isawaitable()`), while every existing synchronous check (`configuration_manager_check`) needs zero changes; `routes/health.py`'s `health_ready` now `await`s it. **A real, second bug found and fixed while building this**: `_register_and_activate_discovered_packs` (from the prior step) used to log *every* `InvalidPackTransitionError` from `activate()` at `info` as "already activated," unconditionally — genuinely wrong for a pack stuck in some other non-activatable state (e.g. `failed`), which this step's own health check needed to be able to detect for real; fixed to check the pack's actual current state before choosing `info` (truly already activated) vs. `error` (genuinely stuck) — visible in logs now, not misclassified. **Proven end to end, real database, real JSON responses, no mocking**: a new file, `tests/integration/test_health_pack_activation.py` (2 tests) — one proves a genuinely successful startup (no manual `register()`/`activate()` call, the prior step's own real composition) makes `/health/ready` report `{"status": "ready", ...: {"status": "ok", "detail": "1 pack(s) discovered, 0 invalid; 1 activated, 0 not activated"}}`; the other pre-seeds a real `catalog.packs` row directly in `failed` (the only way to reach a state no method this codebase calls ever produces) and proves the real Kernel startup leaves it genuinely stuck and `/health/ready` reports `{"status": "degraded", ...: {"status": "degraded", "detail": "1 pack(s) discovered, 0 invalid; 0 activated, 1 not activated (<pack-id> [state=failed])"}}` — the real reason, not a generic message. **Proven: zero regression** — every check that used to be purely synchronous keeps behaving identically; the 3 pre-existing health-check unit tests were updated only to call the now-async `readiness()` via `asyncio.run()` (a mechanical calling-convention change, not a behavior change), plus 2 new unit tests proving an async check is genuinely awaited; `pytest -q` (root suite) → **961 passed, 12 skipped, 0 failed** (957 → 961, exactly the 4 new tests, nothing else moved); `mypy`/`ruff check`/`ruff format --check` clean repo-wide. `docs/03_architecture/kernel/health_lifecycle.md` updated: §10's "synchronous callable" decision revised to "callable or awaitable," and the "no database check" gap under §5 marked resolved. Prior entry, 2026-07-30: **Fourth normal feature step since the Platform SDK initiative closed: real pack discovery is now wired into Kernel startup — a fresh Kernel process pointed at a real pack directory genuinely registers and activates that pack with zero manual intervention.** New `ai_os_kernel.bootstrap._register_and_activate_discovered_packs`, called from `_lifespan` once a real database engine exists, right after `app.state.pack_lifecycle_repository` is built. For each pack `manifest_loader.scan()` reports as discovered (schema-valid), it calls `SqlPackLifecycleRepository.register(..., pack_root=<that manifest's own directory>)` — the real manifest → catalog installer path from the previous step — then `.activate()`, reading `sdk_version`/`min_kernel_version` from the manifest's own `dependencies.sdkVersion`/`compatibility.minKernelVersion` fields rather than hardcoding them (a real, fixed "no hardcoded values" gap the prior step's own hand-written test harness left as separate literals that merely happened to match). **Idempotent across restarts by construction, not a new special case**: a repeat `register()` raises `PackAlreadyRegisteredError` (duplicate `pack_id` primary key) and a repeat `activate()` on an already-`ACTIVATED` pack raises `InvalidPackTransitionError` — both caught and logged at `info`, proven by a real second "Kernel restart" against the same, already-populated database, not simulated. **Per-pack resilience, not all-or-nothing**: a manifest failing schema validation is logged at `warning` and skipped (`report.failed`, the identical severity `manifest_loader_check` already uses); a genuine registration/activation failure for an otherwise-valid manifest is logged at `error` — visible, not silently swallowed — but does not abort discovery of the remaining packs or crash Kernel startup. **One real, documented consequence recorded, not silently introduced**: activating unconditionally means a pack an operator manually `deactivate()`'d through the HTTP route is genuinely re-activated on the next Kernel restart (capability_manager.md §4 already documents `deactivated` as "reactivatable") — there is no "stay deactivated across restarts" mechanism in this codebase yet. **Proven end to end, real database, zero manual test intervention**: a new file, `tests/integration/test_bootstrap_pack_discovery.py` (4 tests) — deliberately the one integration test in this suite that does NOT set `capability_pack_dirs=[]` (every other test stays isolated from the real `capability_packs/` tree; this one's whole point is proving the un-isolated, real path) — proves a fresh `build_app()` against an empty database genuinely activates both the real Software Engineering pack and the schema-valid, capability-less `_template` example pack, that a second "restart" is idempotent (no duplicate `catalog.agents` rows, pack still `ACTIVATED`), that the auto-registered `qa-test` agent genuinely resolves through `SqlAgentRegistry` and genuinely runs a real file with no live credential, and that the real HTTP pipeline route now fails for a materially deeper reason (`"...no llm_gateway/prompt_engine was supplied..."`) than before this step (`"no agent registered"`) — real proof that genuine catalog rows, not just plumbing, changed. **Proven: zero regression** — every existing test that builds `PlatformConfig` already explicitly sets `capability_pack_dirs=[]` (confirmed by a full-repo grep before this step began), so this step's new discovery call is a provable no-op for all of them; `pytest -q` (root suite) → **957 passed, 12 skipped, 0 failed** (953 → 957, exactly the 4 new tests, nothing else moved); `mypy`/`ruff check`/`ruff format --check` clean repo-wide. Prior entry, 2026-07-30: **Third normal feature step since the Platform SDK initiative closed: a real manifest → catalog installer now derives `catalog.agents`/`catalog.prompts`/`catalog.tools` rows automatically from a pack's own `manifest.yaml`, replacing hand-duplicated raw-SQL seeding across the codebase.** New pure-derivation module `ai_os_kernel.capability_manager.manifest_catalog_installer` (`derive_agent_rows`/`derive_prompt_rows`/`derive_tool_rows`) resolves each manifest-declared `inputSchema`/`outputSchema` dotted import path to a real Pydantic model and calls `.model_json_schema()`, and reads each declared prompt file's real content, computing its own `sha256` content hash. Wired into `SqlPackLifecycleRepository.register()` via a new, backward-compatible, opt-in `pack_root: Path | None = None` parameter — `None` (the default, and the only mode the real `POST /api/v1/packs` HTTP route ever uses, since it receives a client-supplied manifest with no filesystem access at all) preserves the exact prior behavior; supplying a real pack directory derives and writes real catalog rows inside the same transaction as the pack's own registration. **Proven against the real Software Engineering pack**: a new equivalence test (`tests/integration/capability_manager/test_manifest_catalog_installer.py`, 4 tests) registers the real pack through this path and asserts the resulting `catalog.agents`/`catalog.prompts`/`catalog.tools` rows carry the exact real permissions, entrypoints, content, and content hashes the manifest and shipped prompt files declare — including 2 fail-closed tests for an unresolvable schema reference and a missing prompt file. **A real, discovered bug in the OLD hand-seeded test fixtures this step replaces, not just duplication**: `test_delivery_pipeline.py`'s own raw-SQL seeding granted `sandbox:execute` to `requirements-analyst`/`architecture` uniformly with `build`/`documentation`, even though the real manifest grants only `llm:invoke` to the first two — silently harmless (neither agent ever invokes a tool) but genuinely over-permissive; the new installer derives each agent's own exact, correct permission set instead. **The hand-duplicated raw-SQL seeding this step's own prior report flagged is now removed, not fourth-copied**: all 6 identified files (`test_architecture_agent_pack.py`, `test_build_agent_pack.py`, `test_documentation_agent_pack.py`, `test_requirements_analyst_agent_pack.py`, `test_verification_agent_pack.py`, `test_delivery_pipeline.py`) now call `repository.register(..., pack_root=PACK_ROOT)` and no longer hand-write any `catalog.agents`/`catalog.prompts` INSERT; `test_delivery_pipeline_route_live.py`'s own import of the now-deleted seeding helpers was fixed to import only `_register_and_activate_pack`. Each file's own genuinely distinct, test-only prompt substitute (`_seed_live_prompt`, used where a real Context Manager round trip is needed instead of the real shipped prompt) was deliberately kept, not removed — it is different data, not duplicated logic. **Clarification on the "LLM-gateway degrade bug" from the prior step**: it was a real bug found AND fixed within that same prior HTTP-route step (before its own commit `60e1d1b`), not a currently-live issue — `_build_se_delivery_pipeline_registry` always returns a real `SqlAgentRegistry`, degrading only the internal `llm_gateway` to `None` on secret-resolution failure, so the route always reaches the real Workflow Engine and returns an honest `failed` outcome rather than a blanket `503`. **Proven: zero regression** — `pytest -q` (root suite) → **953 passed, 12 skipped, 0 failed** (949 → 953, exactly the 4 new equivalence/fail-closed tests, nothing else moved); the 5 per-agent pack integration tests plus both real Delivery Pipeline end-to-end tests plus the HTTP-route integration tests all re-verified passing; `mypy`/`ruff check`/`ruff format --check` clean repo-wide. This pack's own `README.md` "Known, documented gaps" entry for the missing installer is removed, since the gap is now closed. Prior entry, 2026-07-30: **Second normal feature step since the Platform SDK initiative closed: `se.delivery_pipeline` is now reachable over real HTTP.** `POST /api/v1/workflows/se.delivery_pipeline` (`ai_os_kernel.routes.delivery_pipeline`), gated by the existing `workflow:start` permission — the same authenticated-route pattern `POST /api/v1/workflows` (the platform demo) already established, extended with a pipeline-specific response carrying the real `documentation_path` this pipeline actually produces. **The real composition this needed (`build_pipeline_trigger`/`build_pipeline_context_manager`/`load_pipeline_definition`) was promoted from test-harness code (`tests/integration/_delivery_pipeline.py`) into real Kernel code (`ai_os_kernel.workflow_engine.delivery_pipeline`)** — production code importing from `tests/` would have been backwards, so the real fix was relocating the one real implementation to where both a production route and the existing tests could share it, not building a second copy. `bootstrap.py` gained its first real `SqlAgentRegistry` construction (`_build_se_delivery_pipeline_registry`) — a genuine, documented gap that class's own docstring named ("bootstrap.py does not construct this class at all yet") is now closed. **A real design bug found and fixed while testing this**: a first version degraded the entire registry to unavailable (`503`) when no Anthropic secret was configured — inconsistent with `_build_prompted_agent_registry`'s own established "degrade the Gateway only, still return a real registry" shape, which reaches the real Workflow Engine and reports an honest, structured `failed` outcome instead. Fixed to match. Proven: 6 new unit tests (auth boundary, no database), 2 new integration tests against a real Postgres container (no live credential — an honest `failed` outcome, matching the platform demo route's own identical proof shape), and a new opt-in live test reusing the existing pack-seeding helpers by import rather than duplicating them, proving a genuine, completed 5-agent run through the real HTTP layer. Both pre-existing Delivery Pipeline end-to-end tests re-verified unchanged after the relocation. Prior entry, 2026-07-30: **First normal feature step since the Platform SDK initiative closed: `requirements-analyst` wired into `se.delivery_pipeline` as its own real first step.** The pipeline now chains all 5 of this pack's built agents (`requirements-analyst` → `architecture` → `build` → `qa-test` → `documentation`), not 4 of 5 — Architecture now designs against Requirements Analyst's own real, refined output, not the raw `requirement` input directly. Reuses the existing `WorkflowStepOutputResolver` hand-off mechanism unchanged (`tests/integration/_delivery_pipeline.py`'s `_STEP_SOURCES`/`_FIELD_SELECTORS` extended: `architecture` now sources from `requirements-analyst`, field-selected to `analysis`; `_StepScopedResolver`'s raw-top-level-input scoping moved from the `architecture` step to the new `requirements-analyst` step). `delivery_pipeline.yaml` and `manifest.yaml`'s own workflow entry bumped to v1.1.0; `workflows/models.py`'s `PipelineInput.requirement` description updated to reflect its new role. **Proven end to end, both tiers**: the deterministic test (`LocalSubprocessSandbox`) and the live-Docker test (`DockerSandbox`) both now construct and register a fifth agent and assert the new hand-off by reading it back from each step's own real, persisted output (not hand-copied) — Requirements Analyst's real output is shown to genuinely reach Architecture's real prompt, three hops downstream into Build's own persisted `instruction` field, in both tiers. Zero regression: the four already-working steps are unchanged beyond the new upstream source feeding the same `{{context}}` variable Architecture already read (no prompt template changes needed). Docs swept for the now-stale "4 of 5"/"not yet chained" claims: `agents.md`, `workflows.md`, `agent_architecture.md`, this pack's own `README.md`, `manifest.yaml`'s header comment, `feature_inventory.md` module 29. Full detail below (§2-§4) and in `feature_inventory.md` module 29/30. Prior entry, 2026-07-30: Platform SDK v1.0.0: **all 18 steps (+2 inserted) complete — the plan is formally closed out.** Step 15 (final) built the genuinely remaining `pack_contract_suite` checks — 2, 3, 4, 5, 6, 8, 9, **seven** not eight (a discovered arithmetic correction to this document's own prior "remaining 8" language: checks 2-9 are eight items total per `platform_sdk.md` §9, one of which, check 7, already shipped at step 8; check 1 was never "remaining" at all, already real via the Kernel's own Manifest Loader) — as `ai_os_sdk.testing.pack_contract_suite`, plus a unified `run_pack_contract_suite()` orchestrator wrapping check 7 and reimplementing check 1 (the SDK cannot import the Kernel's loader; re-validates the same schema with the same `jsonschema` library instead). All 9 checks proven together against the real, fully-migrated Software Engineering pack in `tests/contract/test_pack_contract_suite.py` (20 tests, activating the already-prepared CI gate) — genuinely green, zero waivers. One further real, discovered gap fixed along the way: the pack's own `pyproject.toml` still declared a runtime dependency on `ai-os-kernel` (stale since step 14) and none on `ai-os-sdk` — check 7's AST scanner never looks at `pyproject.toml`, so this survived undetected until this step's "dependencies satisfied" check required actually reading it; fixed. **Platform SDK v1.0.0 is done — the codebase is ready to resume normal feature development.** Full closeout record and what remains deliberately deferred: `platform_sdk_v1_scope.md` §6s/§10. Prior entry, 2026-07-29: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, 11, 12, 12a, 13, and 14 of 18 (+2) complete — the sixth and final module migration, closing out the entire pack.** `pack.py` now implements `ai_os_sdk.contracts.CapabilityPack`, importing `PackContext`/`PackRegistration`/`HealthReport` from `ai_os_sdk.contracts` directly. Zero `ai_os_kernel` imports remain anywhere in this pack, for the first time in this project's history. `pack_contract_waiver.yaml` is **deleted outright**; check 7 enforces unconditionally, with zero waivers and zero exceptions. **The Capability Pack growth gate is LIFTED** (product-owner decision, `docs/process/standing_rules.md`) — a genuine revision of that gate's own previously-recorded "only after step 15" condition, since the gate's real substance is now satisfied; step 15's remaining 8 checks continue as scheduled, real completeness work, no longer a precondition for this gate. `capability_pack_contract.md`'s dated exception language retired; `feature_inventory.md` modules 29/32/33/34 unfrozen. Both real Delivery Pipeline end-to-end tests still pass; a real wheel build still succeeds. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, 11, 12, 12a, and 13 of 18 (+2) complete** — `documentation` migrated onto the real Platform SDK, the fifth and final agent migration. Its write uses `PLATFORM_PYTHON_INTERPRETER` (the step 12a fix) from the start — no repeated tension. This agent needs no lock at all (unlike `build`'s narrower one) — the clearest confirmation yet that the lock-obsolescence finding was real. **A real, discovered correction to this step's own initial framing, surfaced and fixed rather than silently forced**: the waiver is NOT literally empty after this step — it retains exactly one entry, `pack.py` (step 14's own explicitly scoped job, never one of the 5 agents). Check 7 passes with exactly 1 module/1 violation waived (down from 2/13). Both real Delivery Pipeline end-to-end tests pass with the entire four-agent pipeline now migrated onto the Platform SDK — a real documentation artifact genuinely written through the new `ToolInvoker` path in both tiers, including live Docker. The `evaluation.llm_calls` gap reaches its full, permanent extent (all 4 LLM-calling agents); `feature_inventory.md`'s §6a marks it expected-to-stay-open. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, 11, 12, and 12a of 18 (+2) complete** — a small, inserted fix step closes the real `python_command` regression step 12 found: a new SDK placeholder token, `PLATFORM_PYTHON_INTERPRETER`, is expanded by `ToolInvokerAdapter.invoke()` (the one place still holding the real `SandboxExecutor`) into that instance's own real interpreter command before dispatch, restoring the pre-migration "always automatically correct" guarantee exactly — `build.py`'s own constructor default is removed entirely, not merely documented better. Proven against both real backends: a real `LocalSubprocessSandbox` and a real, live `DockerSandbox` (new test file, a real file genuinely written end to end). `feature_inventory.md`'s §6a second Known Regression entry marked resolved; `documentation` (step 13) confirmed to need the identical fix from the start. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, 11, and 12 of 18 (+1) complete** — `build` migrated onto the real Platform SDK, the fourth migration and the first needing real gateway AND tool injection together (`context.llm`/`context.prompts`/`context.tools`). Its write now happens through `context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, ...)`, proven against a real, live Docker sandbox — a real file genuinely written inside a real container. Two real findings: (1) the lock is not fully obsolete here, unlike steps 10/11 — it still guards lazy working-directory creation, a concern unrelated to LLM composition; (2) a migrated agent can no longer dynamically discover its sandbox backend's own Python interpreter command (`ToolInvoker` is deliberately backend-agnostic), resolved with a constructor default matching the real system-wide default backend and tracked as a second entry in `feature_inventory.md`'s §6a "Known Regressions." The `evaluation.llm_calls` capability loss recurs a third time. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, and 11 of 18 (+1) complete** — `architecture` migrated onto the real Platform SDK, the third migration, following step 10's own pattern exactly with no new tension between the (already-removed) lazy-build lock and injection. The one real, new finding: `architecture` is the first migrated agent actually chained into `se.delivery_pipeline`, so both real Delivery Pipeline end-to-end tests needed a genuine fix (their own `InMemoryAgentRegistry` construction and, for the live tier, `SqlAgentRegistry`'s real `llm_gateway`/`prompt_engine` parameters) — found and fixed as part of this step, both tests now passing unchanged. The `evaluation.llm_calls` capability loss recurs, confirming it as a structural v1.0.0 gap rather than a one-off; now tracked in `feature_inventory.md`'s new §6a "Known Regressions" section so it survives past individual step reports. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, and 10 of 18 (+1) complete** — `requirements-analyst` migrated onto the real Platform SDK, the second migration and the first needing real gateway injection. The lazy-build lock it used to need is gone entirely — found obsolete, not conflicting, since `bind_pack_context()` is synchronous and happens strictly before any `execute()` call is possible. A real, previously-latent circular import between `ai_os_kernel.workflow_engine.registry` and `ai_os_kernel.sdk_adapters.pack_context` (introduced by step 9a) was found and fixed with a function-local import. A real, accepted capability loss recorded: no more `evaluation.llm_calls` recording (no SDK Telemetry surface exists). Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, and 9a of 18 (+1) complete** — `SqlAgentRegistry`/`SqlToolRegistry` now genuinely call `PackContextReceiver.bind_pack_context()` for real, in production, closing step 9's own discovered gap before it could repeat across steps 10–13. `EntrypointLoader` untouched. `test_verification_agent_pack.py`'s manual binding is gone entirely; `qa-test` resolves and runs end to end through the real registry with zero test-side workaround. Zero regression across all 8 files using either registry, including the four still-unmigrated agents. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, and 9 of 18 complete** — `qa-test` migrated onto the real Platform SDK, the first of six migration steps and the first real (non-test-entrypoint) use of the step 6b injection mechanism. Two real gaps found and resolved: the real Workflow Engine context object is still Kernel-typed (resolved via duck-typing, not `isinstance`); `SqlAgentRegistry` never calls `bind_pack_context()` (a real, unconditionally-run integration test now does it itself, standing in for still-missing production wiring). Proven via the real Delivery Pipeline end-to-end tests, including the live Docker-backed one, producing identical output. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, and 8 of 18 complete** — `pack_contract_suite` check 7 (a real AST import-scanner, `ai_os_sdk.testing.forbidden_imports`) and its documented, expiring waiver mechanism (`ai_os_sdk.testing.waiver`) are both real, proven both ways (fails without a waiver — 44 real violations found in the real pack — passes with one) by an automated test, and wired into CI via `scripts/check_import_boundaries.py`, landing in a gate `ci.yml` already had prepared. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, and 7 of 18 complete** — `pipeline.py` relocated out of the pack's shipped wheel into `tests/integration/_delivery_pipeline.py` (proven via a real wheel build with the file and its `sqlalchemy` import both confirmed absent, and both relocated integration tests — including the real Docker-backed one — passing unchanged); `ContextService` boundary models landed, narrowed against the real Kernel shape; the full entry-point contract (`PackContext`/`CapabilityPack`/`PackRegistration`/`HealthReport`) relocated from the Kernel into `ai_os_sdk.contracts.capability_pack`, additively, with the Kernel's own module now a real re-export. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, and 6b of 18 complete** — the `EntrypointLoader` zero-argument injection blocker is resolved without changing `EntrypointLoader` itself: a new SDK Protocol (`PackContextReceiver`) lets a zero-arg-constructible entrypoint receive a real, permission-gated `PackContext` via one standard `bind_pack_context()` call, proven end to end against a real test entrypoint (real construction → real injection → genuine Echo LLM completion, prompt render, and sandboxed command). Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, and 6a of 18 complete** — real Kernel-side adapters (`kernel/src/ai_os_kernel/sdk_adapters/`) now genuinely implement `LLMGateway`, `PromptRegistry`, and `ToolInvoker` over `DispatchingLLMGateway`, the Prompt Engine, and `LocalSubprocessSandbox`, proven against real underlying objects; the timeout-precedence nuance step 6 left open is resolved (`min()` of the two timeouts). Prior entry: **Steps 1, 2, 2a, 3, 4, 5, and 6 of 18 complete** — the `ToolInvoker` Protocol now exists, proven against the real, executed sandbox, which found and fixed a too-strict validator and a circular import. Prior entry: **Steps 1, 2, 2a, 3, and 4 of 18 complete** **Steps 1, 2, 2a, 3, and 4 of 18 complete** — the `LLMGateway` Protocol now exists and the real `DispatchingLLMGateway` provably satisfies it unmodified. Prior entry: **Steps 1, 2, 2a, and 3 of 18 complete** — the `Agent`/`Tool` Protocols now exist and every real agent and tool provably satisfies them unmodified. Prior entry: **Steps 1, 2, and 2a of 18 complete** — packaging scaffold, error taxonomy + shared boundary models, and the five Protocol/reality reconciliation decisions. Step 3 next, and smaller than originally scoped. Prior entry: Step 1 of 15 complete — `platform_sdk/` scaffolded as a real, installable `ai-os-sdk` distribution, packaging only, no Protocols/models/errors yet. See §2 and §6. Prior entries: the SDK build was scoped as its own plan document (`../03_architecture/platform/platform_sdk_v1_scope.md`); before that, a targeted fix step corrected stale test/mypy numbers in §2 below and closed 15 real mypy errors with a `py.typed` marker; before that, the product-owner decision recorded **the direct-Kernel-import exception is now a hard gate — no new agent or Capability Pack may be added until the Platform SDK exists**. See §4 and `history/026_platform_sdk_gate_and_audit_completion.md`.)

---

## 1. Purpose

A living record of what has actually been built, checked against the exit criteria in `implementation_roadmap.md`, so any session (human or AI) can resume work without rescanning the repository or relying on chat history.

This document is deliberately **short** — current stage, what exists, current blockers, and the next recommended step only. Full chronological detail, subsystem by subsystem, lives in `docs/19_roadmap/history/` (see `history/INDEX.md`). For the complete, granular, per-module completion tracker (every feature/module/phase with a percentage and status), see `docs/19_roadmap/feature_inventory.md` — that document, not this one, is the authority on "what % done is X." This document is descriptive, not authoritative — on any conflict, the Implementation Roadmap and the ADRs govern.

**Read this file first, every session, before anything else** (see `CLAUDE.md`). Read a `history/` file only when the current task specifically needs that subsystem's detail.

---

## 2. Current Stage

**Stage A – Platform Skeleton** is process-complete but not exit-criteria-complete (see §4). **Stage B – Minimum Viable Kernel** is underway and has reached a real, demonstrable, **now genuinely Docker-verified** milestone: a complete five-agent software-engineering delivery pipeline (Requirements Analyst → Architecture → Build → Test → Documentation) runs end to end through the real Workflow Engine and **`DockerSandbox` as the genuine default Tier 1 sandbox backend**, proven for real against a live daemon — including a real proof that network isolation and filesystem containment hold for code the pipeline itself generated and ran, not just in an isolated guarantee check. All 5 of this pack's built agents are now chained into this one pipeline.

**Numbers current as of 2026-07-28 (re-verified fresh, not carried forward from an earlier step — see the lesson recorded in `docs/process/standing_rules.md` about exactly this failure mode):** `pytest -q` (full suite, unit + integration + pack, Docker available) → **803 passed, 11 skipped** (all 11 are the same opt-in-live-provider suite, no API key set — expected per ADR-0015), **0 failed**. `mypy --strict kernel/src kernel/alembic tests` (the exact scope CI runs) → **0 errors, 267 source files checked**, clean. `ruff check .` → clean. This is the first genuinely real-Docker-verified pass of the entire integration suite since `DockerSandbox` became the default.

A prior version of this paragraph claimed "849 tests passed" and "mypy --strict (283 source files) clean" — both were stale by the time a hard-evidence audit re-ran them fresh and found 803/803 passing but **15 real mypy errors** (root cause: `capability_packs/software-engineering` shipped with no `py.typed` marker, so mypy treated cross-boundary imports from `tests/` into the pack as untyped). Fixed this step by adding the marker; see `history/026_platform_sdk_gate_and_audit_completion.md`'s successor step for the full before/after. The "283 files" figure was real once, under a *different* mypy invocation that additionally passes `capability_packs/software-engineering/src` and its `tests/` directory on the command line (matching `[tool.mypy].files` in `pyproject.toml` exactly) — CI itself runs the narrower `kernel/src kernel/alembic tests` invocation quoted above, which is what these numbers now reflect.

Three prior steps (2026-07-28) were documentation/infrastructure-only. The step recorded above this paragraph closed the documentation audit and recorded a hard product-owner gate (see §4, first item) — no code changed. This paragraph itself was corrected by a small, targeted follow-up fix step (also 2026-07-28) that touched only: the `py.typed` marker, this paragraph's numbers, a new standing-rules lesson, and two documentation-formatting fixes.

**Platform SDK build: the sequence was revised from 15 to 18 steps after an independent architecture review (2026-07-29), and Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, 11, 12, 12a, 13, and 14 are complete.** The review found 7 real problems in the original plan, three of them blocking — most importantly that no step made the Kernel able to *supply* the Protocols the pack would be migrated onto, and that the injection path (`EntrypointLoader` hardcodes `cls()`; `PackContext` is identity-only) was listed as a non-goal. All findings accepted; three new steps (**2a** Protocol/reality reconciliation, **6a** Kernel-side adapters, **6b** `PackContext` construction + injection) were inserted, `SecretResolver` was dropped, migration order reversed to `qa-test`-first, and `pipeline.py` relocation became step 7. A fourth insertion, **9a**, was added mid-migration (2026-07-29) after step 9 discovered `SqlAgentRegistry` never wired the step 6b injection mechanism for real — closed once, here, rather than once per remaining migration step. A fifth insertion, **12a**, was added mid-migration (2026-07-29) after step 12 discovered a migrated write-through-sandbox agent lost the pre-migration guarantee of automatically knowing its own backend's Python interpreter command — closed at the adapter layer, before steps 13–14 could hit the identical pattern. Full findings and reasoning: `../03_architecture/platform/platform_sdk_v1_scope.md` §6a.

- **Step 1 (done):** `platform_sdk/` is a real, installable PEP 621 distribution (`ai-os-sdk`), a workspace member; `import ai_os_sdk` works.
- **Step 2 (done):** `ai_os_sdk.errors` holds the real `ErrorCategory` (all 6 documented categories), `StructuredError`, and the `AiOsError` hierarchy with its six subclasses, each mapping 1:1 onto a `StructuredError`. `ai_os_sdk.models` holds `ArtifactRef`, the canonical §4.1 `TraceContext`, `SecurityContext`, and `StepBudget`. **Consumed by nothing; no existing code changed.** Note: this creates the *third* `TraceContext` in the repository — `ai_os_kernel.observability.trace` and `ai_os_kernel.llm_gateway.models` each hold a narrower one, and both of their docstrings already name §4.1 as the canonical shape they reduce. Consolidating them is Kernel-side work — step 2a confirmed it belongs to the Kernel-adapter step (6a), not to the SDK.
- **Step 2a (done, docs only):** all five Protocol/reality reconciliation decisions made and recorded as dated blocks in `platform_sdk.md` §4.2/§4.3/§5.1/§5.2/§5.6. Net: **four narrowings** (`Agent` and `Tool` to their dict-based Kernel shapes; `LLMGateway` from 5 methods to the 2 that exist; `PromptRegistry`'s `version` to required), **two targeted extensions** where the real Kernel is richer than the spec (`ProviderCapabilities` 10 → 13 fields; `ToolResult` gains `timed_out`/`truncated`, two real outcomes the spec could not express), **one deliberate reversal in the spec's favour** (`PromptRegistry` keeps the documented keyword call style rather than the Kernel's request-object envelope — a pack-facing Protocol is shaped for its caller), and **one from-scratch design** (`ToolInvoker`, grounded in a `platform.sandbox.run_command` platform-provided tool id, which also fixes `SandboxedCommandTool.execute(inputs)` ignoring its own `inputs`). Summary table with evidence: `platform_sdk_v1_scope.md` §6b.
- **Step 3 (done):** `ai_os_sdk.contracts` holds the `Agent` and `Tool` Protocols at their narrowed shapes, plus the SDK's own `TrustTier` (defined rather than imported — the SDK is the dependency floor). **Structural compatibility is proven against real code, not mocks:** all five pack agents and `EchoAgent` satisfy `Agent`; `EchoTool` and the real `SandboxedCommandTool` satisfy `Tool` — 13 assertions in `tests/unit/platform_sdk/test_kernel_satisfies_sdk_contracts.py`, with **zero modification to any Kernel or pack source**. `AgentRequest`/`AgentResult`/`ToolRequest` remain deferred past v1.0.0 (no consumer under the narrowed shapes); `ToolResult` lands in step 6 with `ToolInvoker`.
- **Step 4 (done):** `ai_os_sdk.contracts.LLMGateway` holds the Protocol narrowed to `complete()`/`capabilities()` — the two methods the real `DispatchingLLMGateway` implements. `ai_os_sdk.models` gained `Message`/`MessageRole`/`LLMRequest`/`StopReason`/`UsageRecord`/`LLMResponse` (field-for-field mirrors of the real Kernel models) and `ProviderCapabilities` (extended to the real 13 fields — the real `StaticCapabilityNegotiator` already implements all 13, and its own docstring names `platform_sdk.md` as the discrepancy it "implements past"). **Structural compatibility proven against the real `DispatchingLLMGateway`**, constructed with an empty router and no providers (zero I/O) — 15 new assertions total. `EchoLLMGateway` correctly does *not* satisfy the SDK Protocol, since it implements only `complete()`, which is evidence the narrowing is exact rather than accidentally loose.
- **Step 5 (done):** `ai_os_sdk.contracts.PromptRegistry` holds the Protocol at the documented keyword call style (`render(prompt_id, variables, *, version)`) — the one deliberate reversal from step 2a, kept over the Kernel's own request-object envelope because a pack-facing Protocol should be shaped for its caller (ADR-0004). `version` is keyword-only and required. `RenderedPrompt` narrowed to `prompt_id`/`version`/`content`. **No real Kernel class satisfies this Protocol** — it is a from-scratch call convention, not a narrowing — so instead of an `isinstance` proof, the conversion to/from the real `PromptRenderRequest`/`PromptRenderResponse` is proven **lossless in both directions**, 7 assertions in `tests/unit/platform_sdk/test_prompt_registry_adapter_conversion.py`; the reference conversion functions are test-local and illustrative, not shipped SDK code.
- **Step 6 (done):** `ai_os_sdk.contracts.ToolInvoker` holds the Protocol at the from-scratch shape decided in 2a — `invoke(tool_id, inputs, *, timeout_seconds) -> ToolResult` + `available_tools() -> tuple[ToolDescriptor, ...]` — grounded in a new platform-provided tool, `PLATFORM_SANDBOX_RUN_COMMAND_DESCRIPTOR` (id `platform.sandbox.run_command`), so `available_tools()` has a genuine non-empty answer. `ToolResult` (moved here from step 3) narrows `stdout_ref`/`stderr_ref` to inline strings and drops `artifacts` (no `StorageService`), and **extends** the documented shape with `timed_out`/`truncated`. **Proven against the real, actually-executed `LocalSubprocessSandbox`** — a genuine clean run, timeout, and output-cap breach — not hand-built `SandboxResult` values, because the whole point of the extension is a distinction real execution produces. **This real execution found and fixed two genuine bugs**: `ToolResult`'s own validator wrongly required `exit_code` whenever `timed_out` was `False` — disproven when a real cap breach left `exit_code=None` with `timed_out=False` too (the sandbox kills the process on cap breach before it can exit); and a real circular import between `ai_os_sdk.errors` and `ai_os_sdk.models`, fixed by relocating `ErrorCategory`/`StructuredError` into a new `ai_os_sdk.models.error` module (both remain fully public via `ai_os_sdk.errors`). Full record: `platform_sdk_v1_scope.md` §6f.
- **Step 6a (done):** three real adapter classes in the new `kernel/src/ai_os_kernel/sdk_adapters/` package (deliberately outside `platform_sdk/`, since the SDK cannot depend on the Kernel) — `LLMGatewayAdapter`, `PromptRegistryAdapter`, `ToolInvokerAdapter` — genuinely implement the SDK's `LLMGateway`/`PromptRegistry`/`ToolInvoker` Protocols by wrapping real `DispatchingLLMGateway`, `PromptEngine`, and `SandboxExecutor` instances. `kernel/pyproject.toml` gained its first dependency change this whole initiative (`ai-os-sdk`), since the Kernel now implements SDK Protocols. Every field mapping was checked against real source, including one genuine, documented narrowing (SDK's 7-field `TraceContext` → Kernel's 2-field one) and one newly-surfaced gap (`ToolInvoker.invoke` carries no trace parameter at all, resolved with a fresh, uncorrelated, per-invocation `TraceContext`). **Resolves the timeout-precedence nuance step 6 left open**: the more restrictive of `invoke()`'s own `timeout_seconds` and the tool's own `inputs["timeout_seconds"]` always wins (`min()`), proven with 4 real-execution tests measuring genuine elapsed wall-clock time around real `time.sleep(5)` subprocess runs. **Proven against real underlying objects, not mocks** (23 new tests: 8 against a real Echo-backed `DispatchingLLMGateway`, 5 against a real `InMemoryPromptEngine`, 10 against a real `LocalSubprocessSandbox`). Not wired into `bootstrap.py`/`EntrypointLoader`/any pack yet — that is step 6b. Full record: `platform_sdk_v1_scope.md` §6g.
- **Step 6b (done):** the `EntrypointLoader` zero-argument injection blocker is resolved **without changing `EntrypointLoader` itself**. A new SDK Protocol, `ai_os_sdk.contracts.PackContextReceiver` (`bind_pack_context(context: Any) -> None`), lets any zero-arg-constructible entrypoint receive a real context once, before first use — the generalized form of the lazy-build workaround all five real pack agents already use, instead of a sixth hand-rolled copy. `ai_os_kernel.capability_manager.pack_contract.PackContext` is extended additively (exactly as its own docstring already anticipated) with `llm`/`prompts`/`tools`, backed by the step 6a adapters; a new `ai_os_kernel.sdk_adapters.pack_context.build_pack_context` constructs one from a set of declared permissions, granting `llm`+`prompts` only on `llm:invoke` and `tools` only on `sandbox:execute` — enforced per-entrypoint (never the pack's aggregate permissions), proven against the real manifest's own asymmetric declarations (`qa-test`: tools only; `architecture`: llm+prompts only; `build`: all three). Proven end to end against a real test entrypoint, not a real migrated agent (deliberately out of this step's scope): real `EntrypointLoader` construction, a real `isinstance(loaded, PackContextReceiver)` check, real `bind_pack_context()`, then a genuine Echo LLM completion, prompt render, and sandboxed command through the injected context. **No blocker found** — `EntrypointLoader`'s zero-argument constraint is resolved cleanly by a caller-side check, no new capability demanded of it. `PackContext` remains Kernel-side for now — formally relocating the whole entry-point contract into `ai_os_sdk` stays step 7's own job, deliberately not pulled forward. Full record: `platform_sdk_v1_scope.md` §6h.
- **Step 7 (done):** `pipeline.py` relocated from `capability_packs/software-engineering/src/ai_os_pack_software_engineering/pipeline.py` to `tests/integration/_delivery_pipeline.py` — test-harness workflow composition, never pack-facing capability, imported only by two Kernel-side tests. Verified by a real wheel build (`python -m uv build --package ai-os-pack-software-engineering`): the resulting `.whl`'s own file list no longer contains `pipeline.py`, and a full `src/` grep confirms zero remaining `sqlalchemy` imports in pack source. Both relocated tests re-run and pass unchanged from the new location, including the real, live Docker-backed one (this session's daemon was reachable). `ContextService` boundary models (`ai_os_sdk.models.context`) and Protocol (`ai_os_sdk.contracts.context_service`) landed, narrowed against the real, already-reduced Kernel `context_manager.models` shape — a genuine, dated reconciliation decision recorded even though none existed for this interface before this step — with structural compatibility proven against the real `DefaultContextManager` via `isinstance`. The full entry-point contract (`PackContext`/`CapabilityPack`/`PackRegistration`/`HealthReport`) relocated from `ai_os_kernel.capability_manager.pack_contract` into `ai_os_sdk.contracts.capability_pack`, additively; the Kernel's own module is now a real re-export (proven by identity, not just import success), so real pack source needed zero changes. **A real, discovered layering correction, recorded rather than silently applied:** `PackContext`/`PackRegistration` reference other Protocols by nature and so land in `contracts/`, not `models/` as an earlier docstring speculatively planned — corrected in the same step that found the conflict. **No blocker found.** Full record: `platform_sdk_v1_scope.md` §6i.
- **Step 8 (done):** `pack_contract_suite` check 7 — `ai_os_sdk.testing.forbidden_imports`, a pure AST import-scanner over a pack's own `src/` tree, checking five denylisted categories (`ai_os_kernel`, `ai_os_services`, another pack's own package, provider SDKs, database drivers) plus a sixth read deliberately broader than the literal spec text (any direct HTTP client import, since "to a provider" cannot be determined from imports alone). A documented, expiring waiver mechanism (`ai_os_sdk.testing.waiver`) reads a real, loud YAML file checked into a pack's own root (`pack_contract_waiver.yaml`, sibling to `manifest.yaml`) naming an exact `expires_at_step` and the **exact** dotted modules it covers — a new violation in any unlisted module still fails immediately, proven by test. Running the real scanner against the real Software Engineering pack found **44 real `ai_os_kernel` imports across exactly 6 modules** (5 agents + `pack.py`); the pack's own real waiver lists exactly those six, expiring at step 14. **Proven both ways by an automated regression test, not just a manual run**: a synthetic pack with a real `ai_os_kernel` import fails check 7 with no waiver present, and passes once a matching waiver is added. Wired into CI via `scripts/check_import_boundaries.py`, landing in a gate `.github/workflows/ci.yml` already had prepared (`hashFiles(...) != ''`) — zero workflow-logic change needed. A real, discovered portability bug (emoji report markers crashing with `UnicodeEncodeError` on Windows' default console codepage) was found and fixed before this step's own proof completed, with a regression test added. **No blocker found.** Full record: `platform_sdk_v1_scope.md` §6j.
- **Step 9 (done):** `qa-test` (`agents/verification.py`) migrated onto the real Platform SDK — the first of six migration steps and the first real (non-test-entrypoint) use of the step 6b `PackContextReceiver` injection mechanism. Now implements `ai_os_sdk.contracts.Agent` + `PackContextReceiver` only; sandbox access is exclusively `context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, ...)` via the injected `PackContext`, no `ai_os_kernel` import anywhere. **Two real, load-bearing gaps found by this first real-world use, both resolved, neither silently:** (1) the real `inputs["context"]` object `AgentStepExecutor` sends is still `ai_os_kernel.context_manager.models.AssembledContext`, a different class from the SDK's own boundary model even after step 7's narrowing — resolved by duck-typing the context (`getattr(context, "items", None)`) instead of an `isinstance` check that would always be `False` against the real object; (2) `SqlAgentRegistry` never calls `bind_pack_context()` after resolving an entrypoint — discovered via a real, **unconditionally-run** integration test (`test_verification_agent_pack.py`, no API-key gate, since this agent makes no LLM call) that would otherwise have hit a real, live regression; the test itself now performs the binding, standing in for production wiring that does not exist yet (a natural candidate for a future step: `SqlAgentRegistry.resolve_agent()` checking `isinstance(loaded, PackContextReceiver)`). A related capability loss (the old `resolved.sandbox.python_command` introspection) was also found and resolved by having the caller build and keep its own sandbox reference. **No blocker in the injection mechanism's own design** — both findings are about a missing *caller* in one specific production path, not a defect in what step 6b built. **Proven: zero behavior change** — both real Delivery Pipeline end-to-end tests (deterministic + live Docker) pass unchanged through the migrated agent, byte-identical output. `qa-test` removed from `pack_contract_waiver.yaml` (6 modules/44 violations → 5 modules/40); check 7 now genuinely passes for that module, unwaived. Full record: `platform_sdk_v1_scope.md` §6k.
- **Step 9a (done, inserted):** `SqlAgentRegistry.resolve_agent`/`SqlToolRegistry.resolve_tool` now genuinely call `bind_pack_context()` for real, in production — closing step 9's own discovered gap once, here, instead of once per remaining migration step (10–13). After the existing `Agent`/`Tool` structural check, a new shared helper checks `isinstance(loaded, PackContextReceiver)` (a true no-op for every not-yet-migrated entrypoint) and, if it implements the Protocol, builds a real `PackContext` from *that row's own* `required_permissions` (never the pack's aggregate) via `build_pack_context`, injecting it before the entrypoint is ever returned. Both registry classes gained optional `llm_gateway`/`prompt_engine`/`sandbox` constructor parameters; `sandbox` defaults to a real `build_default_sandbox_executor()` (no equivalent default exists yet for `llm_gateway`/`prompt_engine`, left `None` until step 10 supplies them for real). `EntrypointLoader` itself is untouched. **`bootstrap.py` does not construct `SqlAgentRegistry` at all yet** — confirmed by search, so this change is risk-free to any real, running system today. `test_verification_agent_pack.py`'s manual `bind_pack_context()` workaround is gone entirely — both its tests now resolve and run `qa-test` through the real, unmodified `SqlAgentRegistry` with zero test-side compensation. Both of step 9's smaller findings reconfirmed: `AssembledContext` duck-typing is unrelated to this gap and stays correct; the `python_command` caller-keeps-reference pattern generalizes cleanly one layer up (the test now hands its own sandbox to the registry's own new `sandbox=` override). **Zero regression across all 8 files using either registry** — the four still-unmigrated agents (`architecture`, `build`, `requirements-analyst`, `documentation`) resolve and run exactly as before. **No blocker found.** Full record: `platform_sdk_v1_scope.md` §6l.
- **Step 10 (done):** `requirements-analyst` (`agents/requirements_analyst.py`) migrated onto the real Platform SDK — the second migration and the first needing real gateway injection (`context.llm`/`context.prompts`), a materially larger surface than qa-test's sandbox-only migration. Now implements `ai_os_sdk.contracts.Agent` + `PackContextReceiver` only, replicating `PromptedCompletionService.complete_from_prompt()`'s real render-then-complete logic (`prompts.render()` → build `LLMRequest` → `llm.complete()`) directly against the injected `PackContext`'s adapters, with zero `ai_os_kernel` import anywhere. **The one interaction this step was specifically scoped to test — the old lazy-build `asyncio.Lock()` vs. injection via `bind_pack_context()` — resolved as an obsolescence, not a conflict**: the lock only ever existed to guard a concurrent double-build race on first `execute()`, a race that cannot occur once `bind_pack_context()` is called synchronously, exactly once, by the resolver before the entrypoint is ever handed to a caller (true since step 9a); the lock and lazy-build method are deleted outright, proven safe by a new concurrent-execution test with no lock at all. **Two further real findings, both resolved:** (1) a previously-latent circular import between `ai_os_kernel.workflow_engine.registry` (step 9a's own added import) and `ai_os_kernel.sdk_adapters.pack_context`, undetected by step 9a's own tests only because of their particular import order — fixed with a function-local deferred import, verified safe across 4 distinct import orderings; (2) `SqlAgentRegistry`'s own deterministic (no-live-call) resolution test for this agent now genuinely requires a real (or Echo-backed) `llm_gateway`/`prompt_engine`, since the agent's declared `llm:invoke` permission now triggers step 9a's own real enforcement — correct, intended behavior, not a bug. **One accepted, documented capability loss**: migrated agents' real LLM completions are no longer recorded to `evaluation.llm_calls`, since Platform SDK v1.0.0 has no `Telemetry`/`TraceabilityService` surface at all (both explicitly deferred past v1.0.0) — a silent observability loss only, does not affect the agent's own visible output, recorded as input for a future SDK step. **Proven: zero behavior change** — pack-local suite passes unchanged (7 tests), and both the deterministic and live-tier pipeline tests exercising this agent pass/correctly-skip as before. `requirements-analyst` removed from `pack_contract_waiver.yaml` (5 modules/40 violations → 4/32); check 7 now genuinely passes for that module, unwaived. Full record: `platform_sdk_v1_scope.md` §6m.
- **Step 11 (done):** `architecture` (`agents/architecture.py`) migrated onto the real Platform SDK — the third migration, following step 10's own established pattern exactly (`Agent` + `PackContextReceiver`, injected `context.llm`/`context.prompts`, same render-then-complete replication). **No new tension between the lazy-build lock and injection** — already resolved as an obsolescence at step 10; this step confirms it generalizes to a second agent that used the identical pattern, not a fresh finding. **The one real, new thing this step surfaced: `architecture` is the first migrated agent actually chained into `se.delivery_pipeline`** (unlike `qa-test`/`requirements-analyst`, neither of which is pipeline-chained) — so both real Delivery Pipeline end-to-end tests' own `InMemoryAgentRegistry` construction broke immediately (`service_factory=` no longer exists), caught by running the tests, not discovered later. **Fixed**: both test files gained an `_architecture_agent_with_prompt()` helper mirroring the already-established `_test_agent_with_sandbox()` zero-arg-construct-then-`bind_pack_context()` pattern; the live tier's `SqlAgentRegistry(engine)` call also needed real `llm_gateway`/`prompt_engine` objects supplied, the same real, discovered consequence of step 9a's enforcement step 10 already proved for `requirements-analyst`, now proven again for the four-agent pipeline trigger. Both real Delivery Pipeline end-to-end tests (deterministic + live Docker) pass after the fix. **The `evaluation.llm_calls` capability loss recurs** — confirming it is structural to v1.0.0 rather than a one-off (this is the second LLM-calling agent to lose it, with `build`/`documentation` to follow at steps 12/13) — now tracked visibly in `feature_inventory.md`'s new §6a "Known Regressions" section rather than left buried in per-step reports. `architecture` removed from `pack_contract_waiver.yaml` (4 modules/32 violations → 3/24); check 7 now genuinely passes for that module, unwaived. Full record: `platform_sdk_v1_scope.md` §6n.
- **Step 12 (done):** `build` (`agents/build.py`) migrated onto the real Platform SDK — the fourth migration, and the first needing real gateway **and** tool injection together (`context.llm`/`context.prompts`/`context.tools`). Its LLM call replicates the identical render-then-complete logic `requirements-analyst`/`architecture` already use; its write now calls `context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, ...)`, the identical Tool injection path `qa-test` (step 9) already proved, now proven a second time for an agent that also makes an LLM call. **The lock is not fully obsolete here — a real, discovered nuance neither step 10 nor step 11 needed to consider**: it still guards lazy working-directory creation, a concern unrelated to LLM composition and not resolved by `PackContext` injection at all — two concurrent first `execute()` calls with no lock would each create their own, unrelated directory instead of sharing one. Proven by a new concurrent-execute test asserting every call shares one directory. **A real, discovered tension using `ToolInvoker` instead of direct sandbox construction — the one this step's own approved framing asked to watch for**: the migrated agent can no longer dynamically ask its own sandbox for its `python_command` (`sys.executable`/`python3`, genuinely backend-specific), since a generic `ToolInvoker` carries no such concept by design. **Resolved** with a constructor default (`("python3",)`, matching `DockerSandbox`, the real, documented system-wide default backend), overridable by a caller that knows better — the same "optional constructor override, always the default in real dispatch" shape every agent in this pack already uses. **A real, narrow regression risk recorded, not silently accepted**: the old code's dynamic lookup was always correct regardless of the active backend; the new default is only correct for whichever backend is active today — if `AIOS_SANDBOX_BACKEND=local` is ever set without also updating this default, the write step fails with a real, clear execution error (interpreter not found), not a silent wrong write. Tracked as a second, distinct entry in `feature_inventory.md`'s §6a "Known Regressions." **Proven: zero behavior change** — pack-local suite passes (15 tests, up from 11), both real Delivery Pipeline end-to-end tests pass after the same construction fix step 11 established, and the **live Docker pipeline test genuinely wrote a real file inside a real container through the new `ToolInvoker` path** — this pack's most important proof of the migration, since Build is the pipeline's real file-writer. `build` removed from `pack_contract_waiver.yaml` (3 modules/24 violations → 2/13); check 7 now genuinely passes for that module, unwaived. Full record: `platform_sdk_v1_scope.md` §6o.
- **Step 12a (done, inserted):** closes the real `python_command` regression step 12 recorded rather than silently accepted. **The investigation weighed two real options**: (a) expose the real backend's `python_command` through `available_tools()`/`ToolDescriptor`, rejected because the descriptor is a frozen, shared SDK constant today (making it instance-aware needs a new model field *and* forces every caller to synchronously query and rebuild its own command with the extracted fact); (b) have `platform.sandbox.run_command` resolve the interpreter internally — chosen, reinterpreted narrowly as one well-known placeholder token rather than open-ended templating. **The fix**: `ai_os_sdk.contracts.tool_invoker.PLATFORM_PYTHON_INTERPRETER`, a new SDK-level token a caller writes in place of a literal interpreter path; `ToolInvokerAdapter.invoke()` — the one place still holding the real `SandboxExecutor` — expands every occurrence into that instance's own real `python_command` before dispatch. `build.py`'s own `python_command` constructor parameter and default (step 12's own first attempt) are removed entirely — no default to keep in sync, no backend knowledge in pack code at all, restoring the pre-migration "always automatically correct" guarantee exactly, not merely documenting the gap better. **Proven against both real backends, not just the currently-active default**: a real, actually-executed `LocalSubprocessSandbox` (3 new tests) and a real, live `DockerSandbox` (2 new tests in a new file, including a real file genuinely written end to end through the substituted interpreter). **Zero regression** across every already-migrated agent (`qa-test`, `requirements-analyst`, `architecture`, `build`) and both real Delivery Pipeline end-to-end tests — none of the first three ever referenced `python_command` or this token, so none needed touching. `documentation.py`'s own pre-migration write path uses the identical `self.sandbox.python_command` pattern `build.py` used to — confirmed to need the identical fix from the start when step 13 migrates it, not a fresh discovery. `feature_inventory.md`'s §6a second Known Regression entry marked ✅ RESOLVED. Full record: `platform_sdk_v1_scope.md` §6p.
- **Step 13 (done):** `documentation` (`agents/documentation.py`) migrated onto the real Platform SDK — the fifth and final agent migration. Implements `ai_os_sdk.contracts.Agent` + `PackContextReceiver` only, imports nothing from `ai_os_kernel`. Its LLM call replicates the identical render-then-complete logic every other migrated agent uses; its write uses `PLATFORM_PYTHON_INTERPRETER` (the step 12a fix) **from the start** — no repeated tension, confirming that fix genuinely generalizes rather than being a one-off patch for `build` alone. **A further, confirming data point on the lock-obsolescence finding, the clearest yet**: this agent needs no lock at all — unlike `build` (which kept a narrower lock for lazy working-directory creation), `documentation` lazily builds/creates nothing, reusing the caller-supplied `workingDirectory` directly. Across the four LLM-calling/tool-using agents this pack has, the lock finding now spans a complete, evidenced range: fully obsolete and removed (`requirements-analyst`, `architecture`), narrower and kept (`build`), never needed at all (`documentation`). **A real, discovered correction to this step's own initial approved framing, surfaced and fixed rather than silently forced through**: the task's own description stated the waiver "should leave the waiver empty, since all 5 agents will now be migrated" — checking this against the real waiver file and the real scanner found this imprecise: `pack.py` (this pack's own `CapabilityPack` entry point) was never one of the 5 agents; migrating it is step 14's own explicitly scoped job. The waiver now covers exactly `pack.py` (1 module, 1 real violation, down from 2 modules/13 violations) — check 7 passes with a real, correctly-shrunk waiver, not a falsely-empty one, which is the accurate signal that step 14 can now retire it entirely. **Proven: zero behavior change** — pack-local suite passes (15 tests, up from 12), both real Delivery Pipeline end-to-end tests pass with the **entire four-agent pipeline now migrated onto the Platform SDK**, and the **live Docker pipeline test genuinely wrote a real documentation artifact inside a real container** through the new `ToolInvoker` path. `documentation` removed from `pack_contract_waiver.yaml` (2 modules/13 violations → 1/1); check 7 now genuinely passes for that module, unwaived. The `evaluation.llm_calls` capability loss reaches its full, final, permanent extent (all 4 LLM-calling agents) — `feature_inventory.md`'s §6a now marks it expected-to-stay-open rather than still-growing. Full record: `platform_sdk_v1_scope.md` §6q.
- **Step 14 (done):** `pack.py` migrated onto the real Platform SDK — the sixth and final module in this pack, closing it out entirely. `SoftwareEngineeringPack` now implements `ai_os_sdk.contracts.CapabilityPack`, importing `PackContext`/`PackRegistration`/`HealthReport` from `ai_os_sdk.contracts` directly instead of the Kernel's own compatibility re-export — a genuinely zero-behavior-change swap, since step 7 already made that Kernel module a real re-export of the identical SDK objects (proven by identity in step 7's own record). **Zero `ai_os_kernel` imports remain anywhere in this pack's own source tree, for the first time in this project's history** — the real scanner returns an empty violation list, pack-wide. `pack_contract_waiver.yaml` is **deleted outright**, not merely emptied; `scripts/check_import_boundaries.py` (unmodified) now reports `[PASS] No unwaived forbidden imports.` with no waiver file present at all — check 7 enforces unconditionally, with zero waivers and zero exceptions, for the first time in this project's history. The waiver *mechanism* itself remains real, tested, reusable infrastructure for a future pack's own genuine transition period — only this pack's own usage of it ends (`test_waiver.py`'s own real-file proof rewritten to prove the "no waiver" default against this pack's now-absent path, not deleted). **The Capability Pack growth gate is correspondingly LIFTED** — a genuine, dated revision of that gate's own previously-recorded "only after step 15" condition (`docs/process/standing_rules.md`, which preserves the original text verbatim for the historical record), made because the gate's real substance ("a real, migrated, and verified pack, not merely an importable SDK") is now true; step 15's own remaining checks continue as scheduled, real work, no longer a precondition for this specific gate. `capability_pack_contract.md`'s dated exception language retired, replaced with a plain statement of permanent compliance; `feature_inventory.md` modules 29 (SE Pack — Agents), 32 (Project Intelligence), 33 (Voice/Jarvis), and 34 (Benchmarking) all unfrozen. Three further docs referencing the now-stale gate were swept for consistency: `capability_pack_development_guide.md`, `agents.md`, `overview.md`. **Proven: zero behavior change** — a real wheel build still succeeds, both real Delivery Pipeline end-to-end tests pass unchanged, pack-local + platform_sdk suites (122 passed) and the broader registry/SDK-contract regression sweep (47 passed, 4 correctly skipped) show zero regression. Full record: `platform_sdk_v1_scope.md` §6r.
- **Step 15 (done, final):** built the genuinely remaining `pack_contract_suite` checks — 2 (entry-point resolution), 3 (I/O-model matching), 4 (workflow step resolution), 5 (trust-tier consistency), 6 (permission vocabulary), 8 (prompt existence), 9 (clean activation) — **seven checks, not the "remaining 8" this document itself said through step 14**, a discovered arithmetic correction: `platform_sdk.md` §9 names checks 2-9 as eight items total, one of which (check 7) already shipped at step 8; check 1 was never "remaining," already real elsewhere via the Kernel's own Manifest Loader. Built as `ai_os_sdk.testing.pack_contract_suite` (new module), plus a unified async `run_pack_contract_suite()` orchestrator that also wraps check 7 unchanged and reimplements check 1 (the SDK's dependency-floor rule forbids importing the Kernel's loader; re-validates the identical schema with the same `jsonschema.Draft202012Validator` instead, taking `schema_path` as an explicit caller-supplied argument — the schema file is not packaged into the SDK's own wheel). **Every check proven against the real, fully-migrated Software Engineering pack, not asserted**: `tests/contract/test_pack_contract_suite.py` (new; activates the already-prepared, zero-`ci.yml`-change CI gate at `hashFiles('tests/contract/**')`) — 20 tests, one exercising each check against the real pack plus a synthetic-negative case per check, and two exercising the full orchestrator together — **all 9 checks pass genuinely**, zero waivers. **A real design correction found while building check 3**: comparing an agent's real `output_schema` attribute to its declared model's `model_json_schema()` for full equality fails for all 5 real agents (the hand-authored `output_schema` is a deliberately separate, stricter schema than Pydantic's own generated one) — compares field names and `required` set instead, which is what the codebase's real invariant actually is. **One further real, discovered gap fixed along the way, found because "dependencies satisfied" required actually reading `pyproject.toml`, which check 7's AST scanner never does**: the pack's own `pyproject.toml` still declared a runtime dependency on `ai-os-kernel` (stale since step 14 never updated it) and none on `ai-os-sdk` (the one real runtime dependency `pack.py` has needed since step 14) — fixed; `ai-os-kernel` remains available to the pack's own tests via the shared workspace dev venv. **Proven: zero regression** — pack-local suite (61 passed), `tests/unit/platform_sdk/` (61 passed), both real Delivery Pipeline end-to-end tests re-run fresh (2 passed, 1 correctly skipped), root suite **929 passed, 11 skipped, 0 failed** (909 → 929, exactly the 20 new `tests/contract/` tests, nothing else moved), `mypy --strict` clean across 344 source files, `ruff check`/`format` clean. **Platform SDK v1.0.0 is formally closed out — the codebase is ready to resume normal feature development.** Full closeout record (what v1.0.0 delivers, what remains deliberately deferred): `platform_sdk_v1_scope.md` §6s/§10.

Verified fresh after step 2: `pytest -q` (root suite) → **803 passed, 11 skipped, 0 failed** (unchanged); `pytest -q capability_packs/software-engineering/tests/` → **46 passed** (unchanged); `pytest -q platform_sdk/tests/` → **65 passed**; `mypy --strict` over the full configured scope → **0 errors, 295 source files**; `ruff check .` and `ruff format --check .` → clean.

Verified fresh after step 6a: `pytest -q` (root suite) → **852 passed, 11 skipped, 0 failed** (829 → 852, the 23 new `sdk_adapters` tests, unchanged skips); `pytest -q capability_packs/software-engineering/tests/` → **46 passed** (unchanged); `mypy --strict` over the full configured scope → **0 errors, 323 source files**; `ruff check .` and `ruff format --check .` → clean; `git status` confirms only `kernel/pyproject.toml` and `uv.lock` modified among pre-existing tracked files.

Verified fresh after step 6b: `pytest -q` (root suite) → **864 passed, 11 skipped, 0 failed** (852 → 864, the 12 new `pack_context`/`pack_context_injection` tests, unchanged skips); `pytest -q capability_packs/software-engineering/tests/` → **46 passed** (unchanged); `mypy --strict` over the full configured scope → **0 errors, 328 source files**; `ruff check .` and `ruff format --check .` → clean; `git status` confirms only `pack_contract.py`, `sdk_adapters/__init__.py`, and `contracts/__init__.py` modified among pre-existing tracked files.

Verified fresh after step 7: `pytest -q` (root suite) → **869 passed, 11 skipped, 0 failed** (864 → 869, the 5 new `test_context_service_and_capability_pack.py` tests, unchanged skips — the relocated pipeline tests moved, not multiplied); `pytest -q capability_packs/software-engineering/tests/` → **46 passed** (unchanged); a real `python -m uv build --package ai-os-pack-software-engineering` followed by wheel inspection confirms `pipeline.py` absent from the shipped `.whl`; a full `src/` grep confirms zero remaining `sqlalchemy` imports in pack source; `mypy --strict` over the full configured scope → **0 errors, 332 source files**; `ruff check .` and `ruff format --check .` → clean; `git status` shows a clean rename (`pipeline.py` → `tests/integration/_delivery_pipeline.py`) plus the expected modified/new files, no stray build artifacts.

Verified fresh after step 8: `pytest -q` (root suite) → **898 passed, 11 skipped, 0 failed** (869 → 898, the 29 new `forbidden_imports`/`waiver`/`check_import_boundaries` tests, unchanged skips); `pytest -q capability_packs/software-engineering/tests/` → **46 passed** (unchanged); the real scanner run against the real pack finds exactly 44 violations across exactly 6 modules, matching the waiver file exactly; `mypy --strict` over the full configured scope (now including `scripts/`) → **0 errors, 339 source files**; `ruff check .` and `ruff format --check .` → clean.

Verified fresh after step 9: `pytest -q` (root suite) → **899 passed, 11 skipped, 0 failed** (898 → 899); `pytest -q capability_packs/software-engineering/tests/` → **48 passed** (46 → 48, the 2 new tests in `test_verification_agent.py`); `pytest -q tests/unit/platform_sdk/` → **61 passed** (60 → 61, the 1 new test in `test_forbidden_imports_check.py`); the real scanner confirms `agents.verification` contributes zero violations and the remaining 5 modules/40 violations match the reduced `pack_contract_waiver.yaml` exactly; `mypy --strict` over the full configured scope → **0 errors, 339 source files** (unchanged file count — no new source modules, only edits); `ruff check .` and `ruff format --check .` → clean; `git status` shows only the expected files modified, no stray artifacts.

Verified fresh after step 9a: `pytest -q` (root suite) → **904 passed, 11 skipped, 0 failed** (899 → 904, the 5 new `test_registry_pack_context_injection.py` unit tests, unchanged skips); `pytest -q capability_packs/software-engineering/tests/ tests/unit/platform_sdk/` → **109 passed** (48 + 61, unchanged from step 9); all 8 files using `SqlAgentRegistry`/`SqlToolRegistry` re-run together — 33 passed, 5 correctly skipped (opt-in live), zero regression for the 4 still-unmigrated agents; the real Delivery Pipeline end-to-end tests (deterministic + live Docker) still pass with `qa-test` resolved through the registry with zero manual binding; `mypy --strict` over the full configured scope → **0 errors, 340 source files** (one new test module); `ruff check .` and `ruff format --check .` → clean; `git status` shows only the expected files modified.

Verified fresh after step 10: `pytest -q` (root suite, `testpaths = ["tests"]` — does not include `capability_packs/` at all, confirmed via `pyproject.toml`) → **904 passed, 11 skipped, 0 failed** (unchanged from step 9a — the only root-visible change this step is `test_forbidden_imports_check.py`'s two renamed-in-place tests, a net-zero count change; the pack-local `test_requirements_analyst.py` tests are outside the root suite's own `testpaths` and counted separately below); `pytest -q capability_packs/software-engineering/tests/` → **51 passed** (48 → 51, the 3 net new tests in `test_requirements_analyst.py`: 7 replacing the prior 4); `pytest -q tests/unit/platform_sdk/` → **61 passed** (unchanged from step 9a); `pytest -q tests/integration/workflow_engine/test_requirements_analyst_agent_pack.py` → **1 passed, 1 skipped** (deterministic resolution now passes with an Echo-backed `llm_gateway`/`prompt_engine`; live-tier correctly skips, no API key set); the real scanner confirms `agents.requirements_analyst` contributes zero violations and the remaining 4 modules/32 violations match the reduced `pack_contract_waiver.yaml` exactly; `mypy --strict` over the full configured scope → **0 errors, 340 source files** (unchanged file count — no new source modules, only edits); `ruff check .` and `ruff format --check .` → clean; `git status` shows only the expected files modified, no stray artifacts.

Verified fresh after step 11: `pytest -q` (root suite) → **903 passed, 1 failed, 11 skipped** — the 1 failure (`test_local_adapter.py::test_complete_maps_a_real_successful_response_end_to_end`) is a pre-existing, already-documented local-HTTP-server flake (§3 module 42's own tracked flake family), confirmed unrelated to this step by re-running that file alone (22 passed, 0 failed); total collected (915) numerically unchanged from step 10, since this step added no new test functions under the root suite's own `testpaths`; `pytest -q capability_packs/software-engineering/tests/` → **54 passed** (51 → 54, net +3: `test_pack.py`'s 6 `service_factory`-based architecture tests removed, `test_architecture_agent.py`'s 7 new ones added); `pytest -q tests/unit/platform_sdk/` → **61 passed** (unchanged); `pytest -q tests/integration/workflow_engine/test_architecture_agent_pack.py` → **1 passed, 1 skipped**; both real Delivery Pipeline end-to-end tests (deterministic + live Docker) → **2 passed, 1 skipped** (fixed this step — see above); zero regression across all `SqlAgentRegistry`/`SqlToolRegistry`-backed tests re-run together → **29 passed, 5 correctly skipped**; the real scanner confirms `agents.architecture` contributes zero violations and the remaining 3 modules/24 violations match the reduced `pack_contract_waiver.yaml` exactly; `mypy --strict` over the full configured scope → **0 errors, 341 source files** (one new test module); `ruff check .` and `ruff format --check .` → clean.

Verified fresh after step 12: `pytest -q` (root suite) → **904 passed, 11 skipped, 0 failed** — numerically identical to steps 10/11's own count (this step added no new test functions under the root suite's own `testpaths`), and clean this run (step 11's own transient local-HTTP-server flake passed here); `pytest -q capability_packs/software-engineering/tests/` → **58 passed** (54 → 58, net +4: `test_build_agent.py` grew from 11 to 15 tests); `pytest -q tests/unit/platform_sdk/` → **61 passed** (unchanged); `pytest -q tests/integration/workflow_engine/test_build_agent_pack.py` → **1 passed, 1 skipped**; both real Delivery Pipeline end-to-end tests → **deterministic 1 passed, 1 skipped; live Docker 1 passed** — a real file genuinely written inside a real container through the new `ToolInvoker` sandbox path; zero regression across all `SqlAgentRegistry`/`SqlToolRegistry`-backed tests re-run together → **29 passed, 5 correctly skipped**; the real scanner confirms `agents.build` contributes zero violations and the remaining 2 modules/13 violations match the reduced `pack_contract_waiver.yaml` exactly; `mypy --strict` over the full configured scope → **0 errors, 341 source files** (unchanged — no new source modules); `ruff check .` and `ruff format --check .` → clean.

Verified fresh after step 12a: `pytest -q` (root suite) → **909 passed, 11 skipped, 0 failed** (904 → 909, the 5 new tests — 3 in `test_tool_invoker_adapter.py`, 2 in the new `test_tool_invoker_adapter_docker.py` — all under `tests/`, unchanged skips); `pytest -q tests/unit/kernel/sdk_adapters/test_tool_invoker_adapter.py` → **13 passed** (10 → 13); `pytest -q tests/integration/sandbox/test_tool_invoker_adapter_docker.py` → **2 passed** (new file, real Docker daemon); `pytest -q capability_packs/software-engineering/tests/` → **58 passed** (unchanged); zero regression across all `SqlAgentRegistry`/`SqlToolRegistry`-backed tests plus both real Delivery Pipeline end-to-end tests re-run together → **44 passed, 5 correctly skipped**; `mypy --strict` over the full configured scope → **0 errors, 342 source files** (one new test module); `ruff check .` and `ruff format --check .` → clean.

Verified fresh after step 13: `pytest -q` (root suite) → **909 passed, 11 skipped, 0 failed** — numerically identical to step 12a's own count (this step added no new test functions under the root suite's own `testpaths`, only pack-local ones); `pytest -q capability_packs/software-engineering/tests/` → **61 passed** (58 → 61, net +3: `test_documentation_agent.py` grew from 12 to 15 tests); `pytest -q tests/unit/platform_sdk/` → **61 passed** (unchanged); `pytest -q tests/integration/workflow_engine/test_documentation_agent_pack.py` → **1 passed, 1 skipped**; both real Delivery Pipeline end-to-end tests → **deterministic 1 passed, 1 skipped; live Docker 1 passed** — a real documentation artifact genuinely written inside a real container, the full four-agent pipeline now migrated; zero regression across all `SqlAgentRegistry`/`SqlToolRegistry`-backed tests re-run together → **29 passed, 5 correctly skipped**; the real scanner confirms `agents.documentation` contributes zero violations and the waiver's one remaining module (`pack.py`) matches exactly; `scripts/check_import_boundaries.py` exits 0 with exactly 1 module/1 violation waived; `mypy --strict` over the full configured scope → **0 errors, 342 source files** (unchanged — no new source modules); `ruff check .` and `ruff format --check .` → clean.

Verified fresh after step 14: the real scanner (`scan_pack_source`) confirms **zero violations across the entire pack** — `agents.pack` (the last remaining violation) now contributes none; `scripts/check_import_boundaries.py` exits 0 with **zero waivers active anywhere in this pack** (no waiver file present at all); a real wheel build (`uv build --package ai-os-pack-software-engineering`) succeeds; both real Delivery Pipeline end-to-end tests → **deterministic 1 passed, 1 skipped; live Docker 1 passed** — unchanged; `pytest -q capability_packs/software-engineering/tests/ tests/unit/platform_sdk/` → **122 passed** (unchanged); zero regression across all `SqlAgentRegistry`/`SqlToolRegistry`-backed tests plus `test_context_service_and_capability_pack.py`/`test_kernel_satisfies_sdk_contracts.py` re-run together → **47 passed, 4 correctly skipped**; `pytest -q` (root suite) → **909 passed, 11 skipped, 0 failed** — numerically unchanged from step 13 (one test added in `test_waiver.py`, one net-removed in `test_forbidden_imports_check.py`, canceling out); `mypy --strict` over the full configured scope → **0 errors, 342 source files** (unchanged — no new source modules); `ruff check .` and `ruff format --check .` → clean.

Verified fresh after step 15 (final): a direct run of `run_pack_contract_suite()` against the real, on-disk Software Engineering pack shows **all 9 checks passing** (real detail lines for each — e.g. check 6 reading a genuine 21-permission closed vocabulary directly from the schema, check 9 reporting `activate() returned a PackRegistration with agents: ['architecture']`); `pytest -q tests/contract/` → **20 passed** (new); `pytest -q capability_packs/software-engineering/tests/` → **61 passed**; `pytest -q tests/unit/platform_sdk/` → **61 passed**; `scripts/check_import_boundaries.py` → `[PASS]`, unchanged; both real Delivery Pipeline end-to-end tests re-run fresh → **2 passed, 1 skipped**, unchanged; `pytest -q` (root suite) → **929 passed, 11 skipped, 0 failed** (909 → 929, exactly the 20 new `tests/contract/` tests, nothing else moved — genuine, accounted-for growth, not drift); `mypy --strict` over the full configured scope → **0 errors, 344 source files** (342 → 344, the two new modules: `pack_contract_suite.py` and its test file); `ruff check .` and `ruff format --check .` → clean.

Verified fresh after the post-Step-15 confirmation pass (commit `3289baf`): a real, discovered bug fixed — `check_1_manifest_is_valid`/`run_pack_contract_suite()` used to raise an uncaught exception on a malformed manifest instead of reporting a clean failure; fixed, plus a new cross-boundary parity test (`test_manifest_check_agrees_with_kernel_loader.py`, 11 tests) proving `check_1_manifest_is_valid` and the real `ManifestLoader` agree on accept/reject for the real manifest and 9 synthetic variants; `pytest -q` (root suite) → **941 passed, 11 skipped, 0 failed** (929 → 941, exactly the 12 new tests — 11 parity + 1 orchestrator-resilience regression test); `mypy --strict`/`ruff` clean.

Verified fresh after this feature step (`requirements-analyst` chained into `se.delivery_pipeline`): `pytest -q tests/integration/workflow_engine/test_delivery_pipeline.py tests/integration/sandbox/test_delivery_pipeline_docker.py` → **2 passed, 1 skipped** — the full 5-agent chain, both tiers, with Requirements Analyst's own real output read back from Architecture's own persisted output, and the raw requirement traced three hops downstream into Build's own persisted `instruction`; `pytest -q tests/contract/` → **21 passed** (the full `pack_contract_suite` still passes against the modified `manifest.yaml`/`delivery_pipeline.yaml`); `pytest -q capability_packs/software-engineering/tests/` → **61 passed** (unchanged — no pack-local test file touched by this step); `pytest -q` (root suite) → **941 passed, 11 skipped, 0 failed** (unchanged from the confirmation pass — this step added no new test *functions* under the root suite's own `testpaths`, only modified existing ones and non-code doc files); `mypy --strict` → **0 errors, 345 source files** (unchanged — no new source modules, only edits); `ruff check .`/`ruff format --check .` → clean.

Verified fresh after the HTTP route step (`se.delivery_pipeline` reachable over real HTTP): `pytest -q tests/unit/kernel/routes/test_delivery_pipeline.py` → **6 passed** (auth boundary, no database); `pytest -q tests/integration/test_delivery_pipeline_route.py` → **2 passed** (real Postgres, no live credential — an honest `failed` outcome reaches through the real trigger); `tests/integration/test_delivery_pipeline_route_live.py` → **1 skipped** (no `AIOS_SECRET_LLM_ANTHROPIC_API_KEY` this session — opt-in, mirrors every other live test in this suite); both pre-existing Delivery Pipeline end-to-end tests re-run after the composition relocation → **2 passed, 1 skipped**, unchanged; `pytest -q` (root suite) → **949 passed, 12 skipped, 0 failed** (941 → 949 passed, 11 → 12 skipped — exactly the 9 new tests this step adds: 8 pass, 1 is the opt-in live skip); `mypy --strict` → **0 errors, 349 source files** (345 → 349: `kernel/src/ai_os_kernel/workflow_engine/delivery_pipeline.py`, `kernel/src/ai_os_kernel/routes/delivery_pipeline.py`, and their two new unit/integration test modules — `_delivery_pipeline_route_live.py` is a third new test module but is skip-collected identically); `ruff check .`/`ruff format --check .` → clean.

Verified fresh after the manifest → catalog installer step: `pytest -q tests/integration/capability_manager/test_manifest_catalog_installer.py` → **4 passed** (new file); `pytest -q tests/integration/workflow_engine/test_architecture_agent_pack.py tests/integration/workflow_engine/test_build_agent_pack.py tests/integration/workflow_engine/test_documentation_agent_pack.py tests/integration/workflow_engine/test_requirements_analyst_agent_pack.py tests/integration/workflow_engine/test_verification_agent_pack.py` → **6 passed, 4 skipped** (all 6 non-live-gated tests pass through the new installer path, zero regression); `pytest -q tests/integration/workflow_engine/test_delivery_pipeline.py tests/integration/test_delivery_pipeline_route_live.py tests/integration/test_delivery_pipeline_route.py` → **3 passed, 2 skipped**; `pytest -q tests/integration/capability_manager/test_pack_lifecycle.py tests/integration/sandbox/test_delivery_pipeline_docker.py` → **11 passed** (real Docker daemon); `pytest -q` (root suite) → **953 passed, 12 skipped, 0 failed** (949 → 953, exactly the 4 new tests); `mypy .` → clean, 351 source files; `ruff check .`/`ruff format --check .` → clean.

Verified fresh after the real pack discovery step: `pytest -q tests/integration/test_bootstrap_pack_discovery.py` → **4 passed** (new file); `pytest -q tests/integration/test_delivery_pipeline_route.py tests/integration/test_delivery_pipeline_route_live.py tests/unit/kernel/routes/test_delivery_pipeline.py tests/integration/test_workflows_route.py tests/integration/test_workflows_route_live.py tests/integration/test_bootstrap_pack_lifecycle.py tests/integration/test_packs_route.py tests/integration/test_bootstrap_prompted_agent_live.py tests/integration/test_bootstrap_workflow_trigger_live.py tests/unit/kernel/test_bootstrap.py tests/unit/kernel/routes/test_packs.py tests/unit/kernel/routes/test_workflows.py tests/unit/kernel/observability/test_middleware.py tests/unit/kernel/entrypoints/test_api.py` (every existing test touching `build_app()`/bootstrap) → **80 passed, 4 correctly skipped, 0 failed** — zero regression, confirmed by the fact that every one of these already sets `capability_pack_dirs=[]`; `pytest -q` (root suite) → **957 passed, 12 skipped, 0 failed** (953 → 957, exactly the 4 new tests); `mypy kernel/src kernel/alembic tests` → clean, 299 source files; `ruff check .`/`ruff format --check .` → clean.

Verified fresh after the health-check real-activation-status step: `pytest -q tests/integration/test_health_pack_activation.py` → **2 passed** (new file); `pytest -q tests/unit/kernel/health/ tests/unit/kernel/entrypoints/ tests/integration/test_bootstrap_pack_discovery.py tests/integration/test_bootstrap_pack_lifecycle.py tests/unit/kernel/test_bootstrap.py tests/integration/test_packs_route.py tests/unit/kernel/routes/` (every health/bootstrap/pack-route test) → **84 passed, 0 failed**; `pytest -q` (root suite) → **961 passed, 12 skipped, 0 failed** (957 → 961, exactly the 4 new tests); `mypy kernel/src kernel/alembic tests` → clean, 300 source files; `ruff check .`/`ruff format --check .` → clean. Real JSON captured directly from a live `TestClient` run confirms both the healthy and degraded response shapes quoted in the header above are genuine, not illustrative.

Verified fresh after the database hard-dependency health-check step: `pytest -q tests/integration/test_health_database_check.py` → **2 passed** (new file); `pytest -q tests/unit/kernel/health/ tests/unit/kernel/entrypoints/ tests/integration/test_health_pack_activation.py` → **20 passed, 0 failed** (both pre-existing real-database health tests re-verified unaffected); `pytest -q` (root suite) → **966 passed, 12 skipped, 0 failed** (961 → 966, exactly the 5 new tests); `mypy kernel/src kernel/alembic tests` → clean, 301 source files; `ruff check .`/`ruff format --check .` → clean.

Verified fresh after the Pack Health Collector step: `pytest -q tests/integration/capability_manager/test_health_poller.py` → **2 passed** (new file); `pytest -q tests/integration/capability_manager/ tests/unit/kernel/test_bootstrap.py tests/unit/kernel/health/ tests/unit/kernel/entrypoints/ tests/integration/test_health_pack_activation.py tests/integration/test_health_database_check.py tests/integration/test_bootstrap_pack_discovery.py tests/integration/test_bootstrap_pack_lifecycle.py tests/integration/test_packs_route.py` → **60 passed, 0 failed** (the regression suite that first caught the design flaw, now fully green after the fix); `pytest -q tests/integration/test_delivery_pipeline_route.py tests/integration/test_delivery_pipeline_route_live.py tests/integration/workflow_engine/ tests/integration/sandbox/test_delivery_pipeline_docker.py` → **68 passed, 7 skipped** (every pack-agent/pipeline test unaffected); `pytest -q` (root suite) → **968 passed, 12 skipped, 0 failed** (966 → 968, exactly the 2 new tests); `mypy kernel/src kernel/alembic tests` → clean, 303 source files; `ruff check .`/`ruff format --check .` → clean.

Verified fresh after the health-detail-surfacing step: `pytest -q tests/integration/test_health_pack_health_detail.py` → **2 passed** (new file); `pytest -q tests/integration/test_health_pack_activation.py tests/integration/test_health_database_check.py tests/unit/kernel/health/ tests/unit/kernel/entrypoints/ tests/integration/test_bootstrap_pack_discovery.py tests/integration/test_bootstrap_pack_lifecycle.py tests/integration/capability_manager/ tests/unit/kernel/test_bootstrap.py` → **56 passed, 0 failed**; `pytest -q` (root suite) → **970 passed, 12 skipped, 0 failed** (968 → 970, exactly the 2 new tests); `mypy kernel/src kernel/alembic tests` → clean, 304 source files; `ruff check .`/`ruff format --check .` → clean.

Verified fresh after the background health-polling-scheduler step: `pytest -q tests/integration/test_health_polling_loop.py` → **2 passed** (new file); `pytest -q tests/integration/test_health_pack_health_detail.py tests/integration/test_health_pack_activation.py tests/integration/test_health_database_check.py tests/unit/kernel/health/ tests/unit/kernel/entrypoints/ tests/integration/test_bootstrap_pack_discovery.py tests/integration/test_bootstrap_pack_lifecycle.py tests/integration/capability_manager/ tests/unit/kernel/test_bootstrap.py tests/integration/test_packs_route.py` → **64 passed, 0 failed**; `pytest -q tests/integration/test_delivery_pipeline_route.py tests/integration/test_delivery_pipeline_route_live.py tests/integration/workflow_engine/ tests/integration/sandbox/test_delivery_pipeline_docker.py` → **68 passed, 7 skipped**; `pytest -q` (root suite) → **972 passed, 12 skipped, 0 failed** (970 → 972, exactly the 2 new tests); `mypy kernel/src kernel/alembic tests` → clean, 305 source files; `ruff check .`/`ruff format --check .` → clean.

Verified fresh after the lease-reap background-scheduler step: `pytest -q tests/integration/test_lease_reap_loop.py` → **2 passed** (new file); `pytest -q tests/integration/workflow_engine/ tests/integration/test_health_polling_loop.py tests/unit/kernel/test_bootstrap.py tests/integration/test_bootstrap_pack_discovery.py tests/integration/test_delivery_pipeline_route.py tests/integration/test_delivery_pipeline_route_live.py tests/integration/sandbox/test_delivery_pipeline_docker.py` → **88 passed, 7 skipped**; `pytest -q` (root suite) → **974 passed, 12 skipped, 0 failed** (972 → 974, exactly the 2 new tests); `mypy kernel/src kernel/alembic tests` → clean, 306 source files; `ruff check .`/`ruff format --check .` → clean.

> **✅ HARD GATE LIFTED (product-owner decision, 2026-07-29) — a new agent or Capability Pack may now be added, subject to every other standing rule (scope discipline, documentation-first, real verification).** This revises the gate's own previously-recorded "only after step 15" condition (`docs/process/standing_rules.md` preserves the original text verbatim for the historical record) — the gate's real substance, "a real, migrated, and verified pack, not merely an importable SDK," is now satisfied: all 5 agents plus `pack.py` are migrated, and check 7 enforces unconditionally with zero waivers. **Step 15 itself is now also done (2026-07-30)** — see above and `platform_sdk_v1_scope.md` §6s/§10 for the full closeout. Platform SDK v1.0.0 is complete; the codebase is ready to resume normal feature development.

---

## 3. What Exists Now (compact)

- **Persistence & Workflow Engine core**: Postgres schema + Alembic migrations for `workflow_instances`/`workflow_events`/`workflow_leases`/`workflow_steps`/`approvals`; definition loading/validation; instance creation, state transitions, multi-step progression to completion; lease acquire/renew/release/reap; single-shot and run-to-completion composition; `list_steps`/`list_events` read accessors. → `history/002`, `003`.
- **Cross-cutting infra**: CI aligned with the real codebase; minimal Secrets Resolution (`EnvSecretProvider`, ADR-0024); real OpenTelemetry spans + one metric (console exporters). → `history/004`.
- **Catalog & evaluation schema**: all 6 `catalog` tables and all 6 `evaluation` tables exist, with FKs retrofitted per `data_model.md`. → `history/005`.
- **LLM Gateway**: `AnthropicAdapter` + `LocalAdapter`, `Router`/`DispatchingLLMGateway`, Retry & Fallback (Circuit Breaker, Backoff, Error Taxonomy), two independent budget ceilings (alias + workflow), `TraceContext`, Capability Negotiator matrix. Only `anthropic` registered by default. → `history/006`, `011`.
- **Prompt Engine**: Protocol + `InMemoryPromptEngine`/`SqlPromptCatalog`, `PromptedCompletionService`. No version resolution/Resolver/caching yet. → `history/006`.
- **Agent/Tool registries**: `AgentRegistry`/`ToolRegistry`, both in-memory and SQL-backed (`SqlAgentRegistry`/`SqlToolRegistry` resolve real catalog rows, gated on pack activation, via `EntrypointLoader`). → `history/007`.
- **Capability Manager**: minimal pack lifecycle (register/activate/deactivate), no discovery/upgrades/health/permissions yet. → `history/008`, `010`.
- **Security Manager**: bearer-token JWT auth, 4 permissions, fronting 9 HTTP routes — all narrower than their documented contracts. Not OIDC. → `history/009`.
- **Context Manager**: `WorkflowStateResolver` + `WorkflowStepOutputResolver`, Size & Token Budget Enforcer. 1 of 6 documented sources built. → `history/012`.
- **Retrieval**: `knowledge.documents`/`chunks` writer + keyword-search reader. No vector/hybrid search, no consumer yet. → `history/013`.
- **Sandbox & Tool execution**: `LocalSubprocessSandbox` (3/5 guarantees) and `DockerSandbox` (5/5, ADR-0016 Tier 1, proven live) both implement `SandboxExecutor`, each now declaring its own `python_command`; `SandboxedCommandTool` + `ToolStepExecutor` dispatch; `ai_os_kernel.sandbox.default_executor` resolves the real default from `AIOS_SANDBOX_BACKEND` (`"docker"` unless set to `"local"`). → `history/014`, `020`, `023`.
- **Software Engineering Capability Pack**: 5 real agents — Requirements Analyst, Architecture, Build, Test, and Documentation — all chained into one declared workflow (`se.delivery_pipeline`) via `WorkflowStepOutputResolver`, defaulting to `DockerSandbox`. → `history/015`–`019`, `023`, `024`.
- **Documentation reconciliation**: agent-catalog naming fixed (`architecture`/`qa-test`/`build`), doc-drift closed, shared Postgres test fixture (`tests/integration/_postgres_fixture.py`) skips cleanly without Docker. → `history/021`.
- **Feature/module/phase inventory**: tracked, weighted completion table — see `docs/19_roadmap/feature_inventory.md`. Update that document's own table at the end of every future step.
- **Prior step**: `capability_packs/analytics/` (stale, undocumented) deleted; `DockerSandbox` wired in as the SE pack's real default sandbox. → `history/023`.
- **Prior step**: the full integration suite (incl. the Docker-gated pipeline test) ran for real and passed; two real bugs found and fixed (a `python_command` mismatch, a hardcoded `sys.executable` in a test); Requirements Analyst Agent built. → `history/024`.
- **This step**: documentation consolidation audit — the docs are now self-sufficient for a zero-context LLM. 82 documents carry honest Implementation Status sections; stage status corrected everywhere; all 25 ADRs annotated; every TBD resolved; the dead `capability_packs/software_engineering/` duplicate removed. → `history/025`.

**Reading the architecture docs correctly (important, new this step):** AI_OS is documentation-first, so most `docs/03_architecture/` documents were written before their code and many describe subsystems that are 0% built. **Every one now carries an `## Implementation Status` section near the top — read it before assuming the document describes callable code.** `docs/DOCUMENTATION_INDEX.md` §2a enumerates the entirely-unbuilt set. `docs/process/folder_structure.md` is definitive on which directories actually exist (git tracks no empty directories, so most "planned" folders are absent from a fresh clone).

---

## 4. Current Blockers

Full detail (every gap, every reasoning) is preserved verbatim in `history/022_gap_analysis_and_blockers_snapshot.md`. The load-bearing ones, distilled:

- **✅ RESOLVED (2026-07-29) — the Capability Pack growth gate recorded 2026-07-28 is lifted.** The direct-Kernel-import exception it was built on no longer exists: `platform_sdk_v1_scope.md` step 14 migrated the last remaining module (`pack.py`) onto `ai_os_sdk`, so the Software Engineering pack has zero `ai_os_kernel` imports anywhere, and `pack_contract_suite` check 7 enforces this unconditionally (`pack_contract_waiver.yaml` deleted outright). A new agent or Capability Pack may now be added, subject to every other standing rule. This is a genuine, dated revision of the gate's own previously-recorded "only after step 15" condition, not a discovery that it was already satisfied — see `docs/process/standing_rules.md` for the full record (original text preserved verbatim). Recorded in the same three places the gate itself was: `capability_pack_contract.md`, `feature_inventory.md` module 27, and `docs/process/standing_rules.md`.
- **✅ RESOLVED (2026-07-30) — Platform SDK v1.0.0 (`platform_sdk_v1_scope.md`) is fully complete.** Step 15 (final) built the full, unconditional 9-check `pack_contract_suite`, proven against the real, fully-migrated Software Engineering pack with zero waivers. This was the last open item of the initiative `implementation_status.md` §6 and `feature_inventory.md` module 27 handed off as a prerequisite step. Full closeout: `platform_sdk_v1_scope.md` §6s/§10.
- **✅ RESOLVED (2026-07-30) — Requirements Analyst is now chained into `se.delivery_pipeline` as its own first step.** `delivery_pipeline.yaml` and (now) `ai_os_kernel.workflow_engine.delivery_pipeline`'s own `_STEP_SOURCES`/`_FIELD_SELECTORS` updated; both real Delivery Pipeline end-to-end tests re-verified passing with the new 5-step chain. This was the first normal feature step after the Platform SDK initiative closed.
- **✅ RESOLVED (2026-07-30) — `se.delivery_pipeline` is now reachable over real HTTP**, not only from a test file. `POST /api/v1/workflows/se.delivery_pipeline` (`ai_os_kernel.routes.delivery_pipeline`), gated by the existing `workflow:start` permission. The real composition (`tests/integration/_delivery_pipeline.py`) was promoted into real Kernel code (`ai_os_kernel.workflow_engine.delivery_pipeline`) so both the route and the existing integration tests share one real implementation, not two.
- **✅ RESOLVED (2026-07-30) — a real manifest → catalog installer now exists.** `ai_os_kernel.capability_manager.manifest_catalog_installer` derives real `catalog.agents`/`catalog.prompts`/`catalog.tools` rows from a pack's own `manifest.yaml`, wired into `SqlPackLifecycleRepository.register(..., pack_root=...)`. All 6 test files that used to hand-write raw-SQL seeding now call this real path instead. This was the recommended next step after the HTTP-route step.
- **✅ RESOLVED (2026-07-30) — real pack discovery is now wired into Kernel startup.** `ai_os_kernel.bootstrap._register_and_activate_discovered_packs`, called from `_lifespan`, genuinely registers (via the manifest → catalog installer) and activates every schema-valid pack `manifest_loader.scan()` finds — no manual `POST /api/v1/packs` call or test-harness seeding required for a real Kernel process. Idempotent across restarts; per-pack degrade-gracefully behavior for an invalid manifest or a genuine registration/activation error. This was the recommended next step after the manifest → catalog installer step.
- **✅ RESOLVED (2026-07-30) — the Health Service's `/health/ready` now reports a discovered pack's real activation status, not just filesystem-level schema validity.** `manifest_loader_check` reads real `catalog.packs.state` via `get_pack()` when a database is configured, distinguishing genuinely activated from stuck/failed from schema-invalid, and reports `degraded` with the real pack id + state for the stuck case. `HealthService` now supports both sync and async checks. This was the recommended next step after wiring real pack discovery into Kernel startup.
- **✅ RESOLVED (2026-07-30) — `health_lifecycle.md` §10's "hard vs. soft dependency" gap is decided and built: the database is hard, and `/health/ready` genuinely returns 503 when it is unreachable.** `ComponentStatus.critical`/`ReadinessReport`'s new `"not_ready"` value implement this generically; `database_check` is the one check marked critical today, backed by a real, timeout-bounded `SELECT 1`. Decision evidence: every functional route already independently 503s without a database. This was the recommended next step after the pack-activation health check.
- **✅ RESOLVED, narrowly (2026-07-30) — the Quality Gate Engine's smallest real slice exists: a genuinely blocking `quality_gate` step now halts `se.delivery_pipeline` on a real test failure.** `ai_os_kernel.workflow_engine.quality_gate.QualityGateStepExecutor`; see the header for the full record. The general engine (Gate Registry, pack-declared gates, `evaluation.gate_results` writer) remains unbuilt — this closes the module's most-cited *symptom* (the no-op step type), not the module itself. This was the recommended next step after the Lease Reaper background scheduler.
- **✅ RESOLVED (2026-07-30) — a failed quality gate now genuinely retries, bounded by both attempts and duration, before halting.** `WorkflowAdvanceRunner.run_to_completion` + `WorkflowInstanceService.retry_after_step_failure` (renamed from `retry_after_gate_failure` in the general-step-retry step below); `RetryPolicy` read at runtime for the first time. See the header for the full record. This was the recommended next step after the Quality Gate Engine's smallest real slice.
- **✅ RESOLVED (2026-07-30) — a second, distinct quality gate category (Static Analysis) proves the gate mechanism genuinely generalizes via configuration alone.** A new Lint Agent (`sandbox:execute` only) plus a second `quality_gate` step; zero changes to `QualityGateStepExecutor`/`DispatchingStepExecutor`/`WorkflowAdvanceRunner`. See the header for the full record, including the real, discovered `ruff` → `py_compile` tool pivot (a cross-sandbox-backend consistency requirement, not a mechanism defect). This was the recommended next step after the bounded quality-gate retry.
- **✅ RESOLVED (2026-07-30) — a genuinely failed step attempt now genuinely leaves a real `workflow_steps` trace, closing quality_gate_engine.md §9's own recording requirement.** `WorkflowInstanceService.advance` + a new `WorkflowInstanceRepository.record_failed_attempt`; see the header for the full record. This was the recommended next step after the second quality gate category.
- **✅ RESOLVED (2026-07-30) — the Pack Health Collector's smallest real slice exists: `catalog.packs.health` is now genuinely written, and a pack genuinely moves to `PackState.FAILED` after real, observed consecutive failures.** `ai_os_kernel.capability_manager.health_poller.poll_pack_health()` decides and implements capability_manager.md §9's own three-value policy (poll interval/timeout/consecutive-failure threshold); wired into `_lifespan`, one real poll per discovered pack per Kernel startup. Poll interval is decided but not yet enforced by a background scheduler (deliberately deferred, matching the Workflow Engine's own already-accepted "no full worker scheduler" gap). This was the recommended next step after the database hard-dependency health check.
- **✅ RESOLVED (2026-07-30) — general, error-category-driven step retry now exists: widened beyond quality gates to any step-executor exception that self-declares `retriable = True`.** `WorkflowAdvanceRunner._maybe_retry_failed_step` matches on the generic `retriable`/`step_id` attributes, never an exception's own type; `se.delivery_pipeline`'s own `_STEP_RETRY_TARGETS` gained a real `{"build": "build"}` entry. See the header for the full record, including the retriable-vs-not category split. `error_handling_retry.md` §4's own "different policies for different error categories" requirement is now genuinely met, in miniature — no longer a still-open item. This was the recommended next step after the failed-attempt observability fix.
- **Only one LLM provider (`anthropic`) is registered by default**; `LocalAdapter`/cross-provider fallback exist but are commented out in checked-in config.
- **Security Manager is pre-shared-secret JWT, not real OIDC** — not production-credible as-is.
- **No pack discovery, upgrade path, health monitoring, or permissions enforcement** in the Capability Manager.
- **`approvals` table has no writer** — no Human Approval Point execution path exists yet.
- **No multi-instance worker loop** — `run_to_completion`/`reap_once` exist for one instance/one bounded pass; nothing yet schedules either across many instances.
- **Context Manager has only 1 of 6 documented sources** (Knowledge/Memory/AI Context Packs/Runtime Config resolvers don't exist); no Filter/Ranker, no Context Audit Logger persistence.
- **No GitHub remote configured** for this repository (see §5) — `gh` CLI unavailable in this environment; a manual step is needed.
- **Docker Desktop availability is intermittent across sessions** in this development environment — see `CLAUDE.md`.

---

## 5. Git / Repository Status

This is now a git repository (`git init` this step), branch `main`, with one baseline commit capturing all work through the naming-reconciliation step. **No GitHub remote exists** — the `gh` CLI is unavailable in this environment, so pushing to GitHub requires a manual step from the product owner (create a remote repository, then `git remote add origin <url> && git push -u origin main`). The repository is otherwise ready to push as-is.

Per standing rule (`docs/process/standing_rules.md`), every step from now on ends with a commit; the hash is reported at the end of each step's report.

---

## 6. Recommended Next Small Step

**Done: general, error-category-driven step retry — widened beyond quality gates to any step-executor exception that self-declares `retriable = True` (2026-07-30)** — see the header for the full record.

**Recommended next: write real `evaluation.gate_results` rows from the now-recorded gate/step outcomes.** `workflow_steps`/`workflow_events` already carry every real attempt (passed, failed, retried) for both quality gates and, as of this step, `build` — genuinely useful raw material the Evaluation Engine (module 12, still schema-only) has never had a real producer for. This bridges into a second, currently-0%-code module rather than extending the Workflow Engine's own already-proven mechanism, so somewhat larger in scope than the last several steps; a smaller alternative, if preferred, is deciding and documenting `AgentRegistryError`/`ToolRegistryError`'s own left-undecided retriable split named in this step's own report (splitting each into a genuinely-transient and a genuinely-permanent exception type) rather than leaving it a named gap.

A separate, smaller, non-blocking follow-up: the prior steps completed the full documentation audit (see §7 and `history/026`), so there is no remaining doc-completeness debt to schedule.

---

## 7. Maintenance

Update this document at the end of every implementation step: refresh §2 (current stage) and §4 (blockers), re-state §6 with the next recommended step, and commit. If a step's own detail is large enough to warrant its own history entry, add a new numbered file to `docs/19_roadmap/history/` and a line to `history/INDEX.md` — do not let this document grow long again (see the big-file convention in `CLAUDE.md`).

**Also update `docs/19_roadmap/feature_inventory.md`'s own completion table (Section 5) and overall weighted percentage (Section 6) at the end of every step** — standing rule recorded 2026-07-28 in `CLAUDE.md` / `docs/process/standing_rules.md`.
