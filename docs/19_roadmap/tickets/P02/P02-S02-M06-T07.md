---
id: P02-S02-M06-T07
title: Budget enforcement
status: done
depends_on: []
evidence: [kernel/src/ai_os_kernel/llm_gateway/budget_enforcer.py, kernel/src/ai_os_kernel/llm_gateway/gateway.py, kernel/src/ai_os_kernel/bootstrap.py]
module_path: kernel/src/ai_os_kernel/llm_gateway
time_spent: 2h30m
---
**Goal:** Refuse a call that would exceed a declared budget (FR-022).
**Input:** Per-step and per-workflow budgets.
**Output:** BudgetExceededError. 4 of 5 checks real (was 2 of 5).

Added `PerScopeCountBudgetEnforcer`/`step_scope` (composite
workflow+step key — bare step_id would double-count across separate
instances); wired `step_token_budget_enforcer`/
`step_wall_time_budget_enforcer` into `DispatchingLLMGateway`,
accumulating already-real `usage.input_tokens+output_tokens`/
`latency_ms`, no new estimation. Closes 2 of `StepBudget`'s 4
dimensions; `max_tool_calls` stays genuinely unbuilt — `LLMRequest` has
no `tools` field at all yet. Also wired all 4 ceilings into
`_build_se_delivery_pipeline_registry` (had none — a real,
previously-disclosed scope limit reversed here since that is the one
gateway path this codebase's real pipelines actually use).
