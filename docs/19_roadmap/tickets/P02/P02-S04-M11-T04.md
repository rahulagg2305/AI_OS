---
id: P02-S04-M11-T04
title: Vector search
status: done
depends_on: [P02-S04-M11-T03]
evidence: [kernel/src/ai_os_kernel/retrieval/vector_search.py, tests/integration/retrieval/test_vector_search.py]
module_path: kernel/src/ai_os_kernel/retrieval
time_spent: 1h45m
---
**Goal:** Nearest-neighbour retrieval over embeddings.
**Input:** A query vector.
**Output:** Ranked neighbours.

Disclosed, not a fork: HNSW needs a fixed dimension nothing has
decided yet, so `SqlVectorSearcher` uses pgvector's real `<=>` cosine
distance directly -- exact, not approximate, ranking. Structurally
filters by model_id/version (doc's own hard rule); `index_generation`
optional pin. 4 real tests, real Postgres/pgvector: hand-computed
distances rank correctly, wrong-model vector excluded, generation pin
narrows results. Zero regression: 1034 unit tests unchanged.
