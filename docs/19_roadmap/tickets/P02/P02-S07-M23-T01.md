---
id: P02-S07-M23-T01
title: Redis client integration
status: done
depends_on: []
evidence: [kernel/src/ai_os_kernel/caching/client.py, kernel/src/ai_os_kernel/caching/settings.py, tests/integration/caching/test_redis_client.py]
module_path: kernel/src/ai_os_kernel/caching
time_spent: 1h45m
---
**Goal:** Actually use the Redis that Compose already provisions.
**Input:** Config.
**Output:** A working client.

Real `build_redis_client(redis_url)` (mirrors `persistence.engine.
build_engine`) + `RedisSettings` (`AIOS_REDIS_URL`, bootstrap-minimum
env var per configuration_management.md §3.3, mirrors
`DatabaseSettings`). Proven against a real `redis:7-alpine` container
(the Compose image): genuine connect+ping, real set/get. 4 new tests.
Zero regression: 980 unit tests (978 + 2 new).
