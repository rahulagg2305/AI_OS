---
id: P02-S03-M08-T10
title: Persisted context audit logger
status: done
depends_on: [P02-S03-M08-T01]
evidence: [kernel/src/ai_os_kernel/context_manager/audit_logger.py, tests/integration/context_manager/test_audit_logger.py]
module_path: kernel/src/ai_os_kernel/context_manager
time_spent: 1h30m
---
**Goal:** Record exactly what context each step actually received.
**Input:** An assembled context.
**Output:** A durable audit row.

Real fork, stopped: data_model.md had no schema for this at all (no
`context` row in §3's own list). Confirmed: add the missing §9b section
now (chosen), new `context.context_assemblies` table (migration 0033),
not reusing `governance.audit_log` (different concern). Wired as an
optional collaborator on `DefaultContextManager` (None = old
behavior). 3 real Postgres tests: full-fidelity round trip enabling
exact replay, real budget-trimmed exclusion persisted, unknown id
returns None. Zero regression: 50 context_manager tests green.
