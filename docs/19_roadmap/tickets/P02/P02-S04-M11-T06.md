---
id: P02-S04-M11-T06
title: Retrieval service
status: done
depends_on: [P02-S04-M11-T05]
evidence: [kernel/src/ai_os_kernel/retrieval/retrieval_service.py, tests/integration/retrieval/test_retrieval_service.py]
module_path: kernel/src/ai_os_kernel/retrieval
time_spent: 1h
---
**Goal:** One service interface over both strategies.
**Input:** A retrieval request.
**Output:** Ranked results.

Not a fork: task scoped composition to exactly the 3 real pieces, so
`RetrievalRequest.query_vector` is caller-supplied, no `embed()` call
here. `RetrievalService.search()` calls the real
`SqlKeywordSearcher`/`SqlVectorSearcher`, hands both to real
`fuse_rankings` -- no reimplementation. 1 real Postgres/pgvector test:
one chunk absent from keyword results, one farthest by vector, one in
both -- fused order only reachable by genuinely running and combining
both real searches. Zero regression.
