---
id: P02-S07-M17-T03
title: Outbox relay
status: done
depends_on: [P02-S07-M17-T01, P02-S07-M17-T02]
evidence: [kernel/src/ai_os_kernel/event_bus/outbox_relay.py, tests/integration/event_bus/test_outbox_relay.py]
module_path: kernel/src/ai_os_kernel/event_bus
time_spent: 2h00m
---
**Goal:** Drain the outbox at least once (FR-014).
**Input:** Outbox rows.
**Output:** Published events with correlation ids.

Real `OutboxRelay`: `SELECT ... FOR UPDATE SKIP LOCKED` claiming
(mirrors `lease.reap_expired`), dispatches through the real, unchanged
`InProcessEventBus`, no parallel mechanism. `event_id`=`outbox_id`,
`trace_id` passed through -- real correlation ids. Failed dispatch
increments `attempts`, leaves `dispatched_at` null for retry. 4 real
tests, real Postgres: relay+deliver, no-redispatch, fail-then-retry,
empty pass. Zero regression: 978 unit tests unchanged.
