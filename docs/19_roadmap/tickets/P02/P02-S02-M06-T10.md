---
id: P02-S02-M06-T10
title: Provider token counting
status: done
depends_on: [P02-S02-M06-T01]
evidence: [kernel/src/ai_os_kernel/llm_gateway/adapters/anthropic_adapter.py, tests/unit/kernel/llm_gateway/adapters/test_anthropic_adapter.py]
module_path: kernel/src/ai_os_kernel/llm_gateway
time_spent: 2h15m
---
**Goal:** Support count_tokens() against the provider endpoint.
**Input:** A prompt.
**Output:** An exact token count.

Real `AnthropicAdapter.count_tokens()` calls the real, documented
`POST /v1/messages/count_tokens` endpoint -- never a heuristic. New
`TokenCounter` Protocol lets `DispatchingLLMGateway.count_tokens()`
dispatch via `isinstance`, kept off `LLMGateway` like `capabilities()`.
Never walks fallback (count is model-specific). 11 real tests: a real
local HTTP server proves the wire format; an opt-in live test (skips
without a real key) proves the real API. Zero regression: 1007 unit
tests (996 + 11 new).
