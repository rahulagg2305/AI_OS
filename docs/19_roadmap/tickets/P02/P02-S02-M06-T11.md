---
id: P02-S02-M06-T11
title: Rate limiter
status: done
depends_on: [P02-S02-M06-T02, P02-S02-M06-T03]
evidence: [kernel/src/ai_os_kernel/llm_gateway/rate_limiter.py, tests/integration/llm_gateway/test_rate_limiter.py]
module_path: kernel/src/ai_os_kernel/llm_gateway
time_spent: 2h45m
---
**Goal:** Respect per-provider rate limits proactively.
**Input:** Request volume.
**Output:** Throttled dispatch.

Real `RedisRateLimiter` (fixed-window, reuses the real Redis client, no
parallel mechanism), wired into `DispatchingLLMGateway` as a new
optional collaborator (defaults `None`, zero regression). Rejection
reuses `RATE_LIMIT_EXCEEDED` (identical to the existing HTTP-429
classification), real `retry_after_seconds`, honored by backoff and
fallback traversal. 6 real tests, real Redis: within-limit succeeds,
over-limit refused before reaching the real gateway. Not wired into
bootstrap.py (real numbers undecided). Zero regression: 996 unit tests.
