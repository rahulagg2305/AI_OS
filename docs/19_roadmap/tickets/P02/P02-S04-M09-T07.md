---
id: P02-S04-M09-T07
title: A real production caller for document ingestion
status: done
depends_on: [P05-S01-M26-T02]
evidence: [kernel/src/ai_os_kernel/knowledge_manager/ingestion.py, tests/integration/test_ingestion_in_lifespan.py, tests/integration/knowledge_manager/test_ingestion.py]
module_path: kernel/src/ai_os_kernel/knowledge_manager
time_spent: 3h10m
---
**Goal:** Make §4's "ingest knowledge from approved sources" automatic, not merely callable.
**Input:** Real project content on disk.
**Output:** Indexed, queryable knowledge produced by a running process.
`index_document_file` had no caller in `kernel/src`; a real startup scan in `bootstrap._lifespan` now drives it over configured dirs.
**Two forks escalated; the source was already decided.**
`knowledge_base_structure.md` §3 fixes `docs/` as the ingested tree.
(1) Trigger — chose a config-gated startup scan, following
`capability_pack_dirs`' ADR-0009 precedent; `None` = nothing runs.
(2) Trust — **ADR-0016 control 1 ("always untrusted") vs §6 ("primary
source of truth")**, a real conflict. Resolved untrusted: §3 lists
"Generated documentation", so ingested text can be LLM-authored, and
`documents.trust` feeds `ContextItem.trust`. A constant, not a parameter.
Also forwarded the 3 embedding params through `index_document_file`
(`M09-T06`'s disclosed gap), so ingested content is vector-searchable.
10 tests. Real `_lifespan` proof: files become rows with no call in test.
