---
id: P02-S05-M18-T04
title: Wire a real ToolRegistry into the production PackContext
status: done
depends_on: [P02-S05-M18-T03, P03-S04-M31-T02]
evidence: [kernel/src/ai_os_kernel/sdk_adapters/pack_context.py, kernel/src/ai_os_kernel/workflow_engine/registry.py, tests/integration/workflow_engine/test_registry.py]
module_path: platform_sdk/src/ai_os_sdk/contracts
time_spent: 2h40m
---
**Goal:** Make manifest-declared Tools genuinely invocable by a real agent.
**Input:** An agent resolved from the real catalog.
**Output:** context.tools.invoke("fs.read", ...) reaching the real registered Tool.

Filed and closed 2026-08-11 (health audit, R-017). T03 gave
`ToolInvokerAdapter` an optional `registry` defaulting to `None`; its one
production construction site (`build_pack_context`) never passed one, so
`SqlToolRegistry` was never constructed in production and every real
tool_id raised `UnknownToolError`. Threaded `tool_registry` through
SqlAgentRegistry → _bind_pack_context_if_receiver → build_pack_context,
with a **real default** (not another silent None). Agent path only: a
tool resolving a tool re-enters SqlToolRegistry unboundedly. 2 new real
Postgres+sandbox tests (34 total in test_registry.py); reverting the
one-line fix genuinely reproduces the original error — verified. Full
reasoning: R-017. Remaining: available_tools(), principal narrowing.
