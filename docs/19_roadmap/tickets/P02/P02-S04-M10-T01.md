---
id: P02-S04-M10-T01
title: Memory store
status: done
depends_on: []
evidence: [kernel/src/ai_os_kernel/persistence/memory_writer.py, tests/integration/persistence/test_memory_writer.py]
module_path: kernel/src/ai_os_kernel/memory_manager
time_spent: 1h
---
**Goal:** Durable store for cross-run memory.
**Input:** Memory writes.
**Output:** A queryable store.

Not a schema-authority gap (unlike two steps ago): `knowledge.memory_items`
already exists, documented, migrated (0029), incl. its real FK to
workflow_instances. `SqlMemoryStore` (in `persistence/`, no owning
Memory Manager yet -- same precedent as knowledge_writer.py):
write_memory + structural query_memories (no FTS column exists).
`promoted_at` never caller-settable -- promotion logic stays a
separate, disclosed gap. 6 real Postgres tests: write+read, optional
fields, type filtering, per-workflow isolation, blank content and
unknown workflow_id both rejected. Zero regression.
