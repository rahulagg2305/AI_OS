---
id: P02-S03-M08-T05
title: Knowledge resolver
status: done
depends_on: [P02-S03-M08-T01, P02-S04-M09-T04]
evidence: [kernel/src/ai_os_kernel/context_manager/resolvers.py, tests/integration/context_manager/test_knowledge_resolver.py]
module_path: kernel/src/ai_os_kernel/context_manager
time_spent: 1h30m
---
**Goal:** Bring knowledge-base content into step context.
**Input:** A query.
**Output:** Ranked knowledge items.

2 real forks, both stopped and resolved: query text via a new,
additive `ContextRequest.knowledge_query` field (chosen); query vector
via a real `embed()` call with an injected model alias (chosen) --
`KnowledgeResolver` is `Embedder`'s 2nd real caller. Reuses real
`QueryEngine` unchanged. `relevance_score` now has real RRF variance
(1st non-constant resolver). 2 real Postgres + real local embeddings
server tests: a real workflow step's assembly genuinely includes real
queried knowledge alongside real workflow state. Zero regression.
