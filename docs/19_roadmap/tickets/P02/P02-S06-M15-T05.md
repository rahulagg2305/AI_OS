---
id: P02-S06-M15-T05
title: Gate Registry
status: done
depends_on: [P02-S06-M15-T01]
evidence: [kernel/src/ai_os_kernel/quality_gate_engine/registry.py, tests/unit/kernel/quality_gate_engine/test_registry.py]
module_path: kernel/src/ai_os_kernel/quality_gate_engine
time_spent: 2h
---
**Goal:** Resolve a declared gate id to a real gate definition.
**Input:** A gateId.
**Output:** A resolved gate.

Investigated M12 (Evaluation Engine) first: Metrics Collector is
genuinely blocked (metrics.run_id is NOT NULL, FK to experiment_runs,
which needs the unbuilt Benchmarking Pack). Run Manifest Recorder is
buildable but much larger (multi-table joins, missing "model
parameters sent" data source, needs threading through 3 composition
sites) -- named as a real, valuable, correctly-sized-for-its-own-ticket
candidate, not built here. Chose this instead: real, schema-conformant
(manifest.schema.json's own qualityGates[] shape), no new catalog
table, gate ids resolved raw (confirmed via delivery_pipeline.yaml's
own real qualityGates: references, not derived pack_id/-prefixed like
agents). Same "build real, wire later" precedent as KnowledgeResolver.
