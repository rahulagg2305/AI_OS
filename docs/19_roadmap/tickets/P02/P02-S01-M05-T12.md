---
id: P02-S01-M05-T12
title: Multi-instance worker loop
status: done
depends_on: [P02-S01-M05-T04]
evidence: [kernel/src/ai_os_kernel/workflow_engine/worker_loop.py, kernel/src/ai_os_kernel/workflow_engine/repository.py]
module_path: kernel/src/ai_os_kernel/workflow_engine
time_spent: 2h30m
---
**Goal:** Drive many instances concurrently, not one per call.
**Input:** Runnable instances.
**Output:** A scheduling loop across instances.

Checked STATUS.md's ready list for a Task wiring decision/parallel/sub_workflow
into a real composition first; none exists, fell back to this next-ready Task.
New `list_runnable_instances` query (status=running, no active lease) plus
`WorkflowWorkerLoop`/`run_worker_loop` drive every discovered instance one step
at a time via real `asyncio.gather` over `WorkflowAdvanceRunner.run_once`.
Reuses T11's composition-level definitions-injection precedent, keyed by
`(definition_id, definition_version)`. Proven via real Postgres: genuine
concurrent advancement, a real two-worker lease race with exactly one winner,
and the real background loop driving completion. Real, proven, not wired into
`_lifespan` (needs system-wide definition discovery, the write-only-catalog
gap T11 hit — undecided, and out of this ticket's own scope).
