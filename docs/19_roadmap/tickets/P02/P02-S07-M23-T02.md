---
id: P02-S07-M23-T02
title: Response cache
status: done
depends_on: [P02-S07-M23-T01, P02-S02-M06-T01]
evidence: [kernel/src/ai_os_kernel/caching/response_cache.py, tests/integration/caching/test_response_cache.py]
module_path: kernel/src/ai_os_kernel/caching
time_spent: 2h30m
---
**Goal:** Cache responses, disabled for experiments (ADR-0025).
**Input:** A cacheable call.
**Output:** A cache hit or miss, recorded.

Real `ResponseCache` over `LLMRequest`/`LLMResponse`, reusing the real
Redis client (no parallel mechanism). Structural gap found: no
`experiment_id` field existed to enforce ADR-0025's hard rule -- product
owner approved extending `TraceContext`/`LLMResponse` (additive,
already-anticipated fields). 5 real tests, real Redis: hit avoids
recompute, miss falls through, experiment calls never cached/read.
Not wired into `gateway.py` yet. Zero regression: 984 unit tests.
