---
id: P04-S02-M16-T05
title: Expose the Traceability Engine over §6.6 HTTP routes
status: todo
depends_on: [P04-S02-M16-T02, P04-S02-M16-T03, P04-S02-M16-T04]
evidence: []
module_path: kernel/src/ai_os_kernel/traceability_engine
---
**Goal:** Make the impact and coverage queries readable over the documented API.
**Input:** trace.links rows now written by production code (T04).
**Output:** api_architecture.md §6.6's own traceability routes, real and tested.

Filed 2026-08-11, the read half T04 split off (T04's own "split if one
step cannot carry both halves"). Now genuinely worth building: T04 gave
the Traceability Engine its first real production writer, so a
`workflow_run --produced--> documentation` row actually exists to read —
a route over a permanently-empty table would have been the same hollow
"proven but idle" shape (R-018) this thread is closing. Build
`GET /api/v1/traceability/impact/{id}` and `.../coverage` over the
already-real `impact_query`/`coverage_query` (both `done`,
`P04-S02-M16-T02`/`T03`), the identical thin-read-route pattern
`GET /gates/results` established. `traceability/query` (the raw link
graph) is a real, disclosed, separate slice — decide its shape first.
