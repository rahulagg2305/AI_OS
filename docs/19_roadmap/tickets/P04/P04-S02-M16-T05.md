---
id: P04-S02-M16-T05
title: Expose the Traceability Engine over §6.6 HTTP routes
status: done
depends_on: [P04-S02-M16-T02, P04-S02-M16-T03, P04-S02-M16-T04]
evidence: [kernel/src/ai_os_kernel/routes/traceability.py, kernel/src/ai_os_kernel/bootstrap.py, tests/integration/test_traceability_route.py]
module_path: kernel/src/ai_os_kernel/traceability_engine
time_spent: 2h00m
---
**Goal:** Make the impact and coverage queries readable over the documented API.
**Input:** trace.links rows now written by production code (T04).
**Output:** api_architecture.md §6.6's own traceability routes, real and tested.

Closes the read half of R-018's Traceability instance (writer landed in
T04). New `routes/traceability.py`: `GET /traceability/impact/{id}` over
the already-real `find_affected_artifacts`, `GET /traceability/coverage`
over `find_uncovered_requirements` — the identical thin-read shape
`GET /gates/results` established. Gated on the already-real
`evaluation:read` (no `traceability:read` documented; same family as
gates). **Disclosed shape:** `{id}` is `external_id`, `artifact_type` a
required query param (the query needs the real `(type, external_id)`
pair). `traceability/query` (raw graph) stays unbuilt. Proven over
real, writer-seeded rows via `build_app`: impact returns a produced-doc
artifact; coverage reports a provisional-only requirement, not confirmed.
