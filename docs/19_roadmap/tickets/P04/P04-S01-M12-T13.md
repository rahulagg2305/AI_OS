---
id: P04-S01-M12-T13
title: Experiment run orchestrator (§6.3 POST /experiments/{id}/run)
status: done
depends_on: [P04-S01-M12-T12, P04-S03-M34-T02]
evidence: [kernel/src/ai_os_kernel/evaluation_engine/experiment_run_orchestrator.py, tests/integration/evaluation_engine/test_experiment_run_orchestrator.py]
module_path: kernel/src/ai_os_kernel/evaluation_engine
time_spent: 5h30m
---
**Goal:** Give evaluation.experiment_runs its first production caller.
**Input:** A defined experiment; the tested SqlExperimentRunRecorder.
**Output:** POST /experiments/{id}/run materialising real run rows.

Finding: experiment_runs already had a tested writer (T34-T02) but no
caller. Its workflow_id/resolved_model_id/served_from_cache are all NOT
NULL, so a row needs a genuinely launched+run workflow — an orchestrator,
not a bare writer. Product-owner-chosen synchronous slice:
ExperimentRunOrchestrator expands the experiment's `model_alias` variants
(overview.md §7: the model is the variable), resolves each via the real
Router, then per variant x replicate creates+starts+run_to_completion a
real workflow and records the row (served_from_cache=False per §7),
transitioning the experiment defined->running->complete. Disclosed
limits: model-only variables; true per-run model pinning into agent LLM
calls not yet plumbed. Proven: 8 orchestrator + 4 route Postgres tests.
