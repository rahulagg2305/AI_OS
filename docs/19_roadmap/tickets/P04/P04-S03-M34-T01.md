---
id: P04-S03-M34-T01
title: Experiment definition with pinned conditions
status: done
depends_on: [P04-S01-M12-T01]
evidence: [capability_packs/benchmarking/src/ai_os_pack_benchmarking/experiment_definition.py, kernel/src/ai_os_kernel/sdk_adapters/workflow_definition_existence_adapter.py]
module_path: capability_packs/benchmarking
time_spent: 2h45m
---
**Goal:** Define an experiment pinning workflow, prompts, tools and config (FR-070).
**Input:** An experiment spec.
**Output:** A validated definition.

First real code in a new pack (Module 27 hard gate already lifted).
Validates against overview.md's own real §7 rules: >=3 replicates,
>=2 variants, no dup keys, no sampling-param variables, pinned
workflow genuinely exists (via injected Protocol; real Postgres check
lives in sdk_adapters, since packs can't import ai_os_kernel). No
manifest.yaml yet — no agent/tool/workflow to activate. Not wired
into submission/execution (later ticket's scope).
