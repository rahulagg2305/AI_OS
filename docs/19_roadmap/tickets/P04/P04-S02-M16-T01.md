---
id: P04-S02-M16-T01
title: trace.links writer
status: done
depends_on: []
evidence: [kernel/src/ai_os_kernel/traceability_engine/link_writer.py, kernel/src/ai_os_kernel/traceability_engine/ids.py, tests/integration/traceability_engine/test_link_writer.py]
module_path: kernel/src/ai_os_kernel/traceability_engine
time_spent: 2h20m
---
**Goal:** Record requirement to architecture to module to test links (FR-019).
**Input:** Traceable artifacts.
**Output:** trace.links rows.

Design fork resolved (product-owner decision): artifact_key is
deterministic (type+external_id), not random, so independent callers
naming the same real artifact converge without a shared lookup.
record_link() upserts both endpoint artifacts inline and is idempotent
for an already-open identical triple; close_link() is real too. Proven
against real Postgres: convergent keys, idempotent re-assertion,
metadata refresh, close-then-reassert, and pre-DB validation.
