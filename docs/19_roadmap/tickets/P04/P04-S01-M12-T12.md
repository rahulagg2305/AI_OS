---
id: P04-S01-M12-T12
title: Experiment definition create+read (§6.3 foundation)
status: done
depends_on: [P04-S01-M12-T01, P02-S08-M36-T01]
evidence: [kernel/src/ai_os_kernel/evaluation_engine/experiment_repository.py, kernel/src/ai_os_kernel/routes/experiments.py, tests/integration/test_experiments_route.py]
module_path: kernel/src/ai_os_kernel/evaluation_engine
time_spent: 4h30m
---
**Goal:** Give evaluation.experiments its first real writer, exposed over §6.3.
**Input:** A caller-supplied experiment definition.
**Output:** A persisted experiment, readable over the documented API.

New `SqlExperimentRepository` (create/get/list) + `routes/experiments.py`
(`POST`/`GET /experiments`, `GET /experiments/{id}`). Delegated design
decisions, disclosed: (1) validation is **Kernel-owned, not pack-coupled**
— the Kernel is pack-agnostic, table+route are platform-level, rules
(>=2 variants, >=3 replicates) generic; the Benchmarking pack's validator
is a legitimate mirror (TrustTier precedent). (2) A real spec↔table
mismatch resolved by **data_model.md §6 as authority**: the definition
stores `variables`; per-variant `variant_key`/`model_alias` are run-time
`experiment_runs` (later ticket). (3) Create first, so reads aren't
hollow (R-018 trap). New `experiment:run` permission (§5 vocabulary;
reads use `evaluation:read`). 6 real `build_app` tests. Run/comparison deferred.
