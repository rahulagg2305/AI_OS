---
id: P04-S03-M34-T03
title: Cost ceiling enforcement
status: partial
depends_on: [P04-S03-M34-T01, P02-S02-M06-T07]
evidence: [capability_packs/benchmarking/src/ai_os_pack_benchmarking/cost_ceiling.py]
module_path: capability_packs/benchmarking
time_spent: 1h30m
---
**Goal:** Refuse an experiment projected over its ceiling before any call (FR-076).
**Input:** A projected cost.
**Output:** Refusal before dispatch.

llm_gateway.md §9 confirms this as its own distinct row ("Experiment
cost ceiling | Refuses to start the run"), separate from
BudgetExceededError. Ticket's own literal Input ("a projected cost")
scopes this to enforcement, not computation — a real, non-approximated
projection needs real count_tokens() + pricing, disclosed as separate,
unbuilt work. Added optional cost_ceiling_usd to ExperimentDefinition.
`status: partial` (own scope fully done): the roadmap's own invariant
refuses a `done` ticket depending on a not-done one, and dependency
P02-S02-M06-T07 is itself `partial` (2 of 5 checks), unrelated future
scope — matching that ticket's own precedent, not a shortfall here.
