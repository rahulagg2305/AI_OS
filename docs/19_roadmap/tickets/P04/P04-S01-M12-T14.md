---
id: P04-S01-M12-T14
title: Experiment comparison + runs read routes (§6.3 completion)
status: done
depends_on: [P04-S01-M12-T08, P04-S01-M12-T13]
evidence: [kernel/src/ai_os_kernel/evaluation_engine/experiment_run_reader.py, kernel/src/ai_os_kernel/routes/experiments.py, tests/integration/test_experiments_route.py]
module_path: kernel/src/ai_os_kernel/evaluation_engine
time_spent: 2h30m
---
**Goal:** Expose the comparison/report readers over §6.3 HTTP.
**Input:** The real SqlComparisonComputer/ReportingInterface + run rows.
**Output:** GET /experiments/{id}/comparison and .../runs.

Completes §6.3 (define -> run -> compare). `GET /comparison` returns the
already-real `SqlComparisonComputer.compute()` output (mean/variance per
variant x metric, cache-served excluded+flagged); `GET /runs` returns the
raw per-run rows via a new, small `SqlExperimentRunReader` (nothing listed
individual runs before — the computer only aggregates). Both gated
`evaluation:read`; a missing experiment is 404, an existing-but-unrun one
a real empty-but-valid 200 (not 404). No new persistence, no aggregation
duplicated. Proven by real-Postgres tests: reader ordering + empty; route
auth/404; empty comparison+runs for an unrun experiment; /runs lists six
real orchestrator-written rows; /comparison returns a seeded report
(values 1/2/3 -> mean 2, variance 1, Decimal-exact).
