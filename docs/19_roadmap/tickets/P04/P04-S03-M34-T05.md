---
id: P04-S03-M34-T05
title: Declare the Benchmarking pack in a real manifest and entry point
status: done
depends_on: [P04-S03-M34-T01, P01-S03-M28-T01]
evidence: [capability_packs/benchmarking/manifest.yaml, tests/unit/kernel/manifest_loader/test_benchmarking_manifest.py]
module_path: capability_packs/benchmarking
time_spent: 1h50m
---
**Goal:** Make this pack discoverable by the real Manifest Loader.
**Input:** The pack's four already-real modules.
**Output:** A schema-valid manifest.yaml the real Manifest Loader loads.

`ManifestLoader.scan()` now finds this pack (4 packs, 0 failures) — it
was invisible to discovery entirely. Closes the last Risk A instance.

**Discoverability, not invocability**, exactly as this ticket predicted:
no agent/tool/workflow is declared because the pack genuinely has none —
four pure validation/planning modules the Kernel's tests import
directly, not Tool entrypoints. Building entrypoints for them would be
new capability, not this Output. **No `entryPoint`/`pack.py`**: the
schema requires one only alongside agents/workflows, proven by a test
that adds an agent and watches the schema reject it. No permissions: no
I/O at all. Unsigned, like every committed pack. 6 real tests.
