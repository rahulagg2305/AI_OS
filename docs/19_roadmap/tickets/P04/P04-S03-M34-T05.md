---
id: P04-S03-M34-T05
title: Declare the Benchmarking pack in a real manifest and entry point
status: todo
depends_on: [P04-S03-M34-T01, P01-S03-M28-T01]
evidence: []
module_path: capability_packs/benchmarking
---
**Goal:** Make this pack discoverable by the real Manifest Loader.
**Input:** The pack's four already-real modules.
**Output:** A schema-valid manifest.yaml the real Manifest Loader loads.

Filed 2026-08-11 by the health audit (Risk A). T01's own body already
said "No manifest.yaml yet ... (later ticket's scope)" — that later
ticket was never filed, so the gap went invisible. Only two manifests
exist repo-wide (`_template`, `software-engineering`); this pack has
neither manifest.yaml nor pack.py, so the Loader can never find it.
**Correction to the audit that filed this:** it claimed this blocks
api_architecture.md §6.3; inspection disproves that —
`evaluation.experiments`, `SqlExperimentRunRecorder` and
`comparison_computer` are real, and the Kernel's own tests already
import this pack as a plain library, so §6.3 needs routes, not a
manifest. This pack declares no agent/tool/workflow either (all
optional in the schema), so a manifest here makes nothing invocable.
