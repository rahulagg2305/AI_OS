---
id: P04-S02-M16-T04
title: Wire the Traceability Engine's writer into a real production path
status: done
depends_on: [P04-S02-M16-T01, P04-S02-M16-T02, P04-S02-M16-T03]
evidence: [kernel/src/ai_os_kernel/workflow_engine/delivery_pipeline.py, kernel/src/ai_os_kernel/routes/delivery_pipeline.py, tests/integration/test_delivery_pipeline_route.py]
module_path: kernel/src/ai_os_kernel/traceability_engine
time_spent: 3h30m
---
**Goal:** Give the already-real link writer its first real production write call site.
**Input:** A real delivery-pipeline run that produces documentation.
**Output:** A real trace.links row written by production code.

Closes R-018's worst instance (writer/queries `done`, zero production
writer). Narrowed to the *writer* half; §6.6 read routes split out as
`P04-S02-M16-T05`. `routes/delivery_pipeline.py` records a real
`workflow_run --produced--> documentation` link (traceability_model.md
§4's own named relationship) via a new bootstrap-wired
`app.state.trace_link_writer`. **Design fork (product owner):** written
the moment documentation exists — a run pauses at `approve-git-push`
*after* documentation, so COMPLETED-only would be nearly dead.
Confidence `confirmed`, created_by_type `process`, all fields
non-fabricated. Best-effort (never breaks a completed run). Proven:
the real route via `build_app` writes a real row and fails if reverted.
