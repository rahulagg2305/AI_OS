---
id: P04-S02-M16-T04
title: Wire the Traceability Engine into a real production path
status: todo
depends_on: [P04-S02-M16-T01, P04-S02-M16-T02, P04-S02-M16-T03]
evidence: []
module_path: kernel/src/ai_os_kernel/traceability_engine
---
**Goal:** Make the already-real link writer and both queries reachable in production.
**Input:** A real workflow run that produces linkable artifacts.
**Output:** A trace.links row written by production code and readable over HTTP.

Filed 2026-08-11 by the health audit (Risk B). T01-T03 are all `done`
and stage P04-S02 reports 100%, yet the subsystem is unreachable: grep
finds zero production importers of `traceability_engine` outside its
own package, zero production `SqlTraceLinkWriter` construction, and no
`/api/v1/traceability/*` route. `persistence/trace_schema.py`'s own
docstring admits "nothing writes a traceability link yet either", so
the roadmap emits no signal that a 100%-done subsystem is idle. Needs
one real production write call site plus api_architecture.md §6.6's own
documented query/impact/coverage routes. Split this if one step cannot
carry both halves.
