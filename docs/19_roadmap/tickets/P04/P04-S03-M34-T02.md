---
id: P04-S03-M34-T02
title: Replicate management
status: done
depends_on: [P04-S03-M34-T01]
evidence: [capability_packs/benchmarking/src/ai_os_pack_benchmarking/replicate_management.py, kernel/src/ai_os_kernel/sdk_adapters/experiment_run_recorder_adapter.py]
module_path: capability_packs/benchmarking
time_spent: 1h45m
---
**Goal:** Run at least 3 replicates per variant (FR-072).
**Input:** A variant.
**Output:** Recorded replicates.

`plan_replicates` expands a variant into N real, numbered replicates
(defensive >=3 re-check). Recording needs a real, pre-existing
workflow_id (experiment_runs' own NOT NULL FK) — real Kernel writer
(SqlExperimentRunRecorder) backs a pack-defined Protocol, mirroring
T01's own pattern. Fixed a real design mistake before it shipped: the
Kernel adapter briefly imported a pack type; fixed to a plain,
primitive-only boundary. Not wired into real submission (later scope).
