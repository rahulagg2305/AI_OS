---
id: P08-S01-M29-T02
title: database agent
status: done
depends_on: [P03-S02-M29-T02]
evidence: [capability_packs/software-engineering/src/ai_os_pack_software_engineering/agents/database.py, capability_packs/software-engineering/tests/test_database_agent.py]
module_path: capability_packs/software-engineering/src/ai_os_pack_software_engineering/agents
time_spent: 2h30m
---
**Goal:** Design schemas and reversible migrations (FR-036).
**Input:** A design.
**Output:** A migration.

Design fork resolved via AskUserQuestion before writing code: a plain
SQL `-- UP`/`-- DOWN` pair (chosen) vs. a full Alembic revision module
vs. a schema-diff compiler. Reuses `build.py`'s own write mechanism
verbatim; the one addition is a required `-- UP`/`-- DOWN` split,
refused before any sandbox write if absent. Proven reversible for real:
a deterministic completion's own `upSql`/`downSql` genuinely applied
and reverted against a real Postgres container. Found and fixed two
adjacent, hardcoded agent-count assertions (`test_health_poller.py`,
`test_manifest_catalog_installer.py`) and one stale, closed-regression
claim in `feature_inventory.md` §6a (`P04-S01-M12-T10` already fixed
it).
