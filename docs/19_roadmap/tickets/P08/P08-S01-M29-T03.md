---
id: P08-S01-M29-T03
title: api-designer agent
status: done
depends_on: [P03-S02-M29-T02]
evidence: [capability_packs/software-engineering/src/ai_os_pack_software_engineering/agents/api_designer.py, capability_packs/software-engineering/tests/test_api_designer_agent.py]
module_path: capability_packs/software-engineering/src/ai_os_pack_software_engineering/agents
time_spent: 2h30m
---
**Goal:** Define versioned API contracts (FR-037).
**Input:** A design.
**Output:** A contract artifact.

Design fork resolved via AskUserQuestion: real `openapi-spec-validator`
(chosen, new pack dependency) vs. a hand-rolled structural check vs. a
narrower non-OpenAPI shape. Reuses `build.py`/`database.py`'s write
mechanism verbatim; adds a required real-OpenAPI-3.1-validation gate,
refused before any sandbox write if the document fails it. Proven with
the real library, not a stand-in: a genuinely spec-violating document
is rejected via its own real exception. Found and fixed the same
hardcoded-agent-count pattern in three files up front this time
(learned from the prior step's own CI surprise) — verified against the
full `tests/integration` suite locally before pushing.
