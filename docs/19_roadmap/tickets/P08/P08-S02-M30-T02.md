---
id: P08-S02-M30-T02
title: se.implement_task workflow
status: done
depends_on: [P03-S02-M29-T08]
evidence: [kernel/src/ai_os_kernel/workflow_engine/implement_task.py, tests/integration/workflow_engine/test_implement_task.py]
module_path: capability_packs/software-engineering/workflows
time_spent: 2h45m
---
**Goal:** The narrower single-task implementation workflow.
**Input:** A task.
**Output:** A completed run.

Real, buildable version of workflows.md §4's own graph, built entirely
from real, existing agents/step types (design fork resolved with the
product owner via `AskUserQuestion`): `build` stands in for the
undocumented `backend-developer`; no `fs.apply_patch`/`test.run` tool
steps (`build`/`qa-test` already write/run directly); two real,
genuinely-looping `decision` steps replace §4's per-loop counter,
bounded by the trigger's own whole-run `max_iterations`. Proven twice
against real Postgres: the straight-through happy path, and a real
loop-back recovery (a controllable fake gateway fails attempt 1,
passes attempt 2 — `implement` genuinely re-executes with a fresh,
higher `attempt`).
