---
id: P08-S02-M30-T01
title: se.product_creation workflow
status: done
depends_on: [P03-S02-M29-T07, P03-S02-M29-T08]
evidence: [kernel/src/ai_os_kernel/workflow_engine/product_creation.py, tests/integration/workflow_engine/test_product_creation.py, kernel/src/ai_os_kernel/workflow_engine/step_executor.py]
module_path: capability_packs/software-engineering/workflows
time_spent: 7h15m
---
**Goal:** The full documented product-creation workflow.
**Input:** A specification.
**Output:** A completed multi-stage run.

8 of 14 steps real, reaching genuine `COMPLETED` end to end — real,
buildable subset, the `P08-S02-M30-T02` "disclosed departure"
precedent. `StepType.FOREACH`/`ForeachStepExecutor` built (ADR-0021);
step 8 fans real `technical-planner` tasks into real `se.implement_task`
children, proven against real Postgres. `AskUserQuestion`-resolved:
widened `se.implement_task`'s `inputs` schema (`title`/`description`)
to match the real plan-item shape, not a foreach-side mapping. Found
+fixed: `ck_workflow_steps_step_type` lacked `foreach` (migration
`0036`); fixed `principal_id` at composition time never worked for a
real, reused trigger (now built fresh per call). Steps 9-14 (parallel
gates, docs, release, PR) remain genuinely unbuilt, disclosed.
