---
id: P08-S02-M30-T01
title: se.product_creation workflow
status: partial
depends_on: [P03-S02-M29-T07, P03-S02-M29-T08]
evidence: [kernel/src/ai_os_kernel/workflow_engine/product_creation.py, kernel/src/ai_os_kernel/workflow_engine/step_executor.py, tests/integration/workflow_engine/test_foreach_step_execution.py]
module_path: capability_packs/software-engineering/workflows
time_spent: 5h30m
---
**Goal:** The full documented product-creation workflow.
**Input:** A specification.
**Output:** A completed multi-stage run.

7 of 14 steps real (unchanged). The step-8 blocker itself is now
closed generically: `StepType.FOREACH`/`ForeachStepExecutor` are real
(ADR-0021), sequential per-item fan-out reusing `SubWorkflowStepExecutor`,
bounded by required `maxFanOut`, proven against real Postgres. A real
DB migration gap found+fixed (`ck_workflow_steps_step_type` lacked
`foreach`). Still `partial`: wiring step 8 here hit a second, separate
mismatch — `technical-planner`'s plan-item shape (`taskId`/`title`/
`description`) doesn't satisfy `se.implement_task`'s own `inputs`
schema (`{task: string}`). Two real options remain: widen
`se.implement_task`'s schema, or add per-item field-selection to the
foreach contract — product-owner call, not resolved here.
