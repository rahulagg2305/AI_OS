---
id: P07-S01-M40-T02
title: NetworkPolicy egress allowlist
status: partial
depends_on: [P07-S01-M40-T01]
evidence: [infra/kubernetes/helm/ai-os/templates/networkpolicy.yaml, infra/kubernetes/helm/ai-os/values.yaml, tests/integration/infra/test_ai_os_helm_chart.py]
module_path: infra
time_spent: 1h45m
---
**Goal:** Restrict egress to declared destinations.
**Input:** Cluster policy.
**Output:** Enforced allowlist.

This ticket's own scope is fully built (see below); marked `partial`
only because its dependency, T01, is itself `partial` (the roadmap's
own dependency-completeness invariant). Opt-in
(`networkPolicy.enabled`, default `false`, product-owner decision —
zero regression). Real DNS + same-namespace baseline always applies
when enabled; Postgres/Redis/provider destinations are
operator-declared via `additionalRules` (real, literal
`NetworkPolicyEgressRule` entries). Proven offline and against a real
`kind` cluster.
