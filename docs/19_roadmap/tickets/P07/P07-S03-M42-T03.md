---
id: P07-S03-M42-T03
title: Fix the webhook notification test's impossible deadline
status: todo
depends_on: [P06-S05-M22-T01]
evidence: []
module_path: tests
time_spent: 0m
---
**Goal:** Remove a real R-015 recurrence found 2026-08-13.
**Input:** `tests/unit/kernel/notification/test_service.py`.
**Output:** A deadline that cannot expire while the code is behaving.
**This is a test defect, not a product defect, and the numbers prove
it.** `WebhookChannel`'s own per-delivery timeout is **5.0s**
(`_DEFAULT_TIMEOUT_SECONDS`), but
`test_real_failure_and_completion_events_both_genuinely_notify` gives
**two sequential deliveries 2.0s total** (`_POLL_TIMEOUT_SECONDS`). The
assertion can fail while every component behaves exactly as designed.
budget is spent.
Observed: passes alone (4.52s), fails under load (the integration suite
was running concurrently) — only one `notification.delivery_attempted`
logged before the deadline.
Fifth R-015 occurrence; commit `afdc6a2` already fixed "socket leak +
tight timeouts" in this same file. No design fork.
