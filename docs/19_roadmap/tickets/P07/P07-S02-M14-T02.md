---
id: P07-S02-M14-T02
title: Full five-role model
status: done
depends_on: [P07-S02-M14-T01, P03-S05-M14-T02]
evidence: [kernel/src/ai_os_kernel/security_manager/role_administration.py, tests/integration/security_manager/test_role_grants_affect_general_routes.py]
module_path: kernel/src/ai_os_kernel/security_manager
time_spent: 2h10m
---
**Goal:** Implement every documented role.
**Input:** Role definitions.
**Output:** Enforced role-based access.

All 5 roles were already modeled/enforced via bearer-token claims; the
real gap was persisted grants only taking effect inside
`ApprovalService.decide`, never general routes. Extracted the union
logic as `resolve_effective_roles()`, reused by both `decide()`
(zero-behavior-change) and `authenticate()` (new). Wired
`app.state.role_grant_repository` in bootstrap. Proven with a real
Postgres-backed grant/revoke cycle against `GET /api/v1/packs`.
