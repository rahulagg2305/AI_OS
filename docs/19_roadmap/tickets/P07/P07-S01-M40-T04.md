---
id: P07-S01-M40-T04
title: gVisor RuntimeClass in the Helm chart
status: todo
depends_on: [P07-S01-M40-T01]
evidence: []
module_path: infra
time_spent: 0m
---
**Goal:** Offer the ADR-0016 hardened sandbox tier as a deployable option.
**Input:** A cluster with `runsc` installed.
**Output:** An opt-in RuntimeClass the chart can schedule pods under.
Carved out 2026-08-13 by the partial-ticket re-verification, from
`P07-S01-M40-T01`'s own words: "a real, now de-risked next slice, not
attempted this step".
**Already de-risked by real evidence, not assumption** (2026-08-10): a
throwaway kind node with `runsc` plus a RuntimeClass genuinely ran a pod,
`/proc/version` and dmesg showing the real gVisor signature — unlike the
rootless-Podman sidecar, blocked one layer deeper (`newuidmap`
"operation not permitted" under containerd CRI).
Opt-in and default-off, mirroring `sandboxSidecar` and
`networkPolicy.enabled`, so zero regression for existing deployments.
Relates to `P03-S01-M20-T05`, which stays `partial` until a real
hardened runtime is verified end to end.
