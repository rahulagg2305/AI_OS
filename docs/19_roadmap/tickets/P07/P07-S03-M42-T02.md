---
id: P07-S03-M42-T02
title: Cost anomaly alerting
status: done
depends_on: [P04-S01-M12-T04, P06-S05-M22-T01]
evidence: [kernel/src/ai_os_kernel/evaluation_engine/cost_anomaly.py, kernel/alembic/versions/0035_llm_calls_created_at.py, tests/integration/evaluation_engine/test_cost_anomaly.py]
module_path: tests
time_spent: 2h45m
---
**Goal:** Alert on abnormal spend (NFR-045).
**Input:** Cost telemetry.
**Output:** An alert.

Structural gap found and resolved (product owner approved): `llm_calls`
had no timestamp column, so "hourly spend" was uncomputable — added
`created_at` (migration 0035), backfill-safe. Detector reads real
spend from `llm_calls`, fires a real `Event("cost.anomaly")` when the
trailing-1h window exceeds 3x the trailing-7-day hourly mean (undefined
gained its 4th real category and its first-ever real production wiring
into `_lifespan` — both previously-disclosed gaps this ticket closed.
Proved end to end: real Postgres detector tests, real webhook delivery
test, loop-scheduling unit tests. **Closed 2026-08-13** (partial-ticket
re-verification): this body named no remaining gap and re-reading
`cost_anomaly.py` found none.
