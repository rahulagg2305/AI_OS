---
id: P01-S01-M40-T04
title: Runtime container image
status: done
depends_on: [P01-S01-M40-T01]
evidence: [Dockerfile, deploy/entrypoint.sh, deploy/healthcheck.py, .dockerignore]
module_path: infra
time_spent: 2h30m
---
**Goal:** Produce a real runnable image for the api and worker processes.
**Input:** The uv workspace.
**Output:** A Dockerfile building a working image.

Multi-stage (builder: uv sync --frozen --no-dev; runtime: digest-pinned
python:3.12-slim, non-root uid 10001). One image, two entry points via
AIOS_ROLE (deploy/entrypoint.sh), per ADR-0020. Found + fixed: a new
top-level docker/ dir shadowed the real docker pip package for mypy's
resolution -- renamed to deploy/. Found + disclosed (product owner
accepted): worker role is a real, pre-existing stub; WorkflowWorkerLoop
only runs inside the api role's own lifespan. Proven: real docker
build; api container reaches Docker's own healthy state, serves
/health/live, /health/ready (503, graceful, no crash), /version; the
real SE pack discovered inside the image; worker container runs its
real stub, exits 0. Unlocks CI's Build image job.
