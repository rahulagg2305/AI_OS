---
id: P01-S05-M04-T07
title: Observability profile readiness gates
status: done
depends_on: [P01-S05-M04-T04]
evidence: [infra/docker-compose.yml, tests/integration/observability/test_compose_observability_profile.py]
module_path: kernel/src/ai_os_kernel/observability
time_spent: 1h15m
---
**Goal:** Gate every observability service that can be gated on real readiness.
**Input:** The compose observability profile.
**Output:** `up --wait` blocks until each service is genuinely serving.

Closes the gap `953b7fc` left open: Grafana got a healthcheck when its
missing readiness window failed real CI, and the other four were
recorded as knowingly ungated. Prometheus, Tempo and Loki now probe
their own documented endpoints (`/-/healthy`, `/ready`, `/ready`).

**`otel-collector` genuinely cannot be gated** — distroless image, no
shell and no wget/curl/nc (verified, not assumed), so no probe can run
inside it; documented in the compose file where a reader looks for it.
Each command was checked against its own image, never copied: Loki ships
busybox at `/busybox/wget`, not on `PATH`. Proven: `up --wait` exits 0
with all four gated services `(healthy)`; suite 4 passed, twice cold.
