---
id: P01-S02-M01-T05
title: Experiment-overrides precedence layer
status: done
depends_on: [P01-S02-M01-T01, P04-S03-M34-T01]
evidence: [kernel/src/ai_os_kernel/configuration_manager/loader.py, tests/unit/kernel/configuration_manager/test_loader.py]
module_path: kernel/src/ai_os_kernel/configuration_manager
time_spent: 1h40m
---
**Goal:** Isolate per-experiment configuration from the base config.
**Input:** An experiment definition.
**Output:** Overrides scoped to one run.

Found overlap with `P01-S02-M01-T07` (feature flags), already `done`:
its own docstring claims Layer 6 in full, but is structurally
boolean-only (`ExperimentOverrideStore`), one real caller. Resolved
via stop-and-ask: kept separate, not unified. `load()`'s new
`pinned_conditions` (plain, per-call `Mapping[str, Any]`, never stored
on `self`) merges above `runtime_overrides`, beating it per §4 — for
any config key, not just booleans. Isolation is structural.
