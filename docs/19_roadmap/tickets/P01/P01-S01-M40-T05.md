---
id: P01-S01-M40-T05
title: Make the worker role genuinely run standalone
status: done
depends_on: [P01-S01-M40-T04]
evidence: [kernel/src/ai_os_kernel/bootstrap.py, kernel/src/ai_os_kernel/entrypoints/worker.py]
module_path: infra
time_spent: 3h15m
---
**Goal:** A worker-role container genuinely runs WorkflowWorkerLoop standalone.
**Input:** bootstrap.py's own inline construction (previously api-lifespan-only).
**Output:** worker.py:main() runs the real loop; api lifespan calls the same builder.

Extracted build_workflow_worker_loop() (bootstrap.py, public) -- called by
both _lifespan and worker.py, zero duplication. Fork resolved (product
owner chose): worker_id was one hardcoded literal shared by every caller
-- now auto-generated per process (hostname+PID) for real N-replica
identity. Real bug found+fixed proving it against the built image:
registering signal handlers *after* any bootstrap call let OpenTelemetry
config break add_signal_handler -- docker stop always hit SIGKILL; fixed
by registering first. Proven end to end, real Postgres: a standalone
worker container discovers+completes a real instance across 2 ticks, then
stops cleanly (exit 0). Zero regression: 969 unit + 311 integration + 34
security tests pass unchanged.
