---
id: P01-S03-M28-T02
title: Signed-manifest fields
status: done
depends_on: [P01-S03-M28-T01]
evidence: [kernel/src/ai_os_kernel/manifest_loader/signature.py, tests/unit/kernel/manifest_loader/test_signature.py]
module_path: platform_sdk/schemas
time_spent: 3h20m
---
**Goal:** Verify a pack manifest's signature against a configured trust anchor before activation (FR-117).
**Input:** A pack manifest and its signature.
**Output:** A recorded verify/refuse decision.

Unblocked by a product-owner scope decision (signed manifests left §10's
"Out of Scope for v1" list, became FR-117) — never blocked on M28-T01,
which was always `done`. No design existed, so all three choices went to
the product owner: **Ed25519 detached** (no new dep — `cryptography` via
`pyjwt[crypto]`), a **PEM trust-store dir** from config (a public key is
not a secret), and **config-gated enforcement, default off** (all three
committed packs are unsigned; fail-closed would need private keys in CI).
Signature covers canonical-JSON SHA-256 in `<manifest>.sig`, so **the
schema is unchanged**. Four honest outcomes; `unverifiable` is refused as
firmly as `invalid`. 18 real tests, no private key committed.
