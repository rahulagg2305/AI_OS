---
id: P06-S04-M38-T02
title: aios workflow retry, and the logs command group
status: todo
depends_on: [P06-S01-M36-T05, P06-S04-M38-T01]
evidence: []
module_path: tools/aios
time_spent: 0m
---
**Goal:** Close the CLI's last two documented gaps (`cli_design.md` §4).
**Input:** CLI arguments.
**Output:** `aios workflow retry` and the 8th command group, `logs`.
Carved out 2026-08-13 by the partial-ticket re-verification.
`P06-S04-M38-T01` reached 7 of 8 groups and named exactly two things
open. **`workflow retry` is now unblocked**: its stated blocker was
R-016, closed 2026-08-13, and the route it needs
(`POST /workflows/{id}/retry`) is real as of `P06-S01-M36-T05`. It is
irreversible-or-costly in the same sense as the four commands the
product owner already chose, so it should take `--yes`.
`logs` is the harder half and may not be buildable yet: **no `/logs`
route exists** (api_architecture.md's own gap list). Check before
building; if still absent, deliver `retry` and re-scope `logs`.
