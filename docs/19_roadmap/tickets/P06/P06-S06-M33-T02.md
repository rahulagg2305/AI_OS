---
id: P06-S06-M33-T02
title: Voice intent HTTP entry point
status: done
depends_on: [P02-S08-M36-T02]
evidence: [kernel/src/ai_os_kernel/routes/voice.py, tests/integration/test_voice_route.py]
module_path: kernel/src/ai_os_kernel/voice_jarvis
time_spent: 1h50m
---
**Goal:** Give the Voice subsystem its first production entry point (R-018 item 5).
**Input:** A structured `VoiceIntent` over HTTP.
**Output:** `POST /api/v1/voice/intent` → a real `VoiceActionResult`.

`P06-S06-M33-T01` built a real `PlatformIntentRouter` that **nothing
constructed**: zero production importers, no route, no entry point. A
thin route now assembles it from already-bootstrapped collaborators; no
intent logic moved.
The contract is invented here and disclosed: no document specifies any
voice endpoint, so path, body and status mapping were product-owner
decisions (2026-08-13), each reusing an existing precedent. Request and
response **are** the router's own models — no parallel DTOs. The route
re-checks no permission: the router gates `workflow:read`, and
`ApprovalService` its own class-scoped rule. No audio yet — STT/TTS
unbuilt, `speech_gateway` stays idle. 5 real Postgres tests.
