---
id: P06-S05-M22-T02
title: Delivery status recording
status: partial
depends_on: [P06-S05-M22-T01]
evidence: [kernel/src/ai_os_kernel/notification/recorder.py, kernel/alembic/versions/0034_notification_deliveries.py, tests/integration/notification/test_recorder.py]
module_path: kernel/src/ai_os_kernel/notification
time_spent: 1h40m
---
**Goal:** Record whether delivery succeeded.
**Input:** A send attempt.
**Output:** A recorded status.

Real `notification.notification_deliveries` table + `0034` migration,
mirrors `SqlGateResultRecorder`'s exact shape (insert-only, no
cross-schema FK on `workflow_id`, same precedent as
`context_assemblies`). `NotificationService` now records the real,
final outcome (sent/failed) after each delivery attempt. Proven against
real Postgres (3 tests) plus the existing unit suite (fake recorder,
ADR-0004). 1199 unit + integration tests pass, zero regressions.
`partial` only because T01's own gap persists; this ticket's own
Goal/Input/Output are fully met. Since `P02-S07-M17-T04` that gap is
narrower, not gone: a real outboxed `workflow.completed` is recorded
here end to end, but `approval.*`/failure still have no producer.
