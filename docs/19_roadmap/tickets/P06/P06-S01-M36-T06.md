---
id: P06-S01-M36-T06
title: GET /gates/trends
status: todo
depends_on: [P04-S01-M12-T16]
evidence: []
module_path: kernel/src/ai_os_kernel/routes
time_spent: 0m
---
**Goal:** api_architecture.md §6.4's "Pass/fail over time".
**Input:** `evaluation.gate_results`.
**Output:** Gate pass/fail counts bucketed over time.
Carved out 2026-08-13 by the partial-ticket re-verification, from
`P06-S01-M36-T04`'s own open list. **The blocker is already gone**:
`gate_results` had no timestamp at all, and `P04-S01-M12-T16` added a
server-defaulted, indexed `created_at` (migration `0038`), proven
bucketable by a real `date_trunc` aggregation over real rows. That step
deliberately stopped at the blocker and did not build the route.
Reuse `GET /gates/results`' own settled decisions rather than reopening
them: `EVALUATION_READ`, the `{"items": [...]}` envelope, and
`SqlGateResultRecorder` as the home for the query. The one open shape
question is the bucket granularity (day vs hour) and whether it is a
caller parameter — small, but no document states it.
