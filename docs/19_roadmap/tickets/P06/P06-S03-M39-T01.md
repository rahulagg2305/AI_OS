---
id: P06-S03-M39-T01
title: Dashboard scaffold on the generated client
status: done
depends_on: [P06-S01-M36-T01]
evidence: [dashboard/package.json, dashboard/src/App.tsx, dashboard/src/api/client.ts]
module_path: dashboard
time_spent: 2h30m
---
**Goal:** Stand up React/TS/Vite consuming only the generated API client (ADR-0018).
**Input:** The published OpenAPI contract.
**Output:** A running dashboard shell.

Real React 19 + TypeScript (strict) + Vite project. `schema.gen.ts`
generated from real `docs/07_api/openapi.json`
(`openapi-typescript`/`openapi-fetch`); `check:api-client` uses that
tool's own real `--check` drift flag, proven to catch real drift.
`App.tsx` genuinely calls `GET /api/v1/version` through the client and
renders it. Real build proven to run (`vite preview`, curled 200). 2
real vitest tests. `npm audit`: 0 vulnerabilities (real `overrides` pin
for a transitive CVE). Design fork resolved via `AskUserQuestion`:
scoped to this shell only, not ADR-0018's full stack (Router/Query,
WebSocket-into-cache, Tailwind/shadcn, Recharts, Playwright deferred).
