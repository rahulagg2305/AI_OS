---
id: P06-S03-M39-T02
title: Overview, workflows and approvals views
status: partial
depends_on: [P06-S03-M39-T01, P03-S05-M14-T04]
evidence: [dashboard/src/views/HealthView.tsx, dashboard/src/views/WorkflowsView.tsx, dashboard/src/auth/token.ts]
module_path: dashboard
time_spent: 2h45m
---
**Goal:** Render health, workflow detail and pending approvals (FR-090 to FR-092).
**Input:** API data.
**Output:** Working views with drill-down.

2 of 3 real: Health and Workflows (list -> detail drill-down), both
against the real generated client. Pending Approvals genuinely
blocked: `ApprovalRepository` has no list method; `routes/approvals.py`
has only the decide route. No list_pending()/GET .../approvals exists.
Added a minimal, disclosed bearer-token input (localStorage), not a
parallel auth mechanism, standing in for real OIDC login. Found and
fixed a real CI gap: `tsc --noEmit` alone checks zero dashboard files
(bare tsconfig); fixed to `tsc -b`. Proven against a real running
Kernel API: real typed 200s, a real 401 unauthenticated, and the same
request with a real signed JWT reaching 503, not 401.
