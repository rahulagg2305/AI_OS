---
id: P06-S03-M39-T02
title: Overview, workflows and approvals views
status: done
depends_on: [P06-S03-M39-T01, P03-S05-M14-T04]
evidence: [dashboard/src/views/PendingApprovalsList.tsx, kernel/src/ai_os_kernel/routes/approvals.py, kernel/src/ai_os_kernel/workflow_engine/human_approval.py]
module_path: dashboard
time_spent: 4h15m
---
**Goal:** Render health, workflow detail and pending approvals (FR-090 to FR-092).
**Input:** API data.
**Output:** Working views with drill-down.

3 of 3 real. Closed the real blocker: `ApprovalRepository.list_pending()`
+ `GET /api/v1/approvals` (new `approval:read` permission, granted only
to `approver`/`admin` per authentication_authorization.md §4.2's own
role table). `PendingApprovalsList.tsx` also genuinely *decides*
(Approve/Reject) through the existing decide route, closing FR-092's
full acceptance criterion, not only the read half. Real proof: 5 real
Postgres-backed backend tests (list/order/auth), 5 real Vitest
component tests (render/empty/error/decide/decide-error), `tsc -b`,
`oxlint`, and a real `vite build` served and fetched over real HTTP —
no browser-automation tool available to click through it live,
disclosed rather than assumed.
