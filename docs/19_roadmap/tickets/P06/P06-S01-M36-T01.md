---
id: P06-S01-M36-T01
title: Published OpenAPI contract artifact
status: done
depends_on: [P02-S08-M36-T01]
evidence: [scripts/export_openapi.py, docs/07_api/openapi.json, tests/contract/test_openapi_contract.py]
module_path: kernel/src/ai_os_kernel/routes
time_spent: 45m
---
**Goal:** Export the OpenAPI 3.1 document to a committed file (ADR-0018).
**Input:** The FastAPI app.
**Output:** A committed openapi.json plus a drift check.

`scripts/export_openapi.py` already existed but had no drift check
wired anywhere, so the committed artifact had gone genuinely stale
(missing the two role-administration routes, `P03-S05-M14-T07`/`T08`).
Fixed by regenerating it and adding `tests/contract/test_openapi_contract.py`,
which `ci.yml` already runs unconditionally once `tests/contract/`
has content — no new CI job needed. Proven to actually catch drift,
not just pass trivially: reverted the artifact and confirmed the test
fails before restoring the fix.
