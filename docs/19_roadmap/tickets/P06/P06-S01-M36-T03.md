---
id: P06-S01-M36-T03
title: Idempotency-Key support
status: done
depends_on: [P02-S08-M36-T02]
evidence: [kernel/src/ai_os_kernel/routes/idempotency.py, tests/integration/test_idempotency_key.py]
module_path: kernel/src/ai_os_kernel/routes
time_spent: 3h
---
**Goal:** Make unsafe requests safely retryable.
**Input:** An Idempotency-Key header.
**Output:** At-most-once effect.

Real ASGI middleware (sees request+response, unlike a dependency),
backed by the real, previously-schema-only platform.idempotency_keys
table. Resolves its own principal_id (reuses the real TokenVerifier,
never a second auth mechanism) since no route dependency has run yet
when it must decide replay-vs-conflict. Digest covers method+path+body;
a principal mismatch is also treated as a conflict (real, disclosed
extension beyond doc's literal words). Proven: real Postgres row counts
prove at-most-once effect, not just matching response bodies.
