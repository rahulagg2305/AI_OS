---
id: P06-S04-M38-T01
title: aios CLI
status: partial
depends_on: [P06-S01-M36-T01]
evidence: [tools/aios/src/ai_os_cli/main.py, kernel/src/ai_os_kernel/routes/approvals.py, kernel/src/ai_os_kernel/routes/workflows.py, docs/07_api/cli_design.md]
module_path: tools/aios
time_spent: 5h20m
---
**Goal:** Scriptable workflows, approvals, experiments and health (FR-098).
**Input:** CLI arguments.
**Output:** Machine-readable output plus exit codes.

Real `ai-os-cli`, 6 of 8 documented groups real: auth, health
(live/ready — no `detail`: `ready` already returns full detail),
workflow (start/list/show/events/manifest/cancel — only `retry`
disclosed, blocked on R-016), approve, pack, config (both complete).
Corrected 2026-08-10 (audit, `P06-S01-M36-T04`): body previously,
stalely, listed `cancel` as unbuilt (real already) and `health
detail` as a gap (never one). Genuinely not built: experiment (no
`/api/v1/experiments` route; Benchmarking Pack is 28% built, not
"0%"), logs (no route). Real --output human/json + 7-code exit table.
41 tests: pure logic, real local uvicorn server, one real Postgres
end-to-end pack lifecycle via the CLI alone.
