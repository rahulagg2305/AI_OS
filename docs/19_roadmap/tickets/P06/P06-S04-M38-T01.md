---
id: P06-S04-M38-T01
title: aios CLI
status: partial
depends_on: [P06-S01-M36-T01]
evidence: [tools/aios/src/ai_os_cli/main.py, kernel/src/ai_os_kernel/routes/approvals.py, tests/integration/test_pending_approvals_route.py, docs/07_api/cli_design.md]
module_path: tools/aios
time_spent: 4h45m
---
**Goal:** Scriptable workflows, approvals, experiments and health (FR-098).
**Input:** CLI arguments.
**Output:** Machine-readable output plus exit codes.

Real `ai-os-cli` (Typer+Rich), 6 of 8 documented groups real: auth
(local token store + local decode — no login/whoami endpoint exists),
health (live/ready), workflow (start/list/show/events), approve
(decide/list/show — list/show closed 2026-08-10 once
GET /api/v1/approvals existed and a new GET /approvals/{id} route was
added over the already-real get_by_id read), pack and config (both
fully complete). Disclosed, not built: workflow cancel/retry/manifest,
experiment (Benchmarking Pack 0%), logs (no route), health detail.
Real --output human/json + the full 7-code exit table. 36 tests: pure
logic, real local uvicorn server (every degrade path), one real
Postgres end-to-end pack lifecycle via the CLI alone.
