---
id: P06-S04-M38-T01
title: aios CLI
status: partial
depends_on: [P06-S01-M36-T01]
evidence: [tools/aios/src/ai_os_cli/main.py, kernel/src/ai_os_kernel/routes/approvals.py, kernel/src/ai_os_kernel/routes/workflows.py, docs/07_api/cli_design.md]
module_path: tools/aios
time_spent: 7h35m
---
**Goal:** Scriptable workflows, approvals, experiments and health (FR-098).
**Input:** CLI arguments.
**Output:** Machine-readable output plus exit codes.

Real `ai-os-cli`, **7 of 8** documented groups real: auth, health,
workflow (only `retry` blocked, R-016), approve, pack, config, and —
2026-08-13 — **experiment** (create/run/show/compare over §6.3's now-real
routes; its stub's "no /api/v1/experiments route" blocker had become
false). No `experiment list`: §4's tree names four, and a fifth would be
invented surface. Only `logs` remains (no route). **Two real gaps found
while here:** `tools/aios/tests` had *never run in CI* — the exact R-014
defect in a second place, now fixed with a CI step; and §4's documented
`--yes` convention is unimplemented CLI-wide, including for
`experiment run`, which spends real money — recorded, not papered over
for one command. 49 tests pass (41 + 8 new).
