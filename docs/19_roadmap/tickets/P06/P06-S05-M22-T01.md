---
id: P06-S05-M22-T01
title: Notification channels
status: partial
depends_on: [P02-S07-M17-T02]
evidence: [kernel/src/ai_os_kernel/notification/service.py, kernel/src/ai_os_kernel/notification/webhook.py, tests/unit/kernel/notification/test_service.py]
module_path: platform_services/notification
time_spent: 2h20m
---
**Goal:** Deliver approval, failure and completion notices (FR-116).
**Input:** A platform event.
**Output:** A delivered notification.

Real, minimal slice, not the full framework. Two forks resolved via
`AskUserQuestion`: (1) built inside the Kernel
(`ai_os_kernel.notification`), not the separate `platform_services`
package this ticket names — no real `uv` workspace member exists and
EventBus has no `ai-os-sdk` Protocol, so a new package would start a
new "pack imports Kernel internals" instance; (2) one real channel
(webhook), no Preference Manager/Router/retry/other adapters. Proven
end to end against a real local HTTP server, 8 tests. Still unwired to
a real producer — the same disclosed gap already accepted for the
WebSocket endpoint.
