---
id: P06-S01-M36-T04
title: Remaining documented routes
status: partial
depends_on: [P02-S08-M36-T02]
evidence: [kernel/src/ai_os_kernel/routes/config.py, kernel/src/ai_os_kernel/routes/approvals.py, kernel/src/ai_os_kernel/routes/agents.py, kernel/src/ai_os_kernel/routes/gates.py]
module_path: kernel/src/ai_os_kernel/routes
time_spent: 11h30m
---
**Goal:** Implement the rest of the ~45 documented endpoints.
**Input:** api_architecture.md.
**Output:** Implemented, tested routes.

§6.2/§6.5 fully closed (every documented endpoint real); §6.1 real
except retry (blocked on R-016, deferred). §6.4 has GET /agents
(registered-agents half only) and GET /api/v1/gates/results
(2026-08-10, new SqlGateResultRecorder.list_all, gated on
EVALUATION_READ). Audit finding (2026-08-10): api_architecture.md's
own Built list/count had gone stale — undercounted §6.2/§6.5, omitted
4 real out-of-band routes (evaluation/cost-and-quality, security/
role-grants, version, workflows/se.delivery_pipeline) entirely.
Corrected: ~23/~45 real, not the stale "17"; health/detail is
deliberately unbuilt (ready covers it, disclosed, not a gap). ~15
routes remain: retry/gates(trends,usage x2)/§6.3(6)/§6.6(5).
