---
id: P06-S01-M36-T04
title: Remaining documented routes
status: partial
depends_on: [P02-S08-M36-T02]
evidence: [kernel/src/ai_os_kernel/routes/usage.py, kernel/src/ai_os_kernel/routes/gates.py, kernel/src/ai_os_kernel/routes/config.py, kernel/src/ai_os_kernel/routes/approvals.py]
module_path: kernel/src/ai_os_kernel/routes
time_spent: 14h10m
---
**Goal:** Implement the rest of the ~45 documented endpoints.
**Input:** api_architecture.md.
**Output:** Implemented, tested routes.

§6.2/§6.3/§6.5 fully closed; §6.1 real except retry (R-016). §6.4 has
GET /agents (registered half), GET /gates/results, and — 2026-08-12 —
**GET /usage/tokens** at the exact documented path: totals by
model/workflow/agent/pack **incl. the cache split**, over
cache_read/write_tokens `llm_calls` always recorded and nothing read.
New `SqlTokenUsageViews`; EVALUATION_READ; no invented hit-rate (nothing
defines the denominator). **GET /usage/cost deliberately not built** —
already real at GET /evaluation/cost-and-quality over the same data, so
a second path is duplicate surface; disclosed in api_architecture.md for
a product-owner call. Open: retry (R-016), gates/trends (no timestamp),
traceability/query, logs/traces, agents "+ stats". 12 real tests.
