---
id: P06-S01-M36-T04
title: Remaining documented routes
status: partial
depends_on: [P02-S08-M36-T02]
evidence: [kernel/src/ai_os_kernel/routes/config.py, kernel/src/ai_os_kernel/routes/approvals.py, kernel/src/ai_os_kernel/routes/agents.py]
module_path: kernel/src/ai_os_kernel/routes
time_spent: 8h10m
---
**Goal:** Implement the rest of the ~45 documented endpoints.
**Input:** api_architecture.md.
**Output:** Implemented, tested routes.

§6.5/§6.2 fully closed; §6.1 real except retry (blocked on R-016,
deferred). §6.4 gained GET /api/v1/agents (2026-08-10): a new
SqlAgentRegistry.list_all, pure catalog.agents+packs metadata,
deliberately not constructing a real Agent object per row the way
resolve_agent does. "Registered agents" half only — "+ stats" needs
the still-unbuilt Evaluation Engine wiring, disclosed (the same
real-half-shipped precedent HPA's CPU-not-queue-depth already
established). New routes/agents.py, gated on workflow:read (no
dedicated agent:read permission is documented). ~14 routes remain:
§6.1(retry, blocked)/§6.4(gates/usage)/§6.6/§6.3.
