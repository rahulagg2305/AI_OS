---
id: P06-S01-M36-T04
title: Remaining documented routes
status: partial
depends_on: [P02-S08-M36-T02]
evidence: [kernel/src/ai_os_kernel/routes/config.py, kernel/src/ai_os_kernel/routes/approvals.py, kernel/src/ai_os_kernel/routes/workflows.py]
module_path: kernel/src/ai_os_kernel/routes
time_spent: 5h50m
---
**Goal:** Implement the rest of the ~45 documented endpoints.
**Input:** api_architecture.md.
**Output:** Implemented, tested routes.

Real scope is ~28 missing routes across 6 groups (flagged oversized,
product owner chose §6.5 first). §6.5/§6.2 fully closed (Config;
Approvals). §6.1 gained run_manifest then GET /workflow_definitions
(2026-08-10): a new WorkflowDefinitionCatalog.list_all read, same
lossless reconstruction as get(). Found+fixed a real, previously
latent bug: tests/integration/workflow_engine's own autouse conftest
fixture hand-inserted an invalid stub definition row (graph='{}') that
crashed the new reader — fixed to use the real register() with a
genuinely valid minimal definition. ~16 routes remain across
§6.1(cancel/retry)/§6.4/§6.6/§6.3 (no backing service for the last two).
