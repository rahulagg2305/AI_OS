---
id: P06-S01-M36-T04
title: Remaining documented routes
status: partial
depends_on: [P02-S08-M36-T02]
evidence: [kernel/src/ai_os_kernel/routes/config.py, kernel/src/ai_os_kernel/routes/approvals.py, tests/integration/test_pending_approvals_route.py]
module_path: kernel/src/ai_os_kernel/routes
time_spent: 4h30m
---
**Goal:** Implement the rest of the ~45 documented endpoints.
**Input:** api_architecture.md.
**Output:** Implemented, tested routes.

Real scope is ~28 missing routes across 6 groups — too large for one
step (flagged, options presented, product owner chose §6.5). §6.5
closed first: GET /packs, GET/PATCH /config, GET /config/flags.
§6.2 Approvals now fully closed too (2026-08-10): GET/decide/list were
already real (P03-S03-M30-T06/P06-S03-M39-T02), GET /approvals/{id}
closed via P06-S04-M38-T01 (CLI-motivated); this step's own real,
new contribution is the last route, GET /approvals/history —
cursor-paginated (new ix_approvals_decided_at index), unlike the
pending queue's own deliberately unpaginated shape. ~20 routes remain
across §6.1/§6.4/§6.6/§6.3 (no backing service yet for the last two).
