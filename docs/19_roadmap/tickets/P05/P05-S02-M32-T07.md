---
id: P05-S02-M32-T07
title: Declare the Project Intelligence pack's five Tools in a real manifest
status: todo
depends_on: [P01-S03-M28-T01]
evidence: []
module_path: capability_packs/project_intelligence
---
**Goal:** Make this pack's five real Tools declarable and installable.
**Input:** The five already-real Tool modules on disk.
**Output:** A schema-valid manifest.yaml declaring all five Tools.

Filed 2026-08-11 by the health audit (Risk A). The pack has 5 real,
tested Tools (repository_ingestion, language_detection, dependency_graph,
architecture_recovery, documentation_generation) but neither
manifest.yaml nor pack.py, so none is discoverable; M32-T01/T02's own
bodies already disclose "not manifest-declared/wired into any agent".
Deliberately depends only on the schema ticket, **not** on M32-T01..T06:
those are all `partial` behind T01's own PDF/DOCX gap, and the
done-cannot-depend-on-not-done invariant (`scripts/roadmap/model.py`)
would trap this ticket permanently — the exact cascade pathology the
audit found. Declaring is necessary but not sufficient to invoke them:
no production composition reads `catalog.tools` today (R-017).
