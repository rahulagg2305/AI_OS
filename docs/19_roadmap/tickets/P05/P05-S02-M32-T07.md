---
id: P05-S02-M32-T07
title: Declare the Project Intelligence pack's five Tools in a real manifest
status: done
depends_on: [P01-S03-M28-T01]
evidence: [capability_packs/project_intelligence/manifest.yaml, tests/integration/capability_manager/test_project_intelligence_manifest.py]
module_path: capability_packs/project_intelligence
time_spent: 2h45m
---
**Goal:** Make this pack's five real Tools declarable and installable.
**Input:** The five already-real Tool modules on disk.
**Output:** A schema-valid manifest.yaml declaring all five Tools.

Real `manifest.yaml` declaring all 5 Tools; proven against real
Postgres — the installer derives 5 real `catalog.tools` rows, and
`language.detect` resolves through `SqlToolRegistry` and runs. **R-017
payoff** (`P02-S05-M18-T04` wired the registry into production): a
declared Tool is now invocable, closing this ticket's own filing caveat.
**Two findings:** (1) 2 tools (`architecture.recover`, `documentation.
generate`) had no `inputSchema` model (schema requires one per tool) — a
real Input model added to each, matching the 3-tool precedent, `execute`
unchanged; (2) `entryPoint`/`pack.py` NOT needed — schema's `allOf`
requires one only for a pack with agents/workflows, so the title's
"entry point" was superseded (tools-only needs none). Zero regression.
