---
id: P05-S02-M32-T08
title: Project Intelligence agent over the five real Tools
status: todo
depends_on: [P05-S02-M32-T07, P05-S02-M32-T05]
evidence: []
module_path: capability_packs/project_intelligence
time_spent: 0m
---
**Goal:** Give this pack's five declared Tools a real caller.
**Input:** A repository path.
**Output:** A recovered-architecture artifact produced by an agent run.
Carved out 2026-08-13 by the partial-ticket re-verification: `T01`–`T06`
each closed with the same real remainder, so it belongs in one ticket
rather than five. The five Tools are real, schema-valid and resolvable
through `SqlToolRegistry`, but the manifest declares **no agent and no
workflow**, so nothing can invoke them in a running system — a fresh
instance of risk-register R-018's "proven but idle".
Also absorbs the LLM-narrative half `T04`/`T05` deferred (five separate
`AskUserQuestion` forks chose deterministic output over an agent).
**Expect a real design fork before any code:** agent id, entrypoint,
trust tier, and whether the manifest gains `agents:`+`entryPoint:` (the
schema requires `entryPoint` only alongside `agents`/`workflows`). Stop
and ask; do not invent the contract.
