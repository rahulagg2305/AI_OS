---
id: P05-S02-M32-T06
title: Untrusted provenance end to end
status: partial
depends_on: [P05-S02-M32-T01, P02-S03-M08-T04]
evidence: [capability_packs/project_intelligence/src/ai_os_pack_project_intelligence/provenance.py]
module_path: capability_packs/project_intelligence
time_spent: 1h05m
---
**Goal:** Tag every derived item untrusted through the whole pipeline (FR-059).
**Input:** Ingested content.
**Output:** trust=untrusted on every derived item.

`ContextItem.trust` is Kernel-internal, unimportable here — mirrors
`TrustTier`'s own independent-mirroring precedent instead: a new,
pack-local `provenance.py`. Every one of the 3 existing Tools' outputs
now carries `trust="untrusted"`, structurally (not caller-configurable
— no code path here could honestly produce "trusted"). One top-level
field per response, not per nested item (all items in one call share
one source). 3 new tests incl. 1 real Docker; 1509 unit+contract+
roadmap tests pass, zero regressions. `partial` only because T01 is
`partial` (PDF/DOCX deferred); this ticket's own Goal is fully met.
