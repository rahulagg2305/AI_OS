---
id: P05-S02-M32-T09
title: Multi-language dependency extraction
status: todo
depends_on: [P05-S02-M32-T03]
evidence: []
module_path: capability_packs/project_intelligence
time_spent: 0m
---
**Goal:** Extend `dependency.graph` beyond Python (FR-052).
**Input:** A structural model containing non-Python sources.
**Output:** Real import edges for at least one further language.
Carved out 2026-08-13 by the partial-ticket re-verification. `T03` built
real Python import-edge extraction via stdlib `ast` and disclosed
"Python only" as deferred; that deferral was never ticketed, so it was
invisible to the roadmap.
`ast` has no equivalent for other languages, so this needs a real parsing
decision (a per-language parser dependency, a regex heuristic — already
rejected once for Python — or tree-sitter). **That is a genuine fork:
stop and present options rather than picking one.** `language.detect`
already reports which languages are really present, so the ticket can be
scoped to whatever the repository actually contains.
