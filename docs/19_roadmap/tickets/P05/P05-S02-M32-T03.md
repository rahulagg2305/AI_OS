---
id: P05-S02-M32-T03
title: Dependency graph
status: done
depends_on: [P05-S02-M32-T01]
evidence: [capability_packs/project_intelligence/src/ai_os_pack_project_intelligence/tools/dependency_graph.py, capability_packs/project_intelligence/tests/test_dependency_graph_live.py]
module_path: capability_packs/project_intelligence
time_spent: 2h10m
---
**Goal:** Construct queryable module and dependency graphs (FR-052).
**Input:** A structural model.
**Output:** A graph artifact.

`dependency.graph`: real Python-only import-edge extraction via stdlib
`ast` (resolved via `AskUserQuestion` over regex-heuristic/multi-lang
and edge-free alternatives). `tier1_sandboxed` (reads+parses real file
content, mirroring T01's own ADR-0016 reasoning, unlike T02's
`tier2_trusted`). Real bug caught in manual verification before tests
were written: `from . import x` resolved to the parent package, not
`x` itself — fixed via per-name resolution. 11 new tests incl. 2 real
Docker; 1509 unit+contract+roadmap pass, zero regressions. **Closed 2026-08-13**: this ticket's own scope (a real, queryable graph)
is met. Multi-language extraction is real remaining work and is now its
own ticket, `P05-S02-M32-T09`.
