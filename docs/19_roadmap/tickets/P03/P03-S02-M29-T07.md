---
id: P03-S02-M29-T07
title: code-reviewer agent
status: done
depends_on: [P03-S02-M29-T03]
evidence: [capability_packs/software-engineering/src/ai_os_pack_software_engineering/agents/code_review.py, capability_packs/software-engineering/tests/test_code_review_agent.py]
module_path: capability_packs/software-engineering/src/ai_os_pack_software_engineering/agents
time_spent: 2h30m
---
**Goal:** Produce structured review findings with file, line, severity (FR-039).
**Input:** Generated code.
**Output:** Structured findings.

MUST-priority (unlike prior SHOULD agents); `lint.py` already disclosed
covering only FR-039's static-analysis portion. First agent to
deliberately use LLM judgment (`confidence`) as the point, not avoid
it. Reads the file back out of the sandbox (`build.py`'s write script,
reversed) instead of the usual FILE_PATH format; model returns real
JSON, parsed+validated via Pydantic. Real bug found and fixed before
shipping: an early draft skipped the JSON-context fallback every
sibling agent has. `P03-S02-M29-T08` (technical-planner) investigated
and found blocked: `foreach` is not a real `StepType` (workflow_engine.md
confirms "genuinely undeclared and unbuilt").
