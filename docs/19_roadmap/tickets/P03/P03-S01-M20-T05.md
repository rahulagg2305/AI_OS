---
id: P03-S01-M20-T05
title: Stronger isolation tier
status: partial
depends_on: [P03-S01-M20-T02]
evidence: [kernel/src/ai_os_kernel/sandbox/docker_executor.py, kernel/src/ai_os_kernel/sandbox/default_executor.py, tests/integration/sandbox/test_docker_sandbox_live.py]
module_path: kernel/src/ai_os_kernel/sandbox
time_spent: 1h50m
---
**Goal:** Offer a gVisor or Firecracker backed tier for higher-risk work.
**Input:** A trust tier request.
**Output:** A stronger sandbox.

Real, config-driven `runtime` param on `DockerSandbox`
(`AIOS_SANDBOX_RUNTIME`), the exact ADR-0016 "drop-in configuration,
not a redesign" hardening path. `partial`: neither gVisor nor
Firecracker is installed in this dev/CI environment (`docker info`:
only runc/nvidia) so hardened isolation is unverified — only the
plumbing is proven. No per-request "trust tier" surface built — one
global knob, matching `AIOS_SANDBOX_BACKEND`'s precedent. Related
finding 2026-08-10 (P07-S01-M40-T01): runsc genuinely runs on this
same Docker Desktop/WSL2 kernel via a throwaway kind node — real,
positive evidence this gap is closeable; not closed here — a
persistent host daemon.json edit, deferred pending approval.
