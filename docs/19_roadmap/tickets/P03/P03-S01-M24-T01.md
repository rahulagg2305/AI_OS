---
id: P03-S01-M24-T01
title: Git integration service
status: done
depends_on: [P03-S05-M14-T04, P03-S05-M14-T05, P01-S02-M19-T02]
evidence: [kernel/src/ai_os_kernel/git_integration]
module_path: kernel/src/ai_os_kernel/git_integration
time_spent: 5h
---
**Goal:** Commit, branch and push generated work under policy.
**Input:** A workspace.
**Output:** Real Git operations, audited.

First real Platform Service (`platform_services/` had zero content). Two forks presented, both recommended options chosen: code lives in
`kernel/src/ai_os_kernel/git_integration/` (module_path stays a board label, T04/T05 precedent), not a new uv workspace member; credentialed
push built now, not deferred. Reuses `SandboxExecutor`, `AuditLogWriter` (hash-chained), `AccessBroker`/`SecretValue` — no parallel mechanism.
`GIT_ASKPASS` (temp script, mode 0700, deleted after) injects a resolved secret only into the push subprocess's own env — never a URL,
config, log, or audit payload. Protected-branch push is structurally absent (§5.1), not gated — how R-001's PR+approval gate is respected
without calling it. Proven: real commit/branch/push against a real local remote, real Postgres audit rows, credential resolved and never
leaked, protected push refused with the remote untouched.
