---
id: P03-S03-M30-T04
title: Wire git-push into se.delivery_pipeline
status: done
depends_on: [P03-S03-M30-T01, P03-S04-M31-T04]
evidence: [capability_packs/software-engineering/src/ai_os_pack_software_engineering/agents/git_push.py, capability_packs/software-engineering/workflows/delivery_pipeline.yaml]
module_path: capability_packs/software-engineering/workflows
time_spent: 6h
---
**Goal:** A real pipeline run genuinely commits/pushes through the full stack.
**Input:** A completed Build result inside a real pipeline run.
**Output:** A real commit + push, or a real, structured no-op.

New, narrowly-scoped `git-push` agent (7th, after `build`/`lint` — not `release`/`devops`, both broader, later, documented entries — force-fitting
either would misrepresent this slice). Reads `build`'s own `workingDirectory`/`filePath` via the existing `_STEP_SOURCES`. Zero-arg constructible with
`remote_url=None` by default (no real, non-hardcoded destination exists) — degrades to a real, structured no-op, the same "absent means unaffected"
shape every other optional step type already established. This is how zero regression holds: `bootstrap.py`'s own real `SqlAgentRegistry` gets no
`git_service`, so every real, current caller is genuinely unchanged; 3 of `test_delivery_pipeline.py`'s tests + 1 of `..._decision_routing.py` + 1
of `..._docker.py` (the ones that complete) needed a one-line unconfigured registry entry to stay green. `build_pack_context()` gained an optional
`git_service` param, threaded to `ToolInvokerAdapter` unchanged. Proven: a real, all-7-agent pipeline run, real Postgres, genuinely commits and
pushes to a real, separate bare repo — verified by reading its refs and blob content back directly, plus real audit rows for both operations.
