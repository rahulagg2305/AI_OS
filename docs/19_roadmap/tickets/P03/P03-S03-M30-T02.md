---
id: P03-S03-M30-T02
title: Structured Markdown specification input
status: done
depends_on: [P03-S03-M30-T01]
evidence: [capability_packs/software-engineering/src/ai_os_pack_software_engineering/workflows/spec_parser.py]
module_path: capability_packs/software-engineering/workflows
time_spent: 2h45m
---
**Goal:** Accept a structured Markdown spec, not a raw string (FR-030).
**Input:** A Markdown specification.
**Output:** Parsed, validated requirement items.

Additive (design fork resolved with product owner): new optional
`specification` field, `requirement` unchanged, zero breakage across 13
existing callers. Real bug found via integration test, not unit tests:
`inputs.get("specification")` is never populated in production (no
per-step input-mapping mechanism exists) — fixed by reading it back out
of `WorkflowStateResolver`'s own existing JSON-dumped context item.
