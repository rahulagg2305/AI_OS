# AI_OS Feature, Module & Phase Completion Tracker

**Status:** Active — Living Document
**Created:** 2026-07-28
**Last Updated:** 2026-08-09, later step (modules 5/30: **`se.product_creation` now genuinely reaches step 8 of 14 and completes end to end** — `foreach_executor` wired into a real composition for the first time, resolving the `technical-planner`/`se.implement_task` schema mismatch the immediately-prior entry disclosed (product-owner decision via `AskUserQuestion`: widen `se.implement_task`'s own `inputs` schema to `title`/`description`, matching the real producer, rather than adding a foreach-side field-mapping mechanism). A second real, previously-latent gap found and fixed: `ForeachStepExecutor`/`SubWorkflowStepExecutor` fix `principal_id` at construction time, which no real production caller had ever exercised before this step — `build_product_creation_trigger` now builds its whole composition fresh per real call, and `resume_product_creation_after_approval` discovers the real principal from the paused instance's own persisted record. Proven end to end against real Postgres: the full 8-step workflow, including one real, separate, completed `se.implement_task` child, reaches `WorkflowRunOutcome.COMPLETED`. `P08-S02-M30-T01` is now `done` — closing Stage H (P08) to 9/9, 100%. Module 5: 85% → 87%; module 30: 52% → 58%. Zero regression: full `tests/integration`/`tests/contract` (471-472 passed, 21 skipped, 2 pre-existing unrelated timing/infra flakes confirmed passing on retry), `tests/unit` (1271-1272 passed, 1 pre-existing unrelated timing flake confirmed passing on retry), `mypy --strict` clean repo-wide. See `workflow_engine.md`/`workflow_architecture.md`/`ADR-0021`'s own Implementation Status sections and module 30's own row for full detail. Prior entry, same day (module 5: **`StepType.FOREACH` and a real `ForeachStepExecutor` now exist** — ADR-0021's own "dynamic decomposition without dynamic control flow" pattern, the last of that decision's two named-but-undesigned step types besides `compensate`. Reuses `SubWorkflowStepExecutor`'s own per-item create/start/run-to-completion/join mechanism sequentially (not concurrently — a real, disclosed, narrower scope than `parallel`), bounded by a required `maxFanOut` checked before any child instance is created. Proven against a real Postgres container: a two-item plan artifact fans into two real, separate, completed children; an over-bound item count is genuinely refused first. A real, previously-latent database gap found and fixed: `workflow.workflow_steps.ck_workflow_steps_step_type` still enumerated only the prior 7 step-type literals — the first genuinely persisted `foreach` step raised a Postgres `CheckViolationError`, fixed by migration `0036_workflow_steps_foreach` plus the matching `ai_os_kernel.persistence.schema._STEP_TYPES` update. `foreach_executor` remains unwired into any real composition, mirroring `parallel_executor`/`sub_workflow_executor`'s own precedent — wiring it into `se.product_creation`'s own step 8 surfaced a second, separate, genuine mismatch: `technical-planner`'s own real plan-item shape (`taskId`/`title`/`description`) does not satisfy `se.implement_task`'s own real `inputs` schema (`{task: string}`, `additionalProperties: false`) — two independently-built, already-`done` tickets whose contracts were never checked against each other until a real `foreach` step tried to connect them; see `P08-S02-M30-T01`'s own ticket for the two real options this leaves open. Module 5: 83% → 85%. Zero regression: full `tests/integration/workflow_engine/` + `tests/integration/persistence/` (230 passed, 6 skipped), `tests/unit/kernel/workflow_engine/` (233 passed), `mypy --strict` clean repo-wide. See `workflow_engine.md`/`workflow_architecture.md`/`ADR-0021`'s own Implementation Status sections for full detail. Prior entry, 2026-08-03, module 5: **Scheduler for delayed/scheduled workflow starts is real** — `ai_os_kernel.workflow_engine.scheduler.WorkflowScheduler`/`run_scheduler_loop`, the identical Lease Reaper/worker-loop pattern, starts a `created` instance once its own real, persisted `scheduled_at` (migration `0032`) is due; started in `_lifespan` as a fifth background task. Proven end to end, real Postgres, no manual call. Module 5: 80% → 83%. See `workflow_engine.md`'s own Implementation Status for full detail. Prior entry, 2026-07-30, modules 30/36: **Second normal feature step since the Platform SDK initiative closed** — `se.delivery_pipeline` is now reachable over real HTTP (`POST /api/v1/workflows/se.delivery_pipeline`), reusing the exact authenticated-route pattern the platform demo route already established (`workflow:start` permission). The real composition (`build_pipeline_trigger`/`build_pipeline_context_manager`/`load_pipeline_definition`) was promoted from test-harness code (`tests/integration/_delivery_pipeline.py`) into real Kernel code (`ai_os_kernel.workflow_engine.delivery_pipeline`), so the route and the pre-existing integration tests now share one real implementation. `bootstrap.py` gained its first real `SqlAgentRegistry` construction. A real design bug found and fixed: a first version degraded the whole registry to unavailable when no Anthropic secret was configured, inconsistent with the platform demo's own established degrade shape — fixed to still return a real, working registry, reaching the real Workflow Engine and reporting an honest `failed` outcome instead of a blanket 503. Module 30: 18% → 22%; module 36: 15% → 17%. Zero regression: both pre-existing Delivery Pipeline end-to-end tests re-verified unchanged after the relocation. See `implementation_status.md`'s own header for the full record. Prior entry, 2026-07-30, modules 29/30: **First normal feature step since the Platform SDK initiative closed** — `requirements-analyst` wired into `se.delivery_pipeline` as its own real first step, reusing the existing `WorkflowStepOutputResolver` hand-off mechanism unchanged. All 5 of this pack's built agents (not 4 of 5) are now genuinely chained and proven end to end (deterministic `LocalSubprocessSandbox` + live `DockerSandbox`), each hop read back from real, persisted step output, not hand-copied. Module 29: 30% → 33%; module 30: 15% → 18%. Zero regression to the 4 already-working steps beyond the new upstream source feeding Architecture's own already-existing `{{context}}` prompt variable (no prompt template changes needed). See `implementation_status.md`'s own header for the full record. Prior entry, 2026-07-30, module 27: **Step 15 of 18 (+2 inserted) complete — the plan's final step. Platform SDK v1.0.0 is fully closed out.** Built and proved all 9 `pack_contract_suite` checks together (`ai_os_sdk.testing.pack_contract_suite`, `run_pack_contract_suite()`) against the real, fully-migrated pack, zero waivers, in `tests/contract/test_pack_contract_suite.py` (20 tests, new — activates the already-prepared CI gate). A discovered arithmetic correction: the genuinely remaining checks were **seven** (2, 3, 4, 5, 6, 8, 9), not the "remaining 8" this document itself said through step 14 — check 7 already shipped at step 8, check 1 was never "remaining" (already real via the Kernel's Manifest Loader). One further real, discovered gap fixed: the pack's own `pyproject.toml` still declared a stale runtime dependency on `ai-os-kernel` and none on `ai-os-sdk`, invisible to check 7's source-only AST scanner. Module 27: 80% → **100%, fully complete**. Full closeout: `platform_sdk_v1_scope.md` §6s/§10. Zero regression: root suite 909 → 929 (exactly the 20 new tests), both real Delivery Pipeline tests unchanged, `mypy --strict`/`ruff` clean. Prior entry, 2026-07-29: **Step 14 of 18 complete — the sixth and final module migration, closing out the entire pack.** `pack.py` now imports `PackContext`/`PackRegistration`/`HealthReport` from `ai_os_sdk.contracts` directly instead of the Kernel's own compatibility re-export — a zero-behavior-change swap, since step 7 already made that Kernel module a real re-export of the identical SDK objects. **Zero `ai_os_kernel` imports remain anywhere in this pack's own source tree, for the first time in this project's history** — the real scanner returns an empty violation list, pack-wide. `pack_contract_waiver.yaml` is **deleted outright**; check 7 now enforces unconditionally, with zero waivers and zero exceptions. **The module-27 hard gate is correspondingly LIFTED** (product-owner decision, `docs/process/standing_rules.md`) — this revises that gate's own previously-recorded "only after step 15" condition, since the gate's real substance ("a real, migrated, and verified pack") is now satisfied; step 15's remaining 8 checks are real, scheduled completeness work, no longer a precondition for this specific gate. `capability_pack_contract.md`'s dated exception language is retired, replaced with a plain statement of permanent compliance. Modules 29/32/33/34 unfrozen. Both real Delivery Pipeline end-to-end tests still pass; a real wheel build still succeeds. 74% → 80%. Prior entry: **Step 13 of 18 complete** — `documentation` migrated onto the real Platform SDK, the fifth and final agent migration. Its write uses `PLATFORM_PYTHON_INTERPRETER` (the step 12a fix) from the start, confirming that step's own prediction — no repeated tension. **A real, further data point on the lock-obsolescence finding**: this agent needs no lock at all (unlike `build`'s narrower one), since it lazily builds/creates nothing, confirming steps 10/11's own finding generalizes fully. The `evaluation.llm_calls` gap reaches its full, final, permanent extent (all 4 LLM-calling agents now affected) — marked in §6a as expected-to-stay-open, not a fresh regression. **A real, discovered correction to this step's own initial framing**: `pack_contract_waiver.yaml` is NOT literally empty after this step — it retains exactly one entry, `pack.py` (this pack's own `CapabilityPack` entry point), since migrating it is step 14's explicitly scoped job, not step 13's; `pack.py` was never one of the 5 agents. Check 7 passes with exactly 1 module waived (down from 2), confirmed by the real scanner, setting up step 14 to be the one that finally empties and deletes the waiver. Both real Delivery Pipeline end-to-end tests pass with the pipeline now **fully migrated onto the Platform SDK** — a real documentation artifact genuinely written through the new `ToolInvoker` path in both the deterministic and live-Docker tiers. 70% → 74%. Prior entry: **Step 12a of 18 (+2 inserted) complete** — closes the real `python_command` regression step 12 found and recorded: a new SDK-level placeholder token, `PLATFORM_PYTHON_INTERPRETER`, that `ToolInvokerAdapter.invoke()` (the one place still holding the real `SandboxExecutor`) expands into that instance's own real `python_command` before dispatch. `build.py` no longer has a `python_command` constructor parameter or any backend knowledge at all — the pre-migration "always automatically correct" guarantee is restored exactly, not merely documented better. Proven against both real backends: a real `LocalSubprocessSandbox` (`test_tool_invoker_adapter.py`) and a real, live `DockerSandbox` (new `test_tool_invoker_adapter_docker.py`). §6a's second Known Regression entry marked ✅ RESOLVED; `documentation` (step 13) confirmed to need the identical fix, not a new one. 69% → 70%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, 11, and 12 of 18 (+1) complete** — `build` migrated onto the real Platform SDK, the first agent needing real gateway AND tool injection together (`context.llm`/`context.prompts`/`context.tools`). Its write now happens through `context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, ...)`, proven against a real, live Docker sandbox — a real file genuinely written inside a real container through the new path, the pipeline's most important ToolInvoker proof since Build is its real file-writer. A real, discovered tension recorded: the migrated agent can no longer dynamically discover its sandbox backend's own Python interpreter command (the old code asked its injected `SandboxExecutor` directly; a generic `ToolInvoker` cannot answer this), resolved with a constructor default matching the real system-wide default backend and tracked as a second, distinct entry in §6a Known Regressions. The `evaluation.llm_calls` gap recurs a third time. 66% → 69%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, and 11 of 18 (+1) complete** — `architecture` migrated onto the real Platform SDK, following step 10's established pattern exactly: zero new tension between the (already-removed) lazy-build lock and injection, since `architecture` used the identical pattern `requirements-analyst` already proved. The `evaluation.llm_calls` observability gap first found at step 10 is confirmed to recur, not a one-off, and is now tracked in a new, dedicated §6a Known Regressions section so it survives past individual step reports; 63% → 66%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, and 10 of 18 (+1) complete** — `requirements-analyst` migrated onto the real Platform SDK, the second migration and the first needing real `context.llm`/`context.prompts` gateway injection; the old lazy-build lock found obsolete (not conflicting) once `bind_pack_context()` genuinely runs in production; a real, previously-latent circular import (`workflow_engine.registry` ↔ `sdk_adapters.pack_context`, introduced by step 9a) found and fixed; one accepted capability loss recorded (no `evaluation.llm_calls` recording — no SDK Telemetry surface exists yet); zero behavior change proven; 60% → 63%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, and 9a of 18 (+1) complete** — `SqlAgentRegistry`/`SqlToolRegistry` now genuinely call `bind_pack_context()` for real in production, closing step 9's own discovered gap before it could repeat 4 more times; `EntrypointLoader` untouched; zero regression across every registry-dependent test; 58% → 60%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, and 9 of 18 complete** — `qa-test` migrated onto the real Platform SDK, the first of six migration steps; two real gaps found and resolved (context duck-typing, `SqlAgentRegistry`'s missing `bind_pack_context()` wiring), zero behavior change proven via the real Delivery Pipeline end-to-end tests; 55% → 58%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, and 8 of 18 complete** — `pack_contract_suite` check 7 (a real AST import-scanner) and its documented, expiring waiver mechanism are both real and CI-wired, proven both ways (fails without a waiver, passes with one) by an automated test against the real pack's own 44 real `ai_os_kernel` imports; 50% → 55%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, and 7 of 18 complete** — `pipeline.py` relocated out of the pack's shipped wheel (proven via a real wheel build and both relocated tests passing unchanged), `ContextService` boundary models landed, and the full entry-point contract formally relocated from the Kernel into `ai_os_sdk`, additively; 45% → 50%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, and 6b of 18 complete** — the `EntrypointLoader` zero-argument injection blocker is resolved without changing `EntrypointLoader` itself, via a new `PackContextReceiver` SDK Protocol and a real, permission-gated `PackContext`, proven end to end against a real test entrypoint; 40% → 45%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, 6, and 6a of 18 complete** — real Kernel-side adapters (`kernel/src/ai_os_kernel/sdk_adapters/`) now genuinely implement `LLMGateway`/`PromptRegistry`/`ToolInvoker` over `DispatchingLLMGateway`/`PromptEngine`/`LocalSubprocessSandbox`, proven against real underlying objects, resolving the timeout-precedence nuance step 6 left open; 34% → 40%. Prior entry: **Steps 1, 2, 2a, 3, 4, 5, and 6 of 18 complete** — the `ToolInvoker` Protocol is real, proven against the actually-executed sandbox, which found and fixed a too-strict validator and a real circular import; 30% → 34%. Prior entry: **Steps 1, 2, 2a, 3, 4, and 5 of 18 complete** **Steps 1, 2, 2a, 3, and 4 of 18 complete** — the `LLMGateway` Protocol is real and the actual `DispatchingLLMGateway` provably satisfies it unmodified; 22% → 26%. Prior entry: **Steps 1, 2, 2a, and 3 of 18 complete** — the first two Protocols exist and every real agent/tool provably satisfies them unmodified; 18% → 22%. Prior entry: **Steps 1, 2, and 2a of 18 complete** — the five Protocol/reality reconciliation decisions are recorded in `platform_sdk.md`; Step 3's scope shrank as a result. 15% → 18%. Prior entry: the build sequence was revised from 15 to 18 steps after an independent architecture review — see `../03_architecture/platform/platform_sdk_v1_scope.md` §6a — and Steps 1–2 are complete (packaging scaffold + error taxonomy + shared boundary models), 10% → 15%. See `../03_architecture/platform/platform_sdk_v1_scope.md`. Prior entries: the build was scoped as a 15-step plan, awaiting approval; before that, the product-owner decision recorded module 27 as a **hard gate** blocking any new agent (module 29) or new pack (modules 32–34) until it is built. See `history/026_platform_sdk_gate_and_audit_completion.md`.)

> **This document is now cross-referenced from every architecture document.** As of the 2026-07-28 consolidation audit, 82 documents carry an `## Implementation Status` section pointing here as the authority on per-module completion. When you change a module's percentage here, that document's own section may also need updating — `docs/process/standing_rules.md` covers the discipline.

---

## 1. Purpose & How to Use

This is the master tracker for **every** documented v1 feature, module, and remaining phase of AI_OS, cross-checked against real source inspection (not assumption) as of 2026-07-28. It complements, and is more granular than, `implementation_status.md` (which stays short and links here).

- **Section 2** — the full documented feature/capability inventory, grouped by area, extracted from every architecture document.
- **Section 3** — the module list: every Kernel component, Platform Service, Capability Pack, and cross-cutting system, as a distinct trackable unit.
- **Section 4** — concrete, checkable implementation phases, reconciled with `implementation_roadmap.md`'s Stage A–H sequence.
- **Section 5** — the completion table: the living tracker. **Update this table's percentages, status, and "what remains" at the end of every future step** (standing rule — see `CLAUDE.md` / `docs/process/standing_rules.md`).
- **Section 6** — overall weighted progress and the single biggest remaining phase.

**Honesty rule for this document:** a percentage reflects real source inspection (code read, `find`/`grep` run, tests counted), never a guess from a document's intent. An empty package directory is 0%, not "in progress." An unstarted item says so plainly.

---

## 2. Full Feature / Capability Inventory (Documented v1 Scope)

Grouped by area. Each group cites its governing document(s). This is what the *documentation* specifies — Section 5 tracks what actually exists.

### 2.1 Platform Architecture & Cross-Cutting Rules
*(`system_architecture.md`, `technology_stack.md`, `kernel_architecture.md`)*
- Modular-monolith Kernel with 17 domain-agnostic components; two deployable process roles (`api`, `worker`) from one image
- Hexagonal/interface-driven design; Capability Packs as the only extension mechanism; Platform SDK as the only pack-facing surface
- 8 mandatory architectural rules (domain logic only in packs; all model/speech traffic through their Gateways; Workflow Engine is sole orchestrator; no runtime agent-to-agent orchestration; declared-not-planned control flow; Tier 1 sandbox for all untrusted execution; etc.)
- Full, ADR-backed technology stack: Python 3.12/`asyncio`/Pydantic v2/`uv`; PostgreSQL 16 + SQLAlchemy Core + Alembic + `asyncpg`; `pgvector` + FTS + RRF hybrid search; Redis 7; FastAPI + OpenAPI 3.1 + WebSocket; React 19 dashboard stack; OpenTelemetry + `structlog`; OIDC + Vault; Docker/Podman/gVisor sandboxing; 25 Accepted ADRs governing all of the above

### 2.2 Workflow Engine (`workflow_engine.md`, `workflow_architecture.md`, `workflow_patterns.md`, `state_management.md`, `error_handling_retry.md`)
- Definition loading/validation against the Workflow Contract; instance create/run/pause/resume/complete
- Event-sourced state (append-only log + snapshot, one transaction) across 9 canonical states (`created`…`cancelled`)
- Lease-based work distribution (`SELECT…FOR UPDATE SKIP LOCKED`, heartbeat, expiry reclaim)
- 7 supported step types: Agent, Tool, Decision, Parallel, Sub-workflow, Quality Gate, Human Approval
- Step Contract: 5 invocation fields (`agentId`, `toolId`, `promptId`, `promptVersion`, `modelAlias`)
- Idempotency keys per `(workflow_id, step_name, attempt)`; mandatory per-instance workspace isolation
- 8 orchestration patterns: Sequential, Parallel (with mandatory `all`/`any`/`collect` join policy), Request–Review–Revise Loop (bounded iterations + cost ceiling), Human-in-the-Loop Gate, Quality Gate Pipeline, Fan-out/Fan-in, Compensation/Saga, Experiment
- Gate Coordinator (interprets Quality Gate Engine results, decides consequence); Human Approval Manager (durable pause/resume); Failure & Retry Manager; Scheduler (delayed/scheduled starts, concurrency limits)
- Unified 6-category error taxonomy (`transient`/`permanent`/`quality`/`infrastructure`/`budget`/`security`) with a single retry-ownership boundary (Gateway owns provider retry, Workflow Engine owns step retry)
- Full execution replay support

### 2.3 LLM Gateway (`llm_gateway.md`)
- Sole provider-access point (generation, tool calls, structured output, streaming, embeddings, token counting) — provider SDKs importable only inside adapters
- Neutral `LLMRequest`/`LLMResponse` contract; alias-only routing (never literal model IDs); per-alias fallback chains
- Capability Negotiator matrix (13 fields) + 4 degradation rules (use/emulate/fail/never-emulate)
- Retry & Fallback Manager: chain traversal, circuit breaker, exponential backoff+jitter, unified error taxonomy
- Policy & Budget Enforcer: 5 documented checks (per-step, per-workflow, per-experiment cost ceilings; allowed-model policy; per-principal rate limit; context-window-fit pre-check)
- Prompt Cache Planner (ADR-0025 breakpoint placement); Rate Limiter; Response Normalizer; Token & Cost Accountant (sole source of spend truth)
- Provider-adapter conformance suite; adding a provider requires zero pack/agent changes (NFR-101)
- Every call emits a span + `evaluation.llm_calls` row; never logs API keys, prompt bodies with customer code, or raw provider responses

### 2.4 Prompt Engine (`prompt_engine.md`)
- Prompt as versioned, immutable artifact (SemVer); Markdown files owned by packs, rendered via restricted (non-executing) templating
- Prompt Registry, Version Manager, Template Renderer, Variable Validator, Prompt Resolver (role/alias-based), Composition Engine (inheritance)
- `cache_boundary_index` marking the stable/volatile split for LLM Gateway prompt caching
- Every render traceable: prompt ID + version + variables + linked LLM call

### 2.5 Context Manager (`context_manager.md`)
- Assembles context from 6 documented sources: Workflow State, Knowledge Manager, Memory Manager, AI Context Packs, Runtime Configuration, User Inputs
- Mandatory `trust: trusted|untrusted` tagging on every `ContextItem`; untrusted content structurally delimited, never grants authority
- Size & Token Budget Enforcer (hard limit, explicit `items_excluded_count`, no silent truncation)
- Context Filter/Ranker as a first-class component; Context Audit Logger (persisted, replayable assembly record)
- Deterministic assembly (same request/conditions → same context) for experiment reproducibility

### 2.6 Knowledge Manager & Memory Manager (`knowledge_manager.md`, `memory_manager.md`, `knowledge_base_structure.md`)
- Knowledge: platform knowledge (Constitution, architecture, ADRs, standards) + project knowledge (requirements, specs, generated docs); ingestion, indexing (structured + vector), query engine, versioning, provenance tracking
- Directory taxonomy: `architecture_memory/`, `best_practices/`, `design_patterns/`, `anti_patterns/`, `engineering_memory/`, `lessons_learned/`, `reusable_patterns/`, `reusable_workflows/`, `known_limitations/`, `onboarding/`
- Memory: workflow (short-lived), engineering (long-lived), reusable assets — explicit promotion/demotion logic, decay/archival; memory never overrides Knowledge; writes mediated only through `MemoryService.write()`

### 2.7 Retrieval / Search & Vector Search (`search_vector_search.md`)
- Keyword (Postgres FTS/GIN), vector (pgvector HNSW/cosine), and hybrid (Reciprocal Rank Fusion) search
- Every vector records `embedding_model_id`/version/dimensions; `index_generation` pinnable for experiment reproducibility
- Metadata filters and access control applied as SQL predicates (never post-filter, never leaks through ranking)
- Documented scale trigger to a dedicated vector store (Qdrant) behind the same `VectorIndex` Protocol

### 2.8 Evaluation Engine & Benchmarking (`evaluation_engine.md`, `benchmarking/overview.md`)
- Metrics: cost, performance, quality (gate pass/fail, coverage, static/security findings), process (retries, human interventions), and pack-contributed domain metrics
- Run manifest recording (full reproducibility bundle, ADR-0022); comparison statistics with **mean and variance across ≥3 replicates**, never a single-run point value; cache-served runs excluded and flagged
- Clear responsibility split: Benchmarking Pack defines/submits experiments; Workflow Engine executes; Evaluation Engine collects/stores/computes statistics
- Reproducibility rules: pinned prompts/tools/config, sampling params excluded from experiment variables, response caching disabled for experiments

### 2.9 Configuration Manager (`configuration_manager.md`, `configuration_management.md`)
- 7-layer precedence: built-in defaults → pack defaults → platform config → environment config → runtime overrides (audited) → experiment overrides (isolated, highest precedence) → secret resolution
- Feature flags; typed/validated config (`pydantic-settings`); resolved config recorded in every run manifest
- Minimal bootstrap env vars only (`AIOS_ENV`, `AIOS_ROLE`, `AIOS_DATABASE_URL`, `AIOS_REDIS_URL`, `AIOS_SECRET_BACKEND`, `OTEL_EXPORTER_OTLP_ENDPOINT`) — everything else is a config file value

### 2.10 Manifest Loader & Manifest Schema (`manifest_loader.md`, `manifest_schema.md`)
- Dual discovery: entry-point (`ai_os.capability_packs`, production) + filesystem scan (development), validated identically
- 21 validation rules (11 schema-level, 10 semantic — kernel/SDK version compatibility, global ID uniqueness, reference resolution, monotonic-permission-subset checks, circular-dependency detection, declared-vs-actual component match)
- Fail-closed: no valid manifest → never loaded, never partially registered
- Explicitly deferred for v1: signed manifests, marketplace metadata, multi-language entrypoints

### 2.11 Capability Manager (`capability_manager.md`, `capability_pack_contract.md`)
- 8-state canonical lifecycle: `discovered → validated → installed → configured → activated → {deactivated|failed} → uninstalled`, every transition audited with actor+reason
- Activation of a behavior-affecting pack requires human approval (ADR-0007)
- Health monitoring, upgrade path, clean deactivation/removal

### 2.12 Security Manager & Threat Model (`security_manager.md`, `security_architecture.md`, `authentication_authorization.md`, `secrets_management.md`)
- 3 principal types (`user`/`service_account`/`agent`); 5 roles (`viewer`/`operator`/`approver`-per-class/`maintainer`/`admin`)
- **Monotonic narrowing**: `principal ∩ workflow ∩ agent ∩ tool = effective permissions` — no runtime elevation, structural containment of prompt injection
- Closed permission vocabulary published in the SDK; unknown permission fails manifest validation
- 12 named threats (T1–T12) each with a documented primary control (sandboxing, provenance tagging, secret isolation, manifest validation, permission narrowing, supply-chain scanning, egress allowlisting, hash-chained audit, resource ceilings, attributable approvals, Git safety rails, workspace isolation)
- Secrets: `secret://<provider>/<name>[#version]` reference scheme; 4 backends (env/file/vault/cloud); never enters Tier 1 sandbox; never sent to a model (prompt-assembly rejection check)

### 2.13 Quality Gate Engine & Quality Gates Framework (`quality_gate_engine.md`, `quality_gates_framework.md`)
- 8 standard gate categories: Build, Static Analysis, Testing, Security, Architecture & Design, Documentation, Performance, Release Readiness
- Blocking vs. Warning severity; LLM-as-judge gates may only ever be Warning severity (never the sole blocking gate)
- Gates execute inside Tier 1 sandbox when running builds/tests/analysis over generated code
- Clean separation from Workflow Engine: this engine only executes/evaluates; the Workflow Engine's Gate Coordinator decides consequence

### 2.14 Traceability Engine (`traceability_engine.md`, `traceability_model.md`)
- 10 core artifact types (Requirement, Architecture Element/ADR, Design, Module, Source File, Test Case, Gate Result, Workflow/Experiment Run, Documentation, Release)
- 8 core relationships; 7 key queries (impact analysis, coverage gaps, ADR-affected components, etc.)
- Links created explicitly only, never inferred; every link carries provenance

### 2.15 Event Bus (`event_bus.md`)
- `EventBus` SDK Protocol, swappable transport: in-process `asyncio` pub/sub (default) → Redis Streams (on documented scale trigger)
- Transactional outbox for durable/cross-process events, written in the same transaction as the state change; at-least-once delivery, idempotent-by-`event_id` consumers required

### 2.16 Health & Lifecycle, Observability & Audit (`health_lifecycle.md`, `observability.md`, `observability_stack.md`)
- Kernel lifecycle states: Starting/Ready/Degraded/Stopping/Stopped/Failed; liveness/readiness endpoints; pack health aggregation
- Two separate pipelines: telemetry (OTel traces/metrics/logs → Collector → Prometheus/Tempo/Loki/Grafana, sampled/buffered) vs. audit (`governance.audit_log`, PostgreSQL, SHA-256 hash-chained, never sampled, daily verification job)
- Standard metric naming (`aios.<subsystem>.<metric>`); 7 named dashboards; required correlation IDs on every record (Trace/Workflow/Step/Agent/Experiment/Pack/Principal)

### 2.17 Sandbox / Tool Execution (`security_architecture.md` §5, ADR-0016)
- Two trust tiers only: Tier 1 (ephemeral OCI container — no network, read-only root, non-root, caps dropped, no host mounts, no secrets, real resource limits) and Tier 2 (trusted, in-process, canonical-path-allowlisted)
- Dependency installation is a separate, explicitly-declared step through an egress proxy allowlist — never ambient network access
- `SandboxRuntime`/`SandboxExecutor` Protocol; gVisor as a hardened, non-default runtime option

### 2.18 Platform Services (`storage_service.md`, `document_processing.md`, `notification_service.md`, `caching_strategy.md`, `git_integration.md`)
- **Storage Service**: content-addressed (`sha256:<hex>`) artifact store, local-FS (dev) / S3-compatible (prod) backends
- **Document Processing**: format detection, parser adapters (Markdown/plain text/PDF/DOCX/code-aware), chunking, provenance-preserving normalization
- **Notification Service**: Dashboard/Voice/Email/Webhook channels, preference management, delivery tracking
- **Caching (Redis)**: config/secret/document/retrieval caching with correctness-preserving key construction; response caching off by default, unconditionally disabled for experiments
- **Git Integration Service**: controlled read/write Git operations; credentials never leave this service; 4 categorically prohibited operations (force-push/delete/history-rewrite/direct-push to protected branches) — absent from the tool surface entirely, not merely permission-gated

### 2.19 Platform SDK (`platform_sdk.md`)
- 15 Protocol interfaces (`LLMGateway`, `PromptRegistry`, `ContextService`, `RetrievalService`, `MemoryService`, `ToolInvoker`, `EventBus`, `ConfigService`, `SecretResolver`, `StorageService`, `WorkspaceService`, `Telemetry`, `TraceabilityService`, `QualityGateRegistry`, `SpeechGateway`)
- Boundary models (`ArtifactRef`, `TraceContext`, `SecurityContext`, `AgentRequest`/`AgentResult`, `StepBudget`, `ToolRequest`/`ToolResult`); `AiOsError` exception hierarchy
- `CapabilityPack` entry-point contract; `pack_contract_suite` (9 automated compliance checks); SemVer compatibility/deprecation rules

### 2.20 Agents & Workflows (Cross-Pack) (`agent_architecture.md`, `agent_communication.md`, `agent_catalog.md`)
- 19 documented v1 agents across packs (18 Software Engineering + 1 Project Intelligence) — corrected 2026-08-08 (found during a full pre-completion health audit): this line was never updated as the SE pack's own catalog grew from 15 to 18; §5's own module-29 row already used the current, correct count
- Mandatory agent contract (id/name/purpose/inputs/outputs/permissions/supportedWorkflows/entrypoint/version); 2 lifecycles (Registration, Invocation — 7 steps)
- 5 allowed / 5 forbidden communication patterns; agents never communicate directly, coordination only via Workflow Engine

### 2.21 Capability Packs
- **Software Engineering** (`software_engineering/*.md`): 18 agents (corrected 2026-08-08, matching `agents.md`'s own current catalog table), 6 named workflows (Full Product Creation, Feature Addition, Bug Fix/Maintenance, Refactoring, Code Review & Quality Improvement, Release), tool categories (source code, build/dependency, test, static analysis, git, documentation, container/deployment) each trust-tiered, 7 gate categories
- **Project Intelligence** (`project_intelligence/*.md`): 1 agent (`existing-project-analyzer`), 5 named workflows (Existing System Analysis, Architecture Recovery, Legacy Documentation Generation, Modernization Opportunity Assessment, Safe Enhancement)
- **Voice (Jarvis)** (`voice_jarvis/overview.md`, `14_voice_jarvis/*.md`): wake-word engine, STT/TTS via platform Speech Gateway, Intent Engine (6+ initial intent categories), Voice Session Manager, Response Generator, multi-modal hand-off with Dashboard/API/CLI
- **Benchmarking** (`benchmarking/overview.md`): experiment definition/pinning, replicate management (≥3), cost ceiling enforcement, comparison report generation

### 2.22 APIs & Interfaces (`api_architecture.md`, `cli_design.md`, `dashboard_architecture.md`, `information_architecture.md`, `monitoring_experiment_views.md`)
- REST `/api/v1` + WebSocket `/api/v1/stream`; OpenAPI 3.1 generated + CI-snapshot-tested; RFC 9457 error shape; cursor pagination; `Idempotency-Key`; per-principal rate limiting
- 9 resource endpoint groups (~45 documented endpoints): Workflows, Approvals, Experiments, Quality/Cost/Agents, Packs/Config, Traceability/Observability, Health, plus the WebSocket stream itself
- CLI (`aios`): 8 command groups (`auth`, `workflow`, `approve`, `experiment`, `pack`, `config`, `health`, `logs`), human/JSON output, 7 exit codes
- Dashboard: 10 navigation sections (Overview, Workflows, Approvals, Experiments, Quality, Cost, Agents, System Health, Logs & Traces, Settings), real-time monitoring, side-by-side experiment comparison views

### 2.23 Deployment & Operations (`deployment_architecture.md`, `observability_stack.md`)
- Two process roles (`api`, `worker`) from one digest-pinned, non-root, multi-stage image
- Docker Compose (local, 3 profiles) → Kubernetes + Helm (prod): Deployment/Service/Ingress/HPA/NetworkPolicy/PodDisruptionBudget, `runtimeClassName: gvisor` option
- Migration-as-Job (never on app startup); staged release process with human approval gate; PITR backup + quarterly rehearsed restore drills including audit-chain verification

---

## 3. Module List

Every Kernel component, Platform Service, Capability Pack, and cross-cutting system, tracked as a distinct unit in Section 5.

| # | Module | Category |
|---|---|---|
| 1 | Configuration Manager | Kernel |
| 2 | Manifest Loader (+ Manifest Schema enforcement) | Kernel |
| 3 | Health & Lifecycle | Kernel |
| 4 | Observability & Audit | Kernel |
| 5 | Workflow Engine | Kernel |
| 6 | LLM Gateway | Kernel |
| 7 | Prompt Engine | Kernel |
| 8 | Context Manager | Kernel |
| 9 | Knowledge Manager | Kernel |
| 10 | Memory Manager | Kernel |
| 11 | Retrieval / Search & Vector Search | Kernel |
| 12 | Evaluation Engine | Kernel |
| 13 | Capability Manager | Kernel |
| 14 | Security Manager | Kernel |
| 15 | Quality Gate Engine | Kernel |
| 16 | Traceability Engine | Kernel |
| 17 | Event Bus | Kernel |
| 18 | Tool Invoker (SDK interface) | Kernel/SDK |
| 19 | Secrets Manager | Cross-cutting |
| 20 | Sandbox / SandboxExecutor (Tier 1/2) | Cross-cutting |
| 21 | Storage Service | Platform Service |
| 22 | Notification Service | Platform Service |
| 23 | Caching (Redis) | Platform Service |
| 24 | Git Integration Service | Platform Service |
| 25 | Speech Gateway | Platform Service |
| 26 | Document Processing | Platform Service |
| 27 | Platform SDK (`ai-os-sdk` package) | SDK |
| 28 | Manifest Schema (JSON Schema itself) | SDK |
| 29 | Software Engineering Pack — Agents | Capability Pack |
| 30 | Software Engineering Pack — Workflows | Capability Pack |
| 31 | Software Engineering Pack — Tools & Quality Gates | Capability Pack |
| 32 | Project Intelligence Pack | Capability Pack |
| 33 | Voice (Jarvis) Pack | Capability Pack |
| 34 | Benchmarking Pack | Capability Pack |
| 35 | ~~Analytics Pack~~ — **removed 2026-07-28**, see note | Capability Pack |
| 36 | API (HTTP surface) | Interface |
| 37 | WebSocket stream (`/api/v1/stream`) | Interface |
| 38 | CLI (`aios`) | Interface |
| 39 | Dashboard | Interface |
| 40 | Deployment & Infrastructure (Compose/K8s/Helm/Terraform) | Ops |
| 41 | Threat Controls (T1–T12, cross-cutting) | Cross-cutting |
| 42 | Testing Infrastructure (unit/integration/contract/security/perf/regression) | Cross-cutting |
| 43 | CI Pipeline | Cross-cutting |
| 44 | Platform-wide `AiOsError` exception hierarchy | SDK |

**Note on #35:** `capability_packs/analytics/` was investigated (2026-07-28) — confirmed empty, never git-tracked, and not referenced in any architecture document; `functional_requirements.md` §10 names "analytics" only as an example of an explicitly out-of-v1-scope future domain pack, alongside IoT and finance. Confirmed stale scaffolding, approved for removal, and deleted (see `history/023_docker_sandbox_default_wiring.md`). Row kept, struck through, rather than silently renumbering every row below it — if a real Analytics pack is ever built, it re-enters this table as a new row with its own real percentage, not a resurrection of this one.

---

## 4. Implementation Phases

Reconciled with `implementation_roadmap.md`'s Stage A–H sequence, made concrete and checkable. The multi-agent pipeline work (built under an explicit 2026-07-27 product-owner reprioritization, ahead of and narrower than full Stage C) is tracked as its own phase, **B.5**, between B and C.

| Phase | Name | Roadmap Stage | Status |
|---|---|---|---|
| A | Platform Skeleton | Stage A | Process-complete, not exit-criteria-complete |
| B | Minimum Viable Kernel | Stage B | In progress |
| B.5 | Multi-Agent Delivery Pipeline Proof | *(ad-hoc, pre-Stage-C)* | Complete |
| C | First Real Capability Pack (full scope) | Stage C | Started (via B.5), majority remaining |
| D | Evaluation & Multi-LLM Experimentation | Stage D | Not started |
| E | Project Intelligence | Stage E | Not started |
| F | Dashboard, Voice, Notifications | Stage F | Not started |
| G | Hardening & Production Readiness | Stage G | Not started |
| H | Expansion | Stage H | Not started |

**Checkable exit criteria per phase** (condensed from `implementation_roadmap.md` §3, cross-referenced to Section 5's modules):

- **A**: process starts, ready ≤15s, discovers/validates a pack, rejects invalid manifest, emits correlated telemetry, CI green. → *Mostly met; OTLP export/Compose observability profile remain (module 4).*
- **B**: pack loads; sequential workflow completes; agent calls Gateway + a tool; **killing a worker mid-step loses no state and another worker resumes it**; tokens/cost recorded; full correlated trace. → *Core loop met (modules 5–8 partial); Tool Invoker/Event Bus/Quality Gate Engine/Platform SDK (modules 18, 17, 15, 27) block full exit.*
- **B.5**: one real, declared workflow chains 4 real agents end-to-end through a proven Tier 1 sandbox. → **Met**, narrower than B's own documented deliverable list (no Tool Invoker/Event Bus/Quality Gate Engine used).
- **C**: a specification produces built+tested code; a blocking gate halts progression; an approval pauses/resumes and never on timeout; a Tier 1 step demonstrably has no network/secrets/host access; audit chain verifies. → *Sandbox proof met (module 20); gates/approvals/audit chain (modules 13/15/4) and 12 of 16 agents (module 29) remain.*
- **D**: same workflow runs against 2 models with ≥3 replicates; report shows mean+variance, excludes cached runs, links run manifests; a completed workflow replays; provider substitution needs zero pack/agent changes. → *Second provider adapter already exists (ahead of schedule); everything else (module 12, 34) not started.*
- **E**: a real repository is analysed/documented within limits; every derived context item is tagged `untrusted`; ingestion respects resource limits. → *Not started (module 32).*
- **F**: an approval is decided from the Dashboard and the workflow resumes; live updates over WebSocket; a `viewer` sees no mutating controls anywhere; a voice status query works end to end. → *Not started (modules 36–39).*
- **G**: NFR targets met/re-baselined; a restore rehearsal succeeds incl. audit-chain verification and workflow resumption; every T1–T12 control has a passing test. → *Not started (modules 40–41).*
- **H**: remaining 8 agents with specs written first; remaining workflows; richer Project Intelligence; CLI polish; additional packs as needed. → *Not started.*

---

## 5. Completion Table (Living Tracker)

**Status categories:** `not started` / `schema-only` / `minimal slice` / `functionally complete` / `production-hardened`.
Percentages are against each module's own **documented** v1 scope (Section 2), based on direct source inspection on 2026-07-28 (file counts, `grep`/`find` against `kernel/src/`, `capability_packs/`, `tests/`, `platform_sdk/`, `infra/`, plus the full history in `docs/19_roadmap/history/`).

| # | Module | Phase | % | Status | What Remains |
|---|---|---|---|---|---|
| 1 | Configuration Manager | B | 75% | precedence mechanism complete, consuming components not yet built | **Updated 2026-08-07 (`P01-S02-M01-T05`, closes Sprint S02 to 14/14)**: this row was severely stale — every one of the "4 of 7 precedence layers unbuilt" it previously claimed has in fact been real since 2026-07-31 (pack defaults `T03`, runtime overrides `T04`, secret-layer integration `T06`, change-audit trail `T08`), and feature flags (`T07`) were also already real; none of those updates had been reflected here. All 7 precedence layers are now genuinely real and tested — layer 6 in two deliberately separate halves: `T07`'s boolean-only, per-`run_id` `ExperimentOverrideStore`/`resolve_feature_flag`, and this ticket's own new, arbitrary-value `pinned_conditions` parameter on `ConfigurationManager.load()`/`load_with_secrets_resolved()` (a plain, per-call `Mapping[str, Any]`, merged strictly above `runtime_overrides` — "experiment overrides (6) beat runtime overrides (5)," isolation structural since nothing is stored on `self`). Investigated and kept separate, not unified: widening `ExperimentOverrideStore` to `Any` would have reopened `T07`'s own already-evidenced code for a real integration depth of exactly one caller (`GET /config/flags`, always passing an empty store) — too shallow to justify the cost. **Not built**: a Configuration API (`PATCH /api/v1/config` route, `aios config` CLI — the writer/store exist, the route doesn't); a Feature Flag Manager or Experiment Manager *service* (both layer-6 mechanisms are proven but nothing yet calls either from a real composition, since the Benchmarking Pack that would own experiment execution has no manifest/submission path yet); Manifest Loader integration into layer 2 (`pack_manifests` still takes already-discovered manifests as plain input). |
| 2 | Manifest Loader (+ Schema) | A | 72% | minimal slice | **Updated 2026-08-01 (`P01-S03-M02-T04`):** two semantic rules are now real — rule 13 (SDK version range, `validate_sdk_version_range`, genuinely enforceable now that `ai-os-sdk` is installed) and rule 16 (workflow-definition existence + step-reference resolution, `validate_workflow_definitions`, reusing the real `WorkflowDefinitionLoader`). Rule 20 (workflow/sub-workflow circularity) is newly confirmed *currently unenforceable*, not merely unbuilt — `WorkflowStep`'s own docstring states sub-workflow linkage has no field-level contract yet, so there is nothing to detect a cycle through. Remaining unenforced: rules 12, 14, 15, 17–19, 21 (kernel-version compatibility, cross-pack ID uniqueness, requiredTools/Prompts/qualityGates resolution, permission-subset checks, modelAlias resolution, activate() component comparison). |
| 3 | Health & Lifecycle | A | 75% | minimal slice | **Updated 2026-07-30**: `POLL_INTERVAL_SECONDS` is now genuinely enforced — `run_health_polling_loop()`, the first real, continuously-running background task this codebase has ever started, re-polls every discovered pack on the real interval for the life of the Kernel process, started in `_lifespan` and cleanly cancelled (awaited, not abandoned) before the database engine is disposed. Closes capability_manager.md §9's own three-value policy fully — decided *and* enforced. Prior (2026-07-30): `/health/ready` surfaces the Pack Health Collector's own real `catalog.packs.health` detail (status/`consecutive_failures`/`checked_at`/`failed_agents`), with a sub-threshold early-warning reporting rule. Prior (2026-07-30): the Pack Health Collector's smallest real slice; the "hard vs. soft dependency" gap decided and built. Still missing: no general graceful-shutdown coordinator (this step's own clean task cancellation is real but narrow, one task only), no Observability Hook (poll results are logged but not emitted as a metric/event). |
| 4 | Observability & Audit | A | 55% | minimal slice | **Updated 2026-08-01 (`P01-S05-M04-T04`):** the real Collector and reference backends now exist too — `infra/docker-compose.yml`'s `observability` profile (Collector, Prometheus, Tempo, Loki, a provisioned Grafana with one real dashboard panel), proven end to end against a real `docker compose up`, not merely a parsing compose file. Console exporters remain every real environment's default (nothing runs this profile automatically). `governance.audit_log` is real too (`P01-S05-M04-T05`/`T06`): a hash-chained writer and a scheduled verification job, both started in `_lifespan`. Still 1 of ~15 documented metrics exists, and Loki has no real log producer wired to it yet (the Kernel is not containerised). |
| 5 | Workflow Engine | B | 87% | minimal slice | **Updated 2026-08-09, later step (`P08-S02-M30-T01`): `foreach_executor` is now wired into a real, running composition too** (`se.product_creation`'s own step 8) — closing the "unwired" caveat the immediately-prior entry left open. Resolving the real schema mismatch that surfaced (`technical-planner` vs. `se.implement_task`) and the real `principal_id`-at-construction-time gap both took real, disclosed decisions — see module 30's own row and `workflow_engine.md`'s own Implementation Status for the full detail; no further Workflow Engine mechanism was needed beyond a shared child-composition factory (`implement_task.py`'s new `build_implement_task_instance_service`). Prior (`P08-S02-M30-T01`, earlier same day): `StepType.FOREACH`/`ForeachStepExecutor` are real — see this document's own header for the short pointer; full detail in `workflow_engine.md`/`workflow_architecture.md`/`ADR-0021`'s own Implementation Status sections. Prior (`P02-S01-M05-T13`): the Scheduler (§5.13) is real — see this document's own header for the short pointer; full detail in `workflow_engine.md`'s Implementation Status.** Prior (`P03-S03-M30-T06`): the generic multi-instance worker loop's own fixed, demo-scoped composition is now a real, *enforced* scope, not an unremarked default — `WorkflowWorkerLoop`/`list_runnable_instances` gained a real `exclude_definition_ids`, and `bootstrap.py` now excludes `se.delivery_pipeline` from that loop's own discovery entirely. Investigation (building the real Human Approval decide route, module 14's own row) found this loop's fixed composition genuinely cannot advance `se.delivery_pipeline` instances at all (no `quality_gate`/`decision`/`human_approval` executor, wrong `agent_registry`) — a real, previously-latent race this closes: the moment a paused instance is resumed to `running`, this already-polling loop could otherwise rediscover and mis-advance it with the wrong composition. `resume_pipeline_after_approval` (module 30's own row) is the real, safe resumption path instead — proven end to end via real HTTP, one real run: a real paused instance, an unauthorized decision refused, an authorized one genuinely resuming to a real commit and push. Prior (`P03-S03-M30-T05`): `human_approval_executor` now reaches a real, non-test production composition for the first time — `ai_os_kernel.workflow_engine.delivery_pipeline._build_pipeline_composition` (shared by `build_pipeline_trigger` and the new `resume_pipeline_after_approval`) wires a real `HumanApprovalStepExecutor`, closing this row's own "service-layer-only" caveat's *reachability* half (the HTTP-route half stays open, unchanged — see below). `resume_pipeline_after_approval` is a small, real, disclosed addition: `build_pipeline_trigger`'s own `trigger()` closure is one-shot (create -> start -> run), with no way to re-enter an instance it did not just create, so re-driving an existing, paused instance after a real decision needed its own function, sharing the identical composition via the new `_build_pipeline_composition` (factored out of what was `build_pipeline_trigger`'s own body). Proven in `se.delivery_pipeline`'s own real, dedicated pipeline (module 30's own row) — full detail there. Prior (`P03-S05-M14-T04`/`T05`): `human_approval` is real too — the last of the 7 step types to genuinely execute, not the last no-op. Investigated the ready list first (T05, the `governance.approvals` writer, wasn't on it — T04 depends on it — so both were built together, matching the T09-era "build what a real Task actually needs" precedent). `HumanApprovalStepExecutor` (new `workflow_engine/human_approval.py`) creates a real, persisted `approvals` row (`SqlApprovalRepository`) and raises a new, self-declaring `HumanApprovalPendingError` — caught specially by `WorkflowInstanceService.advance` (the identical "typed signal exception caught before the generic handler" pattern quality-gate/decision already established), which durably transitions the real instance to `waiting_for_human` via a new, guarded `WorkflowInstanceRepository.mark_waiting_for_human` — reusing the pre-existing lease-acquire guard unchanged, so a waiting instance is automatically excluded from the worker loop's own `list_runnable_instances` query, zero new polling-exclusion code needed. A real bug found and fixed along the way: a second, separate `run_to_completion` call against an already-waiting instance used to misreport `FAILED` (its first `run_once` fails at the lease guard *before* `advance()` is ever reached, falling into the generic handler) — fixed with a genuine `WorkflowRunOutcome.WAITING_FOR_HUMAN` (`advance_runner.py`) plus a new `WorkflowInstanceService.get_instance` passthrough so the runner can tell "genuinely waiting" apart from "genuinely failed." A `human_approval` step's own `id` and its matching `humanApprovalPoints` entry's `id` share one namespace — an implicit, previously-unenforced convention (found already honored by two pre-existing test fixtures) now made real via a new, one-directional `WorkflowDefinition` cross-validator (a step must reference a real point; an unreferenced point is still allowed, matching an existing orphaned-point test). Real, attributable resumption only: `SqlApprovalRepository.decide()` requires a real, non-blank `principal_id`, atomically both records the decision and (on `approved`) resumes the instance in one transaction, and refuses a double-decide (`ApprovalNotPendingError`) — service-layer only, by explicit product-owner decision (no HTTP route/Bearer-auth exists yet, the identical "Workflow-Engine-level mechanism first" precedent every other step type this session established; RBAC was closed as its own immediately-following step, `P03-S05-M14-T06` — see module 14's own row). Proven by 4 real Postgres-backed integration tests plus a rewritten `tests/security/test_t10_unauthorized_approval.py` (a pre-existing, deliberately disclosed security tripwire for Threat T10): genuine repeated pause across real `advance()` attempts and real wall-clock time: real, attributable resume only via `decide()`; **a real, declared `timeout` genuinely elapsing never flips a pending decision to approved** (R-001's own permanent hard rule, now a proven guarantee, not a vacuous one); an anonymous or double decision is refused; a `rejected` decision genuinely halts the pipeline before its next step. Full design and disclosed remaining scope (RBAC is now real, module 14's own row; no notifications, no auto-timeout-sweep, no `changes_requested`): `human_approval.py`'s own module docstring, `../03_architecture/governance/human_approval_points.md`'s Implementation Status. Prior (`P02-S01-M05-T15`): `se.delivery_pipeline` now declares a real `decision` step, `route-after-build` — the last of the four `P02-S01-M05-T09`–`T12` "proven, unused" capabilities to reach a real, running composition. Chose `decision` over `parallel`: concurrently running `lint`/`test` would need each branch's own output independently persisted and re-plumbed through `_GATE_SOURCES`/`_STEP_SOURCES` — real, already-proven pipeline behavior this step's own zero-regression constraint declines to disturb; `sub_workflow` had no genuine use in this single, linear pipeline. `route-after-build` reads `build`'s own real, previously-uninspected `written` field (`ai_os_pack_software_engineering.agents.build`'s own `exit_code == 0 and not timed_out`) and routes to `lint` unchanged when `true`, or straight to `test` — skipping a wasted Lint invocation and its own gate — when `false`; `documentation`'s existing `["build", "test"]` source requirement stays satisfied on both paths since `test` always runs regardless. `delivery_pipeline.yaml` bumped to `1.5.0` (manifest.yaml's own workflow entry kept in sync); `build_pipeline_trigger` now supplies `decision_executor=DecisionStepExecutor(repository)`, the identical composition shape `quality_gate_executor` already has there. Proven by two real Postgres-backed end-to-end pipeline runs: a successful write takes the `true` branch, `route-after-build` persists `{"outcome": true, "branch": "lint"}`, and `lint`/`quality-gate-lint-clean` genuinely run and pass exactly as before this step; a failed write takes the `false` branch, persists `{"outcome": false, "branch": "test"}`, and `lint`/`quality-gate-lint-clean` are genuinely never invoked at all (no `workflow_steps` row for either, and the test's own registry omits a lint agent entirely, so a wrongly-taken `true` branch would fail loudly, not silently) — `test` still runs, genuinely fails against the real, correctly-missing file (`TestInstructionError`), and the pipeline halts before `documentation`. Prior (`P02-S01-M05-T14`): `WorkflowDefinitionCatalog` gained a real reader, and the worker loop is now genuinely running — product-owner-directed (build discovery to unblock the 4 proven-but-idle capabilities: decision/parallel/sub_workflow steps and the worker loop). Investigation found the write-only catalog genuinely blocks only 2 of the 4 — `sub_workflow` and the worker loop, both needing to resolve an *arbitrary* referenced/discovered definition; `decision`/`parallel` need no cross-definition lookup at all (self-contained within one definition), staying unwired for the unrelated reason that no real pipeline has declared one yet. Added a single, exact-key `get(definition_id, version)` reader — a lossless reconstruction from exactly the `graph`/`inputs_schema`/`outputs_schema`/`id`/`version` columns `register` already writes, deliberately not a general catalog/versioning system (no listing, no update, no delete, no "latest version"). `WorkflowWorkerLoop` now resolves each discovered instance's definition through this reader instead of a composition-injected mapping — genuine, system-wide discovery. As the concrete proof of value, `run_worker_loop` is now started in `_lifespan` as a fourth real background task (`app.state.workflow_worker_task`, mirroring the lease reaper's own established pattern) — the first of the four `P02-S01-M05-T09`–`T12` "proven, unused" capabilities to become "proven, running." Proven live: a real Postgres-backed `TestClient` test creates a running instance directly against the database, then the real, `_lifespan`-started worker loop discovers and completes it with no manual `tick_once()` call anywhere in the test, and a second test proves the task is genuinely cancelled on shutdown. `SubWorkflowStepExecutor` itself was deliberately left unchanged (still its own T11 composition-injected mapping) — switching it to the new reader is real, disclosed, unclaimed follow-up work, not done as part of this step. Prior (`P02-S01-M05-T12`): a real multi-instance worker loop now exists — checked `STATUS.md`'s fresh ready list first for a Task that wires `decision`/`parallel`/`sub_workflow` into a real running composition (none exists yet), so fell back to this, the next genuinely-ready Task in the same module. Every real advance path before this step required a caller to already name one specific `workflow_id`; `WorkflowWorkerLoop`/`run_worker_loop` (`worker_loop.py`) discovers runnable instances via a new, purpose-built `WorkflowInstanceRepository.list_runnable_instances` query (`status = 'running'`, no active lease) and drives every discovered instance one step at a time, concurrently, via one real `asyncio.gather` over the same `WorkflowAdvanceRunner.run_once` a single-instance caller already uses. Definition resolution reuses the exact `P02-S01-M05-T11` composition-level-injection precedent (`WorkflowDefinitionCatalog` is still write-only), keyed by the full `(definition_id, definition_version)` pair — broader than T11's single-version case, since a worker loop can discover instances spanning more than one still-running version of the same definition. Proven by a real Postgres-backed integration test: three real instances genuinely advanced concurrently (real wall-clock timing under 0.4s for three 0.2s steps, the identical proof technique T10's `ParallelStepExecutor` established, now applied across real *instances* rather than branches), an already-leased instance correctly excluded from discovery, two real workers racing for the same freshly-runnable instance producing exactly one winner and one genuine `WorkflowLeaseUnavailableError` skip, and `run_worker_loop` itself genuinely driving a real instance to completion across real, separate ticks with no manual call in the test. Real and proven — deliberately **not** wired into `_lifespan` yet (unlike the lease reaper): doing so needs "every currently-registered `WorkflowDefinition`, system-wide," the same write-only-catalog gap the composition-level mapping sidesteps for a caller who already knows its own definitions, a real, undecided question this ticket's own Input/Output does not ask it to resolve — left real, tested, and available to a composition that does, the identical "proven, unused" precedent already established for `decision`/`parallel`/`sub_workflow`. Prior (`P02-S01-M05-T11`): the `sub_workflow` step type is no longer a no-op — unlike `decision`/`parallel`, this genuinely required creating and tracking a separate `WorkflowInstance`, not just a new field; investigation confirmed `WorkflowDefinitionCatalog` is write-only (its own docstring: "No reader, no update, no delete"), so no real path exists to look up a `WorkflowDefinition` by id at runtime. Presented three options, the product owner approved composition-level injection over building a new catalog reader or deferring to a dedicated architecture step. `SubWorkflowStepExecutor` is constructed with a plain, composition-level `{workflow_definition_id: WorkflowDefinition}` mapping (the identical shape `gate_sources`/`step_retry_targets` already establish); it genuinely creates the child instance, starts it, and runs it to completion through its own, independently-injected `WorkflowInstanceService`/`WorkflowAdvanceRunner` pair — the same classes any top-level workflow already uses, not a second execution mechanism — then joins on the completed child's own real, persisted last-step output (read via `current_step_id` + the reused `_latest_completed_output` helper, since `WorkflowInstance.outputs` is never actually written by `advance_workflow`'s completion branch — verified by reading it directly). `WorkflowStep` gained the matching field-level contract (`subWorkflowId`, the identical plain-reference shape `DecisionCondition`'s own `sourceStepId` already uses). Proven by a real Postgres-backed integration test creating a genuinely separate, completed child `WorkflowInstance` and one proving a refused, unconfigured `subWorkflowId` leaves the parent untouched, plus a fake-based unit test for the executor's own call sequencing. Unwired into any real composition yet, mirroring `quality_gate_executor`/`decision_executor`/`parallel_executor`'s own precedent. Prior (`P02-S01-M05-T10`): the `parallel` step type is no longer a no-op — `ParallelStepExecutor` genuinely runs its declared `parallelSteps` concurrently (real `asyncio.gather`/`asyncio.wait`, proven by real wall-clock timing under 0.4s for three 0.2s branches, not sequential) and joins per the real `all`/`any`/`collect` semantics (`any` genuinely cancels every still-running branch the moment one succeeds, proven by real elapsed time staying near the fast branch's own duration despite 5s slow branches). `WorkflowStep` gained the matching field-level contract (`parallelSteps`: at least two nested, inline `agent`/`tool` branches — no document defined this either, and the product owner again explicitly approved a minimal one as part of this step, the identical pattern `decision` established). Branches are declared inline, not as separate top-level steps — a deliberate scope limit avoiding a much larger change to `WorkflowInstance`'s own single `current_step_id` model. Unwired into any real composition yet, mirroring `quality_gate_executor`/`decision_executor`'s own precedent. Prior (`P02-S01-M05-T09`): the `decision` step type is no longer a no-op — `DecisionStepExecutor` genuinely evaluates a real, closed-vocabulary condition (`condition`/`branches`, a new field-level contract this step added — no document defined one before, and building one unilaterally was previously recorded as out of this module's authority; the product owner explicitly approved a minimal one as part of this step) against a named prior step's own persisted output, and `WorkflowInstanceService._resolve_next_step` reads the decision's own persisted outcome back to resolve the real next step — proven jumping over a positionally-adjacent step to reach a non-adjacent real target, not a positional walk that happens to look like branching. Unwired into any real composition yet, mirroring `quality_gate_executor`'s own precedent (real, proven, `None` by default). Prior (2026-07-31, later same day): the `AgentRegistryError`/`ToolRegistryError` retriable split, left deliberately undecided two steps ago, is now investigated and explicitly decided — no exception-type split (nothing catches either type any narrower than itself today); both now carry the identical `retriable: bool` constructor parameter `LLMProviderError` already established, set explicitly at each of the 3-4 real raise sites in `registry.py` (defaulted `False`, since three of four real causes are structural/permanent; overridden `True` at the one genuinely transient persistence-layer-failure site). A real, discovered gap fixed along the way: `except sa.exc.SQLAlchemyError` never actually caught a failure to *establish* the connection at all — that surfaces as a raw `OSError` subclass, never wrapped by SQLAlchemy; both registries now also catch `OSError`, the identical broad catch `bootstrap.py`'s own `database_check` already uses. Full reasoning: `errors.py`'s own docstrings, `registry.py`'s own per-site comments, `error_handling_retry.md`. Prior (2026-07-31): `evaluation.gate_results` now has a real writer, composed by `WorkflowInstanceService`, not built inside `QualityGateStepExecutor` — `_maybe_record_gate_result` reads a resolved gate step's own just-committed `workflow_steps` row back (needed since only `advance_workflow`/`record_failed_attempt` compute the real `attempt` number, after the executor already ran) and hands it to an injected, optional `SqlGateResultRecorder` (module 12's first real data producer); `None` by default, zero behavior change for every caller without one. Full placement reasoning and column mapping: `gate_result_recorder.py`'s own module docstring. Prior (2026-07-30, later same day): general, error-category-driven step retry now exists — `WorkflowAdvanceRunner._maybe_retry_failed_step` no longer matches `QualityGateFailedError` by type; it retries any exception self-declaring `getattr(exc, "retriable", None) is True` (the identical convention `LLMProviderError` already carries) whose `step_id` (now attached to every caught exception uniformly by `WorkflowInstanceService.advance`) has a configured target, reusing `RetryPolicy`/`reset_current_step`/`record_failed_attempt` completely unchanged. `se.delivery_pipeline`'s own `_STEP_RETRY_TARGETS` gained a real `{"build": "build"}` entry — `build` itself now retries on a real, transient `LLMProviderError`, proven end to end (retry-then-succeed, and a `retriable=False` case that still fails immediately despite a configured target). Full category-split reasoning: `advance_runner.py`'s own module docstring, `error_handling_retry.md`. Prior (2026-07-30, same day): a genuinely failed step attempt is now genuinely recorded — `WorkflowInstanceService.advance` wraps the executor call in `try`/`except`; on any exception, a new `WorkflowInstanceRepository.record_failed_attempt` writes a real `workflow_steps` row (`status="failed"`, real error detail, the same `MAX(attempt)+1`-computed attempt number the successful path already uses) and a real `step.failed` event before re-raising unchanged — closing quality_gate_engine.md §9's own recording requirement, applying to every step-executor exception, not only gate failures. Prior (2026-07-30, same day, no % change — the mechanism itself needed zero changes): a second real `quality_gate` (Static Analysis, module 15) now uses the identical `QualityGateStepExecutor`/retry mechanism as the first (Testing) via configuration alone — genuine evidence the design generalizes, not new Workflow Engine code; see module 29's own row. Prior (2026-07-30, same day): a real, bounded retry (`RetryPolicy` — declared/validated since Stage A, read at runtime for the first time) now sits on top of the quality-gate halt — `WorkflowAdvanceRunner` retries a failed gate from a configured earlier step, bounded by both attempts and duration, before genuinely giving up. `error_handling_retry.md`'s own Implementation Status has the full mechanism. Prior (2026-07-30, same day): the `quality_gate` step type is no longer unconditionally a no-op — `QualityGateStepExecutor` genuinely blocks progression for one real, composition-configured case, wired into `se.delivery_pipeline`; module 15's own Implementation Status has the full detail. Prior (2026-07-30): the Lease Manager's own proactive reclaim (`WorkflowLeaseReaper.reap_once`) is now genuinely, continuously scheduled — `run_reap_loop()` re-polls every real `LEASE_REAP_INTERVAL_SECONDS` (15.0, half the 30-second lease duration every real caller uses) for the life of the Kernel process, closing this codebase's second (of two) "future worker process framework" deferral. Still missing: all 7 step types now genuinely execute when configured (Quality Gate for two configured cases; Decision genuinely running in `se.delivery_pipeline`; Human Approval genuinely pauses/resumes with real RBAC but is service-layer-only, no HTTP route; Parallel and Sub-workflow remain real but unwired into any real composition; the multi-instance worker loop is genuinely running in `_lifespan`; the Scheduler, §5.13, is real too as of `P02-S01-M05-T13`, see the header above); no Gate Coordinator consequence logic beyond the two configured Quality Gate cases (Quality Gate Engine package itself is still 0%); no Notification Service for Human Approval's own declared channels. Core (definition load → instance → lease → agent/tool step → completion) is solid. |
| 6 | LLM Gateway | B | 70% | minimal slice | **Updated 2026-08-05 (`P02-S02-M06-T12`)**: the Prompt Cache Planner is real — `prompt_cache_planner.py`'s `plan_prompt_cache_segments()`, provider-neutral, consuming the real `cache_boundary_index` the Prompt Engine already produces (module 7, `P02-S03-M07-T06`). `AnthropicAdapter` is its one real consumer: a new, additive `LLMRequest.system_cache_boundary_index` field (`None` default, zero behaviour change) drives a real Anthropic content-block `system` payload with a genuine `cache_control: {"type": "ephemeral"}` marker — verified against the installed SDK's own real types, proven end to end with a real local server capturing the actual outgoing wire-format request body. Scope: `system` only, `AnthropicAdapter` only — no `messages`-level breakpoints, no other provider. Prior (`P02-S02-M06-T08`): `stream()` is real — `AnthropicAdapter.stream()` calls the real Anthropic streaming endpoint (`messages.create(..., stream=True)`), yielding §4.3's own normalised `LLMStreamEvent` set (real `content_block_*` names mapped to §4.3's shorter `content_*` names; a real `ping` keep-alive silently skipped; `usage` genuinely populated only on `message_delta`, matching the real API). `DispatchingLLMGateway.stream()` dispatches via a new `Streamer` Protocol, eagerly checked (not lazily on first iteration), never walking the fallback chain (a stream that already delivered partial content cannot safely restart against a different provider). Proven against a real local SSE server with real inter-event delays (genuine incremental delivery, not a blocking response dressed up as one), plus a real unsupported-content-block refusal. `complete()` does not internally stream for large `max_output_tokens` (§4.3's own further-described behavior) — a deliberate, disclosed, narrower scope, not built this step. Prior (`P02-S02-M06-T09`): `embed()` real — `LocalAdapter.embed()`, the real OpenAI-compatible `POST /v1/embeddings` endpoint (Anthropic has none), via an `Embedder` Protocol; not wired into any real caller (Knowledge Manager package is empty). Prior (`P02-S02-M06-T10`): `count_tokens()` real — `AnthropicAdapter.count_tokens()`, the real `POST /v1/messages/count_tokens` endpoint (§12), via a `TokenCounter` Protocol; no caching by content hash yet. Prior (`P02-S02-M06-T11`): a real, proactive, per-provider Rate Limiter (`RedisRateLimiter`), wired as an optional collaborator; no real per-provider numbers wired into `bootstrap.py` yet. Tool-calling loop and structured-output emulation defined in models but not exercised end-to-end; 2 of 5 budget checks real (alias, workflow); no per-principal rate limiting. Prior (2026-08-04): `models.py` gained `TraceContext.experiment_id`/`LLMResponse.served_from_cache`; `ResponseCache` not wired into `gateway.py` yet. |
| 7 | Prompt Engine | B | 65% | minimal slice | **Updated 2026-08-05 (`P02-S03-M07-T06`)**: `cache_boundary_index` (ADR-0025 §2, prompt_engine.md §12) is now real — `cache_boundary.py`'s `render_with_cache_boundary()`. Real, stopped-and-resolved fork: no automatic heuristic exists to detect a stable/volatile split from arbitrary text, and none was invented (the same class of gap `P02-S04-M10-T03` avoided); instead a real, disclosed `{{cache_boundary}}` marker the template author places explicitly, consistent with `{{variable}}` syntax. The reported index is computed on real *rendered* text (each side rendered separately through the unchanged `render_template()`), not the raw template, since substitution before the boundary can shift the real length. Standalone utility, no `PromptRenderResponse`/`PromptEngine` change. Prior (`P02-S03-M07-T05`): prompt composition/inheritance real — `composition.py`'s `compose_fragments()`/`compose_with_inheritance()`/`render_composed()`, a pure, in-memory fragment joiner (no new persisted schema) layered on the unchanged `render_template()`. Same step also corrected a stale claim: `resolver.py`'s `PromptResolver` (role→bound prompt) was already real and previously undocumented here. Still no persisted fragment catalog, no dedicated Observability writer, and nothing in the LLM Gateway yet calls `render_with_cache_boundary()` or places a real provider cache breakpoint from its result. |
| 8 | Context Manager | B | 88% | minimal slice | **Updated 2026-08-05 (`P02-S03-M08-T14`, investigated, correctly blocked, no code)**: `AIContextPackResolver`'s own production wiring — the last of the four disclosed-unwired resolvers — is genuinely, correctly blocked, not merely undone. Unlike `T11`-`T13`, no real content exists to wire against: `ai_context/` has zero tracked packs anywhere in this repository (`ai_context_strategy.md`'s own Implementation Status: "Built: nothing"). A `pack_references=()` wiring would contribute nothing while being presented as real production value — the explicit "no forced/empty wiring" constraint this step's own scope named. Authoring the first real pack is a curatorial content decision (`ai_context_strategy.md` §5: "curated over complete"), not an engineering one this ticket should invent — the identical class of gap `P02-S04-M10-T03` (Promotion Logic) already established. Blocked on: at least one real, authored, curated pack, and a decision on which composition(s) should reference which packs. Prior (`P02-S03-M08-T13`): a third resolver reached real production wiring — `MemoryResolver`, the simplest of the three: no per-step query derivation needed (unlike `KnowledgeResolver`'s `T12`) and no environment-validation failure mode to degrade from (unlike `RuntimeConfigResolver`'s `T11`), added unconditionally to the identical two real compositions `T11` already extends (`bootstrap._lifespan`/`build_workflow_worker_loop`), alongside `WorkflowStateResolver`. `memory_type="engineering"` — memory_manager.md's own documented, cross-run, long-lived category. Proven against real Postgres: the real `_build_workflow_trigger` path, given the real composition `_lifespan` now builds, genuinely renders a real, cross-run memory item — written under one workflow instance — into a *different*, later-triggered instance's own real agent output. Verified under CI's own real `AIOS_ENV=ci` locally before pushing. Prior (`P02-S03-M08-T12`): `KnowledgeResolver` into `se.delivery_pipeline`'s `requirements-analyst` step (not `documentation`, ruled out: its own contract requires the entire assembled context to be one parseable JSON object). A new, pipeline-local `_QueryFromRequirementInputResolver` derives the real query text from the pipeline's own real, required `requirement` input — zero change to `WorkflowStep`/`AgentStepExecutor`/the documented Step Contract. Prior (`P02-S03-M08-T11`): the disclosed "no production composition wires a real resolver beyond `WorkflowStateResolver`" gap closed for `RuntimeConfigResolver` first, degrading gracefully when `AIOS_ENV` isn't one of `ConfigurationManager`'s four real, documented environments. Prior (`P02-S03-M08-T07`): all 6 of 6 documented sources are now real — `AIContextPackResolver`. Still no filtering beyond relevance/budget, no automatic `applies_to`-based pack selection. |
| 9 | Knowledge Manager | B | 40% | minimal slice | **Updated 2026-08-05 (`P02-S03-M08-T12`)**: the real `KnowledgeResolver` (module 8) now has a real production composition wiring it into a running workflow — `se.delivery_pipeline`'s `requirements-analyst` step, via `bootstrap._build_knowledge_resolver` + `delivery_pipeline._QueryFromRequirementInputResolver`. Degrades to no knowledge contribution (not a crash) when no local embeddings server is configured. Prior (`P02-S04-M09-T05`): every `QueryEngine` result now carries real version provenance too — `chunk_strategy_version` (always present) plus `embedding_model_id`/`embedding_model_version`/`index_generation` (present only when a real embedding row exists under the exact model/version queried; a genuine, disclosed `None` otherwise — never fabricated). Not a schema-authority fork (unlike `T10`): every field is already real and documented in data_model.md §7, just newly surfaced through `QueryEngine`'s existing join, no new table. Deliberately not built: a Version Manager deciding what a new `index_generation` means (a separate, larger, still-open gap). Prior (`P02-S04-M09-T04`): a real Query Engine — `knowledge_manager/query_engine.py`'s `QueryEngine`, composing the real `RetrievalService` (module 11) with a real provenance join back to `knowledge.chunks`/`knowledge.documents` (`document_id`/`source_uri`/`trust`/`content` per result), also filtering `archived_at`. Prior (`P02-S04-M09-T03`): the first real code in this package itself — `indexing.py`'s `IndexingService`, real deterministic fixed-size chunking with overlap plus a real archive-and-replace change policy. Still no format detection/parser adapters, no dedicated Version-Manager/Provenance-Tracker/Access-Filter component. |
| 10 | Memory Manager | B | 18% | minimal slice | **Updated 2026-08-05 (`P02-S03-M08-T13`)**: the real `MemoryResolver` (module 8) now has a real production composition wiring it into a running workflow — `bootstrap._lifespan`/`build_workflow_worker_loop`, unconditional (`memory_type="engineering"`), proven cross-run against real Postgres through the real `_build_workflow_trigger` path. Prior (`P02-S04-M10-T01`): a real Memory store now exists one layer down — `ai_os_kernel.persistence.memory_writer.SqlMemoryStore`, real `write_memory()`/`query_memories()` against the already-real, already-documented `knowledge.memory_items` table (no schema-authority gap, unlike `P02-S03-M08-T10`). `promoted_at` is real but write-only-as-`NULL` from this store — no promotion logic exists yet, a disclosed, separate gap. Proven against real Postgres: a real memory item written and read back with real optional fields, filtered correctly by `memory_type`, isolated correctly per `source_workflow_id`, a blank `content` and an unknown `source_workflow_id` (real FK violation) both genuinely rejected. Package itself (`memory_manager/`) remains an untouched stub. **Updated 2026-08-04 (`P02-S03-M08-T06`)**: the real Context Manager `MemoryResolver` (module 8) now reads from this store, cross-run — its first genuine consumer. **`P02-S04-M10-T02` (Memory writer) investigated, not rebuilt**: its own Goal/Input/Output ("record memory items with scope and provenance" / "a memory item" / "a persisted row") are already fully satisfied by `SqlMemoryStore.write_memory()` above — same evidence, no new code, no percentage change. The genuinely distinct, pack-facing `MemoryService.write()` mediated path (memory_manager.md §6/§9 "APIs") remains unbuilt, blocked on a missing promotion permission/principal/audit destination this ticket's own scope does not ask to solve. **`P02-S04-M10-T03` (Promotion logic) investigated, marked `blocked`, nothing built**: its own Input ("Observed repetition") is exactly the automatic heuristic §6.1's own dated correction forbids ("a guess encoded as architecture"); manual promotion is v1-in-scope per §6.1, but genuinely needs the identical 3 prerequisites the "APIs"/"Promotion criteria" rows already name (permission, principal, audit destination) plus undocumented `promoted_by`/`reason` columns — real architecture decisions, not this ticket's own to invent. `promoted_at` stays real but permanently write-only-`NULL` from every real caller until one of those decisions is made. |
| 11 | Retrieval / Search & Vector Search | B | 55% | minimal slice | **Updated 2026-08-04 (`P02-S04-M11-T06`)**: a real, callable Retrieval Service now exists — `retrieval/retrieval_service.py`'s `RetrievalService`, composing the real keyword searcher and real vector searcher and handing both real result lists to the real `fuse_rankings()` behind one `search(RetrievalRequest)` call. Does not itself call `embed()` — `RetrievalRequest.query_vector` is caller-supplied, a disclosed scope boundary (the task named exactly the 3 pieces to compose). Proven against real Postgres/pgvector: one real service call returns a genuinely fused result — a chunk absent from the real keyword results and a chunk ranked worst by real vector distance both land in their real, hand-computed fused positions, reachable only by genuinely running and combining both real searches. Prior (`P02-S04-M11-T05`): real Hybrid RRF fusion — `retrieval/hybrid_search.py`'s `fuse_rankings()`, standard Reciprocal Rank Fusion (`RRF_K = 60`, the published constant, matching ADR-0013's own "no tuning parameter per corpus" reasoning), pure/no-I/O over two already-real result lists, keyed on `chunk_id`, with per-list rank provenance. Proven with 5 real, hand-computed-score tests: the fused order genuinely differs from either individual ranking, a chunk present in only one list still scores correctly, ties break on `chunk_id`. Prior (`P02-S04-M11-T04`): real vector search — `retrieval/vector_search.py`'s `SqlVectorSearcher`, pgvector's real `<=>` cosine-distance operator, `ORDER BY`/`LIMIT` (exact, not approximate, nearest-neighbour ranking; no HNSW index yet — the fixed-dimension decision it needs still has not been made). Structurally filters by `embedding_model_id`/`embedding_model_version`; `index_generation` is an optional pin. Prior (`P02-S04-M11-T03`): a real embedding writer (`embedding_writer.py`) persisting genuine `embed()` output into `knowledge.embeddings`. Keyword search also real. Still no scale-trigger logic; a real Indexing component now feeds real chunk text into the real keyword searcher (module 9's own row, `P02-S04-M09-T03`), but produces no embeddings, so vector search still has no real ingestion path — and no search-request consumer, no Context Manager resolver calls `RetrievalService`. |
| 12 | Evaluation Engine | D | 45% | schema-only, four real writers, two real readers, one real reporting interface, both real call paths, chain proven | **Updated 2026-08-09 (`P07-S03-M42-T02`)**: `evaluation.llm_calls` gained a real `created_at` column (migration `0035`) — had none at all before this step, discovered as a genuine structural blocker while building Cost Anomaly Alerting (module 42's own row), which needs a real per-call timestamp to bucket spend by hour. Backfilled honestly (existing rows get the migration's own execution time, not a fabricated historical value). No behavior change to the writer (`SqlLLMCallRecorder`'s explicit column list is unaffected; the new column fills via `server_default`). Prior (`P06-S03-M39-T03`): a second real reader now exists — `evaluation_engine.cost_and_quality_views.SqlCostAndQualityViews`, a real SQL aggregation (SQLAlchemy 2.0 Core, `func.count`/`func.sum`/`func.coalesce`, real `GROUP BY`/`JOIN`s) directly over the already-populated `evaluation.llm_calls`/`evaluation.gate_results` tables — cost breakdown by model/workflow/agent/pack (FR-095) reconciling exactly with seeded rows, and a gate-failure frequency summary ordered most-frequent-first (FR-094, honestly built as frequency, not time-series: neither table carries a timestamp column). Deliberately distinct from `EvaluationReportingInterface` below, which stays experiment-scoped and blocked on the Benchmarking Pack — this reader needs no experiment at all. Exposed via a new, real HTTP route (`GET /api/v1/evaluation/cost-and-quality`, module 39's own row) gated by a new `evaluation:read` permission granted to all 5 roles (`authentication_authorization.md` §4.2 already named "experiments, gate results" under `viewer`'s own grant). Proven by a real Postgres-backed integration test and 8 real HTTP-boundary unit tests. Prior (`P04-S01-M12-T08`, closed Module 12's own 11/11): the last of the five §5 components named in this module's own architecture doc — `ai_os_kernel.evaluation_engine.reporting_interface.EvaluationReportingInterface.get_report()` composes the real `SqlComparisonComputer` (no parallel mechanism) and returns a `ComparisonReport`, a pure, synchronous, queryable wrapper over the already-computed `ExperimentComparison`: `get_variant`, `get_metric`, and the one genuinely new capability, `compare_by_metric` — a real, cross-variant pivot ("how did every variant do on this metric") the raw, per-variant-first comparison shape does not itself support, matching `overview.md` §4's own cross-variant "Comparison Report" definition. Deliberately no persisted Results Store: the report is recomputed live from already-persisted `metrics`/`experiment_runs` on every call rather than adding an unapproved new table, a disclosed scope boundary, not an oversight. Proven end to end against real Postgres: a two-variant experiment queried by variant, by variant-and-metric, and by metric across both variants at once, plus a real, empty-but-valid report for an experiment with no runs. No real production caller yet — the same disclosed limit every real component in this module carries (module 34's Benchmarking Pack still has no manifest, so no real `experiments`/`experiment_runs` row is ever created in production). Prior (`P04-S01-M12-T07`)**: closes the second, genuinely distinct half of `overview.md` §7's own "excluded... and flagged" rule — `T06`'s own query already excluded cache-served runs from aggregates (real, tested); this step adds the real, visible flag `SqlComparisonComputer` was still missing: `VariantComparison.excluded_cache_served_count`, a second, deliberately un-joined query (a cache-served run may have no real `evaluation.metrics` row at all, so counting via the metrics join would silently miss exactly the runs this flag exists to surface). Handles the real edge case a naive implementation would silently omit: a variant whose every real replicate was cache-served now still appears, with `metrics: []` and a real, non-zero flag, rather than looking identical to "no runs happened at all." No parallel mechanism — one more query folded into the same, already-real `compute()` method. Prior (`P04-S01-M12-T06`)**: this package's own second real code — `SqlComparisonComputer` reads exactly what the Metrics Collector already wrote (real `evaluation.metrics` rows joined to `evaluation.experiment_runs`), groups by `(variant_key, metric_name)`, and reports real, `Decimal`-precise sample mean/variance via stdlib `statistics` — no parallel metric-computation mechanism. `overview.md` §7's own two real rules enforced: a cache-served run is excluded from aggregates, and only genuinely `status='completed'` runs contribute (`SqlMetricsCollector.collect()` has no real caller yet to guarantee it is only ever invoked for one). `variance` is `None`, not a fabricated `0` or a crash, when fewer than 2 real data points contribute. Proven end to end against a real Postgres: 3 real replicates per variant (mean/variance hand-verified), a cache-served run and an incomplete run both genuinely excluded, a single-replicate variant correctly reporting `variance=None`, and an experiment with zero real runs reporting zero variants. No real production caller yet — the same disclosed limit every real Benchmarking Pack/Evaluation Engine component this session carries (nothing in production creates a real `experiments`/`experiment_runs` row today). Prior (`P04-S01-M12-T04`)**: this package's own first real code — `ai_os_kernel.evaluation_engine.metrics_collector.SqlMetricsCollector`, a narrow, real Metrics Collector writing five real `evaluation.metrics` rows (one per FR-073 category: total tokens/cost, duration, gate failures, step retries), sourced entirely from already-real, already-persisted data, no new instrumentation. **A real, structural blocker found and disclosed, not silently worked around**: `evaluation.metrics.run_id` is a `NOT NULL` foreign key to `experiment_runs`, and no experiment has ever been created in production (module 34, the Benchmarking Pack that owns experiment *definition*, is 0% built) — unlike `gate_results`/`run_manifests` (both `workflow_id`-only), no ordinary `se.delivery_pipeline` run can get a real metrics row today. Design fork resolved with the product owner: build the real writer now, proven end to end against a real Postgres with a real, schema-valid, hand-seeded `experiments`/`experiment_runs` row — the identical "build real, wire later" precedent this same module already established for the Gate Registry/`gate_results`/`run_manifests`. Not composed into any production caller — `WorkflowInstance` has no real `run_id` of its own to supply. Prior (`P04-S01-M12-T11`)**: the Run Manifest Recorder's own gap 2 (resolved provider/model) is now proven closed for `se.delivery_pipeline` too, not just inferred — a real, dedicated integration test (`test_delivery_pipeline.py`) drives the real, catalog-installed pack through the real `SqlAgentRegistry` to genuine completion (the real `approve-git-push` resume) and confirms every LLM-calling step's manifest entry genuinely carries `resolved_provider`/`resolved_model_id` (and real `agent_version`/`pack_version`, since this pack is genuinely catalog-installed). **A real, structural gap surfaced building this proof, not just missing coverage:** the shared `EchoLLMGateway` echoes its input verbatim, but this pack's real, shipped `build.write_file` prompt asks a model to *follow* a `FILE_PATH`/`FILE_CONTENT_BEGIN`/`FILE_CONTENT_END` format in natural-language instructions — echoing that prompt text back is not a completion that follows it, and `BuildAgentEntrypoint`'s own real parser correctly refused it (a real `LintInstructionError` on the first attempt, not assumed). Fixed with a small, local, deterministic fake gateway (ADR-0015's own "fake network, real everything else") returning a fixed, valid `FILE_PATH`/`FILE_CONTENT_BEGIN`/`FILE_CONTENT_END` block regardless of caller. Prior (`P04-S01-M12-T10`): `evaluation.llm_calls` now has real rows from `se.delivery_pipeline`'s own SDK-native agents too — they call `LLMGatewayAdapter.complete()` directly (never `PromptedCompletionService`), disclosed as out of scope at `P04-S01-M12-T09`. Investigation found a genuine structural gap before writing code: `agent_id`/`prompt_id`/`prompt_version` are all required together, real FKs, but `LLMGatewayAdapter.complete(request)`'s signature is fixed by the SDK's own published Protocol, and the SDK's canonical `TraceContext` (`platform_sdk.md` §4.1, "field names are normative") had no field for prompt correlation at all. Product owner chose extending `TraceContext` with two new, additive, optional fields (`prompt_id`/`prompt_version`) over a new pack-facing recording capability — keeps the actual database write Kernel-internal (`LLMGatewayAdapter`, never exposed to packs), matching how `LLMGatewayAdapter`/`PromptRegistryAdapter` already hide Kernel internals. `agent_id` threaded from `SqlAgentRegistry`'s own already-resolved id at construction time (`build_pack_context`, `registry.py`); each of the 4 real LLM-calling agents in the one real pack already builds a `TraceContext` and now also passes its own already-known `prompt_id`/`prompt_version` into it. Checked for the same FK-seeding risk `P04-S01-M12-T09` found: genuinely different here — `SqlPackLifecycleRepository.register()` already derives and writes real `catalog.agents`/`catalog.prompts` rows from the real manifest (`manifest_catalog_installer.py`), so no seeding gap applies. Recording wrapped in its own `try`/`except` inside `LLMGatewayAdapter` — a downstream recording failure can never lose an already-succeeded completion's response. Proven by a real, dedicated integration test (`test_requirements_analyst_agent_pack.py`) driving the real, catalog-installed entrypoint through `SqlAgentRegistry` with an Echo-backed gateway. Prior (`P04-S01-M12-T09`): `evaluation.llm_calls` gained real rows for the `PromptedAgent`/demo composition — `AgentStepExecutor` never forwarded `stepId`/`agentId`/`workflowId` into `inputs`, so `PromptedCompletionService`'s call-recording guard never fired; fixed by forwarding all three, plus seeding two previously-missing `catalog.agents`/`catalog.prompts` rows for that synthetic composition (`bootstrap._seed_prompted_agent_catalog_rows`). Also closed the Run Manifest Recorder's own gap 2 (resolved provider/model) for that composition — proven by a dedicated test; the identical join logic now also proven closed for `se.delivery_pipeline` too, at `P04-S01-M12-T11` above. Prior (`P04-S01-M12-T05`): `evaluation.run_manifests` has a real writer — `ai_os_kernel.workflow_engine.run_manifest_recorder.SqlRunManifestRecorder`, built in the Workflow Engine (not this package), the identical placement precedent `gate_result_recorder.py` established. Fires exactly once, at genuine `advance()`-observed completion, injected into `WorkflowInstanceService` and composed at all three real sites (`bootstrap.py` ×2, `delivery_pipeline.py`); joins the run's own `workflow_steps` (highest `attempt` per `step_name`) against `catalog.agents`/`catalog.tools`/`catalog.packs` and `evaluation.llm_calls`, writes one `run_manifests` row, and stamps the previously-unwritten `workflow_instances.run_manifest_id` FK. Three gaps remain disclosed, not fabricated (full detail: `evaluation_engine.md`'s Implementation Status): (1) "model parameters actually sent" has no real source anywhere; (2) context-pack/retrieval-index data absent (`SqlContextAuditLogger` unwired anywhere); (3) "the resolved configuration set" is available but deliberately deferred to its own follow-up step. Metrics Collector (`P04-S01-M12-T04`) remains genuinely blocked — `evaluation.metrics.run_id` is `NOT NULL` with a real FK to `evaluation.experiment_runs.run_id`, which requires the unbuilt Benchmarking Pack; no real, non-experiment workflow run can ever satisfy this FK today. Prior (2026-07-31): `evaluation.gate_results` has a real, narrow writer (`SqlGateResultRecorder`) — one real row per resolved `quality_gate` step, pass or fail, for both real gate categories. Every other table (`experiments`, `experiment_runs`, `metrics`) remains schema-only; zero collector/aggregator/comparison-statistics/reporting code exists in this package itself. |
| 13 | Capability Manager | B | 68% | minimal slice | **Updated 2026-08-01 (`P02-S05-M13-T07`)**: `SqlPackLifecycleRepository.upgrade()` is real — migrates an already-`activated` pack's `catalog.packs`/`catalog.agents`/`catalog.tools`/`catalog.prompts` rows to a new manifest version in one transaction (reconciling agents/tools, appending immutable prompt versions), refusing a same-or-older version before touching any row. Answers 2 of §9's 3 named open questions from the existing schema itself (no two pack versions can be simultaneously activated; no distinct `upgrading` state exists to model); (b) — in-flight workflow instances of the old version — remains genuinely open. Built without the ADR §9 previously said this work "warrants," a disclosed product-owner decision to proceed, not an oversight. Prior (`P02-S05-M13-T08`): `SqlAgentRegistry`/`SqlToolRegistry` refuse to resolve an agent/tool whose own manifest-declared `required_permissions` exceed its pack's own manifest-declared `permissions`, reusing `security_manager.narrowing`'s intersection primitive. Prior (2026-07-30): lifecycle register/activate/deactivate/**mark_failed** real — 6 of 8 canonical §4 states now have a real write path (only `configured`/`uninstalled` remain vocabulary-only). `record_health()` writes real `catalog.packs.health` snapshots, called by `health_poller.py`. Still missing: a `FAILED` pack has no re-activation path yet (`_ACTIVATABLE_FROM_STATES` excludes it — a deliberate, deferred gap); permissions enforcement is the pack-grant ceiling only, not the full ADR-0023 principal/workflow chain. |
| 14 | Security Manager | C | 50% | minimal slice | **Updated 2026-08-07 (`P07-S02-M14-T02`)**: real, persisted role grants now take effect for *every* permission-checked route, not only `ApprovalService.decide()` — closing the "Full five-role model" gap. All 5 documented roles (`viewer`/`operator`/`approver`/`maintainer`/`admin`) were already correctly modeled/enforced via bearer-token-claimed roles, and the role-administration persistence/HTTP-route layer (`P03-S05-M14-T07`/`T08`) was already fully generic (any `role: str`, not restricted to the `approver:<class>` family); the real, remaining gap was that a persisted grant only ever took effect inside `ApprovalService.decide()`'s own narrow check. The union logic (`principal.roles | active persisted grants`) is now extracted as a shared `security_manager.role_administration.resolve_effective_roles()`, reused unchanged by `decide()` (zero behavior change, pure refactor) and newly by the general `authenticate()` FastAPI dependency (`security_manager.dependencies`), reading an optional `request.app.state.role_grant_repository` (wired in `bootstrap._lifespan`, mirroring `pack_lifecycle_repository`'s own construction; `None` for any caller with no real database, identical behaviour to before). Proven end to end, real Postgres, real HTTP: a principal whose bearer token carries no roles at all is refused `GET /api/v1/packs` (gated on `pack:read`, granted only to `maintainer`/`admin`), a real, persisted `maintainer` grant (the identical, unchanged bearer token throughout) then genuinely enables the identical request, and a real revoke disables it again — mirroring `test_role_administration.py`'s own refused→granted→enabled→revoked→refused-again proof, now for a general route instead of only approval decisions. Prior (2026-08-07, `P07-S02-M14-T01`): real OIDC verification now exists — `security_manager.token_verifier.OidcBearerTokenVerifier` verifies a JWT against a real, JWKS-fetched RS256 public key (`jwt.PyJWKClient`, real HTTP fetch and caching) and validates issuer/audience, not merely signature and expiry. `bootstrap._build_token_verifier` selects it only when `PlatformConfig.oidc_issuer`/`oidc_audience`/`oidc_jwks_uri` are all configured together — every current environment, which configures none of them, keeps the pre-shared-secret `JWTBearerTokenVerifier` unchanged, a deliberate, zero-regression design decision (not a fork) given the ticket's own literal "replace" wording would otherwise break every environment before any real provider exists anywhere. Proven against a real, self-hosted JWKS HTTP server and real RS256-signed tokens — rejects a token signed by a key absent from the real JWKS document, a wrong issuer, a wrong audience, and an expired token, each a real, distinct proof, not one shared assertion. 5 of the published permission vocabulary still modeled (`workflow:read`/`workflow:start`/`pack:read`/`pack:manage`/`secret:access`). This row's own header previously claimed "Pre-shared-secret JWT, not OIDC" outright, without noting `P03-S05-M14-T10`'s own already-real workflow-term update below — corrected. Prior (2026-08-03, `P03-S05-M14-T10`, not previously reflected in this row despite already being real): the monotonic-narrowing chain's workflow term is real too — `catalog.workflow_definitions.declared_permissions` is derived at pack-registration time and checked at resolution via a generalized `bounding_permissions` parameter shared with the principal term. Prior (`P03-S05-M14-T09`): the monotonic-narrowing chain's principal term is now real too, threaded into the identical `SqlAgentRegistry`/`SqlToolRegistry` resolution point module 13's own row already enforces the pack-grant term at — `security_manager.narrowing.over_permitted_permissions` (new function, reuses `intersect_declared_permissions`). Resolution can happen long after the triggering HTTP request ended (a worker-loop tick has no bearer token at all), so a real design decision was made: a triggering principal's `SecurityContext.permissions` is captured once, at trigger time, into a new, nullable `workflow_instances.principal_permissions` column (migration `0031`) and read back at every later step via `StepExecutor.execute()`'s own new `principal_permissions` kwarg, mirroring `workflow_id`'s existing precedent exactly. `NULL` means unenforced, never an empty-set "holds nothing" claim; the one real caller with a live `SecurityContext` (`routes/workflows.py`'s `start_workflow`) now supplies it, every other trigger call omits it unchanged. Proven end to end, real Postgres: a pack grants both `llm:invoke` and `sandbox:execute` — enough for the pre-existing pack-grant term alone to resolve the agent — but a principal holding only `sandbox:execute` is genuinely refused resolving that identical agent, isolating the principal term as the cause. Still missing: the workflow term, and the full four-way chain running in one call. Prior (`P03-S05-M14-T08`): role administration is now reachable over real HTTP too — `POST`/`DELETE /api/v1/security/role-grants` (`ai_os_kernel.routes.role_administration`), closing the "service-layer only" caveat T07 disclosed. Identical shape to the decide route (`P03-S03-M30-T06`): bare `authenticate`, the entire `admin`-only authorization decision deferred to `RoleAdministrationService`, no parallel check. Proven end to end, real Postgres, real HTTP: an admin genuinely grants then revokes a role through the route (read back directly from `security.role_grants`, each producing one real `governance.audit_log` row); a non-admin request to either endpoint is refused `403` and makes no real grant. Prior (`P03-S05-M14-T07`): role *administration* is now real, closing this row's own previously-disclosed gap — `security_manager.role_administration.RoleAdministrationService` (new file) grants/revokes a real, persisted `approver:<class>` role for a principal in a new `security.role_grants` table (migration `0030`), `admin`-gated, reusing `ADMIN_ROLE` unchanged. Persisted grants *augment*, never replace, a bearer token's own `roles` claim: `ApprovalService.decide()` (module 5's own row) now optionally unions active grants into `principal.roles` before the unchanged class-scoped check runs, so a token-only principal's behaviour is unaffected. Every grant/revoke is a real, hash-chained `governance.audit_log` row (allowed and denied alike) — the first Security Manager event type to reach that tamper-evident path at all; the rest of this module's own authn/authz events remain structlog-only. Proven end to end, real Postgres: a decision refused, then genuinely enabled by a real grant for the identical, unchanged bearer token, then genuinely disabled again by a real revoke. Service-layer only, no HTTP route yet. Prior (`P03-S03-M30-T06`): `authenticate` (bare Bearer/JWT verification, no flat permission check) is now exported directly, a real, deliberate addition after `require_permission()` was tried against the new approvals route first and found genuinely broken — a principal holding only the realistic `approver:approve-git-push` role (never a separate bare `approver`) was refused before the real, class-scoped `ApprovalService` check ever ran, since `permissions_for_roles()`'s flat, exact-string lookup cannot recognize a class-scoped role. `ai_os_kernel.routes.approvals` is the first real caller — a real HTTP route to decide a paused workflow's own Human Approval Point, closing module 5's own "no HTTP route" disclosed gap; full detail there. Prior (`P03-S05-M14-T06`): the `approver` role's own "grantable per class" clause (ADR-0023) is real — `security_manager.approval_authorization.is_authorized_to_decide_approval` (new file), the first genuinely class-scoped role check in this codebase (`admin` or exact `approver:<class>`, not the fixed 5-role `permissions_for_roles()` dict, which structurally cannot express a role family). Its one real caller, `workflow_engine.human_approval.ApprovalService.decide()` (module 5's own row), refuses an unauthorized decision *before* any write — proven against both fakes and a real Postgres row, which stays exactly `pending`. Real, disclosed gap: no role administration (nothing grants/revokes an `approver:<class>` role for a real principal yet); `permissions.py`'s own flat dict is untouched. Prior (2026-08-01, `P03-S05-M14-T03` + `P02-S05-M13-T08`): the monotonic-narrowing intersection (principal ∩ workflow ∩ agent ∩ tool) is now a real, tested computation (`security_manager.narrowing`), and its agent/tool terms are now enforced end-to-end at resolution (module 13's own row) — but that enforcement lives in the Capability Manager, not here: `SecurityContext` (the principal term) is still never threaded into resolution, and no code parses a workflow's declared permissions from a manifest, so the full four-way chain still never runs; no role administration; no `audit_log` writer for authn/authz events. |
| 15 | Quality Gate Engine | B | 40% | minimal slice, module 15's own sprint (S06) complete | **Updated 2026-08-06 (`P02-S06-M15-T08`, closes S06 to 9/9)**: §5's own "gates are executed (optionally in parallel when safe)" is real too — for one step checking several independent gates, not the Workflow Engine's `parallel` step type. Investigated reusing `ParallelStepExecutor` first and ruled it out on the validator's own already-documented grounds: `parallelSteps` branches are restricted to `agent`/`tool` because a nested branch's own cross-step reference cannot resolve inside an isolated concurrent branch — a `quality_gate` branch's `gate_sources`/`sourceStepId` reference has the identical problem. Instead, `QualityGateStepExecutor` gained a new, optional `gate_checks` mapping (a step id -> several real `(gateId, sourceStepId)` pairs): real `asyncio.gather` concurrency over each check's own registry resolution, joined under an all-must-pass policy mirroring `ParallelStepExecutor`'s own `all` (every check runs to real completion before any consequence is decided; a failing `"warning"` check never blocks). Requires a real `gate_registry`. Proven by real wall-clock timing (three checks each genuinely resolving in 0.2s complete in well under 0.6s), the identical technique `ParallelStepExecutor`'s own tests established. Unwired into any real composition yet — no manifest anywhere declares a multi-gate checkpoint today, the same "build real, wire later" precedent this package has followed since the Gate Registry itself. Prior (`P02-S06-M15-T07`): evaluating a result is now genuinely separated from enforcing its severity — the Policy Enforcer, and the one real, disclosed limit `P02-S06-M15-T09` left open. A resolved gate's real `severity` now decides the *consequence* of a non-passing evaluation, never the evaluation itself: `"blocking"` (unchanged default) still raises and halts; `"warning"` now returns normally with a real `severity: "warning"` flag in its output — genuinely recorded (`gate_result_recorder.py`'s own `severity` column reads this real value instead of a hardcoded constant), never blocking. The now-dead `UnsupportedGateSeverityError` was removed. Both of the one real pack's own declared gates stay `severity="blocking"`, so this is a genuine zero-behaviour-change for `se.delivery_pipeline`'s real runs — proven for the new `"warning"` case with a real, schema-conformant fixture (no manifest anywhere declares one yet, the identical "build real, wire later" precedent the Gate Registry itself used). Prior (`P02-S06-M15-T09`): `QualityGateStepExecutor` cross-wired through the real Gate Registry. New `gate_registry`/`gate_ids` constructor params (both `None`-default, every existing caller/test unaffected) resolve a step's real, manifest-declared `gateId`/`gateVersion`, replacing the workflow-local step id in its output. **The evaluation itself was completely unchanged** — still the identical source-step field check. Wired into `se.delivery_pipeline`'s real production composition (`delivery_pipeline.py`'s own `_build_gate_registry()`); proven against the real pipeline itself — the existing end-to-end test also asserts both real gates' real, resolved `gateId`/`gateVersion`, with every pre-existing pass/fail/retry assertion in that file unchanged. Prior (`P02-S06-M15-T06`): the one real pack's own manifest gained a real top-level `qualityGates:` section (`se.build_lint_clean`/`se.build_tests_pass`, the exact ids `delivery_pipeline.yaml` already referenced with nothing to back them) — each `entrypoint` names the real Lint/QA-Test Agent class that actually performs the check today. Prior (`P02-S06-M15-T05`): the Gate Registry itself went real — the first real code `kernel/src/ai_os_kernel/quality_gate_engine/` has ever had. `registry.py`'s `InMemoryGateRegistry`/`derive_gate_definitions` resolve a declared `gateId` to a real `GateDefinition`, derived from a pack manifest's own real, already-schema-validated `qualityGates[]` array — no new `catalog.quality_gates` table, a real, deliberately avoided schema-authority fork. Gate ids resolve raw, never `pack_id/`-prefixed; a real id collision across packs is refused, not silently resolved. Prior (2026-07-31): §9's own full Gate Result Contract (Workflow ID/Step ID, Gate ID and version, Result, Duration, Details of failure) is now genuinely, completely satisfied — a real `evaluation.gate_results` row per resolution, not only the `workflow_steps` row §9 already had. Built in the Workflow Engine (module 5), not this package — see that module's own row and `gate_result_recorder.py` for the full detail. Prior (2026-07-30, later same day): §9's own "every gate execution must record ... error details" requirement is now genuinely satisfied — a failed gate attempt used to leave no `workflow_steps` trace at all; `record_failed_attempt` (module 5) now writes a real `status="failed"` row with real error detail for every one, successful retry or not. Prior (2026-07-30, same day): a second, distinct gate category — Static Analysis (`quality_gates_framework.md` §5.2), via a new Lint Agent running a real `python -m py_compile` check (not `ruff`, tried first and found to genuinely fail against `DockerSandbox`'s own default image) — proves the `gate_sources`/`success_field`/retry-target mechanism genuinely generalizes beyond the one Testing-category case (§5.3) it was proven against before. Zero changes to `QualityGateStepExecutor`/`DispatchingStepExecutor`/`WorkflowAdvanceRunner` — config only (new pipeline steps + two dict entries); real Kernel-side code was needed only to *produce* the `passed` signal (the new agent), not to *evaluate* it. Prior (2026-07-30): a failed gate now genuinely retries (bounded by attempts and duration) before halting — answering §7's own "support re-evaluation after corrective actions" for this one real case; still no Gate Registry. Prior (2026-07-30): `kernel/src/ai_os_kernel/quality_gate_engine/` itself is still a docstring-only package — no Gate Registry/Executor/Result Evaluator/Policy Enforcer, no `evaluation.gate_results` writer, no pack-declared gates. But the functional gap this module represents (ADR-0006's "blocking gates cannot be skipped") is no longer purely aspirational: `ai_os_kernel.workflow_engine.quality_gate.QualityGateStepExecutor` genuinely blocks `se.delivery_pipeline` on a real, failing test run — the smallest real slice, deliberately built in the Workflow Engine (module 5), not this package. See `docs/03_architecture/kernel/quality_gate_engine.md`'s own Implementation Status for the exact boundary. 0 of the "two Kernel gates" Stage B calls for exist as *this package's* own code. |
| 16 | Traceability Engine | D | 85% | minimal slice | **Updated 2026-08-08 (`P04-S02-M16-T03`)**: the Coverage Analyzer is real too (FR-115) — `traceability_engine.coverage_query.find_uncovered_requirements()`: every real requirement artifact with no real, open, `confirmed`-confidence `verifies` link pointing at it. A third real, product-owner-decided design fork: only `confirmed` confidence discharges the coverage obligation — a merely `inferred`/`provisional` link (an agent's own unreviewed guess) does not, so a real gap cannot hide behind an unconfirmed link. No recursion needed — coverage is a direct property of each requirement's own incoming links, unlike impact analysis. Proven against real Postgres: a confirmed verifying link is recognized as coverage; a provisional-only link still reports the requirement as uncovered; a requirement with no verifying link at all is reported; a verifying link for a *different* requirement does not cross-cover; closing a previously-confirmed verifying link makes the requirement uncovered again. This closes the Traceability Engine's own three-ticket sequence (writer, impact query, coverage query) — still missing only a real Agent/Workflow caller, which nothing in this codebase provides yet. Prior (2026-08-08, `P04-S02-M16-T02`): the Impact Analyzer is real — `traceability_engine.impact_query.find_affected_artifacts()`, data_model.md §8's own documented "recursive CTEs over trace.links" mechanism, the first recursive CTE anywhere in this codebase. A second real, product-owner-decided design fork: traversal is bidirectional, not source-only — neither data_model.md §8 nor traceability_model.md §4 names a canonical per-relationship direction (e.g. an ADR `affects` a module records the module as the link's *target*, not its source), so a one-directional query risked silently missing a real, recorded relationship. Only open (`closed_at IS NULL`) links are traversed. Proven against real Postgres: a direct link makes both endpoints mutually reachable (proving bidirectionality, not merely claiming it); impact is genuinely transitive across a real two-hop chain; a real `A --affects--> B --affects--> A` cycle terminates correctly and reports each artifact exactly once (the recursive path-tracking cycle guard is real, not decorative); a closed link is never traversed; an artifact with no links returns none. Prior (2026-08-08, `P04-S02-M16-T01`): the `trace.links` writer is real — `traceability_engine.link_writer.SqlTraceLinkWriter.record_link()`/`close_link()`, this table's first writer (previously "schema and migration only" since migration `0007`). A real, product-owner-decided design fork: `trace.artifacts.artifact_key` is deterministic (`artifact_type`+`external_id` — see `ids.compute_artifact_key`), not a random id like every other id in this codebase, so two independent callers (a Documentation Agent today, a QA Agent tomorrow) naming the same real artifact (e.g. requirement `FR-019`) converge on one row with no shared lookup — a random key would have required every caller to already know an artifact's own row id before it could link to it. `record_link()` upserts both endpoint artifacts inline and is idempotent for an already-open identical `(source_key, relationship, target_key)` triple (data_model.md §8's own partial unique index scopes uniqueness to `WHERE closed_at IS NULL` specifically to allow this). Proven against real Postgres: convergent keys across independent calls, idempotent re-assertion, artifact metadata refresh on re-upsert, close-then-reassert opening a genuinely new row, and every closed-vocabulary field (`artifact_type`/`relationship`/`confidence`/`created_by_type`) refused before any real database call if invalid. Still missing: impact-analysis query (`P04-S02-M16-T02`), coverage query (`P04-S02-M16-T03`), and any real Agent/Workflow caller — nothing invokes this writer yet. |
| 17 | Event Bus | B | 40% | minimal slice | **Updated 2026-08-04 (`P02-S07-M17-T03`)**: the §4 Outbox Relay box is now real too — `OutboxRelay.tick_once` drains `platform.event_outbox` (`SELECT ... FOR UPDATE SKIP LOCKED` claiming, mirroring the Lease Reaper) into the same, unchanged `InProcessEventBus`; 4 real tests against a real Postgres container prove relay-and-deliver, no-redispatch, and a genuine fail-then-retry. `platform.event_outbox` still has no writer (nothing yet produces a durable event; rows are seeded directly in tests), so this closes the *reading* half only. Prior (`P02-S07-M17-T02`): the §4 in-process `asyncio` pub/sub box — `InProcessEventBus` (bounded per-subscriber queues, non-blocking drop-on-backpressure, typed/versioned `Event` model), Kernel-local `EventBus` Protocol, 9 real tests. No caller publishes to either mechanism yet (Workflow Engine/Quality Gate Engine/Capability Manager still emit nothing here). No Topic/Channel Manager, Schema Registry component, or `platform_sdk` Protocol. |
| 18 | Tool Invoker (SDK interface) | B | 15% | minimal slice | No `ToolInvoker` Protocol/package exists anywhere. A narrower, workflow-engine-internal substitute (`ToolStepExecutor` + `SandboxedCommandTool`) covers a slice of the same need but isn't the documented, pack-facing interface. |
| 19 | Secrets Manager | A | 65% | minimal slice | **Updated 2026-08-08 (`P07-S02-M19-T01`)**: the `vault` backend is real — `VaultSecretProvider` resolves `secret://vault/<name>[#<version>]` against a real HashiCorp Vault KV v2 mount (token auth only), proven against a real, self-hosted Vault dev-mode container (no `hvac` dependency; raw `httpx`, already a real kernel dependency). Also closes a real, previously-undisclosed gap this row's own prior text never named: secrets_management.md §5 documents a real `AIOS_SECRET_BACKEND` selection switch that was never actually built — every real caller (`bootstrap.py`) hardcoded `EnvSecretProvider()` three separate times. `backend_selection.build_secret_provider_from_env()` is the one real place that variable is now read; all three callers go through it. A real design fork, found and product-owner-decided: the two pre-existing hardcoded `secret://env/...` references (Anthropic API key, JWT signing key) would have silently stayed pinned to `env` even with the switch set to `vault` (a reference names its own backend, independent of which provider object is selected) — closed via a new `secret_reference_for()`, deriving a reference's provider segment from the identical `AIOS_SECRET_BACKEND` value the provider factory reads, so the two can never disagree. This row's own prior text was stale even before this step, understating real, already-built work from four earlier tickets never reflected here: the mounted-file backend (`file_provider.py`, `P01-S02-M19-T03`), the Access Broker (`access_broker.py`, `P01-S02-M19-T04` — real, hash-chained per-access audit gated on `secret:access`), the TTL cache with rotation invalidation (`cache.py`, `P01-S02-M19-T05`), and the prompt-assembly leak scan (`leak_scan.py`, `P01-S02-M19-T06`) all already existed. Still missing: cloud secret managers, age/SOPS decryption, Vault AppRole/Kubernetes auth, and wiring the Access Broker/cache/leak scan into any real consumer (LLM Gateway, Git Integration Service) — none of those exist yet, so none of them use this module's own gating/caching/scanning layers yet; only the bare provider chain resolves today's two real secrets. |
| 20 | Sandbox / SandboxExecutor | C | 90% | functionally complete | **Updated 2026-08-09 (`P03-S01-M20-T05`)**: the gVisor hardening path is real — `DockerSandbox` accepts a `runtime` parameter (`AIOS_SANDBOX_RUNTIME`, unset by default, omitted from the Docker API call entirely when unset — zero regression), passed straight through to `containers.create(..., runtime=...)`, exactly the "configuration change, not a redesign" ADR-0016 promises. Disclosed: this environment installs no gVisor/Firecracker runtime (`docker info` here registers only `io.containerd.runc.v2`/`nvidia`/`runc`, and CI has no install step for one), so what's proven end to end is the plumbing itself — a valid runtime (`runc`) executes identically to the default, and a genuinely-unknown one is refused by the real Docker Engine — not observed hardened isolation; that remains real, disclosed, unbuilt verification work gated on a host that has one installed. Prior (2026-07-28): the real, Docker-gated end-to-end pipeline test ran for real against a live daemon and passed — network isolation and filesystem containment genuinely held for code the pipeline itself generated and ran, not just an isolated guarantee check (see `history/024`). Two real bugs found and fixed along the way (unrelated to the sandbox mechanism itself — a `python_command`-consistency bug in `pipeline.py`'s own composition, and a test with a hardcoded `sys.executable`). Remaining: digest-pinning the default image and the egress-proxy dependency-install step ADR-0016 documents. |
| 21 | Storage Service | B | 25% | minimal slice | **Updated 2026-08-04 (`P02-S07-M21-T01`)**: the local-filesystem backend is real — `storage_service/local_store.py`'s `LocalFilesystemArtifactStore`, content-addressed by real SHA-256 digest, returning the real, pre-existing SDK `ArtifactRef` (`sha256:<hex>`). Proven against a real filesystem: identical content deduplicates to one real file; different content produces different files; round trips return the exact original bytes. No S3-compatible "prod" backend (no client dependency exists to build one against yet); no `Protocol` (a second real implementation is documented intent, not yet real); nothing in a real Kernel composition constructs this store yet — workflow artifacts remain plain files in per-agent temp directories, unchanged. |
| 22 | Notification Service | F | 48% | minimal slice | **Updated 2026-08-09 (`P07-S03-M42-T02`)**: wired into a real Kernel process for the first time — `bootstrap._lifespan` now conditionally constructs a real `NotificationService`/`WebhookChannel`/`SqlNotificationDeliveryRecorder` when `PlatformConfig.notification_webhook_url` is configured (`None`, every current environment, means genuinely not started). A fourth real category, `cost_anomaly`, added — Cost Anomaly Alerting (module 42's own row) is this service's own first real production publisher, closing the previously disclosed "no real Kernel component publishes ... yet" gap. `approval`/`failure`/`completion` still have no real production publisher. Prior (`P06-S05-M22-T02`): delivery status is now durably recorded — a real `notification.notification_deliveries` table (migration `0034`, no cross-schema FK on `workflow_id`, mirroring `context_assemblies`' own precedent) and `SqlNotificationDeliveryRecorder`, the identical insert-only shape `SqlGateResultRecorder` already establishes. `NotificationService` now requires a `NotificationDeliveryRecorder` collaborator and persists the real, final outcome (`sent`/`failed`) after each delivery attempt, not before. Proven against real Postgres (3 new integration tests: a real row with given values, genuinely-null `workflow_id`/`trace_id`, repeated calls each inserting their own distinct row) plus the existing unit suite (a fake recorder, ADR-0004 — proving the service's own classification/delivery logic, not persistence). Prior (`P06-S05-M22-T01`): real, minimal first increment — `ai_os_kernel.notification.NotificationService` genuinely subscribes to the real `InProcessEventBus` and delivers via one real channel (`WebhookChannel`, a real HTTP POST) for this ticket's own three literal categories (approval/failure/completion). Proven end to end against a real local HTTP server, 8 real tests: a real event reaching a real POST, an unrelated event type genuinely not notified, `close()` genuinely unsubscribing, and the webhook channel's own success/failure/unreachable-target cases. Design forks resolved via `AskUserQuestion`: built inside the Kernel (`ai_os_kernel.notification`), not the separate `platform_services/notification` package this module's own row names — that package has no real `uv` workspace member yet and the real `EventBus` has no `ai-os-sdk` Protocol to depend on instead, so a new package importing `ai_os_kernel.event_bus` directly would start a new instance of the discouraged "pack imports Kernel internals" pattern; and scoped to one real channel, not `notification_service.md`'s full framework (Preference Manager, Channel Router, Delivery Manager, retry policy, Dashboard/Voice/Email adapters — all deferred). Still genuinely unwired to a real producer: the identical, already-disclosed gap `routes/stream.py`'s own docstring names for the WebSocket endpoint — no real Kernel component publishes to the bus in production yet. |
| 23 | Caching (Redis) | B | 30% | minimal slice | **Updated 2026-08-04 (`P02-S07-M23-T02`)**: the Response Cache is real — `caching/response_cache.py`'s `ResponseCache`, real `LLMRequest` → `LLMResponse` caching keyed on every result-affecting field, ADR-0025 §3's experiment-exclusion rule enforced structurally via the new `TraceContext.experiment_id`/`LLMResponse.served_from_cache` fields (module 6's own row). Proven against a real Redis container: a genuine hit avoids recomputation, a miss falls through, an experiment call is never cached and never read back. Not wired into `llm_gateway.gateway`'s real call path yet. Prior (`P02-S07-M23-T01`): `build_redis_client`/`RedisSettings` real, proven connection + `set`/`get`. No `Cache` Protocol; no config/secret/document/retrieval caching. **A second real caller of this client now exists**: module 6's own Rate Limiter (`llm_gateway/rate_limiter.py`, `P02-S02-M06-T11`) — a genuinely different concern (per-provider request throttling), not built in this package. |
| 24 | Git Integration Service | C | 68% | minimal slice | **Updated 2026-08-02 (`P03-S01-M24-T03`):** a real, dedicated `git:write` permission check now gates Git-tool reachability — closing this row's own previously-listed "the manifest-declared `git:write` permission check via the monotonic-narrowing chain" gap. `ai_os_kernel.sdk_adapters.pack_context.build_pack_context` now forwards a configured `git_service` into an entrypoint's `ToolInvokerAdapter` only when that entrypoint's own declared `permissions` include `git:write`, not merely `sandbox:execute` — the real, confirmed gap this closes: every `sandbox:execute` agent in the pack (not only `git-push`) previously received a working `git_service` whenever the registry had one configured at all. Reuses the real declared-permission primitive (`ai_os_kernel.security_manager.narrowing`), the identical partial scope (declared-permission term only, no `SecurityContext` reaches this call site) `capability_manager.permission_grant`'s own pack-ceiling check already carries. Proven both directions against the identical real `GitIntegrationService`: `git-push`'s own real `[sandbox:execute, git:write]` genuinely commits and pushes; `lint`'s/`qa-test`'s own real `[sandbox:execute]` alone gets `UnknownToolError` on every Git tool id, zero audit rows for the attempt (`tests/unit/kernel/sdk_adapters/test_pack_context.py::TestGitWritePermissionGating`). Prior (`P03-S01-M24-T02`): a real config source now exists — `ai_os_kernel.git_integration.settings.GitIntegrationSettings` reads `AIOS_GIT_REMOTE_URL`/`AIOS_GIT_AUTHOR_NAME`/`AIOS_GIT_AUTHOR_EMAIL`/`AIOS_GIT_PROTECTED_BRANCHES` directly from the environment (not Configuration Manager — the identical `AIOS_DATABASE_URL`/`OTEL_EXPORTER_OTLP_ENDPOINT` exclusion principle `configuration_manager/models.py` already states), closing the "Configuration-Manager-sourced repository URLs or protected-branch policy" gap this row previously listed as missing. `ai_os_kernel.git_integration.default_service.build_git_integration_service_from_env` builds a real `GitIntegrationService` from it — `None` (the existing safe no-op) when unconfigured, `GitIntegrationConfigError` if configured but author identity or `protected_branches` is missing/empty (closing R-001's own named "empty `protected_branches` is not a safe default" finding). `bootstrap.py`'s real `se.delivery_pipeline` composition now threads the result through `SqlAgentRegistry(git_service=...)`, so the live HTTP route is genuinely wired to push once configured — closing module 30's own previously-listed "no `git_service` threaded into `bootstrap.py`" gap too. Proven: `tests/unit/kernel/git_integration/test_default_service.py` (env → real, behaviorally-verified `GitIntegrationService`; the safe-no-op-when-absent guarantee re-confirmed) plus the full, unchanged Docker/Postgres-gated suite passing with `git_service=None` still the default for every existing caller. Prior (`P03-S03-M30-T04`): a real caller now exists — `se.delivery_pipeline`'s new `git-push` step, through the real `git-push` agent (module 29's own row), genuinely reaches this service from an actual running workflow, not just a direct call. Prior (`P03-S04-M31-T04`, same day): the service became genuinely reachable through `ToolInvoker` (`platform.git.commit`/`create_branch`/`push`, module 31's own row) — an agent's `context.tools.invoke(...)` call reaches the real service, sandbox, and `git` subprocess, proven end to end. Prior (`P03-S01-M24-T01`): the first real Platform Service. `GitIntegrationService` (`kernel/src/ai_os_kernel/git_integration/` — a Kernel subpackage, not a separate `platform_services/` uv workspace member, a disclosed packaging decision) genuinely commits, branches, and pushes, reusing `SandboxExecutor` (real git subprocess exec, secret exclusion), `AuditLogWriter` (real hash-chained rows), and `AccessBroker`/`SecretValue` (real credential resolution) — no parallel mechanism for any of the three. Protected-branch push is structurally absent from the tool surface (`git_integration.md` §5.1), not merely permission-gated — proven refusing before any subprocess runs, remote untouched. A resolved push credential travels only via a real, per-call `GIT_ASKPASS` temp script (mode `0700`, deleted after use) on the push subprocess's own environment — never a URL, `.git/config`, log, or audit payload, proven against a real Postgres audit trail alongside a real local bare-repo push. Still missing: clone/fetch/status/diff/pull; Configuration-Manager-sourced repository URLs or protected-branch policy (both explicit, caller-supplied parameters for now); PR-creation/merge; credential support in the Tool's own input schema (no `SecurityContext` reaches that call site); the manifest-declared `git:write` permission check via the monotonic-narrowing chain (closed `P03-S01-M24-T03`, same day as this row's own newest paragraph above — the pack's manifest always declared `git:write` for `git-push`, but nothing checked it until this step); a real pack agent that actually calls these tool ids. |
| 25 | Speech Gateway | F | 40% | real abstraction, no real provider | **Updated 2026-08-09 (`P06-S06-M25-T01`)**: `ai_os_kernel.speech_gateway` is real — `SpeechToTextProvider`/`TextToSpeechProvider`/`WakeWordProvider` Protocols, a composed `SpeechGateway` Protocol, real alias-based routing (`DispatchingSpeechGateway`, genuinely distinct STT/TTS/wake-word namespaces, "deny, do not guess" for an unconfigured alias), and one real, deterministic reference implementation (`echo_adapter.py`) mirroring `EchoLLMGateway`'s own identical role — proves the request/response contract end to end. Investigated and stopped before writing any provider code (resolved via `AskUserQuestion`): every ADR-0019-named reference adapter (`faster-whisper`, Piper, `openWakeWord`, a cloud STT/TTS adapter) needs infrastructure this repo has none of yet — no audio/ML runtime installed, no speech-provider credential configured anywhere. Not built: any real provider, retry/fallback/circuit-breaker routing, the `local_only` privacy policy, wiring into `bootstrap.py` (no real caller exists — the Voice Capability Pack, module 33, is still 0%). Proven by 12 real unit tests (deterministic transcription/synthesis/wake-word round-trips, alias routing, unconfigured-alias refusal). |
| 26 | Document Processing | E | 40% | minimal slice | **Updated 2026-08-09 (`P05-S01-M26-T02`)**: the Chunking Engine box is real — investigation found `knowledge_manager.indexing.chunk_content()`/`IndexingService` (built earlier, module 9, `P02-S04-M09-T03`) already real and tested but with zero production callers; rather than build a second, parallel chunker, `document_processing.indexing.index_document_file()` is new glue reusing it unchanged — its first real caller. Proven by 4 real Postgres tests (multi-chunk Markdown write, code file with `text/x-source` media type, unchanged re-index genuinely skipped, deferred PDF refused before any write). `knowledge_writer.py` is now reachable from a real document on disk end to end; still not wired into any real production composition/route. Prior (`P05-S01-M26-T01`): real format detection plus 3 of 5 documented parser adapters (Markdown/Plain Text/Code), built inside the Kernel (`ai_os_kernel.document_processing`), not the `platform_services/document_processing` package this module's own row names (no real `uv` workspace member exists yet, the identical fork already resolved for the Notification Service). Real `sha256:` provenance hash, CRLF/CR normalized to LF. PDF/DOCX are disclosed, deferred — both need a new third-party dependency (`pypdf`/`python-docx`) this project does not yet declare; a deliberate scope decision (`AskUserQuestion`), not an oversight. Proven by 27 real tests, real files on real disk. Remaining: PDF/DOCX adapters, Structure Extractor/Metadata Enricher/Output Normalizer/Observability Hook boxes, and the Chunking Engine (`P05-S01-M26-T02`, now unblocked); the Knowledge writer still accepts only already-chunked input. |
| 27 | Platform SDK (`ai-os-sdk` package) | B | 100% | functionally complete | **✅ v1.0.0 COMPLETE (2026-07-30) — plan formally closed out, `platform_sdk_v1_scope.md` §6s/§10. Nothing remains from the 18-step (+2 inserted) plan itself.** What remains is explicitly out of v1.0.0's own documented scope, not unfinished work: 10 Protocol interfaces with no consumer yet (0%-built underlying subsystems), `SecretResolver` (dropped in the 18-step revision), streaming/embeddings/token-counting on `LLMGateway`, the documented `AgentRequest`/`AgentResult` envelope shape (a future major version), and the `evaluation.llm_calls` observability gap (§6a — needs a future SDK Telemetry surface). **✅ HARD GATE LIFTED (product-owner decision, 2026-07-29) — see `docs/process/standing_rules.md`.** 18-step sequence (revised from 15 after an architecture review found 3 blocking gaps — §6a), plus two inserted fix steps (9a, 12a). **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, 11, 12, 12a, 13 done:** real installable distribution with `py.typed`; `ai_os_sdk.errors` + `ai_os_sdk.models` (error taxonomy, `ArtifactRef`, canonical `TraceContext`, `SecurityContext`, `StepBudget`, the `LLMGateway` model set, `RenderedPrompt`, `ToolDescriptor`/`ToolStatus`/`ToolResult`, and the `ContextService` boundary models); `ai_os_sdk.contracts` holds `Agent`/`Tool`/`TrustTier`, `LLMGateway`, `PromptRegistry`, `ToolInvoker`, `PackContextReceiver`, `ContextService`, `CapabilityPack`/`PackContext`/`PackRegistration`/`HealthReport`; `ai_os_sdk.testing` holds `forbidden_imports`/`waiver` (check 7 + its expiring waiver mechanism). **Structural compatibility proven against all 5 real pack agents, both real tools, the real `DispatchingLLMGateway`, and the real `DefaultContextManager`.** **Step 6a** built real Kernel-side adapters. **Step 6b** resolved the `EntrypointLoader` injection blocker via `PackContextReceiver`. **Step 7**: `pipeline.py` relocated; `ContextService` landed; the entry-point contract relocated into `ai_os_sdk.contracts.capability_pack`. **Step 8**: check 7 + its expiring waiver are real and CI-wired. **Step 9 — the first real migration**: `qa-test` now depends only on `ai_os_sdk` (`Agent`+`PackContextReceiver`, sandbox via injected `ToolInvoker`); found the context duck-typing nuance and `SqlAgentRegistry`'s missing binding. **Step 9a (inserted)**: `SqlAgentRegistry`/`SqlToolRegistry` now genuinely call `bind_pack_context()` for real in production (per-row `required_permissions`, never the pack's aggregate) — closing step 9's own gap before steps 10–13 hit it four more times; `EntrypointLoader` untouched; `bootstrap.py` doesn't construct either registry yet, so zero real-system risk; zero regression across all 8 files using either registry, including the 4 still-unmigrated agents. **Step 10 — the first migration needing real gateway injection**: `requirements-analyst` now depends only on `ai_os_sdk` (`Agent`+`PackContextReceiver`), replicating `PromptedCompletionService.complete_from_prompt()`'s real render-then-complete logic directly against the injected `context.llm`/`context.prompts`; the old lazy-build `asyncio.Lock()` is deleted outright, found obsolete (not conflicting) now that `bind_pack_context()` runs synchronously before any `execute()` is possible; a real, previously-latent circular import introduced by step 9a (`workflow_engine.registry` ↔ `sdk_adapters.pack_context`) found and fixed with a function-local import; one accepted, documented capability loss (`evaluation.llm_calls` recording — no SDK Telemetry surface exists in v1.0.0). Zero behavior change proven throughout via both real Delivery Pipeline end-to-end tests (deterministic + live Docker) producing identical output, plus the pack-local suite and this agent's own pipeline tests. `qa-test` and `requirements-analyst` both removed from the waiver (6→5→4 modules, 44→40→32 violations); check 7 now genuinely passes for both, unwaived. No blocker in the injection mechanism's own design at any point. Zero pack source modified beyond the one agent being migrated per step (§6c–§6m). **Step 11**: `architecture` migrated identically to step 10's own pattern — `Agent`+`PackContextReceiver` only, same render-then-complete replication, same trace-narrowing reasoning; zero new tension found (the lazy-build lock was already proven obsolete at step 10, not merely unused for this one agent). Both real Delivery Pipeline end-to-end tests (deterministic + live Docker) needed a real fix, not just a pass-through: `architecture` is chained into `se.delivery_pipeline`, unlike `qa-test`/`requirements-analyst`, so `test_delivery_pipeline.py`/`test_delivery_pipeline_docker.py`'s own `InMemoryAgentRegistry` construction and the live tier's `SqlAgentRegistry(engine)` call both needed updating to the zero-arg+`bind_pack_context()` pattern — found and fixed as part of this step, not deferred. The `evaluation.llm_calls` capability loss recurs, confirming it is a real, structural gap in v1.0.0, not a one-off — now tracked in the new **§6a Known Regressions** section. `architecture` removed from the waiver (5→4→3 modules, 40→32→24 violations); check 7 now genuinely passes for it, unwaived. **Step 12 — the first migration needing real gateway AND tool injection together**: `build` now depends only on `ai_os_sdk` (`Agent`+`PackContextReceiver`), replicating the render-then-complete logic for its own LLM call and calling `context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, ...)` for its write, exactly as `qa-test` already does for its own sandbox use. **The lock is not fully obsolete here** — a real, discovered nuance: it still guards lazy working-directory creation (unrelated to LLM composition), proven by a new concurrent-execute test asserting a shared directory. **A real, discovered tension using `ToolInvoker` instead of direct sandbox construction**: the migrated agent can no longer dynamically ask its own sandbox for its `python_command` (a generic `ToolInvoker` has no such concept); resolved with a constructor default (`("python3",)`, matching the real system-wide default backend), tracked as a second, distinct entry in §6a. Both real Delivery Pipeline end-to-end tests needed the same construction fix step 11 already established, plus this agent's own `python_command` matched explicitly per test; the live Docker test is this pack's most important ToolInvoker proof, since Build is the pipeline's real file-writer — a real file was genuinely written inside a real container through the new path. `build` removed from the waiver (4→3→2 modules, 32→24→13 violations); check 7 now genuinely passes for it, unwaived. **Step 12a (inserted)** — closes the `python_command` regression step 12 recorded rather than silently accepted: `ai_os_sdk.contracts.tool_invoker.PLATFORM_PYTHON_INTERPRETER`, a new well-known placeholder token, is expanded by `ToolInvokerAdapter.invoke()` (the one place still holding the real `SandboxExecutor`) into that instance's own real `python_command` before dispatch. `build.py`'s own `python_command` constructor parameter is removed entirely — no default to keep in sync, no backend knowledge in pack code at all, restoring the pre-migration "always automatically correct" guarantee exactly. Proven against both real backends, not just the currently-active one: a real `LocalSubprocessSandbox` (3 new tests in `test_tool_invoker_adapter.py`) and a real, live `DockerSandbox` (2 new tests in a new `test_tool_invoker_adapter_docker.py`). Zero regression across every already-migrated agent (`qa-test`, `requirements-analyst`, `architecture`, `build`) and both real Delivery Pipeline end-to-end tests. `documentation` (step 13) confirmed to need the identical fix from the start, not a fresh discovery. **Step 13 — the fifth and final agent migration**: `documentation` now depends only on `ai_os_sdk` (`Agent`+`PackContextReceiver`), using the identical render-then-complete replication and `PLATFORM_PYTHON_INTERPRETER` from the start — no repeated tension, confirming step 12a's own fix generalizes. **A further data point on the lock-obsolescence finding**: this agent needs no lock at all, since it lazily builds/creates nothing (unlike `build`'s narrower one) — the least internal state of any agent in this pack, and the clearest confirmation yet that the lock pattern is fully obsolete for any agent that doesn't independently need one. **A real, discovered correction to this step's own initial framing, surfaced and corrected rather than silently forced**: the waiver is NOT literally empty after this step — `pack.py` (this pack's own `CapabilityPack` entry point) remains, since migrating it is step 14's own explicitly scoped job, not step 13's; `pack.py` was never one of the 5 agents. `documentation` removed from the waiver (2→1 modules, 13→1 violations); check 7 now genuinely passes for it, unwaived, with exactly 1 module (`pack.py`) still waived. Both real Delivery Pipeline end-to-end tests pass with the full pipeline now migrated onto the Platform SDK — a real documentation artifact genuinely written through the new `ToolInvoker` path in both tiers, including live Docker. The `evaluation.llm_calls` gap reaches its full, final, permanent extent (§6a) — all 4 LLM-calling agents affected, expected to stay open until a real SDK Telemetry surface exists. **Step 14 — the sixth and final module migration, and the one that closes out the entire pack**: `pack.py` now imports `PackContext`/`PackRegistration`/`HealthReport` from `ai_os_sdk.contracts` directly instead of the Kernel's own compatibility re-export — a genuinely zero-behavior-change swap, since step 7 already made the Kernel module a real re-export of the identical SDK objects. **Zero `ai_os_kernel` imports remain anywhere in this pack's own source tree, for the first time in this project's history**, confirmed by the real scanner returning an empty violation list pack-wide. `pack_contract_waiver.yaml` is **deleted outright**, not merely emptied — check 7 now enforces unconditionally, with zero waivers and zero exceptions, against this pack. The waiver mechanism itself (`ai_os_sdk.testing.waiver`) remains real, tested, reusable infrastructure for a future pack's own genuine transition period; only this pack's own usage of it ends. **The Capability Pack growth gate is correspondingly lifted** — a product-owner decision recorded in `docs/process/standing_rules.md`, revising that gate's own previously-recorded "only after step 15" condition: the real substance the gate named ("a real, migrated, and verified pack") is now satisfied; step 15's remaining 8 `pack_contract_suite` checks are real, valuable completeness work, still scheduled, but no longer a precondition for this gate specifically. Both real Delivery Pipeline end-to-end tests (deterministic + live Docker) still pass unchanged; a real wheel build (`uv build --package ai-os-pack-software-engineering`) still succeeds. `capability_pack_contract.md`'s dated exception language is retired, replaced with a plain statement of permanent, unconditional compliance. **Step 15 — the final step, closing the plan out:** built the genuinely remaining checks (2, 3, 4, 5, 6, 8, 9 — **seven**, not the "remaining 8" this table itself said before this step corrected it — check 7 already shipped at step 8, check 1 was never "remaining") as `ai_os_sdk.testing.pack_contract_suite`, plus an async `run_pack_contract_suite()` orchestrator wrapping check 7 unchanged and reimplementing check 1 (dependency-floor rule forbids importing the Kernel's Manifest Loader; re-validates the same schema with the same `jsonschema` library, `schema_path` supplied explicitly since the schema isn't packaged into the SDK's own wheel). All 9 checks proven together against the real, fully-migrated pack in `tests/contract/test_pack_contract_suite.py` (20 tests, activating the already-prepared CI gate) — genuinely green, zero waivers. One further real, discovered gap fixed: the pack's own `pyproject.toml` still declared a runtime dependency on `ai-os-kernel` (stale since step 14) and none on `ai-os-sdk` — check 7's AST scanner never inspects `pyproject.toml`, so this survived until this step's "dependencies satisfied" check required reading it; fixed. Zero regression: root suite 909→929 (exactly the 20 new tests), both real Delivery Pipeline tests unchanged, `mypy --strict`/`ruff` clean. |
| 28 | Manifest Schema (JSON Schema) | A | 95% | functionally complete | The schema itself is real, versioned, and actively enforced. Only documented v1 exclusions (signed manifests, marketplace metadata) remain, by design. |
| 29 | SE Pack — Agents | C | 76% | minimal slice | **Updated 2026-08-09 (`P08-S01-M29-T07`, FR-044)**: 16 of 18 documented agents real — `performance` (`ai_os_pack_software_engineering.agents.performance:PerformanceAgentEntrypoint`), the only remaining ready Task above post-v1 priority once every other ready Task was exhausted (FR-044 is `LATER`/post-v1, the single such requirement in the whole 75-FR backlog). "A running system" (this ticket's own literal Input) reduced honestly to one real source file — no real APM/instrumentation infrastructure exists anywhere in this codebase to attach to a genuinely running process. Computes real McCabe cyclomatic complexity per function from the file's own real AST (stdlib `ast`, no LLM call at all — the identical `lint`/`qa-test` "deterministic, no LLM guess" shape), reporting real hotspots and mechanically-derived recommendations for any function whose real complexity crosses the real, named threshold of 10 — never an LLM's own opinion about a file it may not have even read correctly. Proven by 16 real tests: pure unit tests of the complexity computation itself (a real low-complexity function, a real 11-branch function genuinely scoring complexity 12 and getting flagged, a real `SyntaxError` on genuinely invalid Python) plus the full real-sandbox/`AgentStepExecutor` proof every sibling agent's own test file establishes. Not chained into any workflow. Only `devops` and `backend-developer` (a separately disclosed gap, "no ticket anywhere in this roadmap") remain unbuilt from the 18-item catalog. Prior (2026-08-07, `P08-S01-M29-T06`)**: 15 of 18 documented agents real — `refactoring` (`ai_os_pack_software_engineering.agents.refactoring:RefactoringAgentEntrypoint`) is the only agent in this pack running a genuine before/after test comparison for one file — FR-043's own criterion ("tests pass before and after") has no other component that could enforce it: no sibling `build.py`-based agent writes twice and compares, and a Quality Gate reads only one already-recorded outcome. Reads the file's own real content back out of the sandbox (`code_review.py`'s own read mechanism, reused verbatim), runs the caller's own `runCommand` against it as a baseline, **refuses before any LLM call or write if that baseline itself does not pass** (there is no valid behaviour to preserve), calls the model for a refactor (writing back through `build.py`'s own mechanism, but always to the caller-supplied `filePath` — never a path the model declares, since this agent already knows which file it is refactoring), then reruns `runCommand` against the refactored content. `refactored` is mechanically `passedBefore and passedAfter`, never a second LLM judgment — the identical gate principle `lint`/`qa-test`/`security-analysis`/`code-review` already establish. FR-043's own "behaviour assertions unchanged" (the test file itself) holds structurally, not by separate verification: this agent has no mechanism to write to any file except the one it refactors. Proven against a real Python file through a real subprocess, twice: a genuine passing refactor, a genuine behaviour-regressing refactor correctly reported as `refactored: false` (the bad write still lands on disk — this agent does not roll back), and a genuinely failing baseline correctly refused before any write. Not chained into any workflow. Prior (2026-08-07, `P08-S01-M29-T01`)**: 14 of 18 documented agents real — `frontend-developer` (`ai_os_pack_software_engineering.agents.frontend_developer:FrontendDeveloperAgentEntrypoint`) reuses `build.py`'s/`database.py`'s/`api-designer`'s own write-through-sandbox mechanism verbatim; the one real, deliberate addition is a frontend-file-extension precondition (`.tsx`/`.jsx`/`.ts`/`.js`/`.vue`/`.svelte`/`.html`/`.css`/`.scss`), enforced before any sandbox call — not FR-035-mandated (FR-035's own literal "builds and passes generated tests in the sandbox" criterion is qa-test's/the Quality Gate Engine's generic job for any file, deliberately not re-implemented here), but this agent's own name is its scope. Establishes the real, catalog-documented `frontend-developer` identity `workflows.md` §4's `se.implement_task` graph names as a `task.kind`-routed choice ("backend-developer (or frontend-developer / database, by task.kind)"). **A real, disclosed roadmap gap found while investigating this ticket, not closed by it: `backend-developer`, this agent's own named sibling, has no ticket anywhere in this roadmap** — searched both `P03-S02-M29` (this pack's original 8 agents) and `P08-S01-M29` (its 7-ticket expansion); neither contains it. Not chained into any workflow. Prior (2026-08-07, `P03-S02-M29-T08`, closed this pack's own Sprint S02 to 8/8)**: 13 of 18 documented agents real — `technical-planner` (`ai_os_pack_software_engineering.agents.technical_planner:TechnicalPlannerAgentEntrypoint`) is this pack's own first implementation of ADR-0021's sanctioned "dynamic decomposition without dynamic control flow" mechanism (FR-033: "Decompose an architecture into an executable plan"). **A prior step (`P03-S02-M29-T07`, below) already investigated this exact ticket and called it "genuinely blocked" — re-investigated, not re-litigated blind: confirmed for real in code (`ai_os_kernel.workflow_engine.models.StepType` has exactly 7 members, no `foreach`), then, per explicit product-owner direction after a stop-and-ask (the identical Evaluation Engine "build real, wire later" precedent this session already established for Metrics Collector/Comparison Computer/Reporting Interface), built anyway rather than left undone: the agent-producing half of this ticket's own scope (design → plan artifact) has no dependency on `foreach` existing at all.** Given a technical design, returns a real, schema-validated plan artifact (`tasks: [{taskId, title, description}]` — field name matches `workflows.md`'s own illustrative `implementation_plan.tasks`/`plan.tasks`, reused not invented); model returns real JSON (the same departure `code_review.py` established), `taskId` assigned deterministically by this module, never trusted from the model. Not chained into `se.delivery_pipeline` (no design-decomposition step exists there) and has no real consumer anywhere — no `foreach` `StepType`/executor exists in the Workflow Engine (confirmed, not merely cited: exactly 7 real `StepType` values, `workflow_engine.md` §7.1). Fixed adjacent, pre-existing documentation staleness while investigating this exact gap: ADR-0021's own Implementation Status section still claimed "only `agent` and `tool` actually execute," though `decision`/`parallel`/`quality_gate`/`human_approval` all genuinely execute too (`workflow_engine.md`'s own "last of the 7 step types" language) — corrected. Also found, disclosed, and left unfixed (real, broader, pre-existing staleness beyond this ticket's own scope): `agent_catalog.md`/`agents.md`'s own "currently real" tables had not been updated across several prior agent-adding steps this session (`database`, `api-designer`, `security`, `code-reviewer` were already real in the manifest but not reflected there) — flagged with a disclosure note in both documents, not silently left wrong, but not fully re-audited either. Prior (2026-08-06, `P03-S02-M29-T07`)**: 12 of 18 documented agents real — `code-review` (`ai_os_pack_software_engineering.agents.code_review:CodeReviewerAgentEntrypoint`) is this pack's fifth genuinely new agent (not a migration) since the module-27 hard gate lifted (FR-039, **MUST**-priority — unlike every prior new agent, all SHOULD: "Review generated code and produce structured findings... Findings carry file, line, severity, and confidence"). `lint.py`'s own docstring already disclosed satisfying only FR-039's static-analysis portion — this closes the rest. The first agent in this pack's own "code analysis" family to deliberately use an LLM's judgment as the point (`confidence` is inherently a model judgment, no mechanical tool computes it), rather than avoiding it like `lint`/`qa-test`/`security-analysis`. Reads the target file's own real content back out of the sandbox — `build.py`'s own write script, reversed, the first agent needing raw content returned rather than a verdict. Responds with real JSON (a deliberate, disclosed departure from this pack's usual `FILE_PATH` delimited format, since structured multi-field findings are exactly what JSON is for), parsed and validated field-by-field via Pydantic. A real omission found and fixed before shipping: an early draft skipped the JSON-context fallback every sibling agent already has. `P03-S02-M29-T08` (technical-planner) investigated and found genuinely blocked, not merely deferred: its own documented output ("a plan artifact driving a foreach step") has no real consumer — `workflow_engine.md` itself confirms `foreach` "is not even one of `StepType`'s 7 real values... genuinely undeclared and unbuilt," a distinct, unbuilt Workflow Engine feature entirely outside this module's scope. Not yet chained into `se.delivery_pipeline` — this ticket's own scope. Prior (2026-08-06, `P08-S01-M29-T05`): 11 of 18 documented agents real — `release` (`ai_os_pack_software_engineering.agents.release:ReleaseAgentEntrypoint`) is this pack's fourth genuinely new agent (not a migration) since the module-27 hard gate lifted (FR-042: "Manage version, changelog, and release readiness... Release gate evaluates and records readiness"). Mirrors `documentation.py` almost exactly (identical Build+QA-Test six-field input, identical LLM-then-write-through-sandbox mechanism). The one real addition: `ready`, mechanically derived from the Test Agent's own `passed` — never an LLM's opinion, the identical gate principle `lint`/`qa-test`/`security-analysis` already establish. Version/changelog content is LLM-generated free text, never independently validated — computing a real semver bump would need a real code diff this agent's own input (a `passed`/`exitCode`/`output` triple) does not carry. `P08-S01-M29-T07` (performance agent) was deliberately passed over this sweep: `functional_requirements.md` classifies FR-044 `LATER` (post-v1), the only FR in the whole document with that classification. Not yet chained into `se.delivery_pipeline` — this ticket's own scope. Prior (2026-08-06, `P08-S01-M29-T04`): 10 of 18 documented agents real — `security-analysis` (`ai_os_pack_software_engineering.agents.security_analysis:SecurityAnalysisAgentEntrypoint`) is this pack's third genuinely new agent (not a migration) since the module-27 hard gate lifted (FR-040: "Perform security analysis of generated code... Security gate result recorded with findings"). SDK-native, no LLM call — mirrors `lint.py`'s own real-tool, exit-code-derived-pass/fail principle exactly. `bandit`, the natural real tool, is unavailable anywhere this agent could run (mirroring `lint.py`'s own already-disclosed `ruff` gap); chosen fix is a hand-written, embedded stdlib `ast` scan (eval/exec, subprocess `shell=True`, unsafe pickle/yaml load), run through the sandbox exactly like `lint.py`'s `py_compile` call — no new dependency, narrower than a real tool but honest about that limit. Proven against a real, deliberately vulnerable file (all 4 patterns detected) and a real, deliberately safe negative case (`yaml.load` with an explicit `Loader=` correctly not flagged). Not yet chained into `se.delivery_pipeline` — this ticket's own scope. Prior (2026-08-06, `P08-S01-M29-T03`): 9 of 18 documented agents real — `api-designer` (`ai_os_pack_software_engineering.agents.api_designer:ApiDesignerAgentEntrypoint`) is this pack's second genuinely new agent (not a migration) since the module-27 hard gate lifted (FR-037: "Define versioned API contracts... Contract artifact produced and validated"). SDK-native from its first line. Reuses `build.py`'s/`database.py`'s own real write-through-sandbox mechanism verbatim, adding a real gate: the written content must parse as YAML and pass real OpenAPI 3.1 validation (`openapi-spec-validator`, a new, real, small pack dependency — chosen over a hand-rolled structural check) or the write is refused before it happens — FR-037's "is validated" criterion enforced as a real precondition, not merely documented. Proven against the real library, not a stand-in: a genuinely spec-violating document (missing `info.version`) is rejected via the library's own real exception. Not yet chained into `se.delivery_pipeline` — this ticket's own scope. Prior (2026-08-06, `P08-S01-M29-T02`): 8 of 18 documented agents real — `database` (`ai_os_pack_software_engineering.agents.database:DatabaseAgentEntrypoint`) is this pack's first genuinely new agent (not a Platform SDK migration) since the module-27 hard gate lifted for real new agents (FR-036: "Design database schemas and migrations... Migration applies cleanly and is reversible"). SDK-native from its first line. Reuses `build.py`'s own real write-through-sandbox mechanism verbatim (identical `FILE_PATH`/`FILE_CONTENT_BEGIN`/`FILE_CONTENT_END` format, identical safe-path containment), adding one real split: the written content must contain a parseable `-- UP`/`-- DOWN` pair or the write is refused before it happens — FR-036's "is reversible" criterion enforced as a real precondition, not merely documented. Proven reversible for real, not just parseable: a deterministic completion's own `upSql`/`downSql` genuinely applied then reverted against a real Postgres container. Not yet chained into `se.delivery_pipeline` — this ticket's own scope, mirroring every other agent's "prove alone first, chain later" history. Prior (2026-08-02, `P03-S04-M31-T04`/`P03-S03-M30-T04`): 7 of 18 documented agents real — `git-push` (`ai_os_pack_software_engineering.agents.git_push:GitPushAgentEntrypoint`) is this pack's second agent added since the module-27 hard gate lifted, and the first to consume a Platform Service (the real Git Integration Service, module 24's own row) via `platform.git.commit`/`platform.git.push` (module 31's own row). Genuinely SDK-native, no LLM call. Chained into `se.delivery_pipeline` as its new, final step, after `documentation`; zero-arg constructible with `remote_url=None` by default (no real, non-hardcoded push destination exists yet), degrading to a real, structured no-op (`pushed: false`) for every current real caller — proven end to end (real commit + push to a real, separate bare repo) only when a caller supplies a real, configured `GitIntegrationService`. Prior (2026-07-30, later same day): 6 of 17 documented agents real — `lint` (`ai_os_pack_software_engineering.agents.lint:LintAgentEntrypoint`) is this pack's first agent added since the module-27 hard gate lifted (2026-07-29), proving growth genuinely works via a real, non-`llm:invoke` agent (`sandbox:execute` only) that runs `python -m py_compile` through the sandbox (not `ruff`, tried first and found to genuinely fail against `DockerSandbox`'s own default image — see `agents/lint.py`'s own docstring); genuinely SDK-native from the start (no migration needed). Chained into `se.delivery_pipeline` between `build` and `test`. Prior (2026-07-30): 5 of 16 documented agents real (`requirements-analyst`, `architecture`, `build`, `qa-test`, `documentation`), all fully migrated onto the Platform SDK (steps 9–13), all chained into `se.delivery_pipeline`. |
| 30 | SE Pack — Workflows | C | 58% | minimal slice | **Updated 2026-08-09, later step (`P08-S02-M30-T01`): `se.product_creation` is now genuinely `done`, not `partial`.** The step-8 blocker this row's own prior update named (`foreach` not a real `StepType`) is closed — see module 5's own row for the full `StepType.FOREACH`/`ForeachStepExecutor` build. Wiring it here surfaced and resolved a second, real fork: `technical-planner`'s own real plan-item shape (`taskId`/`title`/`description`) did not satisfy `se.implement_task`'s own then-real `inputs` schema (`{task: string}`); presented via `AskUserQuestion`, the product owner chose to widen the consumer's schema (`se.implement_task.yaml` now declares `title`/`description` directly) over inventing a foreach-side field-mapping mechanism. `se.product_creation` now genuinely runs all 8 of its real steps to `WorkflowRunOutcome.COMPLETED` end to end against real Postgres, including a real, one-task fan-out into one real, separate, completed `se.implement_task` child. A second real, previously-latent gap found and fixed: `ForeachStepExecutor`/`SubWorkflowStepExecutor` fix `principal_id` at construction time, which no real production caller had ever exercised — `build_product_creation_trigger` now builds its whole composition fresh per real call instead. Both this workflow's own real remaining tickets (`T01`, `T02`) are `done`; this row's own % stays below 100 since `feature_inventory.md`'s broader scope — steps 9-14 of `se.product_creation`'s own documented graph (parallel gates, docs, release, PR) plus the 5 other documented workflow categories (Feature Addition, Bug Fix, Refactoring, Code Review, Release) — remains unbuilt. Prior (`P08-S02-M30-T02`)**: `se.implement_task` — Stage C's own other named target, `still 0%` per every prior update below — is now genuinely `done`, real end to end, not partial. Built entirely from real, already-existing components (design fork resolved via `AskUserQuestion` before writing code): `build` (documented as "deliberately generic and stack-agnostic") stands in for workflows.md §4's own undocumented `backend-developer`; no separate `fs.apply_patch`/`test.run` tool steps are declared — `build` writes and `qa-test` runs directly through their own `sandbox:execute` permission, the identical shape every other real agent in this pack already uses. **The two real `decision` steps this workflow needs (`tests-passed`, `no-blocking-findings`) genuinely loop backward to an earlier step (`implement`) — confirmed by direct inspection, not assumed, that `WorkflowDefinition`'s own forward-reference validator only restricts a decision's `condition.sourceStepId`, never its `branches` targets, and `WorkflowInstanceService._resolve_next_step` already resolves a branch target at any index.** No new Kernel mechanism was needed — this is `DecisionStepExecutor`'s first real use outside `se.delivery_pipeline`. Bounded only by this workflow's own whole-run `max_iterations` (a coarser, honestly-disclosed safety net, not §4's own labelled "escalate after 3 attempts"). `quality-gate-tests-pass`, unlike `se.product_creation`'s own two disclosed no-op gates, is a real, configured gate reading `qa-test`'s own real `passed` field. Proven twice against real Postgres: a straight-through happy path, and a genuine loop-back recovery (a controllable fake gateway fails `implement`'s first attempt, passes the second — `workflow_steps` shows two real rows for `implement`, `attempt` 1 and 2, the second genuinely higher). Not yet chained into any larger workflow (no `foreach` exists to invoke it from `se.product_creation`/`se.feature_addition` — the same, already-disclosed Workflow Engine gap `P03-S02-M29-T07`/`T08`/`P08-S02-M30-T01` all name). Prior (`P08-S02-M30-T01`)**: `se.product_creation` — the "Full Product Creation" named category, `still 0%` per every prior update below — is genuinely no longer 0%, though genuinely not `done` either. A real, buildable 7-of-14-step prefix (`capability_packs/software-engineering/workflows/product_creation.yaml`, `ai_os_kernel.workflow_engine.product_creation`) now runs end to end against real Postgres, through both real Human Approval Points, to genuine completion: requirements-analyst -> `quality-gate-requirements-complete` -> `approve-requirements` -> architecture -> `quality-gate-architecture-compliance` -> `approve-architecture` -> technical-planner. Both quality gates are real `quality_gate` steps but deliberately unconfigured (`gate_sources={}`) — `QualityGateStepExecutor`'s own documented "an unconfigured gate id resolves as a pass with empty outputs" behaviour, a disclosed no-op, not a fabricated pass/fail signal (neither `requirements-analyst` nor `architecture` has a pass/fail-shaped output field to read yet). Genuinely, structurally blocked at step 8 (`foreach implementation_plan.tasks -> sub_workflow se.implement_task`): `foreach` is not a real `StepType` — confirmed again, still exactly 7 members — the identical gap `P03-S02-M29-T07`/`T08` already found for `technical-planner`'s own output. Design fork resolved with the product owner before writing code: build the real, buildable prefix and mark the ticket `partial`, rather than inventing a `foreach` substitute or leaving the ticket untouched. **A real, discovered-and-fixed naming collision, not a regression**: this workflow's own `(id, version)` originally collided with `tests/integration/workflow_engine/conftest.py`'s own autouse placeholder-definition fixture, pinned to the exact same `("se.product_creation", "1.0.0")` composite key for ~20 unrelated tests that predate this real workflow — this definition's own `register()` was silently losing an `ON CONFLICT DO NOTHING` race to that placeholder's empty `graph`. Fixed by pinning this real definition to `0.1.0` instead (also now consistent with every prompt it invokes, all pinned `@0.1.0`), leaving the shared placeholder fixture and its ~20 dependent tests completely untouched. `se.implement_task` (Stage C's other named target) remains separately, unaffected, still 0%. Prior (`P03-S03-M30-T02`)**: FR-030 — `se.delivery_pipeline` gained a new, optional, additive `specification` input (structured Markdown, alongside the pre-existing, still-required `requirement`; 13 existing callers unaffected — design fork resolved with the product owner before writing code). Requirements Analyst parses and validates it (`ai_os_pack_software_engineering.workflows.spec_parser`, a real, dependency-free bullet-list parser) into a real `specificationItems` output field, folded into its own prompt context. **A real bug found and fixed via the integration test, not the unit tests**: an early draft read the new field directly off the agent's own `inputs` dict, which `AgentStepExecutor` never populates with arbitrary `instance.inputs` keys (no per-step input-mapping mechanism exists) — only a genuine `AgentStepExecutor`/Context Manager/`WorkflowStateResolver` chain, which the deterministic pack-only unit tests did not exercise, caught it; fixed by reading the field back out of `WorkflowStateResolver`'s own existing JSON-dumped context item, the one real channel that already carries it. All 6 of this module's own tickets are now `done` at the ticket level; this row's own % stays below 100 since `feature_inventory.md`'s broader scope — the 5 other documented workflow categories (Feature Addition, Bug Fix, Refactoring, Code Review, Release) — remains 0%, unaffected by this step. Prior (`P03-S03-M30-T03`)**: FR-045's own bounded review-revise loop — `se.delivery_pipeline` (bumped to `1.8.0`) gained a `code-review` agent step and a third real, blocking, bounded-retry quality gate (`quality-gate-code-review-clean`), reading `CodeReviewerAgentOutput`'s own new, mechanically-derived `passed` field (`True` iff no finding's own `severity` is `high`) — proving the same, unchanged `QualityGateStepExecutor`/retry mechanism (module 5's own row) generalizes to a third gate category with zero Kernel changes, only composition-level wiring (`_GATE_SOURCES`/`_GATE_IDS`/`_STEP_RETRY_TARGETS` in `delivery_pipeline.py`). Now 13 declared steps: 8 agents, 1 `decision`, 3 `quality_gate`, 1 `human_approval`. Design fork resolved with the product owner before writing code: extend this mature, versioned pipeline directly rather than build a separate standalone proof workflow. Disclosed, unfixed, pre-existing gap, not introduced or worsened here: FR-045 itself also names a token-ceiling bound; no workflow anywhere in this codebase enforces one today — this loop, like every other gate, is bounded by attempts/duration only. Prior (`P03-S03-M30-T06`)**: the previous update's own "no HTTP route exists yet" gap is closed — `POST /api/v1/workflows/{workflow_id}/approvals/{approval_id}/decisions` (`ai_os_kernel.routes.approvals`, module 14's own row) lets a real, authenticated, authorized human resume a real, paused `se.delivery_pipeline` instance, and `resume_pipeline_after_approval` (`delivery_pipeline.py`) genuinely re-drives it to completion — proven end to end via real HTTP against a real Postgres instance: a real pause, a refused unauthorized attempt (remote untouched), and a real, authorized decision resuming to a genuine commit and push, verified by reading the remote's own refs directly. Resumption deliberately does not go through the generic multi-instance worker loop (module 5's own row has the full investigation: that loop's fixed composition cannot execute this pipeline's own `quality_gate`/`decision`/`human_approval` steps at all) — `se.delivery_pipeline` is now excluded from its discovery entirely, closing a real race the investigation surfaced. Prior (`P03-S03-M30-T05`)**: `se.delivery_pipeline` (bumped to `1.7.0`, manifest.yaml kept in sync) now has a real Human Approval Point, `approve-git-push`, between `documentation` and `git-push` — closing R-001's own standing concern that this pipeline ran fully autonomously with no human in the loop before its one real, external, irreversible action. Reuses the Human Approval Manager unchanged (module 5's own row); no new Workflow Engine mechanism. Proven end to end, one real run (`test_delivery_pipeline_git_push.py`, rewritten): the pipeline genuinely pauses before git-push runs at all (the real remote receives nothing while pending); an unauthorized decision attempt is refused, remote still untouched; only a real, attributable, RBAC-authorized decision (`approver:approve-git-push`) resumes the pipeline to a genuine commit and push. A direct push to a genuinely protected branch remains separately, permanently, structurally impossible regardless of any approval (`GitIntegrationService.push`'s own §5.1 refusal, module 24's own row) — this gate targets the autonomy gap, not a bypass path around that refusal. This row's own real HTTP route for deciding a paused instance is `P03-S03-M30-T06`'s own paragraph above — closed the same module, one step later. Every other real caller/test whose run now reaches this point (7 of them) genuinely receives `WorkflowRunOutcome.WAITING_FOR_HUMAN` instead of `COMPLETED` — a real, deliberate behavior change, not a regression; each was updated to assert the new, correct outcome, none needed its own real assertions (about steps that already ran before this gate) to change. Prior (`P03-S01-M24-T02`) the "no `git_service` threaded into `bootstrap.py`" gap this row previously listed is closed — `bootstrap.py`'s real `_build_se_delivery_pipeline_registry` now builds a `git_service` from real `AIOS_GIT_*` env vars (module 24's own row) and threads it into `SqlAgentRegistry`, so the live HTTP route is genuinely wired to commit and push once a real remote is configured. Still degrades to the identical safe no-op today (`AIOS_GIT_REMOTE_URL` unset in every current environment), so no observable pipeline behavior changed for any current real caller. Prior (`P03-S03-M30-T04`), same day: now 10 real steps — a new, final `git-push` agent step genuinely commits and pushes Build's own file through the real Git Integration Service when configured, the first real, end-to-end exercise of that service and its ToolInvoker-reachable tools through an actual running workflow (not a direct service/adapter call). Reads `build`'s own `workingDirectory`/`filePath` via `_STEP_SOURCES` (`ai_os_kernel.workflow_engine.delivery_pipeline`). Real, dedicated proof (`test_delivery_pipeline_git_push.py`) supplies a real local bare-repo remote and a real `GitIntegrationService`, verified by reading the remote's own refs and blob content back directly. Prior (2026-07-30, later same day): now 8 real steps — `lint` (a 6th agent step) plus a second real, blocking, bounded-retry `quality_gate` (`quality-gate-lint-clean`) now sit between `build` and `test`, proving the gate mechanism generalizes to a second category (Static Analysis) via configuration alone (module 5/15). Prior (2026-07-30): 1 real workflow (`se.delivery_pipeline`) — a narrower, different shape than either of Stage C's own named targets (`se.product_creation`, `se.implement_task`), both still 0%; 6 real steps including the first quality gate (`quality-gate-tests-pass`); genuinely reachable over real HTTP (`POST /api/v1/workflows/se.delivery_pipeline`). Still missing: an automated manifest → catalog installer (agents/prompts are still hand-seeded by tests, real caveat unchanged) and the 5 other documented workflow categories (Feature Addition, Bug Fix, Refactoring, Code Review, Release), all still 0%. |
| 31 | SE Pack — Tools & Quality Gates | C | 28% | minimal slice | **Updated 2026-08-07 (`P03-S04-M31-T03`):** a verification step, not a build step — this ticket's own Goal ("move the pipeline's gates into pack-declared definitions") was already substantively achieved by `P02-S06-M15-T06`/`T09` plus `P03-S04-M31-T02`'s own prior row above, but the ticket itself was still `todo`. Checked field-by-field and found one real, concrete gap: the third real gate (`quality-gate-code-review-clean`, added `P03-S03-M30-T03`) had pass/fail/retry proof but no dedicated proof that it genuinely resolves through the real Gate Registry with its own correct `gateId`/`gateVersion` — the exact proof the other two gates already had; added. Also found and closed a second real gap in `platform_sdk/src/ai_os_sdk/testing/pack_contract_suite.py`'s own check 2 (`entry points resolve`): its own docstring claimed "every" entrypoint-bearing manifest section resolves, but the loop only ever covered `agents[]` — `tools[]`/`qualityGates[]` were added to the schema later and never folded in, unexercised until this ticket's own new tools/gates gave it something real to check against. Now genuinely validates all 3 gate entrypoints and both of `P03-S04-M31-T02`'s own tool entrypoints resolve to real classes. `catalog.quality_gates` remains a disclosed, deliberately-unopened schema-authority fork (`quality_gate_engine.md`'s own explicit statement, twice) — out of this ticket's own real scope, not silently skipped. Prior (`P03-S04-M31-T02`):** the first two real, manifest-declared, registry-resolvable Tools this codebase has ever had — `fs.read`/`build.run`, `tier1_sandboxed`, zero-argument-constructible, no `PackContextReceiver` needed. **Two real, previously-undiscovered Kernel bugs found and fixed before either could resolve at all**, both in `SqlToolRegistry.resolve_tool` (`ai_os_kernel.workflow_engine.registry`): (1) the trust-tier consistency check compared object *identity* (`is not`) against `ai_os_kernel.workflow_engine.tool.TrustTier` — a real Capability Pack, forbidden from importing `ai_os_kernel` at all, can never be identical to that exact enum object, so every real pack tool would have been refused regardless of tier; every entrypoint this check had ever been exercised against before this ticket imported that exact Kernel enum directly, so the bug was never caught. Fixed to value equality (`!=`). (2) `PackContext` (`llm`/`prompts`/`tools` only) gave a zero-argument-constructible Tool no way to receive a real `SandboxExecutor`, so no such Tool could ever satisfy `SandboxBackedTool` (`tool.sandbox is not None`), which both real dispatch paths require before running a `tier1_sandboxed` tool. Design fork resolved with the product owner: `SqlToolRegistry` now injects the sandbox directly onto the resolved instance, bypassing `PackContext` for this one Kernel-side structural need, rather than widening the SDK's own frozen `PackContext` model. Proven end to end against a real Postgres catalog and a real `LocalSubprocessSandbox` — a real file genuinely read, a real command genuinely run. Not yet adopted by any agent (deferred, matching this pack's own established "prove standalone first" precedent). Prior (2026-08-02, `P03-S04-M31-T04`):** three more real, platform-provided (non-manifest-declared) tools — `platform.git.commit`/`create_branch`/`push` — dispatched by `ToolInvokerAdapter` straight to the real Git Integration Service (module 24's own row), the identical shim shape `platform.sandbox.run_command` already established, chosen over the zero-arg registry-resolved path since `PackContext` exposes no Kernel-internal DB access and the Git service genuinely needs one (its own audit log). Proven end to end: a real `invoke()` call reaches the real service, the real sandbox, and a real `git` subprocess. A real caller now exists too (`P03-S03-M30-T04`, same day, module 30's own row): the new `git-push` agent, chained as `se.delivery_pipeline`'s own final step. `SandboxedCommandTool` (also internal, non-manifest-declared) remains real too. Still (as of this prior step): no manifest-declared Tool at all (closed above, `P03-S04-M31-T02`); 0 of 7 documented gate categories implemented as a *pack-declared* gate (manifest still has no `gates/`) — the manifest-independent Testing-category gate module 5/15 enforces for `se.delivery_pipeline` is a Workflow-Engine mechanism, not this. |
| 32 | Project Intelligence Pack | E | 58% | minimal slice | **Updated 2026-08-09 (`P05-S02-M32-T05`)**: a fifth real Tool, `documentation.generate`, consumes `architecture.recover`'s own `modules`/`moduleEdges`/`circularDependencies` and renders a real, deterministic Markdown documentation set (FR-056) — a module overview table, a dependency table, an "Architectural Concerns" section flagging real cycles and modules past a named, disclosed coupling threshold. `tier2_trusted`. Deliberately not LLM-written semantic prose (design fork resolved via `AskUserQuestion`, the 5th time this module has made this exact choice) — real, disclosed, deferred work for the eventual `existing-project-analyzer` agent. Proven by 10 deterministic tests. Prior (`P05-S02-M32-T04`): a fourth real Tool, `architecture.recover`, consumes `dependency.graph`'s own `nodes`/`edges` and performs real deterministic graph analysis (FR-053) — module-level boundary aggregation, real DFS-based circular-dependency detection, real fan-in/fan-out coupling. `tier2_trusted`, mirroring `language.detect`'s own reasoning. Deliberately not LLM-driven narrative documentation (design fork resolved via `AskUserQuestion`) — that half is real, disclosed, deferred work for the eventual `existing-project-analyzer` agent. A real model bug (missing pydantic aliases) caught by its own test before mypy ever ran. Proven by 12 deterministic tests, including real 2- and 3-module dependency cycles. Prior (`P05-S02-M32-T06`): every one of the 3 real Tools now carries a real, structural `trust: "untrusted"` field (`provenance.py`, FR-059) — this pack's own independently-mirrored counterpart to the Context Manager's already-real `ContextItem.trust` (`ai_os_kernel` cannot be imported here), mirroring `ai_os_sdk.models.tool.TrustTier`'s own established independent-mirroring precedent. Not caller-configurable — no code path here could ever honestly produce `"trusted"`. Proven by 3 new tests (one per Tool) including a real, unmocked Docker round-trip. Prior (`P05-S02-M32-T03`): a third real Tool, `dependency.graph`, consumes `repository.ingest`'s own `files` output (filtered to Python) and constructs a real, queryable module/dependency graph via stdlib `ast` (FR-052) — real import edges, not a heuristic or an edge-free artifact (design fork resolved via `AskUserQuestion`). Back to `tier1_sandboxed` — reading and parsing real file content is "processing untrusted repository content" per ADR-0016, unlike `language.detect`'s path-only pattern matching. A real bug caught in manual verification before any test was written: `from . import x` first resolved to the parent package rather than `x` itself — fixed via per-name resolution, re-verified. Proven by 11 tests including 2 real, unmocked Docker tests. Scoped to Python only; other languages disclosed, deferred. Prior (`P05-S02-M32-T02`): a second real Tool, `language.detect`, consumes `repository.ingest`'s own `files` output and detects languages/build systems/frameworks with real, disclosed confidence per finding (FR-051's own acceptance criterion). Classified `tier2_trusted`, not `tier1_sandboxed` — a real, disclosed distinction from `repository.ingest`: this Tool touches no filesystem, network, or execution at all, only pattern-matching over an already-extracted path list. Proven by 12 deterministic tests (pure computation, no sandbox/fake needed). Prior (`P05-S02-M32-T01`): the pack is scaffolded (no `ai-os-kernel` dependency, real from its first commit, mirroring `benchmarking`'s own precedent) with one real Tool — `repository.ingest`, a genuine `tier1_sandboxed` walk of a repository directory into a file/module inventory, proven against a real Docker daemon. ADR-0016 correction caught mid-investigation: repository-content-processing tools are unconditionally Tier 1, not Tier 2 as first assumed — fixed before any code was written. Not yet manifest-declared, not wired into any agent/workflow; the `existing-project-analyzer` agent and all 5 documented workflows remain unbuilt. A real, separate CI gap found and partly fixed along the way: no Capability Pack's own tests were ever run in CI (confirmed for `benchmarking`/`software-engineering` too) — fixed scoped to this pack; widening it further is blocked on a second, pre-existing, unrelated test bug in `software-engineering`'s own suite (disclosed, deferred, not this ticket's scope). |
| 33 | Voice (Jarvis) Pack | F | 25% | Platform Integration Layer real, inside the Kernel, no real voice pipeline | **Updated 2026-08-09 (`P06-S06-M33-T01`)**: the §4.5 Platform Integration Layer is real — `ai_os_kernel.voice_jarvis.intent_router.PlatformIntentRouter` — but lives inside the Kernel, not `capability_packs/voice_jarvis`, a genuine structural finding, not the originally planned home. A first attempt built this as a pure external HTTP-client Capability Pack, mirroring `tools/aios`'s own proven shape; `pack_contract_suite` check 7 (`platform_sdk.md` §9/§10) caught it for real — **any** direct HTTP client import in pack code is forbidden, full stop, including a call to the platform's own Kernel API, not only a third-party provider. No SDK Protocol for "read workflow status"/"decide an approval" exists yet, so this lives here instead — the identical "build inside the Kernel, not a separate package" precedent Notification Service and the Speech Gateway itself already establish — calling the real `HealthService`/`WorkflowInstanceRepository`/`ApprovalService` directly (no HTTP, no serialization round trip). 4 real intents (`check_health`, `list_workflows`, `get_workflow_status`, `decide_approval`), a real `workflow:read` permission gate, `ApprovalService`'s own real class-scoped authorization reused unchanged. Not built: wake word, STT, intent recognition (module 25's own row — Speech Gateway is real but has no real provider), real spoken TTS output (`SpeechGateway.synthesize()` is reachable directly now that this lives in the Kernel too, but not wired this step), Voice Session Manager, wake-word orchestration, Notification Bridge, and no `capability_packs/voice_jarvis` distribution exists at all (removed after the rejected first attempt). Proven by 13 real tests: 10 unit tests (real `HealthService`, real `ApprovalService` backed by a fake repository — the identical pattern `tests/security/test_t10_unauthorized_approval.py` already establishes) plus 3 real Postgres-backed integration tests (a real seeded instance, a real seeded pending approval genuinely decided, a real unauthorized principal genuinely refused). |
| 34 | Benchmarking Pack | D | 28% | minimal slice, all 4 own tickets closed | **Updated 2026-08-07 (`P04-S03-M34-T04`)**: `prompt_adaptation.py` closes this module's own 4-ticket set — `record_prompt_adaptation`/`with_recorded_prompt_adaptation` detect a genuine per-model prompt difference (pure string comparison, no Kernel dependency — rendering is `InMemoryPromptEngine`'s own job) and fold it into `ExperimentSpec.variables` as a real, declared variable, closing `overview.md` §7's own "anything varying that was not declared... invalidates the experiment" rule for this one real case; a byte-identical prompt (the default) records nothing. Also fixed `overview.md`'s own Implementation Status, left saying "Built: nothing" across all 4 of this module's own tickets — now accurately describes all 4 real modules (`experiment_definition`/`replicate_management`/`cost_ceiling`/`prompt_adaptation`) and their shared "not yet wired into submission" limit. Still: no `manifest.yaml`, no real submission/execution/reporting path — Module 34's own next real milestone is wiring these 4 real components into one, once a real submission mechanism exists. Prior (`P04-S03-M34-T03`)**: `cost_ceiling.py`'s `enforce_cost_ceiling` — `llm_gateway.md` §9's own "Experiment cost ceiling | Refuses to start the run" row, a real, distinct failure mode (`CostCeilingExceededError`) from `BudgetExceededError`'s own per-call ceilings. `ExperimentDefinition` gained an optional `cost_ceiling_usd` field (`None` by default — every existing test/caller unaffected). **A real, disclosed scope boundary, not silently widened**: this ticket's own literal Input ("a projected cost") scopes it to enforcement, not computation — a genuine, non-approximated cost projection needs the real `count_tokens()` API (real for exactly one provider adapter today) run against every planned replicate's own rendered prompt, times real configured pricing, summed across variants — real, separate, unbuilt work no caller has asked for yet. Not wired into any real dispatch path (nothing computes a real projection to enforce against today) — `llm_gateway.md`'s own Implementation Status updated to disclose this precisely. Prior (`P04-S03-M34-T02`)**: `replicate_management.py` expands one validated `ExperimentVariant` into `runs_per_variant` real, numbered `ReplicatePlan`s (a defensive `>= 3` re-check, since `runs_per_variant`'s own real minimum is already enforced upstream by `validate_experiment_spec`) and records each as a real `evaluation.experiment_runs` row. The identical "pack declares a Protocol, `ai_os_kernel.sdk_adapters` implements it for real" shape `T01`'s own `WorkflowDefinitionExistenceCheck` already established, applied to a second Protocol (`ExperimentRunRecorder`) — `SqlExperimentRunRecorder`, proven end to end against a real Postgres, writing one real, correct row per real replicate (three real, distinct `run_id`s for three real, distinct real `workflow_instances` rows in the same test). **A real design mistake found and fixed before it shipped, not after**: an early draft had the Kernel-side adapter import `ReplicatePlan` from the pack — the reverse of this codebase's one, consistently-enforced architectural rule (Kernel never depends on pack code, confirmed by `delivery_pipeline.py`'s own docstring-only cross-references, never a real import) — fixed by crossing the Protocol boundary with plain, primitive fields (`variant_key`/`model_alias`/`replicate_index`) instead of a compound pack-defined object, in both directions. Not wired into any real submission path yet, matching this module's own next tickets. Prior (`P04-S03-M34-T01`)**: this pack's own first real code — `capability_packs/benchmarking/` is now a real, buildable distribution (`pyproject.toml` + `src/ai_os_pack_benchmarking`), a genuine workspace member, discovered and cleared by `check_import_boundaries.py`'s own generic pack scan (zero forbidden imports, proving the boundary discipline generalizes to a second real pack for the first time). `experiment_definition.py` validates a raw experiment spec against `overview.md` §7's own real, already-decided rules — `runs_per_variant >= 3`, `>= 2` variants, no duplicate variant keys, no sampling-parameter (`temperature`/`top_p`/`top_k`) variables, and the pinned `(definition_id, definition_version)` genuinely exists — and returns a real, schema-compatible `ExperimentDefinition`. The existence check is a pack-defined Protocol (`WorkflowDefinitionExistenceCheck`), since this pack's own source may never import `ai_os_kernel`; the real, Postgres-backed implementation (`SqlWorkflowDefinitionExistenceCheck`) lives in `ai_os_kernel.sdk_adapters` instead — the identical "Kernel implements a Protocol this pack only declares" shape that package already establishes for the SDK's own Protocols, now proven for a pack-defined one too. No `manifest.yaml` yet — nothing here needs pack activation (no agent/tool/workflow declared). Not wired into any real submission/execution path yet, matching this module's own next tickets. Still 0% of the pack's own Agent/API/Report surfaces (nothing beyond experiment definition exists). Prior (2026-07-29): directory empty; docs exist, no code. **No longer blocked by the module-27 hard gate**, same as module 32. |
| 35 | ~~Analytics Pack~~ | — | N/A | **removed** | Investigated and deleted 2026-07-28 — confirmed stale, undocumented, never git-tracked. Excluded from Section 6's weighted total. See Section 3 note and `history/023`. |
| 36 | API (HTTP surface) | B/F | 34% | minimal slice | **Updated 2026-08-06 (`P06-S01-M36-T04`, partial)**: closes api_architecture.md §6.5's own remaining routes — `GET /api/v1/packs` (list, real `PackLifecycleRepository.list_packs()`), `GET`/`PATCH /api/v1/config` (real `ConfigurationManager.load()`/`RuntimeOverrideStore.apply()`, the exact "writer, then route" sequence that class's own docstring already named as pending), `GET /api/v1/config/flags` (real `resolve_feature_flag()` over every activated pack's declared flags). Two new real permissions (`config:read`/`config:manage`, mirroring `pack:read`/`pack:manage`'s own grant reasoning). Real design correction found and fixed mid-build: an initial "reject unknown config_key" check would have silently broken feature-flag overrides, which read the identical `RuntimeOverrideStore` layer by flag name, never a `PlatformConfig` field — removed before it shipped. `PATCH /config` validates the *would-be* result before writing any audit row (proven: a rejected change leaves zero trace, real or in-memory). Ticket's own real remaining scope (~24 of ~28 missing routes: §6.1/§6.2/§6.4/§6.6, plus §6.3 Experiments with no backing service yet) is disclosed, not attempted — flagged as oversized for one step, product owner chose this slice via explicit options. 14 real authenticated routes vs. ~45 documented now. Prior (`P06-S01-M36-T01`): the committed `docs/07_api/openapi.json` (`scripts/export_openapi.py`) is real and, for the first time, genuinely drift-checked — `tests/contract/test_openapi_contract.py`, wired into `ci.yml`'s existing `tests/contract` step with no new job needed. The artifact had already gone stale (missing the two role-administration routes) with nothing to catch it; proven the new test actually catches drift, not just passes trivially, by reverting the artifact and watching it fail first. Prior (`P06-S01-M36-T03`): `Idempotency-Key` handling is now real — `ai_os_kernel.routes.idempotency.IdempotencyKeyMiddleware`, a real ASGI middleware (registered alongside `TraceIdMiddleware`, the same "sees request and eventual response" shape) backed by the real, previously-schema-only `platform.idempotency_keys` table, generic across every mutating route rather than one at a time. A replay (identical key/method/path/body/principal) returns the cached original response; a reused key with a different body **or a different principal** returns a real `409` (the principal check is a real, deliberate extension beyond `api_architecture.md` §9's literal words). Proven end to end against real Postgres and a real `POST /api/v1/workflows`: at-most-once effect verified by counting real `workflow_instances` rows, not just comparing response bodies. One real, disclosed race window under genuine concurrency (two brand-new identical keys racing the same insert) — not solved with distributed locking, out of this step's own scope. Prior (`P06-S01-M36-T02`): every route's own error is a real, consistent RFC 9457 `application/problem+json` body — `ai_os_kernel.routes.problem_details.register_problem_detail_handlers` installs three generic FastAPI exception handlers (`HTTPException`, `RequestValidationError` with a real `violations` array, and a catch-all `Exception` for 500s that never leaks the real exception message), replacing FastAPI's bare `{"detail": ...}` default everywhere at once. `error_code`/`type`/`title` are honestly coarse (status-derived) — the real `error_code` catalogue still does not exist. 10 real authenticated routes vs. ~45 documented across 9 resource groups remain otherwise unchanged — `POST /api/v1/workflows/se.delivery_pipeline` (2026-07-30), the first pack-specific (not platform-demo) workflow trigger; no OpenAPI generation/snapshot test, no rate limiting. |
| 37 | WebSocket stream | F | 60% | minimal slice | **Updated 2026-08-08 (`P06-S02-M37-T01`)**: `/api/v1/stream` is real — `ai_os_kernel.routes.stream`, a real `WebSocket` route reusing the existing `InProcessEventBus` directly (no parallel delivery mechanism). Real auth (the same `TokenVerifier`/`resolve_effective_roles`/`permissions_for_roles` chain `authenticate` uses, called directly since `Depends(authenticate)`'s own `Request`-typed signature does not resolve for a `WebSocket` route) and a real `WORKFLOW_READ` permission check both refuse the connection before it is ever accepted. Two real, disclosed gaps investigated before building, not found after (design fork resolved via `AskUserQuestion`): no Topic/Channel Manager exists (`event_bus.md`'s own Implementation Status) — this route maps ADR-0014's own four topic shapes onto `Event`'s own real fields itself (`workflow:{id}` -> `event.workflow_id`; `system`/`approvals` -> an `event_type` prefix); `experiment:{id}` is refused with a clear reason, since `Event` has no `experiment_id` field to match against. **No real Kernel component publishes to the bus in production yet** (same document: "the Workflow Engine... does not yet... call `InProcessEventBus.publish`") — this closes the real consumer side only; a real producer (Workflow Engine step/human-approval events) is separate, unbuilt work. Proven end to end, 9 real tests (`tests/unit/kernel/routes/test_stream.py`): missing/invalid token refused, missing permission refused, a bare `TestClient` (no `_lifespan`) refused, an unknown topic refused, `experiment:{id}` refused with its own real reason, a real published `Event` genuinely reaching a subscribed client, topic-filtering genuinely excluding a non-matching `workflow_id`, and `system` topic prefix matching. `app.state.event_bus` (a real `InProcessEventBus`) is now built unconditionally in `_lifespan` and closed in its own `finally` block — the first real caller of this bus outside its own package and `outbox_relay.py`. |
| 38 | CLI (`aios`) | F | 55% | 5 of 8 documented command groups real | **Updated 2026-08-09 (`P06-S04-M38-T01`)**: `tools/aios` is a real, installable `ai-os-cli` distribution (Typer + Rich, exactly as `cli_design.md` §4 specifies) — a pure client of the platform API (ADR-0014), no Kernel import, no database credential. Real, backed by a real HTTP endpoint: `auth` (login/logout store a bearer token locally — no `/auth/login` endpoint exists to exchange credentials for one; `whoami` decodes the stored token's own claims locally, correct without a server round trip), `health` (live/ready), `workflow` (start/list/show/events), `approve` (decide), `pack` (list/show/activate/deactivate — the one fully real, complete group), `config` (get/set/flags — also fully real and complete). `--output human`/`--output json` (TTY-detected default) and the full, real, documented 7-value exit-code table (driven by the actual HTTP response, not guessed) are both real. Disclosed, not built — every one of these commands is still discoverable via `--help` but fails with a real, specific reason, never silently: `workflow cancel`/`retry`/`manifest` (no route exists), `approve list`/`show` (`ApprovalRepository` has no method that lists approvals at all — the identical gap Dashboard's own `P06-S03-M39-T02` report disclosed), `experiment` (Benchmarking Pack still 0% built), `logs` (no log-query route), `health detail` (no distinct endpoint — `ready` already returns full detail). Proven by 32 real tests: pure-logic (config file round-trip, output rendering, exit-code mapping), CLI-level tests against a real, locally bound `uvicorn` server running the real Kernel ASGI app (every 401/403/503 degrade path, never a fabricated response), and one full real end-to-end test (real Postgres, real `SqlPackLifecycleRepository`) driving a genuine pack through `list -> show -> activate -> deactivate -> a real, correctly-refused second deactivate` entirely through the CLI. |
| 39 | Dashboard | F | 55% | minimal slice | **Updated 2026-08-09 (`P06-S03-M39-T03`, closes Module 39's own 3/3 documented views)**: the third and last documented view is real — Cost & Quality (`CostQualityView.tsx`), rendering cost breakdowns by model/workflow/agent/pack and a gate-failure frequency table against the real generated client (`GET /api/v1/evaluation/cost-and-quality`, module 12's own new row), no parallel mechanism. Proven against a real, live-running Kernel API (loading/ready/error states, mocked at the `apiClient.GET` boundary the same way `HealthView`'s own tests already establish) plus a real production `tsc -b && vite build`. Prior (`P06-S03-M39-T02`): 2 of 3 documented views real — Health (liveness/readiness/component status) and Workflows (a real list -> detail drill-down: instance status, current step, steps table, events table), both against the real generated client, no parallel mechanism. **Pending Approvals is genuinely blocked, not deferred**: `ApprovalRepository`'s real interface (`get_by_step`/`get_by_id`/`create_pending`/`decide`) has no method that lists approvals, and `routes/approvals.py` has exactly one real route (deciding one, by id) — there is no way, at the query layer or the HTTP layer, to fetch "all pending approvals" today; a workflow's own detail view can show it is `waiting_for_human` but not the approval's own id/class/options needed to decide it. Added a minimal, disclosed bearer-token input (`auth/token.ts`, `localStorage`) standing in for a real OIDC login UI — reuses the exact same real `Authorization: Bearer` contract the Kernel already expects, not a parallel auth scheme. **A real, previously-silent CI gap found and fixed**: the Dashboard job's own `npx tsc --noEmit` step checked **zero files** (the bare root `tsconfig.json` is `{"files": [], "references": [...]}` — confirmed directly via `tsc --noEmit --listFiles`), so it always passed regardless of any real type error; fixed to `npx tsc -b`, the invocation that actually type-checks project references. Proven against a real, live-running Kernel API (`uvicorn`, no mocks): real `200`s with correctly-typed bodies for `/health/live`/`/version`/`/health/ready`, a real `401` (RFC 9457 body) with no token, and — the genuine auth-wiring proof — the identical request with a real, signed JWT reaching a real `503` ("workflow engine is not available") instead of `401`, proving the client's own `Authorization` header injection genuinely works end to end. Prior (`P06-S02-M39-T01`)**: `dashboard/` is a real, running React 19 + TypeScript (`strict: true`) + Vite project — no longer an empty directory. Consumes only the generated API client (ADR-0018): `src/api/schema.gen.ts` is generated from the real, published `docs/07_api/openapi.json` (`npm run generate:api-client`; `npm run check:api-client`, using `openapi-typescript`'s own real, built-in `--check` flag, is the CI-enforced drift gate ADR-0018 line 27 requires — proven to genuinely catch drift, not pass trivially, by introducing then reverting a real diff). `App.tsx` makes one real call through the generated, typed client (`GET /api/v1/version`) and renders the result — genuine consumption, not just scaffolding. Real production build (`tsc -b && vite build`) proven to genuinely run: served via `vite preview` and curled for a real 200. 2 real vitest tests (mocking the client module's own `GET` method, since `openapi-fetch` captures `globalThis.fetch` once at client-construction time — a real, discovered testability constraint, not a design flaw glossed over). `npm audit`: 0 vulnerabilities (a transitive `js-yaml` CVE in `openapi-typescript`'s own dependency chain forced via a real `overrides` pin, `4.3.1`). Deliberately deferred, per this ticket's own narrower Goal than ADR-0018's full stack (design fork resolved via `AskUserQuestion`): TanStack Router/Query, the WebSocket-into-cache pattern, Tailwind/shadcn/ui, Recharts, and Playwright e2e — separate, later dashboard tickets. |
| 40 | Deployment & Infrastructure | G | 55% | minimal slice | **Updated 2026-08-08 (`P07-S01-M40-T03`)**: real backup/restore tooling and a real, automated rehearsal now exist — `infra/scripts/backup.sh`/`restore.sh` (`pg_dump`/`pg_restore`, custom format, portable across every Postgres deployment this module's own architecture doc names) and `tests/integration/infra/test_backup_restore_rehearsal.py`, closing `deployment_architecture.md`'s own disclosed "no backup/restore tooling and no rehearsed restore drill" gap. Proven against two genuinely independent, real Postgres containers: a source is migrated, seeded with a real, hash-chained `governance.audit_log`, backed up, and restored into a completely separate, genuinely empty database — the restored rows pass a real `verify_chain()`, proving the hash chain itself (not merely the raw bytes) survives the round trip. A real, disclosed bug was found and fixed while building this: `testcontainers`' own `get_connection_url()` defaults to a SQLAlchemy-dialect URL (`postgresql+asyncpg://...`) that `pg_dump`/`pg_restore` (real libpq clients) cannot parse — passed that form, `pg_dump` silently hangs waiting on a password prompt that never arrives; fixed by requesting the plain, libpq-compatible form (`driver=None`) for the two real client-tool calls specifically. Still disclosed, unbuilt: PITR/continuous archiving (a cloud-managed-service-specific capability — no provider decided anywhere in this repo), the audit log's own offsite export, and the "confirms an in-flight workflow resumes" half of the architecture doc's own rehearsal description (this rehearsal seeds only `audit_log`, not a paused `workflow_instances` row). Prior (2026-08-08, `P07-S01-M40-T02`): the `NetworkPolicy` egress allowlist is real too, opt-in (`networkPolicy.enabled`, default `false` — zero regression for a default install, product-owner decision). A real, non-invented DNS (`kube-dns`) + same-namespace baseline (covers `otel-collector`) always applies when enabled; Postgres/Redis/provider destinations are environment-specific managed services this chart does not deploy (`deployment_architecture.md`'s own table names them "Managed Postgres" in every real environment), so they are the operator's own responsibility to declare via `networkPolicy.egress.additionalRules` — a real, literal Kubernetes `NetworkPolicyEgressRule` list, never an invented CIDR/FQDN. Proven both offline (`helm template`) and against a real, temporary `kind` cluster — the identical proof shape `P07-S01-M40-T01` already established, extended rather than duplicated (`tests/integration/infra/test_ai_os_helm_chart.py`). Prior (2026-08-07, `P07-S01-M40-T01`): this row was severely stale — a real `Dockerfile` and a real, standalone worker entry point have existed since `P01-S01-M40-T04`/`T05` (2026-08-03), neither reflected here. A real, partial Helm chart now also exists — `infra/kubernetes/helm/ai-os/` — `Deployment/aios-api` (replicas 2)/`Deployment/aios-worker` (replicas 3), `Service/aios-api`, `ConfigMap/aios-config`, `ServiceAccount/aios`, `PodDisruptionBudget` (one per role), `HorizontalPodAutoscaler/aios-worker` (CPU-based). Validated via `helm lint`/`helm template` and, further, against a real, temporary `kind` cluster: `kubectl apply --dry-run=server` then a genuine, non-dry-run apply against a live Kubernetes API server, confirmed via `kubectl get` — pods correctly report `ErrImagePull` (no container registry decided anywhere in this repo, a real, disclosed, expected limitation, not a manifest defect). A real, three-way documentation conflict was found and resolved before writing any manifest: `deployment_architecture.md` §6's own Probes table names `/api/v1/health/...`, a prior ticket's own evidence note said `/health/...` (no prefix), and the real router (`routes/health.py`) declares `APIRouter(prefix="/api/v1", ...)` — the document's own path is correct; the prior ticket's shorthand note was imprecise. **Marked `partial`, not `done`: the ticket's own literal Goal ("deploy... with the supported sandbox patterns") is not met** — no Podman-sidecar or gVisor sandbox pattern is built, a real, separate, larger piece of work `ADR-0020` itself already calls "deliberate node configuration." Also genuinely unbuilt, each for a real, disclosed reason (see the chart's own README.md): `Ingress` (no ingress controller/TLS issuer/hostname decided anywhere), `NetworkPolicy` egress allowlist (no concrete provider IPs/FQDNs decided — inventing them would violate "no hardcoded values" literally), `ExternalSecret` (needs the External Secrets Operator plus a selected K8s secrets backend — the Vault backend itself, module 19's own row, is still unbuilt), and queue-depth HPA scaling (needs a metrics-adapter — Prometheus Adapter or KEDA — neither built nor installed). `infra/terraform/` remains empty. |
| 41 | Threat Controls (T1–T12) | C/G | 35% | minimal slice | **Updated 2026-07-28**: T1 (generated-code containment) now has a real, live-verified proof — network isolation and filesystem containment held for code the pipeline itself generated and ran, not just an isolated check. Sandboxing (T1/T12 — workspace isolation still not yet *mandatory*) and supply-chain scanning (T6, via CI) are the strongest; audit-chain (T8), Git safety (T11, no Git service exists), provider-egress controls (T7), and approval-fraud prevention (T10, no approvals writer) are all effectively unstarted. **Updated 2026-08-01 (`P01-S06-M42-T04`)**: every T1–T12 threat now has a dedicated regression test in `tests/security/` exercising its real, current control — this closes the testing gap, not the control gap; T10/T11 have no real enforcement to test, so their tests are disclosed tripwires proving that absence, not positive controls, and the percentage above is unchanged by this step. |
| 42 | Testing Infrastructure | A–G | 48% | minimal slice | **Updated 2026-08-09 (`P07-S03-M42-T02`)**: Cost Anomaly Alerting (NFR-045) is real — `evaluation_engine.cost_anomaly.SqlCostAnomalyDetector` computes real hourly spend against a real trailing-7-day hourly mean (bucketed from a new `evaluation.llm_calls.created_at` column, migration `0035` — that table had no timestamp column at all until this step) and a real background loop (`run_periodic_cost_anomaly_check`, wired into `_lifespan`) fires a real `Event` through the Notification Service (module 22's own row) the instant current-hour spend exceeds 3x the mean. Proven by 3 real Postgres-backed tests (spike flagged, normal spend not flagged, no-history honestly not flagged) plus 4 unit tests for the loop's own scheduling/resilience. Prior (2026-08-08, `P07-S03-M42-T01`): `tests/chaos/` is real too — 2 of the 3 named scenarios (kill worker, drop provider), against real, already-wired production mechanisms (`WorkflowLeaseReaper`, `DispatchingLLMGateway` + `InMemoryCircuitBreaker`), not the mechanisms unit-tested in isolation. A crashed worker's real, abandoned lease is genuinely reclaimed and a second, independent worker finishes the work for real; a real, self-hosted HTTP server simulating a dropped provider genuinely opens the circuit, short-circuits (proven by the server's own real request count staying flat), then recovers once the provider is genuinely back. **Kill-Redis is investigated and found genuinely blocked, not merely deferred** (the identical "genuinely blocked" finding this session's own technical-planner ticket already established as a real category, not a euphemism for "didn't get to it"): `bootstrap.py` has zero references to Redis anywhere. Module 23's own `ResponseCache` and `RedisRateLimiter` are real, tested, and do call the real Redis client — but neither is wired into `bootstrap.py`'s real composition root (both of their own module docstrings disclose this as real, separate follow-up work, not done in either of their own tickets), so no real, *running* Kernel process depends on Redis for anything today. There is nothing real for a chaos test to kill. Prior (2026-08-01, `P01-S06-M42-T04`): `tests/security/` is no longer empty — 12 files, one per T1–T12 threat ID, 30 tests total (25 passing locally, 5 Docker-gated skipping cleanly with no local daemon, matching the established `tests/integration/sandbox` pattern). Each test exercises a real, current defense against a genuine attempt at that threat (T1/T12 live-container isolation, T2 schema-validation rejection, T3 leak-scan block, T4 forbidden-import detection, T5 deny-by-default, T6 real `uv lock --check` drift detection, T7 fail-closed routing, T8 hash-chain tamper detection, T9 real budget-ceiling refusal) or, for T10/T11 (no real enforcement exists yet), a disclosed absence-of-capability tripwire. `tests/performance/`, `tests/regression/`, `tests/benchmarks/` remain **zero tracked files** (absent from a fresh clone). Unit + integration remain strong — **849 passing, 11 skipped, 0 failing** combined as of the last full count. A known flake family is documented in `../10_testing/test_strategy.md`: real-local-HTTP-server tests occasionally fail under full-suite load and pass in isolation (3 instances observed). |
| 43 | CI Pipeline | A | 50% | minimal slice | Lint/types/unit/integration real and green; contract/security/build(image)/frontend stages are correctly gated off (no-op) pending their prerequisites, per design — never actually executed. |
| 44 | Platform-wide `AiOsError` hierarchy | B | 65% | partial — real, unconsumed | **Updated 2026-08-01:** the hierarchy and `StructuredError` contract (§8) are real and verified against `error_handling_retry.md` §8 field-by-field (`platform_sdk/src/ai_os_sdk/errors/taxonomy.py`, `models/error.py`), 144 passing tests, `ruff`/`mypy --strict` clean (`P02-S07-M44-T01`, risk register R-008 closed). Not yet real: the `error_code` catalogue (§3) has no entries, and `LLMProviderError` etc. still inherit from plain `Exception`, not this hierarchy — no Kernel raise site uses it yet. |

---

## 6. Overall Progress Summary

**43 modules tracked** (44 rows retained for traceability; #35, Analytics Pack, was investigated and removed 2026-07-28 — excluded from this total, see its own row). Weighted by each module's relative share of documented v1 scope (core orchestration/security/interface modules weighted higher than peripheral ones):

**Overall weighted completion: ≈ 24%** (modules 5/30, moved 85% → 87% and 52% → 58% — `P08-S02-M30-T01`'s own later-step resolution: `foreach_executor` wired into a real composition (`se.product_creation`'s step 8), the `technical-planner`/`se.implement_task` schema mismatch resolved (`AskUserQuestion`, widened the consumer's schema), and a real `principal_id`-at-construction-time gap fixed (composition now built fresh per real call). `se.product_creation` reaches genuine `WorkflowRunOutcome.COMPLETED` through 8 of 14 documented steps, including one real, separate, completed `se.implement_task` child. `P08-S02-M30-T01` moved `partial` → `done`, closing Stage H (P08) to 9/9, 100%; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 5, Workflow Engine, moved 83% → 85% — `P08-S02-M30-T01`, `StepType.FOREACH`/`ForeachStepExecutor` are real, closing ADR-0021's own last-but-one named-but-undesigned step type; a real database migration gap (`ck_workflow_steps_step_type`) found and fixed along the way; unwired into any real composition yet, the identical `parallel_executor`/`sub_workflow_executor` precedent; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 29, SE Pack — Agents, moved 72% → 76% — `P08-S01-M29-T07`, the Performance Agent, FR-044/LATER, the only remaining ready Task once every higher-priority one was exhausted; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 33, Voice (Jarvis) Pack, moved 0% → 25% — `P06-S06-M33-T01`, a real Platform Integration Layer (4 real intents), a genuine structural finding moved it from a planned Capability Pack into the Kernel (a pure-HTTP-client pack design was caught and rejected by the real `pack_contract_suite` import-boundary check), no real voice pipeline yet; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 25, Speech Gateway, moved 0% → 40% — `P06-S06-M25-T01`, a real STT/TTS/wake-word abstraction and alias router, no real provider yet (a genuine structural gap found and disclosed via `AskUserQuestion` before any provider code was attempted); rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 38, CLI, moved 0% → 55% — `P06-S04-M38-T01`, `tools/aios` real for the first time, 5 of 8 documented command groups backed by real HTTP endpoints; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: modules 12, 22, and 42 all moved — Evaluation Engine's `llm_calls` gained a real timestamp column, Notification Service 38% → 48% (wired into a real Kernel process for the first time), and Testing Infrastructure 45% → 48% (Cost Anomaly Alerting, NFR-045, real end to end) — `P07-S03-M42-T02`; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: modules 12 and 39 both moved — Evaluation Engine 40% → 45% and Dashboard 45% → 55% — `P06-S03-M39-T03`, a real cost-and-quality aggregation reader plus the third and last documented Dashboard view consuming it; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 32, Project Intelligence Pack, moved 50% → 58% — `P05-S02-M32-T05`, a fifth real Tool (`documentation.generate`, `tier2_trusted`) renders a real, deterministic Markdown documentation set from `architecture.recover`'s own output; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 32, Project Intelligence Pack, moved 42% → 50% — `P05-S02-M32-T04`, a fourth real Tool (`architecture.recover`, `tier2_trusted`) performs real deterministic graph analysis over `dependency.graph`'s own output — module-level boundaries, real cycle detection, coupling metrics; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 32, Project Intelligence Pack, moved 35% → 42% — `P05-S02-M32-T06`, every one of the 3 real Tools now carries a real, structural `trust: "untrusted"` field (`provenance.py`, FR-059), mirroring the Context Manager's own already-real `ContextItem.trust` since `ai_os_kernel` cannot be imported here; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 32, Project Intelligence Pack, moved 25% → 35% — `P05-S02-M32-T03`, a third real Tool (`dependency.graph`, `tier1_sandboxed`) constructs a real, queryable Python import graph via stdlib `ast`, consuming `repository.ingest`'s own output; a real bug (relative-import resolution) caught and fixed before any test was written; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 32, Project Intelligence Pack, moved 15% → 25% — `P05-S02-M32-T02`, a second real Tool (`language.detect`, `tier2_trusted`) detects languages/build systems/frameworks with real confidence per finding, consuming `repository.ingest`'s own output; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 32, Project Intelligence Pack, moved 0% → 15% — `P05-S02-M32-T01`, the pack is scaffolded with one real, genuinely `tier1_sandboxed` Tool (`repository.ingest`), proven against a real Docker daemon; a mid-investigation ADR-0016 correction (repository-content tools are unconditionally Tier 1, not Tier 2) was caught before any code was written; a real, separate CI gap (no pack's own tests ever ran in CI) found and fixed for this pack, widening blocked on a disclosed, pre-existing, unrelated bug elsewhere; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 26, Document Processing, moved 25% → 40% — `P05-S01-M26-T02`, the Chunking Engine box is real by reusing an existing, tested, previously-uncalled chunker (`knowledge_manager.indexing`) rather than duplicating it — `index_document_file()` is its first real production caller; still a small-weight module, so this rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 26, Document Processing, moved 0% → 25% — `P05-S01-M26-T01`, real format detection plus 3 of 5 documented parser adapters (Markdown/Plain Text/Code), built inside the Kernel; PDF/DOCX disclosed as deferred (need a new, undeclared dependency); this is the first real code in Phase E (Stage E, Project Intelligence), still a small-weight module relative to the whole platform, so this rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 20, Sandbox / SandboxExecutor, moved 85% → 90% — `P03-S01-M20-T05`, the gVisor hardening path (ADR-0016) is now a real, config-driven `runtime` line on `DockerSandbox`, proven end to end against the real, available `runc` runtime (a genuinely-unknown runtime name refused by the real Docker Engine); real gVisor/Firecracker execution stays disclosed, unbuilt verification since neither is installed in this environment; rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 22, Notification Service, moved 30% → 38% — `P06-S05-M22-T02`, delivery status is now durably recorded (`notification.notification_deliveries`, migration `0034`, `SqlNotificationDeliveryRecorder`), proven against real Postgres; module 22 is a small-weight peripheral module (Phase F, not yet wired to a real producer), so this rounds to the same 24% the prior movement already crossed, unchanged this step. Prior movement: module 12, Evaluation Engine, moved 36% → 38% — closes the genuinely distinct "flagged" half of `overview.md` §7's own cache-served exclusion rule, adding a real, visible `excluded_cache_served_count` to `SqlComparisonComputer`'s own already-real exclusion logic, no parallel mechanism; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 12, Evaluation Engine, moved 32% → 36% — this package's own second real code, `SqlComparisonComputer`, reading exactly what the Metrics Collector already wrote and reporting real `Decimal`-precise sample mean/variance per variant/metric, excluding cache-served and incomplete runs per `overview.md` §7; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 34, Benchmarking Pack, moved 23% → 28% — `prompt_adaptation.py` closes this module's own 4-ticket set, detecting and recording a genuine per-model prompt difference as a declared experiment variable, closing `overview.md` §7's own undisclosed-variation fairness rule for this one real case; also fixed that same doc's own Implementation Status, stale since "Built: nothing" across all 4 tickets; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 34, Benchmarking Pack, moved 18% → 23% — `cost_ceiling.py` enforces `llm_gateway.md` §9's own "Experiment cost ceiling" row (a real, distinct "refuse before starting" failure mode), with a real, disclosed boundary: enforcement only, since a genuine, non-approximated cost projection needs real token counting + pricing no caller supplies yet; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 34, Benchmarking Pack, moved 12% → 18% — `replicate_management.py` expands a validated variant into its own real, numbered replicates and records each as a real `evaluation.experiment_runs` row via a second real Kernel-side Protocol implementation (`SqlExperimentRunRecorder`), the identical pattern `T01`'s own existence check already established; a real design mistake (the Kernel adapter briefly importing a pack type) was found and fixed before it shipped, restoring the one-directional dependency rule this codebase consistently enforces; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 34, Benchmarking Pack, moved 0% → 12% — this pack's own first real code: a real, buildable distribution and `experiment_definition.py`, validating a raw experiment spec against `overview.md` §7's own real rules (replicate count, variant count, no duplicate keys, no sampling-parameter variables, a genuinely-existing pinned workflow via a pack-defined Protocol the Kernel's own `sdk_adapters` package now implements); rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 12, Evaluation Engine, moved 28% → 32% — this package's own first real code, a narrow Metrics Collector writing 5 real `evaluation.metrics` rows per completed run, sourced entirely from already-real data; a real, disclosed structural blocker found (`metrics.run_id`'s `NOT NULL` FK to `experiment_runs`, which no ordinary run can satisfy since Module 34 is 0% built) and resolved with the product owner via the same "build real, wire later" precedent already used for the Gate Registry; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 31, SE Pack — Tools & Quality Gates, moved 22% → 28% — a verification step, not a build step: closed the one real remaining gap in the pipeline's own third gate's registry-resolution proof, plus a second real gap in the platform SDK's own contract-suite check 2 (never covered `tools[]`/`qualityGates[]`, only `agents[]`); rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 31, SE Pack — Tools & Quality Gates, moved 15% → 22% — the first two real, manifest-declared, registry-resolvable Tools this codebase has ever had (`fs.read`/`build.run`), closing two real, previously-undiscovered Kernel bugs in `SqlToolRegistry.resolve_tool` that blocked any pack tool from ever resolving (an `is not` identity check against a Kernel-only enum no pack could import, and no sandbox-injection path at all); rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 30, SE Pack — Workflows, moved 40% → 42% — FR-030's own additive `specification` input, parsed and validated by Requirements Analyst via a new, dependency-free Markdown parser; a real bug (reading the field off the wrong dict) found and fixed via the integration test, not the unit tests; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 30, SE Pack — Workflows, moved 37% → 40% — FR-045's own bounded review-revise loop: a `code-review` agent step and a third real, blocking, bounded-retry quality gate (`quality-gate-code-review-clean`) wired into `se.delivery_pipeline`, proving the same, unchanged retry/gate mechanism (module 5's own row) generalizes to a third gate category with zero Kernel code changes, only composition-level wiring; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 4, Observability & Audit, moved 40% → 55% — the real Collector and reference backends now exist (`P01-S05-M04-T04`): `infra/docker-compose.yml`'s `observability` profile (Collector, Prometheus, Tempo, Loki, a provisioned Grafana), proven end to end against a real `docker compose up` (a real span/metric genuinely reaching the Collector and becoming queryable in Prometheus, driving the actual committed compose file via `testcontainers.compose.DockerCompose`, not a hand-rolled stand-in for it); Loki genuinely has no log producer wired to it yet, disclosed in its own config's header rather than papered over, since the Kernel is not containerised and emits logs to its own stdout, not OTLP; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 4, Observability & Audit, moved 20% → 40% — real OTLP/HTTP export (`P01-S05-M04-T03`): `configure_tracing()`/`configure_metrics()` use a real `OTLPSpanExporter`/`OTLPMetricExporter` when `OTEL_EXPORTER_OTLP_ENDPOINT` is set (a bootstrap-minimum env var per configuration_management.md §3.3, read directly via the new `ObservabilitySettings`, mirroring `DatabaseSettings` — not a `PlatformConfig`/YAML field, a real, corrected divergence from an earlier draft of this same step), proven against a real, in-process HTTP receiver, not merely asserted as configured; console exporters remain the default, since no Collector is deployed anywhere yet. Also folded in: `governance.audit_log`'s real hash-chained writer and verification job (`P01-S05-M04-T05`/`T06`, already built earlier but not yet reflected in this row); rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 2, Manifest Loader, moved 65% → 72% — two semantic rules landed (`P01-S03-M02-T04`): rule 13 (SDK version range) is genuinely enforceable now that `ai-os-sdk` is a real installed distribution, and rule 16 (workflow-definition existence + step-reference resolution) reuses the real `WorkflowDefinitionLoader` rather than reimplementing it; a real, discovered gap fixed along the way — a platform_sdk test (`test_manifest_check_agrees_with_kernel_loader.py`) had documented rule 13's absence as "the one real known divergence" between the Kernel loader and the SDK's own `check_1_manifest_is_valid`, now updated to prove agreement instead, since the divergence is closed; rule 20 (circularity) is newly confirmed genuinely unenforceable rather than merely unbuilt, since `WorkflowStep`'s own docstring states sub-workflow linkage has no field-level contract yet; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 2, Manifest Loader, moved 55% → 65% — entry-point discovery (`P01-S03-M02-T03`) is real: `discover_entry_point_manifests()` enumerates installed distributions via `importlib.metadata.entry_points(group="ai_os.capability_packs")` and locates each one's `manifest.yaml` through the distribution's own recorded file list (`Distribution.files`/`locate_file()`), never by importing the entry point's own target (manifest_loader.md §6: must not execute pack code); `ManifestLoader.scan()` now combines both ADR-0009 mechanisms and de-duplicates identical resolved paths; proven against a real, synthetic installed distribution (a real `*.dist-info` placed on `sys.path`), since no pack in this repository registers a real entry point yet; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 44, `AiOsError` hierarchy, moved 0% → 65% — a verification step, not a build step: the hierarchy and `StructuredError` contract already existed in `ai-os-sdk` but the ticket (`P02-S07-M44-T01`) was still marked `todo` and this row still said "not started"; checked field-by-field against `error_handling_retry.md` §8 and confirmed genuinely real (144 passing tests), closing risk register R-008; not yet consumed anywhere in the Kernel and the `error_code` catalogue (§3) remains unpopulated, hence 65% not higher; a small-weight module, rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 5, Workflow Engine, moved 65% → 66% — the seventeenth normal feature step since the Platform SDK initiative closed: the `AgentRegistryError`/`ToolRegistryError` retriable split, left deliberately undecided two steps ago, is now investigated and explicitly decided — no exception-type split, a `retriable: bool` constructor parameter (the identical convention `LLMProviderError` already carries) set explicitly at each real raise site in `registry.py`, defaulted `False`; a real, discovered gap fixed along the way (`except sa.exc.SQLAlchemyError` never caught a genuine connection-*establishment* failure, which surfaces as a raw `OSError`, never wrapped by SQLAlchemy — both registries now also catch `OSError`); full detail in `errors.py`'s own docstrings, `registry.py`'s own per-site comments, `error_handling_retry.md`; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 5, Workflow Engine, moved 64% → 65%, module 15, Quality Gate Engine, moved 12% → 14%, and module 12, Evaluation Engine, moved 10% → 12% — the sixteenth normal feature step since the Platform SDK initiative closed: `evaluation.gate_results` now has a real writer (`SqlGateResultRecorder`), the Evaluation Engine's first real data producer, composed by `WorkflowInstanceService` and built in the Workflow Engine, not the Evaluation Engine package itself; every column sourced from real, already-persisted data (the gate's real `passed` output, the step's real `attempt` number, the real, if honestly-always-zero-today, `started_at`/`completed_at` timestamps) — full placement reasoning and column mapping in `gate_result_recorder.py`'s own module docstring; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 5, Workflow Engine, moved 63% → 64% — the fifteenth normal feature step since the Platform SDK initiative closed: general, error-category-driven step retry, widened beyond quality gates to any step-executor exception that self-declares `retriable = True` (the identical convention `LLMProviderError` already carries); `se.delivery_pipeline`'s own `_STEP_RETRY_TARGETS` gained a real `{"build": "build"}` entry, proven end to end both ways (a real, retriable `LLMProviderError` retries `build` and completes; a `retriable=False` one still fails immediately despite a configured target); full detail in `advance_runner.py`'s own module docstring and `workflow_engine.md`/`error_handling_retry.md`'s own Implementation Status; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 5, Workflow Engine, moved 62% → 63%, and module 15, Quality Gate Engine, moved 10% → 12% — the fourteenth normal feature step since the Platform SDK initiative closed: a genuinely failed step attempt is now genuinely recorded (`WorkflowInstanceRepository.record_failed_attempt`), closing quality_gate_engine.md §9's own recording requirement — every existing retry/gate test now shows two real, distinct `workflow_steps` rows (one `failed`, one `completed`) instead of just the eventually-successful one; full detail in `workflow_engine.md`'s own Implementation Status; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 15, Quality Gate Engine, moved 7% → 10%, and modules 29/30, SE Pack Agents/Workflows, moved 33% → 36% and 22% → 24% respectively — the thirteenth normal feature step since the Platform SDK initiative closed: a second, distinct gate category (Static Analysis, via a new Lint Agent running a real `python -m py_compile` check, not `ruff` — tried first, found to genuinely fail against `DockerSandbox`'s own default image) proves the gate mechanism genuinely generalizes via configuration alone — module 5, Workflow Engine, stays at 62% unchanged, since zero Workflow Engine code needed to change; full detail in each module's own row and `ai_os_kernel.workflow_engine.delivery_pipeline`'s own docstring; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 15, Quality Gate Engine, moved 5% → 7%, and module 5, Workflow Engine, moved 60% → 62% — the twelfth normal feature step since the Platform SDK initiative closed: a real, bounded retry (`RetryPolicy`, read at runtime for the first time) now sits on top of the quality-gate halt, retrying a failed gate from a configured earlier step before genuinely giving up, bounded on both attempts and duration — full detail in `error_handling_retry.md`'s own Implementation Status; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 15, Quality Gate Engine, moved 0% → 5%, and module 5, Workflow Engine, moved 58% → 60% — the eleventh normal feature step since the Platform SDK initiative closed: `QualityGateStepExecutor` genuinely blocks `se.delivery_pipeline` on a real, failing test run, the smallest real slice of this project's own most-referenced open gap — full detail in each module's own row and `docs/03_architecture/kernel/quality_gate_engine.md`'s Implementation Status; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 5, Workflow Engine, moved 55% → 58% — `LEASE_REAP_INTERVAL_SECONDS` is now genuinely enforced by `run_reap_loop()`, the second real, continuously-running background task in this codebase (applying the exact pattern the prior step proved for the Pack Health Collector), started in `_lifespan` and cleanly cancelled before engine disposal — the tenth normal feature step since the Platform SDK initiative closed, proven with a real, genuinely-crashed-worker lease reclaimed by the loop itself across real intervals and genuine task cancellation on shutdown; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 3, Health & Lifecycle, moved 70% → 75% — `POLL_INTERVAL_SECONDS` is now genuinely enforced by a real, continuously-running background task (`run_health_polling_loop`), the first of its kind in this codebase, started in `_lifespan` and cleanly cancelled on shutdown before engine disposal, closing capability_manager.md §9's own three-value policy fully (decided *and* enforced) — the ninth normal feature step since the Platform SDK initiative closed, proven with real, captured `checked_at` timestamps strictly increasing across independent real waits and genuine task cancellation on shutdown; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 3, Health & Lifecycle, moved 65% → 70% — `/health/ready` now genuinely surfaces the Pack Health Collector's own real `catalog.packs.health` detail, with a new, real early-warning reporting rule (an activated pack with real, non-zero sub-threshold consecutive failures is now visibly `degraded`, not silent until it actually fails), the eighth normal feature step since the Platform SDK initiative closed, proven with real, captured JSON for both a genuinely healthy pack and a genuinely degrading (sub-threshold) one; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: modules 3/13, Health & Lifecycle / Capability Manager, moved 58% → 65% and 55% → 60% respectively — the Pack Health Collector's smallest real slice: `ai_os_kernel.capability_manager.health_poller.poll_pack_health()` decides and implements capability_manager.md §9's own three-value policy (poll interval/timeout/consecutive-failure threshold), genuinely writes `catalog.packs.health`, and genuinely escalates a pack to `PackState.FAILED` via a new `mark_failed()` transition after real, observed consecutive failures — the seventh normal feature step since the Platform SDK initiative closed, closing the last named gap either `health_lifecycle.md` §10 or `capability_manager.md` §9 still had open, proven both directions against real Postgres containers; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 3, Health & Lifecycle, moved 50% → 58% — `health_lifecycle.md` §10's "hard vs. soft dependency" gap is decided and built: the database is a real hard dependency (every functional route already independently 503s without one), `/health/ready` now genuinely returns 503 via a new, generic `ComponentStatus.critical`/`ReadinessReport` `"not_ready"` mechanism backed by a real, timeout-bounded `SELECT 1`, the sixth normal feature step since the Platform SDK initiative closed, proven both directions against real Postgres containers; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 3, Health & Lifecycle, moved 40% → 50% — `/health/ready`'s `manifest_loader_check` now reports a discovered pack's real activation status via `catalog.packs.state`, not just filesystem-level schema validity, proven with real, captured JSON responses for both a genuinely healthy and a genuinely degraded (stuck) pack against a real Postgres container; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 13, Capability Manager, moved 45% → 55% — real pack discovery is now wired into Kernel startup (`ai_os_kernel.bootstrap._register_and_activate_discovered_packs`), the fourth normal feature step since the Platform SDK initiative closed: a real Kernel process pointed at a real pack directory genuinely registers and activates it with zero manual intervention, idempotent across restarts, proven end to end with a dedicated new test file that deliberately does not isolate itself from the real `capability_packs/` tree; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: module 13, Capability Manager, moved 35% → 45% — registration now genuinely derives real `catalog.agents`/`catalog.prompts`/`catalog.tools` rows from a pack's own `manifest.yaml` (`ai_os_kernel.capability_manager.manifest_catalog_installer`), the third normal feature step since the Platform SDK initiative closed, proven against the real Software Engineering pack and replacing hand-duplicated raw-SQL seeding across 6 test files; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: modules 30/36, SE Pack Workflows/API, moved 18% → 22% and 15% → 17% respectively — `se.delivery_pipeline` is now genuinely reachable over real HTTP (`POST /api/v1/workflows/se.delivery_pipeline`), the second normal feature step since the Platform SDK initiative closed, reusing the exact authenticated-route pattern the platform demo route already established; the real composition this needed was promoted from test-harness code into real Kernel code so route and tests share one implementation. Rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step. Prior movement: modules 29/30, SE Pack Agents/Workflows, moved 30% → 33% and 15% → 18% respectively — the first normal feature step since the Platform SDK initiative closed: `requirements-analyst` wired into `se.delivery_pipeline` as its own first step, so all 5 of this pack's built agents (not 4 of 5) are now genuinely chained and proven end to end, deterministic and live Docker both; rounds to the same 24% the prior movement already crossed, so the overall figure is unchanged this step, but this is real, proven, not-yet-reflected progress worth recording precisely rather than silently absorbed into "no change." Prior movement: module 27, Platform SDK, moved 80% → **100%, fully complete** — step 15, the plan's final step, built and proved all 9 `pack_contract_suite` checks together against the real, fully-migrated pack with zero waivers, formally closing out the 18-step (+2 inserted) initiative (`platform_sdk_v1_scope.md` §6s/§10); this crosses the rounding threshold into 24%, the second movement of the overall figure since the check-7 CI-wiring step. Prior movement: 74% → 80% — the sixth and final module migration (`pack.py`) is done: zero `ai_os_kernel` imports remain anywhere in this pack, for the first time in this project's history, and `pack_contract_waiver.yaml` is deleted outright, not merely emptied — check 7 now enforces unconditionally, with zero exceptions. **The module-27 hard gate is lifted** (product-owner decision) — a project-wide milestone, not just an internal module-27 one: modules 29/32/33/34 are all unfrozen, and this is what crosses the rounding threshold into 23%, the first movement of the overall figure since the check-7 CI-wiring step. Prior movement: 70% → 74% — the fifth and final agent migration (`documentation`) is done and proven with zero behavior change, closing the full 5-agent migration set; the real Delivery Pipeline this pack chains is now entirely migrated onto the Platform SDK, proven end to end including a real, live-Docker-written documentation artifact; the waiver is down to its own last, genuinely final entry (`pack.py`, step 14's own scope) — still not yet fully real (`pack.py` and step 15's own compliance completion remain), so the rounded overall figure is unchanged, but this is the largest single milestone module 27 has crossed since the injection mechanism itself was proven. Prior movement: 69% → 70% — step 12a (inserted) closes the `python_command` regression step 12 recorded rather than papered over: a new SDK-level placeholder token, resolved by the one adapter that still holds the real sandbox, restores the pre-migration "always automatically correct" guarantee exactly, proven against both real backends (`LocalSubprocessSandbox` and a real, live `DockerSandbox`); still 1 of 5 agents plus `pack.py` remain, so the rounded overall figure is unchanged. Prior movement: 66% → 69% — the fourth real agent migration (`build`) is done and proven with zero behavior change, the first to exercise real gateway AND tool injection together and the first genuinely live-Docker-proven write through the new `ToolInvoker` sandbox path; a real, discovered tension (the migrated agent can no longer dynamically discover its sandbox backend's own Python interpreter) resolved with a documented constructor default, not silently papered over; still 1 of 5 agents plus `pack.py` remain, so the rounded overall figure is unchanged. Prior movement: 63% → 66% — the third real agent migration (`architecture`) is done and proven with zero behavior change, following step 10's own established pattern with no new tension; the real Delivery Pipeline end-to-end tests needed a genuine fix (this is the first migrated agent actually chained into `se.delivery_pipeline`), found and applied as part of this step; still 2 of 5 agents plus `pack.py` remain, so the rounded overall figure is unchanged. Prior movement: 60% → 63% — the second real agent migration (`requirements-analyst`) is done and proven with zero behavior change, the first to exercise real `context.llm`/`context.prompts` gateway injection and the first to prove the old per-agent lazy-build pattern is now genuinely obsolete rather than merely unused; still 3 of 5 agents plus `pack.py` remain, so the rounded overall figure is unchanged. Prior movement: 58% → 60% — the injection mechanism is now genuinely wired into production (`SqlAgentRegistry`/`SqlToolRegistry`), not merely proven per-agent by test-side workarounds; every remaining migration step (10–13) now inherits real, working registry wiring for free, materially de-risking and simplifying each of them; still nothing changes for any already-real system since neither registry is constructed in `bootstrap.py` yet, so the rounded overall figure is unchanged. Prior movement: 55% → 58% — the first real agent migration (`qa-test`) is done and proven with zero behavior change, the first genuine evidence the whole reconciliation/adapter/injection design actually works end to end for a real, running pipeline, not merely in isolated proofs; still 4 of 5 agents plus `pack.py` remain, so the rounded overall figure is unchanged. Prior movement: 50% → 55% — `pack_contract_suite` check 7 and its expiring waiver are real, CI-wired, and proven both ways against the real pack's own 44 real violations; the compliance gate this project has been missing since the direct-Kernel-import exception was first recorded now genuinely exists and enforces, even though the migration it will eventually gate is not done yet — this crosses the rounding threshold into 22%. Prior movement: 45% → 50% — `pipeline.py` relocated out of the pack's shipped wheel (resolving check 7's permanent-failure risk), `ContextService` boundary models landed, and the full entry-point contract formally relocated into `ai_os_sdk`; still nothing wires the injection mechanism into a real pack, so the rounded overall figure is unchanged. Prior movement: 40% → 45% — the `EntrypointLoader` zero-argument injection blocker (the biggest remaining structural gap before real migration) is resolved and proven end to end against a real test entrypoint, without changing `EntrypointLoader` itself; still nothing wires this into a real pack, so the rounded overall figure is unchanged. Prior movement: 34% → 40% — real Kernel-side adapters now genuinely implement all three from-scratch/narrowed Protocols against real underlying objects (`DispatchingLLMGateway`, `PromptEngine`, `LocalSubprocessSandbox`), resolving the timeout-precedence nuance left open by the prior step; still nothing wires these adapters into a pack, so the rounded overall figure is unchanged. Prior movement: 30% → 34% — a fifth Protocol (`ToolInvoker`) is real, proven against actually-executed sandbox behavior which caught 2 real bugs pre-emptively; still nothing consumes the package, so the rounded overall figure is unchanged. Prior movement: 26% → 30% — a fourth Protocol (`PromptRegistry`) is real, proven compatible by lossless conversion rather than isinstance since it's a from-scratch shape; still nothing consumes the package, so the rounded overall figure is unchanged. Prior movement: 22% → 26% — a third Protocol (`LLMGateway`) is real and provably satisfied by the real production gateway; still nothing consumes the package, so the rounded overall figure is unchanged. Prior movement: 18% → 22% — the first two Protocols are real and provably satisfied by every existing agent and tool, which de-risks the migration steps materially; still nothing consumes the package, so the rounded overall figure is unchanged. Prior movement: 15% → 18% — all five interface shapes are now decided against real source, which de-risks every remaining SDK step; still no Protocol code, so the rounded overall figure is unchanged. Prior movement: 10% → 15% — the error taxonomy and shared boundary models are real, though still consumed by nothing; not enough on its own to move the rounded overall figure. See `../03_architecture/platform/platform_sdk_v1_scope.md` steps 1–2. Prior movement: modules 20/29/41 moved up when real Docker verification landed and a fifth Software Engineering agent, Requirements Analyst, was built; see `history/024_docker_verification_and_requirements_analyst.md`).

This is deliberately not a simple average of the percentages above — an unweighted mean would overstate progress, since many 0% rows (Dashboard, CLI, Voice Pack, Notification Service, Speech Gateway) are genuinely large, separate bodies of work, not small missing details.

**Single biggest remaining phase by size: Phase F (Dashboard, Voice, Notifications).** It requires an entire separate frontend application (10 navigation sections, real-time views, experiment comparison UI), a full CLI application (8 command groups), a complete voice pipeline (wake word → STT → intent → TTS), a Notification Service, and the WebSocket transport — none of which shares code with anything built so far, unlike Phase C, D, or G, which all extend real infrastructure already in place (Workflow Engine, LLM Gateway, Sandbox). Phase C is a close second and more immediately actionable, since it already has a real, proven foundation (module 20, 29) to build on.

---

## 6a. Known Regressions — Platform SDK Migration (tracked deliberately, not to be silently lost)

**✅ CLOSED (`P04-S01-M12-T10`, discovered stale here 2026-08-06 while scoping `P08-S01-M29-T02`) — the `evaluation.llm_calls` gap this section used to describe as "still open" is fixed.** `SqlAgentRegistry` now accepts an optional `call_recorder`, threaded through `build_pack_context`/`LLMGatewayAdapter` (real in production: `bootstrap.py` constructs it with a real `SqlLLMCallRecorder(engine)`); every real completion through an SDK-native agent's own real `TraceContext` is recorded again, proven without a live credential by `test_requirements_analyst_agent_pack.py::test_a_deterministic_completion_genuinely_records_a_real_llm_calls_row`. **Not yet fully cleaned up**: `test_architecture_agent_pack.py`/`test_build_agent_pack.py`/`test_documentation_agent_pack.py` still carry the pre-fix docstring language (accurate about their own test composition, which doesn't itself wire a `call_recorder`, but stale in implying no mechanism exists at all) — found, disclosed, not fixed this step; a real, small cleanup for whichever future step next touches one of those three files.

- **Root cause:** every unmigrated `PromptedAgent`-backed agent's own `_build_real_service()` calls `build_anthropic_prompted_completion_service()`, which always wires a real `SqlLLMCallRecorder` into the `PromptedCompletionService` it returns — every live completion that agent ever makes is recorded. A migrated agent no longer builds a `PromptedCompletionService` at all; it calls `context.llm.complete()` directly against the injected SDK `LLMGateway` Protocol. **Platform SDK v1.0.0 has no `Telemetry`/`TraceabilityService` surface at all** (both explicitly deferred past v1.0.0, `platform_sdk.md` §2), so there is no SDK-sanctioned way for a migrated agent to preserve this recording — reaching back into `ai_os_kernel.llm_gateway.call_recorder` directly would reintroduce exactly the forbidden import the migration exists to remove.
- **Does not change any agent's own visible output** — the response content returned to a caller is byte-identical whether recorded or not. This is a pure loss of observability data (module 12's own `evaluation.llm_calls` rows), not a functional regression to anything module 27's own migration steps prove zero-behavior-change against.
- **Affected — the full, final set, closed out at step 13:** `requirements-analyst` (step 10), `architecture` (step 11), `build` (step 12), and `documentation` (step 13) — all four make real LLM completions and are all migrated. **`qa-test` (step 9) remains the one unaffected agent** — it makes no LLM call at all (sandbox-only agent), so there was nothing to stop recording. No further agent will ever join this list under the current 5-agent pack — this is the gap's complete, permanent extent unless a 6th agent is ever added (no longer blocked by any hard gate — the module-27 gate lifted 2026-07-29, and Platform SDK v1.0.0 is now fully complete, §6s/§10 of `platform_sdk_v1_scope.md`) or an existing agent's own scope changes to add an LLM call.
- **Expected to stay open** — this is not a step-13 regression to fix; it is the same, already-understood structural v1.0.0 gap reaching its final, full extent, exactly as steps 10-12's own reports already anticipated. Closing it requires a real SDK Telemetry/call-recording surface, explicitly out of scope for v1.0.0.
- **What closes this gap:** a future Platform SDK version adding a real `Telemetry`/call-recording surface a pack can depend on without reaching into Kernel internals — explicitly out of scope for v1.0.0 (`platform_sdk.md` §2's own deferral list). Until then, **`evaluation.llm_calls` will under-count real completions for all four LLM-calling agents** (`requirements-analyst`, `architecture`, `build`, `documentation`), permanently, not just during the migration window. Any future consumer of `evaluation.llm_calls` (cost dashboards, usage audits, the Evaluation Engine itself once built) must know this before trusting that table's totals as complete.
- **First recorded:** step 10 (`platform_sdk_v1_scope.md` §6m); reconfirmed as a second occurrence at step 11 (§6n), a third at step 12 (§6o), and the fourth and final occurrence at step 13 (§6q) — the gap's full extent is now known and stable, not still growing.

**✅ RESOLVED (step 12a, 2026-07-29) — a second, distinct regression, found at step 12: migrated agents that write files through the sandbox could no longer dynamically discover the correct Python interpreter command for the active sandbox backend.**

- **Root cause:** the pre-migration `build.py` asked its own directly-injected `SandboxExecutor` for its `python_command` property (`sys.executable` for `LocalSubprocessSandbox`, `python3` for `DockerSandbox` — a real, backend-specific fact the sandbox object itself already knew). A migrated agent holds only a generic `ToolInvoker`, which has no `python_command` concept and cannot answer this question — the Protocol is deliberately backend-agnostic, so this fact does not cross it.
- **Step 12's own first attempt (superseded):** `python_command` as a `BuildAgentEntrypoint` constructor default (`("python3",)`) — correct for the real system-wide default backend, but silently wrong if `AIOS_SANDBOX_BACKEND=local` was ever set without also reconfiguring that default. Documented as a real, narrow regression risk rather than shipped as a false "fixed," per the standing rule against declaring something resolved when it is only better-documented.
- **The real fix (step 12a):** a new, well-known placeholder token, `ai_os_sdk.contracts.tool_invoker.PLATFORM_PYTHON_INTERPRETER`, that a caller writes as any element of `inputs["command"]` in place of a literal interpreter path. `ToolInvokerAdapter.invoke()` — the one place that still holds the real `SandboxExecutor` — expands every occurrence into that instance's own real `sandbox.python_command` before dispatch. `build.py` no longer has a `python_command` constructor parameter, a default to keep in sync, or any backend knowledge at all — the pre-migration "always automatically correct" guarantee is restored exactly, at the correct layer, not merely documented better.
- **Proven against both real backends**, not just the currently-active default: `tests/unit/kernel/sdk_adapters/test_tool_invoker_adapter.py::TestPlatformPythonInterpreterSubstitution` (real `LocalSubprocessSandbox`, real subprocess, asserts the resolved interpreter equals `sys.executable`) and `tests/integration/sandbox/test_tool_invoker_adapter_docker.py` (real, live `DockerSandbox`, asserts the resolved interpreter is the container's own `python3`, plus a real file genuinely written end to end).
- **Affected and now fixed:** `build` (step 12/12a). **Will not recur in `documentation` (step 13)** — that agent's own pre-migration write path uses the identical `self.sandbox.python_command` pattern `build.py` used to, so its migration will use the identical `PLATFORM_PYTHON_INTERPRETER` token from the start, not rediscover this finding.
- **First recorded:** step 12 (`platform_sdk_v1_scope.md` §6o). **Resolved:** step 12a (§6p).

---

## 7. Maintenance

**Standing rule (recorded 2026-07-28, see `CLAUDE.md` / `docs/process/standing_rules.md`):** update this document's Section 5 percentages, status categories, and "what remains" — and Section 6's overall weighted completion — **at the end of every future implementation step**, as part of the standard report. A module whose real state changed but whose row here wasn't updated in the same step is treated the same as an undocumented decision: a gap to flag and fix, not defer.

When a new module, pack, or phase is approved, add it to Sections 3/4/5 in the same step that begins work on it — don't wait until it's finished to start tracking it.
