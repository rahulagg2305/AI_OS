# Platform SDK v1.0.0 — Scope and Build Sequence

**Project:** AI_OS (AI Operating System)
**Document:** Platform SDK v1.0.0 Scope and Build Sequence
**Version:** 2.16
**Status:** Approved — 18 steps (plus two inserted fix steps, 9a and 12a); **Steps 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, 11, 12, 12a, and 13 complete**, Step 14 next (migrate `pack.py`; retire the check-7 waiver entirely)
**Last Updated:** 2026-07-29 (v2.16: **Step 13 complete** — `documentation` migrated onto the real Platform SDK, the fifth and final agent migration. Implements `ai_os_sdk.contracts.Agent` + `PackContextReceiver` only, imports nothing from `ai_os_kernel`. Its LLM call replicates the identical render-then-complete logic every other migrated agent uses; its write uses `PLATFORM_PYTHON_INTERPRETER` (the step 12a fix) from the start — no repeated tension, confirming that fix generalizes as designed. **A further, confirming data point on the lock-obsolescence finding**: this agent needs no lock at all (unlike `build`'s narrower one, still needed for lazy working-directory creation) — it lazily builds/creates nothing, the least internal state of any agent in this pack, closing out the full range of "how much does injection actually simplify a real agent" findings this migration series set out to answer. Removed from `pack_contract_waiver.yaml` — but **a real, discovered correction to this step's own initial framing, surfaced and corrected rather than silently forced through**: the waiver is NOT literally empty afterward. It retains exactly one entry, `pack.py` (this pack's own `CapabilityPack` entry point), since migrating it is step 14's own explicitly scoped job, not step 13's — `pack.py` was never one of the 5 agents. Check 7 passes with exactly 1 module/1 violation waived (down from 2 modules/13 violations), confirmed by the real scanner — this is the correct, honest signal that step 14 is ready, not a false "already empty" claim. Both real Delivery Pipeline end-to-end tests (deterministic + live Docker) pass with the **entire four-agent pipeline now migrated onto the Platform SDK** — a real documentation artifact genuinely written through the new `ToolInvoker` path in both tiers. The `evaluation.llm_calls` gap reaches its full, final, permanent extent (all 4 LLM-calling agents) — `feature_inventory.md`'s §6a marks it expected-to-stay-open, not a fresh regression. Record in §6q. Prior, v2.15: **Step 12a complete (inserted)** — closes the real regression step 12 found and recorded rather than silently accepting: a migrated agent that writes files through the sandbox lost the pre-migration "always automatically correct" interpreter-command guarantee, since a generic `ToolInvoker` cannot expose backend-specific facts (`sys.executable` vs. `python3`) the way a directly-injected `SandboxExecutor` could. **The fix**: a new, well-known SDK placeholder token, `ai_os_sdk.contracts.tool_invoker.PLATFORM_PYTHON_INTERPRETER`, that a caller writes as any element of `inputs["command"]` in place of a literal interpreter path; `ToolInvokerAdapter.invoke()` — the one place that still holds the real `SandboxExecutor` — expands every occurrence into that instance's own real `python_command` before dispatch. Chosen over exposing `python_command` through `ToolDescriptor`/`available_tools()` (the task's own alternative option) because it needs zero SDK model changes, zero caller-side lookup code, and restores the "write it once, correct against every backend" property directly, rather than handing the fact back to callers who would each have to remember to query and rebuild their own command with it. `build.py`'s own `python_command` constructor parameter (step 12's own first-attempt fix) is removed entirely — no default to keep in sync, no backend knowledge in pack code at all. **Proven against both real backends, not just the currently-active default**: a real, actually-executed `LocalSubprocessSandbox` (3 new tests, `test_tool_invoker_adapter.py`) and a real, live `DockerSandbox` (2 new tests, new `test_tool_invoker_adapter_docker.py`, including a real file genuinely written end to end). Zero regression across every already-migrated agent (`qa-test`, `requirements-analyst`, `architecture`, `build`) and both real Delivery Pipeline end-to-end tests. `documentation.py`'s own pre-migration write path uses the identical `self.sandbox.python_command` pattern `build.py` used to — confirmed to need the identical fix from the start when step 13 migrates it, not a fresh discovery. `feature_inventory.md`'s §6a second Known Regression entry marked ✅ RESOLVED. Record in §6p. Prior, v2.14: **Step 12 complete** — `build` migrated onto the real Platform SDK, the fourth migration and the first needing real gateway AND tool injection together (`context.llm`/`context.prompts`/`context.tools`). Its own LLM call replicates the identical render-then-complete logic `requirements-analyst`/`architecture` already use; its write now happens through `context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, ...)`, the identical Tool injection path `qa-test` (step 9) already proved. Imports nothing from `ai_os_kernel`. **The lock is not fully obsolete here — a real, discovered nuance neither step 10 nor step 11 needed to consider**: it still guards lazy working-directory creation (a concern unrelated to LLM composition, and not resolved by `bind_pack_context()` injection at all), proven by a new concurrent-execute test asserting every call shares one directory. **A real, discovered tension using `ToolInvoker` instead of direct sandbox construction — the one this step's own approved framing asked to watch for**: the migrated agent can no longer dynamically ask its own sandbox for its `python_command` (`sys.executable`/`python3`, backend-specific), since a generic `ToolInvoker` carries no such concept; resolved with a constructor default (`("python3",)`, matching `DockerSandbox`, the real system-wide default backend), overridable by a caller that knows better — the same real, narrow regression risk (a stale default vs. a reconfigured `AIOS_SANDBOX_BACKEND`) recorded rather than silently accepted. Removed from `pack_contract_waiver.yaml` (4→3→2 modules, 32→24→13 violations); check 7 now genuinely passes for it, unwaived. Both real Delivery Pipeline end-to-end tests pass after the same construction fix step 11 already established, plus this agent's own `python_command` matched explicitly per test — the live Docker test is this pack's most important real proof of the ToolInvoker migration, since Build is the pipeline's real file-writer, and it genuinely wrote a real file inside a real container through the new path. The `evaluation.llm_calls` capability loss recurs a third time; both known regressions now tracked in `feature_inventory.md`'s §6a. Record in §6o. Prior, v2.13: **Step 11 complete** — `architecture` migrated onto the real Platform SDK, following step 10's own established pattern exactly (`Agent`+`PackContextReceiver`, injected `context.llm`/`context.prompts`, the lazy-build lock deleted outright, no new tension found). Imports nothing from `ai_os_kernel`. Removed from `pack_contract_waiver.yaml` (5→4→3 modules, 40→32→24 violations); check 7 now genuinely passes for it, unwaived. **The one real, new finding this step surfaced (not present at step 10, since neither `qa-test` nor `requirements-analyst` are chained into `se.delivery_pipeline`): `architecture` IS one of the four agents in the real Delivery Pipeline, so both `test_delivery_pipeline.py`'s `InMemoryAgentRegistry` construction and `test_delivery_pipeline_docker.py`'s own copy needed a real fix — `service_factory=` no longer exists — updated to the same zero-arg-construct-then-`bind_pack_context()` pattern already established for `qa-test`, and the live tier's `SqlAgentRegistry(engine)` call needed real `llm_gateway`/`prompt_engine` objects supplied, for the same step-9a-enforcement reason step 10 already proved for `requirements-analyst`.** Both real Delivery Pipeline end-to-end tests (deterministic + live Docker) pass unchanged after the fix. The `evaluation.llm_calls` capability loss recurs — confirmed as a real, structural v1.0.0 gap rather than a one-off, now tracked in `feature_inventory.md`'s new **§6a Known Regressions** section so it survives past individual step reports. Record in §6n. Prior, v2.12: **Step 10 complete** — `requirements-analyst` migrated onto the real Platform SDK, the second migration and the first needing real gateway injection (`context.llm`/`context.prompts`, no sandbox). Replicates `PromptedCompletionService.complete_from_prompt`'s own real render-then-complete logic directly against the SDK's `LLMGateway`/`PromptRegistry` Protocols; imports nothing from `ai_os_kernel`. **The lazy-build lock this agent (and `ArchitectureAgentEntrypoint`) used to need is gone entirely — the real tension this step was scoped to test, found to be an obsolescence, not a conflict:** `bind_pack_context()` is synchronous and called once by the resolver strictly before any `execute()` call is possible, so the old "lazy-build under an `asyncio.Lock()`" pattern has nothing left to guard against. A second real, accepted capability loss recorded: migrated completions are no longer recorded to `evaluation.llm_calls` (no SDK `Telemetry` surface exists in v1.0.0). A real, previously-latent circular import between `ai_os_kernel.workflow_engine.registry` and `ai_os_kernel.sdk_adapters.pack_context` (introduced by step 9a, surfaced by this step's own new test import ordering) was found and fixed with a function-local import — verified against four different import orderings, including `bootstrap.py` itself. A real, discovered consequence of step 9a proven here: `SqlAgentRegistry`'s own deterministic resolution test now genuinely requires a real (Echo-backed) `llm_gateway`/`prompt_engine`, since the agent's own declared `llm:invoke` permission makes step 9a's injection logic require one. `requirements-analyst` removed from `pack_contract_waiver.yaml`; check 7 now genuinely passes for it, unwaived. Record in §6m. Prior, v2.11: **Step 9a complete (inserted)** — `SqlAgentRegistry`/`SqlToolRegistry.resolve_agent`/`resolve_tool` now genuinely call `PackContextReceiver.bind_pack_context()` for real, in production, closing the exact gap step 9 discovered before it could repeat four more times across steps 10–13. `EntrypointLoader` itself untouched, per the standing constraint. `test_verification_agent_pack.py`'s manual binding is gone entirely — `qa-test` now resolves and runs end to end through the real registry with zero test-side workaround. Both of step 9's smaller findings reconfirmed rather than left assumed: the `AssembledContext` duck-typing is unrelated to this gap and stays correct; the `python_command` caller-keeps-reference pattern generalizes cleanly one layer up (the test now hands its own sandbox to the registry's own new `sandbox=` constructor override). Record in §6l. Prior, v2.10: **Step 9 complete** — `qa-test` (`agents/verification.py`) migrated onto the real Platform SDK, the first of six migration steps and the first real (non-test-entrypoint) use of the step 6b injection mechanism. Now implements `ai_os_sdk.contracts.Agent` + `PackContextReceiver`; its sandbox access is now exclusively `context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, ...)`, no `ai_os_kernel` import anywhere. Two real, load-bearing gaps discovered by this first real-world use, both resolved within this step, neither silently: (1) the real `inputs["context"]` object the Workflow Engine sends is still Kernel-typed, so the agent duck-types it instead of `isinstance`-checking against the SDK's own boundary model; (2) `SqlAgentRegistry` never calls `bind_pack_context()` after resolving an entrypoint — a real, unconditionally-run integration test (`test_verification_agent_pack.py`) exercises exactly this gap and now does the binding itself, standing in for production wiring that does not exist yet. Proven via the real Delivery Pipeline end-to-end tests (including the live Docker-backed one) producing identical output through the migrated agent. `qa-test` removed from `pack_contract_waiver.yaml`; check 7 now genuinely passes for that module unwaived. Record in §6k. Prior, v2.9: **Step 8 complete** — `pack_contract_suite` check 7 (`ai_os_sdk.testing.forbidden_imports`, a real AST import-scanner) plus the documented, expiring waiver mechanism (`ai_os_sdk.testing.waiver`) are both real. Proven, not assumed: the checker genuinely fails against the real Software Engineering pack (44 real `ai_os_kernel` imports across 6 modules) without a waiver, and genuinely passes with the pack's own real, dated `pack_contract_waiver.yaml` in place (expiring at step 14) — both directions proven by an automated regression test, not only a one-off manual run. Wired into CI for real via `scripts/check_import_boundaries.py`, landing in a gate `.github/workflows/ci.yml` already had prepared (`hashFiles(...) != ''`) — zero workflow-file change needed beyond a stale comment. Record in §6j. Prior, v2.8: **Step 7 complete** — `pipeline.py` relocated out of the pack's shipped wheel into `tests/integration/_delivery_pipeline.py` (resolving P4's permanent check-7 failure — proven via a real wheel build with the file and its `sqlalchemy.ext.asyncio` import both confirmed absent, and both relocated integration tests, including the real Docker-backed one, passing unchanged from the new location); `ContextService` boundary models landed and narrowed against the real, already-reduced Kernel shape, with structural compatibility proven against the real `DefaultContextManager`; the full entry-point contract (`PackContext`/`CapabilityPack`/`PackRegistration`/`HealthReport`) relocated from the Kernel into `ai_os_sdk.contracts.capability_pack`, additively, with the Kernel's own module becoming a real re-export (not a redefinition) so no existing import — including real pack source — needed to change. Record in §6i. Prior, v2.7: **Step 6b complete** — the `EntrypointLoader` zero-argument injection blocker is resolved without changing `EntrypointLoader` itself: a new SDK Protocol, `ai_os_sdk.contracts.PackContextReceiver`, lets any zero-arg-constructible entrypoint receive a real, permission-gated `PackContext` (extended additively on the Kernel side with the three step 6a adapters) via one standard `bind_pack_context()` call — the generalized form of the lazy-build workaround all five real pack agents already use. Proven end to end with a real test entrypoint: real `EntrypointLoader` construction, real `isinstance` check, real injection, then a genuine Echo LLM completion, prompt render, and sandboxed command through the injected context. Record in §6h. Prior, v2.6: **Step 6a complete** — real Kernel-side adapters (`kernel/src/ai_os_kernel/sdk_adapters/`) genuinely implementing `LLMGateway`, `PromptRegistry`, and `ToolInvoker` over `DispatchingLLMGateway`, the Prompt Engine, and `LocalSubprocessSandbox` — the first step to touch real Kernel code, resolving P1 in full. The `ToolInvoker` adapter also resolves the timeout-precedence nuance step 6 left open (`min()` of the two timeouts) and surfaces one new, documented gap (the Protocol carries no trace parameter, resolved with a fresh, uncorrelated per-invocation `TraceContext`). Record in §6g. Prior, v2.5: **Step 6 complete** — `ToolInvoker` Protocol, `ToolDescriptor`, `ToolResult`, and the `platform.sandbox.run_command` tool built at the from-scratch shape decided in 2a. Proven against the **real, actually-executed** `LocalSubprocessSandbox` (clean/timed-out/truncated runs), which found and corrected two real bugs before they could reach step 6a: a too-strict `ToolResult` validator, and a circular import between `ai_os_sdk.errors` and `ai_os_sdk.models`. Record in §6f. Prior, v2.4: **Step 5 complete** — `PromptRegistry` Protocol built at the documented keyword call style (the one reversal decision from 2a), `RenderedPrompt` narrowed to 3 fields, conversion to/from the real Kernel prompt models proven lossless. Record in §6e. Prior, v2.3: **Step 4 complete** — `LLMGateway` Protocol narrowed to `complete()`/`capabilities()`, `ProviderCapabilities` extended to 13 fields, proven against the real `DispatchingLLMGateway`. Record in §6d. Prior, v2.2: **Step 3 complete** — `Agent`/`Tool` Protocols proven against all five real pack agents and both real tools. Record in §6c. Prior, v2.1: **Step 2a complete** — all five reconciliation decisions recorded; summary in §6b. Prior, v2.0: an independent architecture review found 7 real problems in v1.0's 15-step sequence; revised to 18 steps. Findings: §6a.)

**Previously:** 2026-07-28 (v1.0 — original 15-step sequence, Step 1 complete)

---

## 1. Purpose

`implementation_status.md` §6 and `feature_inventory.md` module 27 both hand off the same instruction: scope the Platform SDK v1.0.0 build as its own dedicated step before starting it. This document is that scoping. It is a **plan, not code** — no `ai_os_sdk` package exists yet, and none is created by this step.

It answers, with evidence from the real, current codebase (not from `platform_sdk.md`'s specification alone):

1. Which of the 15 documented Protocol interfaces the one real pack actually needs right now.
2. Whether the 5 real agents migrate onto the SDK in the same step it's built, or a fast-following one.
3. Whether `pack_contract_suite` ships with the SDK or after.
4. What goes in `platform_sdk/`'s empty subdirectories, file by file.
5. The ordered sequence of small, individually-approvable steps this becomes.
6. What v1.0.0 deliberately does not do.

This document is subordinate to:

1. Project Constitution
2. `platform_sdk.md` (the specification this document sequences the build of)
3. `capability_pack_contract.md` (the growth gate this build closes)
4. `docs/process/standing_rules.md` (the Platform SDK growth gate and scope-discipline rules)

---

## 2. Real usage evidence: what the one real pack actually imports today

`grep -rn "^from ai_os_kernel\|^import ai_os_kernel" capability_packs/software-engineering/src/` on 2026-07-28 returns **57 import statements across 7 files**, covering **24 unique Kernel modules**. Full command output, deduplicated by module:

```
ai_os_kernel.capability_manager.pack_contract      <- pack.py
ai_os_kernel.context_manager.manager               <- pipeline.py
ai_os_kernel.context_manager.models                <- agents/documentation.py, agents/verification.py, pipeline.py
ai_os_kernel.context_manager.resolvers             <- pipeline.py
ai_os_kernel.llm_gateway.adapters.anthropic_adapter <- agents/architecture.py, agents/build.py, agents/documentation.py, agents/requirements_analyst.py
ai_os_kernel.llm_gateway.adapters.model_config      <- (same 4 agent files)
ai_os_kernel.llm_gateway.router                     <- (same 4 agent files)
ai_os_kernel.persistence.engine                     <- (same 4 agent files)
ai_os_kernel.persistence.settings                   <- (same 4 agent files)
ai_os_kernel.prompted_completion                    <- (same 4 agent files)
ai_os_kernel.sandbox.default_executor               <- agents/build.py, agents/documentation.py, agents/verification.py, pipeline.py
ai_os_kernel.sandbox.executor                       <- agents/build.py, agents/documentation.py, agents/verification.py
ai_os_kernel.secrets_manager.env_provider           <- (same 4 agent files as LLM Gateway)
ai_os_kernel.workflow_engine.advance_runner         <- pipeline.py
ai_os_kernel.workflow_engine.definition_catalog     <- pipeline.py
ai_os_kernel.workflow_engine.lease                  <- pipeline.py
ai_os_kernel.workflow_engine.loader                 <- pipeline.py
ai_os_kernel.workflow_engine.models                 <- pipeline.py
ai_os_kernel.workflow_engine.prompted_agent         <- (same 4 agent files as LLM Gateway)
ai_os_kernel.workflow_engine.registry               <- pipeline.py
ai_os_kernel.workflow_engine.repository             <- pipeline.py
ai_os_kernel.workflow_engine.sandboxed_tool          <- agents/build.py, agents/documentation.py, agents/verification.py
ai_os_kernel.workflow_engine.service                <- pipeline.py
ai_os_kernel.workflow_engine.step_executor           <- pipeline.py
```

### 2.1 A finding that changes the shape of this plan: `pipeline.py`'s imports are not pack-facing

11 of the 24 modules are imported **only** by `pipeline.py`, never by an agent: both `context_manager.manager`/`resolvers`, and all **nine** `workflow_engine.{advance_runner,definition_catalog,lease,loader,models,registry,repository,service,step_executor}` modules. *(v1.0 of this document said "8" here while listing nine — corrected per finding P7; 2 + 9 = 11, and 13 + 11 = 24.)*

`pipeline.py`'s own docstring states plainly what it is: a composition script that chains the 4 real agents into `se.delivery_pipeline`, built "the identical shape `ai_os_kernel.bootstrap._build_workflow_trigger` already establishes for the Kernel's own demo workflow, reused here for a real pack-owned one." It constructs the Workflow Engine's own lease service, repository, instance service, and step executors by hand — work a real Capability Manager would do once, generically, for every pack, handing each pack a `PackContext` instead. That generic "activate a pack's declared workflow through the real engine" mechanism does not exist yet (Capability Manager is a minimal slice — see `feature_inventory.md` module 8), so this reprioritization step's test harness had to do it by hand.

**Consequence for this plan:** `pipeline.py` is not a pack-boundary violation the SDK should fix. It plays the platform's own composition-root role, physically located inside the pack's source tree for lack of anywhere else to put it. Migrating its 11 imports onto SDK Protocols would be modeling platform-internal orchestration as if it were pack-facing capability — wrong shape. **This plan's SDK surface and migration scope is therefore driven only by the 13 modules the 5 agents and `pack.py` actually use**, not all 24.

**Revised in v2.0 (finding P4):** v1.0 concluded from this that `pipeline.py` should simply be left alone until a future Capability Manager arrives, and listed it as a permanent non-goal. That was wrong in one important respect — leaving a Kernel-importing, `sqlalchemy`-importing module inside the pack's **shipped package** means `pack_contract_suite` check 7 can never pass on this pack, at any point, ever. Since **no pack source module imports `pipeline.py`** (only two Kernel-side tests do), relocating it into `tests/` resolves both problems at once and costs far less than a Capability Manager. It is now **step 7** — see §4.1.

### 2.2 The 13 agent/pack-facing modules, mapped to §5's interfaces

| Kernel module (real import) | Used by | Maps to (`platform_sdk.md` §) | Needed now? |
|---|---|---|---|
| `llm_gateway.adapters.anthropic_adapter` (`PROVIDER_NAME`) | architecture, build, documentation, requirements_analyst | §5.1 `LLMGateway` | **Yes** |
| `llm_gateway.adapters.model_config` (`load_provider_config`) | same 4 | §5.1 `LLMGateway` | **Yes** |
| `llm_gateway.router` (`RoutingDecision`, `StaticRouter`) | same 4 | §5.1 `LLMGateway` | **Yes** |
| `prompted_completion` (`PromptedCompletionService`, `build_anthropic_prompted_completion_service`) | same 4 | §5.1 `LLMGateway` + §5.2 `PromptRegistry` (combined) | **Yes** |
| `persistence.engine` (`build_engine`) | same 4 | §5.2 `PromptRegistry` (DB access needed only to construct the SQL-backed prompt catalog underneath it; the SDK hides this entirely) | **Yes** |
| `persistence.settings` (`DatabaseSettings`) | same 4 | §5.2 `PromptRegistry` | **Yes** |
| `secrets_manager.env_provider` (`EnvSecretProvider`) | same 4 | §5.9 `SecretResolver` | **Yes** |
| `workflow_engine.prompted_agent` (`PromptedAgent` base class) | same 4 | §4.2 `Agent` Protocol — needs a new SDK-provided convenience base, not one of the 15 numbered interfaces itself | **Yes** (as a helper, see §5 of this doc) |
| `sandbox.default_executor` (`build_default_sandbox_executor`) | build, documentation, verification | §5.6 `ToolInvoker` | **Yes** |
| `sandbox.executor` (`SandboxExecutor`) | build, documentation, verification | §5.6 `ToolInvoker` | **Yes** |
| `workflow_engine.sandboxed_tool` (`SandboxedCommandTool`) | build, documentation, verification | §5.6 `ToolInvoker` + §4.3 `Tool` Protocol | **Yes** |
| `context_manager.models` (`AssembledContext`) | documentation, verification | §5.3 `ContextService` — **boundary model only**, see note below | **Yes (model only)** |
| `capability_manager.pack_contract` (`HealthReport`, `PackContext`, `PackRegistration`) | pack.py | §6 `PackContext` + §7 `CapabilityPack`/`PackRegistration`/`HealthReport` | **Yes** |

**Note on `ContextService`:** no agent calls `.assemble()` themselves — `AgentStepExecutor` (Workflow Engine) already assembles context and hands it to the agent via `AgentRequest.context` (per `agent_architecture.md`'s Invocation Lifecycle, already real — see `step_executor.py`'s `AgentStepExecutor._invocation_inputs`). The two agents above only import the **model** (`AssembledContext`) to type-annotate what they receive, never the assembly path. v1.0.0 therefore needs `ContextService`'s boundary models (`AssembledContext`, `ContextItem`, `SourceRef`) but not a working `.assemble()` implementation behind the Protocol — the Protocol method is declared for future callers, unused by real code today.

### 2.3 Answer to Question 1: which interfaces does the pack need right now

**Revised 2026-07-28 (v2.0) from "5 of 15" to "3 capability Protocols of 15."** v1.0 of this document answered 5 (`LLMGateway`, `PromptRegistry`, `SecretResolver`, `ToolInvoker`, `ContextService`-models). The architecture review found the derivation flawed — see §2.4 and finding **P5** in §6a. The corrected answer:

**3 capability Protocols**, and they map exactly onto the pack's two **declared** permissions (`capability_packs/software-engineering/manifest.yaml` lines 71–73: `llm:invoke`, `sandbox:execute`):

| Declared permission | Protocols required to serve it |
|---|---|
| `llm:invoke` | `LLMGateway` (§5.1), `PromptRegistry` (§5.2) |
| `sandbox:execute` | `ToolInvoker` (§5.6), + the `Tool` contract (§4.3) |

**Plus the non-capability contracts**, which are not permission-gated because they are the shape of a pack itself rather than something granted to it: `Agent` (§4.2), `PackContext` (§6), `CapabilityPack`/`PackRegistration`/`HealthReport` (§7), and `ContextService`'s **boundary models only** (§5.3 — `AssembledContext`/`ContextItem`/`SourceRef`, imported for type annotation; no agent calls `.assemble()`).

**`SecretResolver` (§5.9) is dropped from v1.0.0.** Its only real use is constructing the pack's *own* LLM Gateway (`architecture.py:161`: `secret_provider=EnvSecretProvider(), api_key_secret_reference=...`). Once a constructed gateway is injected (step 6b), the pack resolves no secrets at all. Decisively: **the manifest declares no secret permission**, and `platform_sdk.md` §6 states PackContext attributes are present *"only if the manifest declared the corresponding capability and it was granted."* Putting `secrets` on this pack's `PackContext` would grant an undeclared capability — a contract violation, not a convenience. Re-add it in the step that first onboards a pack declaring a secret permission.

**11 of 15 stay deferred** (was 10; `SecretResolver` joins them): `RetrievalService`, `MemoryService`, `EventBus`, `ConfigService`, `StorageService`, `WorkspaceService`, `Telemetry`, `TraceabilityService`, `QualityGateRegistry`, `SpeechGateway`, `SecretResolver`. For the first ten, the reason is unchanged and still correct: every subsystem behind them is 0%-built or a docstring-only stub, so shipping the pack-facing Protocol first would mean an interface with nothing to route to.

**One correction to v1.0's deferral reasoning.** v1.0 deferred `ConfigService` (§5.8) on the stated grounds of *"zero real usage."* That is factually wrong, and the review caught it (**P5**): all four prompted agents read platform configuration directly off disk and out of the environment — `_CONFIG_PATH = Path.cwd() / "config" / "llm.yaml"` plus `load_provider_config(...)`, and `DatabaseSettings()` for `AIOS_DATABASE_URL` (`architecture.py:112,152,161`; identically in `build.py`, `documentation.py`, `requirements_analyst.py`). Both are prohibited by `platform_sdk.md` §10 today ("direct… filesystem access outside `WorkspaceService`/`StorageService`", "literal absolute paths"). `ConfigService` still stays deferred — but for the *correct* reason: the need disappears entirely once step 6b injects a fully-constructed gateway, so no pack-facing config Protocol is required to close it.

### 2.4 Why v1.0's count was wrong: construction vs. consumption (finding P5)

v1.0 mapped each imported Kernel module 1:1 onto a Protocol. That conflates two different things:

- **Imports the pack needs to *construct* its own dependencies** — `anthropic_adapter` (`PROVIDER_NAME`), `model_config` (`load_provider_config`), `router` (`StaticRouter`/`RoutingDecision`), `persistence.engine` (`build_engine`), `persistence.settings` (`DatabaseSettings`), `secrets_manager.env_provider` (`EnvSecretProvider`), `sandbox.default_executor` (`build_default_sandbox_executor`). **Seven of the thirteen.**
- **Capabilities the pack actually *consumes*** — completing a prompted LLM call, and executing a command in a sandbox.

All seven construction imports exist for one reason: **nothing injects anything, so each agent builds its own object graph.** They do not each need a corresponding Protocol; they all disappear together the moment a `PackContext` carries constructed services (step 6b). Mapping them 1:1 to Protocols overstated the required surface by roughly a factor of two and, worse, produced a Protocol (`SecretResolver`) the pack has no declared right to.

The corrected derivation starts from **declared permissions**, not from imports — because permissions are what `platform_sdk.md` §6 actually gates `PackContext` attributes on.

---

## 3. Migration plan: same step or fast-following?

**Recommendation: fast-following, not the same step — but with no other work permitted to land between the two.**

**Reasoning:**

- **Size and risk.** Building 5 Protocols + their boundary models + `AiOsError` + `PackContext`/`CapabilityPack` is already a multi-step body of work by this project's own established cadence (see §6). Bundling 5-agent migration into the same step roughly doubles the diff size and the number of things that can break in one commit — this project's standing rule is small, individually-approvable steps, and a combined step would be the largest single step in the project's history.
- **The agents are currently proven and passing** (803/803 real tests, per the last two audits). Migrating them is a mechanical, verifiable, per-agent change (swap an import, swap a constructor argument, re-run that agent's existing tests) — exactly the shape of change this project has always done one-at-a-time (`015_architecture_agent.md` through `018_documentation_agent.md` each built one agent in its own step; there is no reason migration should be coarser than construction was).
- **But the task's own concern is real and must be bounded**: shipping the SDK and *not* migrating promptly would leave the hard gate satisfied in letter (an SDK exists) but not in spirit (the flagship pack still violates it) — an even worse state than today's honestly-documented exception, because it would look closed while staying open. **The fix is sequencing discipline, not step-merging**: the migration steps (§6, steps 9–14) are treated as mandatory, adjacent next steps once the SDK-build steps land — no unrelated feature work, and specifically no 6th agent or new pack, is approved in between. This is recorded as a standing-rules addition (see `../../process/standing_rules.md`).

**Migration order reversed in v2.0 (finding P6).** v1.0 ordered the migrations `requirements-analyst` → `architecture` → `build` → `documentation` → `qa-test`, describing `requirements-analyst` as "simplest." It is not. `qa-test` (`verification.py`) is: it **makes no LLM call at all**, imports only 4 Kernel modules, and constructs no gateway, no router, no database engine, and no secret provider. `requirements-analyst` needs that entire construction chain replaced at once. Migrating `qa-test` first validates the `Agent` Protocol and the new injection path **in isolation from any gateway complexity** — if it breaks, exactly one thing is new. The corrected order is `qa-test` → `requirements-analyst` → `architecture` → `build` → `documentation` → `pack.py`.

---

## 4. `pack_contract_suite` timing

**Recommendation: split it.** Check 7 (forbidden imports) ships **with** the SDK, as its own small step (step 8, §6). The remaining 8 checks ship **after** migration, as their own dedicated step (step 15, §6).

**Reasoning:**

- The task's own framing is exactly right: check 7 is the mechanism that would have caught the current, months-long, hand-recorded violation. Every day it doesn't exist is another day the same blind spot is live. **Check 7 has zero dependency on anything else in this plan** — it's a pure AST import-scanner over a pack's own source tree (`ast.walk` for `Import`/`ImportFrom` nodes naming `ai_os_kernel`, `ai_os_services`, or another pack's package), needing no `PackContext`, no `CapabilityPack` semantics, no activation lifecycle. It can be built the moment the SDK package exists to hold it, independent of everything else.
- Checks 2, 3, 4, and 9 (entry-point resolution, I/O-model matching, workflow step resolution, clean activation/deactivation) **do** depend on `PackContext`/`CapabilityPack` being real and on the SE pack actually being migrated onto them — running them before migration would mean testing against the soon-to-be-replaced Kernel-internal `capability_manager.pack_contract` shapes, work that would be partly thrown away. Checks 5, 6, 8 (trust-tier consistency, permission vocabulary, prompt existence) are cheap and could ship early, but splitting the suite into "check 7 now, 5/6/8 early, 2/3/4/9 late" fragments one document's own coherent 9-check contract for a marginal gain; shipping the full remaining 8 together, once there's a real migrated pack to run them against, keeps the suite itself simple and the "does this pack pass" answer meaningful the first time it's asked.

### 4.1 What v1.0 got wrong about check 7 (finding P4)

v1.0 said check 7 would be "wired into CI" at step 8 and left it there. The review established that this **breaks CI and cannot ever pass on this pack** without two additions:

1. **It fails immediately, not trivially.** At step 8 the Software Engineering pack still has **57 `ai_os_kernel` imports**. Check 7 run against it *fails*. With no waiver mechanism, **CI goes red at step 8 and stays red through step 13** — six steps of a knowingly-broken pipeline, which this project's own zero-regression rule forbids. Step 8 therefore must ship the check **together with an explicit, documented, expiring waiver** for the not-yet-migrated pack, removed at step 14.
2. **`pipeline.py` can never pass, under v1.0's own rules.** It is inside the shipped wheel (`packages = ["src/ai_os_pack_software_engineering"]`), it imports 11 Kernel modules **and** `sqlalchemy.ext.asyncio` (`pipeline.py:107` — also check-7-forbidden as a database driver), and v1.0 declared it a **permanent** non-goal. So check 7 would fail on this pack forever, even after step 14 completed every migration.

**The fix the review found, which v1.0 missed entirely: relocate `pipeline.py` into `tests/`.** Verified: **zero pack source modules import it** — its only importers are two Kernel-side tests (`tests/integration/workflow_engine/test_delivery_pipeline.py`, `tests/integration/sandbox/test_delivery_pipeline_docker.py`). It is a test harness sitting in shipped-package space. Moving it makes check 7 pass **honestly** rather than by exemption, and dissolves §2.1's "11 composition-root imports" problem — **without** building a Capability Manager first. This is now step 7.

---

## 5. `platform_sdk/` subdirectories: file-by-file plan

Verified current state (`find platform_sdk -type d`): `contracts/`, `models/`, `prompts/`, `sdk/`, `utilities/` all exist and are empty; `schemas/` holds the one real file. `errors/` and `testing/` **do not exist at all** (not even as empty directories) — `platform_sdk.md` §3 already flags this precisely.

*(Note: the handoff in `implementation_status.md` §6 said "4 empty subdirectories" — the real count, re-verified this step, is 5 empty directories on disk, plus 2 that don't exist yet. `prompts/` is the one this plan doesn't otherwise mention — see below.)*

```
platform_sdk/
├── pyproject.toml                    NEW — PEP 621 distribution, name="ai-os-sdk", package ai_os_sdk
├── src/ai_os_sdk/
│   ├── __init__.py
│   ├── errors/
│   │   ├── __init__.py
│   │   └── taxonomy.py               AiOsError, TransientError, PermanentError, QualityError,
│   │                                  InfrastructureError, BudgetExceededError, SecurityError,
│   │                                  StructuredError (§4.4)   [directory does not exist — create it]
│   ├── models/
│   │   ├── __init__.py
│   │   ├── common.py                 ArtifactRef, TraceContext, SecurityContext, StepBudget (§4.1)
│   │   ├── agent.py                  AgentRequest, AgentResult (§4.2)
│   │   ├── tool.py                   ToolRequest, ToolResult (§4.3)
│   │   ├── llm.py                    LLMRequest, LLMResponse, UsageRecord, ProviderCapabilities,
│   │                                  Message, ContentBlock, CacheHint (§5.1)
│   │   ├── prompt.py                 RenderedPrompt, PromptDefinition (§5.2)
│   │   ├── context.py                ContextRequest, AssembledContext, ContextItem, SourceRef (§5.3)
│   │   └── pack.py                   PackContext, PackRegistration, HealthReport (§6, §7)
│   │                                 (secret.py / SecretValue dropped in v2.0 — see §2.3)
│   ├── contracts/
│   │   ├── __init__.py
│   │   ├── agent.py                  Agent (Protocol) (§4.2)
│   │   ├── tool.py                   Tool (Protocol) (§4.3)
│   │   ├── llm_gateway.py            LLMGateway (Protocol) (§5.1)
│   │   ├── prompt_registry.py        PromptRegistry (Protocol) (§5.2)
│   │   ├── context_service.py        ContextService (Protocol) (§5.3) — declared, unimplemented caller
│   │   ├── tool_invoker.py           ToolInvoker (Protocol) (§5.6)
│   │   └── capability_pack.py        CapabilityPack (Protocol) (§7)
│   │                                 (secret_resolver.py dropped in v2.0 — see §2.3)
│   ├── sdk/
│   │   ├── __init__.py
│   │   └── prompted_agent.py         PROPOSED (see below) — a concrete, non-Protocol Agent base
│   │                                  built on LLMGateway + PromptRegistry, mirroring
│   │                                  ai_os_kernel.workflow_engine.prompted_agent.PromptedAgent's
│   │                                  real shape. 4 of 5 real agents already depend on the
│   │                                  Kernel-internal equivalent of exactly this class.
│   └── utilities/
│       └── __init__.py               EMPTY beyond this — see reasoning below
└── testing/
    ├── __init__.py
    └── forbidden_imports.py          pack_contract_suite check #7 only, v1.0.0
                                        [directory does not exist — create it]
```

**Two things flagged rather than silently decided, per this project's own standing rule to stop and report a genuine gap:**

1. **`platform_sdk/sdk/` has no description anywhere** — `platform_sdk.md` §3's own layout tree doesn't list it at all (only `contracts/`, `models/`, `errors/`, `testing/`, `utilities/`, `prompts/`, `schemas/` have a stated purpose). This plan **proposes** `sdk/` as the home for concrete, opinionated helper implementations built *on top of* the Protocols (starting with a `PromptedAgent` base) — Protocols are abstractions a pack could implement differently; `sdk/` is where the SDK saves a pack the boilerplate of doing so, the same role `ai_os_kernel.workflow_engine.prompted_agent.PromptedAgent` plays inside the Kernel today. Approving this plan is this proposal's approval; if the product owner intends something else for `sdk/`, say so before step 1 starts.
2. **`platform_sdk/prompts/` stays untouched.** `platform_sdk.md` §3 marks it "(present on disk, undocumented)" and nothing in this plan's real-usage evidence needs it — the real pack's prompts already live in `capability_packs/software-engineering/prompts/`. Left empty pending a product-owner decision on what it's for; not addressed by any step in §6.

**`utilities/` stays a bare `__init__.py` in v1.0.0.** Checked directly: `grep -rn "ulid\|hashlib\|sha256\|canonical" capability_packs/software-engineering/src/` finds no pack code generating its own IDs, hashes, or canonical JSON — the Kernel does all of that internally today. Per this project's coding standard ("fields exist only once something reads them"), building out `ids.py`/`hashing.py`/`canonical_json.py` speculatively, with no current caller, is exactly the premature scaffolding the standards forbid. Revisit when a real pack need appears.

---

## 6. Ordered step sequence (revised v2.0 — 18 steps)

Each step below is scoped to become its own future prompt, in this order, each individually approved before it starts — matching this project's established cadence (one agent per step for the original 5, one fix per step in the two most recent audits).

**Net change from v1.0's 15 steps:** three insertions (**2a**, **6a**, **6b**), one repurposed step (**7**, now the `pipeline.py` relocation), one deletion (`SecretResolver`, per §2.3), and the migration order reversed (§3). Reasoning for every change: §6a.

**Reconciliation & foundation**

| # | Scope | Status |
|---|---|---|
| 1 | Scaffold `platform_sdk` as a real `ai-os-sdk` PEP 621 distribution — packaging only, six stub subpackages, workspace member. | **Done** (`fc0973a`) |
| 2 | `AiOsError` hierarchy + `StructuredError` + shared boundary models (`ArtifactRef`, `TraceContext`, `SecurityContext`, `StepBudget` — §4.1, §4.4). Dependency-free foundation everything else builds on. | **Done** |
| **2a** | **Protocol/reality reconciliation decision (docs only).** All five decisions made and recorded as dated blocks in `platform_sdk.md` §4.2/§4.3/§5.1/§5.2/§5.6 — see §6b below for the summary. Resolves P1 + P3. | **Done** |
| 3 | `Agent` + `Tool` Protocols **only** — the narrowed, dict-based shapes decided in 2a, plus the SDK's own `TrustTier`. `AgentRequest`/`AgentResult`/`ToolRequest` out of scope (deferred past v1.0.0); `ToolResult` moved to step 6 with its consumer. **Structural compatibility proven against real classes**, not mocks: all 5 pack agents + `EchoAgent` satisfy `Agent`, and `EchoTool` + `SandboxedCommandTool` satisfy `Tool`, with **zero modification to any Kernel or pack source** (`tests/unit/platform_sdk/test_kernel_satisfies_sdk_contracts.py`, 13 assertions). | **Done** |

**Contracts**

| # | Scope | Status |
|---|---|---|
| 4 | `LLMGateway` Protocol narrowed to `complete()` + `capabilities()` (the two methods `DispatchingLLMGateway` implements); `ProviderCapabilities` extended to the real 13 fields; `LLMRequest`/`Message`/`MessageRole`/`StopReason`/`UsageRecord`/`LLMResponse` as field-for-field mirrors of the real Kernel models. **Structural compatibility proven against the real `DispatchingLLMGateway`**, not a mock, constructed with an empty `StaticRouter` and no providers — zero I/O, zero Kernel/pack source modified. `EchoLLMGateway` correctly does *not* satisfy the SDK Protocol (it implements only `complete()`), which is itself evidence the narrowing is exact, not accidentally loose. | **Done** |
| 5 | `PromptRegistry` Protocol at the documented keyword call style (`render(prompt_id, variables, *, version)`) — the one reversal decision from 2a, kept over the Kernel's own request-object envelope; `version` required. `RenderedPrompt` narrowed to 3 fields. **No real Kernel class satisfies this Protocol** (it is a from-scratch call convention, not a narrowing), so instead of an `isinstance` proof, the conversion to/from the real `PromptRenderRequest`/`PromptRenderResponse` is proven **lossless in both directions** (`tests/unit/platform_sdk/test_prompt_registry_adapter_conversion.py`, 7 assertions) — the reference conversion functions are test-local, illustrative only, not production code. | **Done** |
| 6 | `ToolInvoker` Protocol (from-scratch, signature kept from 2a) + `ToolDescriptor` + `ToolResult` (moved here from step 3) + the `platform.sandbox.run_command` tool contract, grounded in `PLATFORM_SANDBOX_RUN_COMMAND_DESCRIPTOR`. **`SecretResolver` removed** — see §2.3. **No Kernel class satisfies `ToolInvoker` directly** (from-scratch design), so proven instead against the **real, executed** `LocalSubprocessSandbox` (clean/timed-out/truncated runs) — which found and fixed two real bugs: an over-strict `ToolResult` validator (§6f), and a circular import between `errors` and `models` (§6f). | **Done** |
| **6a** | **NEW — Kernel-side adapters implementing the SDK Protocols**, over `DispatchingLLMGateway`, the Prompt Engine, and the sandbox. Without this, the Protocols from 4–6 have no conforming implementation anywhere. Resolves P1. Proven with real, non-mocked underlying objects (Echo-backed `DispatchingLLMGateway`, `InMemoryPromptEngine`, `LocalSubprocessSandbox`); the timeout-precedence nuance step 6 left open is resolved via `min()` ("more restrictive wins"). Record in §6g. | **Done** |
| **6b** | **NEW — `PackContext` construction + injection path.** A real, permission-gated `PackContext` builder (`ai_os_kernel.sdk_adapters.pack_context.build_pack_context`) plus a new SDK Protocol, `PackContextReceiver`, resolving the zero-argument blocker without changing `EntrypointLoader`. **This is the actual unlock for every migration step.** Resolves P2. Proven end to end against a real test entrypoint (real `EntrypointLoader` construction → real injection → real Echo LLM completion, prompt render, sandboxed command). Record in §6h. | **Done** |
| 7 | **REPURPOSED — relocate `pipeline.py` out of the shipped package into `tests/`** (§4.1), and land `ContextService` boundary models (§5.3) + the entry-point contract `PackContext`/`CapabilityPack`/`PackRegistration`/`HealthReport` (§6, §7) **formally into `ai_os_sdk`** — relocating the Kernel-side `PackContext` step 6b extended additively, now three fields richer, into its real, final SDK home. Record in §6i. | **Done** |

**Compliance gate**

| # | Scope |
|---|---|
| 8 | `pack_contract_suite` check 7 (forbidden-import AST scanner) + **an explicit, documented, expiring waiver** for the still-unmigrated pack, so CI stays green through steps 9–13 and the waiver is removed at step 14. Resolves P4's red-CI half. Proven to genuinely fail without the waiver and genuinely pass with it (44 real violations, 6 modules); wired into CI. Record in §6j. **Done.** |

**Migration** (fast-following, no other feature work interleaved — §3; order reversed per P6)

| # | Scope |
|---|---|
| 9 | Migrate **`qa-test`** (`verification.py`) — simplest: no LLM call, 4 Kernel imports, no gateway construction. Proves `Agent` + `ToolInvoker` + the new injection path in isolation. Two real gaps found and resolved (context duck-typing; `SqlAgentRegistry` binding). Record in §6k. **Done.** |
| **9a** | **INSERTED — wire `SqlAgentRegistry`/`SqlToolRegistry` to genuinely call `bind_pack_context()` for real**, closing step 9's own discovered production-wiring gap before steps 10–13 hit it four more times. `EntrypointLoader` untouched. Record in §6l. **Done.** |
| 10 | Migrate **`requirements-analyst`** — the first gateway-injection migration. Lazy-build lock found obsolete, not conflicting; a real, previously-latent circular import found and fixed; call-recording capability loss recorded. Record in §6m. **Done.** |
| 11 | Migrate **`architecture`** — the second gateway-injection migration, following step 10's pattern exactly. A real fix needed in both Delivery Pipeline end-to-end tests, since this is the first migrated agent actually chained into `se.delivery_pipeline`. Record in §6n. **Done.** |
| 12 | Migrate **`build`** — the first gateway+tool-injection migration together. Lock kept (narrower — working-directory only); a real tension using ToolInvoker instead of direct sandbox construction found and resolved (python_command). Record in §6o. **Done.** |
| 12a | (Inserted) Close the `python_command` regression step 12 recorded: `PLATFORM_PYTHON_INTERPRETER` placeholder token, resolved by `ToolInvokerAdapter` from the real, active sandbox. Proven against both `LocalSubprocessSandbox` and a real, live `DockerSandbox`. Record in §6p. **Done.** |
| 13 | Migrate **`documentation`** — the fifth and final agent. No lock needed at all (least internal state of any agent). `PLATFORM_PYTHON_INTERPRETER` used from the start, no repeated tension. Waiver down to its own last entry (`pack.py`), not yet empty. Record in §6q. **Done.** |
| 14 | Migrate **`pack.py`**'s entry point onto the real SDK types; **remove the step-8 check-7 waiver**; retire the dated direct-Kernel-import exception in `capability_pack_contract.md`. **Next.** |

**Completion**

| # | Scope |
|---|---|
| 15 | Build the remaining 8 `pack_contract_suite` checks, run them end to end against the migrated pack, and formally record the gate as **satisfied** (not merely built) in `feature_inventory.md`, `capability_pack_contract.md`, and `standing_rules.md`. |

*(18 steps total, plus one inserted fix step: 1, 2, 2a, 3, 4, 5, 6, 6a, 6b, 7, 8, 9, 9a, 10, 11, 12, 13, 14, 15.)*

**Gate semantics (unchanged):** the Platform SDK growth gate (`standing_rules.md`) lifts only after step 15 — not after step 1, and not after step 8. An SDK existing without a migrated, verified pack is still "no compliant pack exists"; the gate's purpose is a compliant pack, not merely an importable SDK.

---

## 6a. Architecture review findings (2026-07-28) — what changed and why

An independent architecture review of v1.0 of this document, conducted against real source rather than against `platform_sdk.md`'s specification, found seven problems. All were accepted in full. The v1.0 sequence was **not executable as written**: it defined Protocols (2–8) then migrated packs onto them (9–14), with nothing in between making the Kernel able to *supply* those Protocols — and with the mechanism that would have listed as a non-goal.

| # | Severity | Finding | Evidence | Resolution |
|---|---|---|---|---|
| **P1** | **Blocking** | **No step made the Kernel satisfy the SDK Protocols.** Protocols are structural, so v1.0 implicitly assumed existing Kernel classes would conform. Three of the needed ones do not. | `LLMGateway` (§5.1) specifies `complete`/`stream`/`embed`/`count_tokens`/`capabilities`; real `DispatchingLLMGateway` has `complete()` (`gateway.py:358`) and `capabilities()` (`:337`) **only** — 3 of 5 absent. `PromptRegistry` (§5.2) specifies `render(prompt_id, variables, version)` + `get(...)`; real `PromptEngine` (`renderer.py:79`) is `render(request: PromptRenderRequest)` with **no `get()`**. `ToolInvoker` (§5.6) has **no counterpart at all**. Only `SecretResolver` matched exactly (`provider.py:34`) — and it is now dropped anyway. | New step **2a** (decide narrow-vs-extend, amend the spec) + new step **6a** (build the adapters). |
| **P2** | **Blocking** | **The injection path does not exist, and v1.0 listed fixing it as a non-goal** — so the migration steps depended on a mechanism their own plan excluded. | `EntrypointLoader.load()` line 95 is `return cls()` — hardcoded zero-argument; its docstring (lines 21–27) states *"Passing manifest-declared configuration into an entrypoint's constructor is real Capability Manager design work, not attempted here."* `PackContext` (`pack_contract.py:56`) is identity-only (`pack_id`, `pack_version`); its docstring confirms all 14 service attributes are deferred. `pack_contract.py:30`: *"Nothing in this codebase calls `CapabilityPack.activate()` yet."* The only `PackContext(...)` construction anywhere is `capability_packs/software-engineering/tests/test_pack.py:50` — a test. | New step **6b**; the corresponding non-goal in §7 is withdrawn. |
| **P3** | **Blocking** | **The SDK `Agent`/`Tool` Protocols are incompatible with the Workflow Engine's real calling convention**, and a migrated agent would fail to load. | Kernel `Agent` (`agent.py:71,73`): `output_schema: dict`, `execute(inputs: dict) -> dict`. SDK `Agent` (§4.2): `agent_id`, `version`, `input_model`, `output_model`, `execute(AgentRequest) -> AgentResult`. `SqlAgentRegistry` (`registry.py:222`) does `isinstance(loaded, Agent)` against the **Kernel** Protocol → an SDK-shaped agent lacking `output_schema` **fails to load** with *"missing output_schema/execute"*. `AgentStepExecutor._validate_output` (`step_executor.py:165-172`) reads `agent.output_schema`. `Tool` is worse: the method **name** differs (`execute` vs SDK `invoke`). | Step **2a** decides; step **3** builds to that decision, not to §4.2/§4.3 as written. |
| **P4** | High | **Check 7 at step 8 fails loudly rather than trivially, and one target could never pass.** | 57 `ai_os_kernel` imports remain at step 8 → check fails → **CI red for six steps**, violating the zero-regression rule. `pipeline.py` is inside the shipped wheel, imports 11 Kernel modules **and** `sqlalchemy.ext.asyncio` (`:107`, also forbidden), and was a *permanent* non-goal → permanent failure. **Missed cheap fix:** zero pack source modules import `pipeline.py`; only two Kernel-side tests do. | Step **8** gains an expiring waiver; new step **7** relocates `pipeline.py` into `tests/`. |
| **P5** | Medium | **The "5 needed / 10 deferred" split was right in outcome but wrong in derivation**, and two entries were misclassified. | `SecretResolver` was included, but its only use is constructing the pack's own gateway, and the manifest (lines 71–73) declares **no secret permission** — `platform_sdk.md` §6 grants PackContext attributes only for declared capabilities. `ConfigService` was deferred as *"zero real usage"*, but all four prompted agents read `config/llm.yaml` via `Path.cwd()` and `AIOS_DATABASE_URL` via `DatabaseSettings()`. Root cause: 7 of 13 imports are **construction-only**, not consumed capabilities. | §2.3 rewritten (3 capability Protocols, derived from declared permissions); §2.4 added; `SecretResolver` dropped from step 6. |
| **P6** | Low | **Migration order was inverted.** v1.0 called `requirements-analyst` "simplest"; it needs the whole construction chain replaced. `qa-test` makes no LLM call at all. | `verification.py` imports 4 Kernel modules, constructs no gateway/router/engine/secret provider. | Order reversed: `qa-test` first (step 9). |
| **P7** | Low | Documentation arithmetic slip. | §2.1 says *"all 8 `workflow_engine.{…}`"* then lists **nine** modules. The 11 pipeline-only modules are 2 `context_manager` + 9 `workflow_engine`. (The 13 + 11 = 24 total is correct.) | Corrected in §2.1. |

**Two findings reduced scope rather than adding it:** dropping `SecretResolver` (P5) and relocating `pipeline.py` instead of waiting on a Capability Manager (P4).

## 6b. Step 2a — the five reconciliation decisions (2026-07-29)

Each decision is recorded in full, with its evidence, as a dated **v1.0.0 Reconciliation Decision** block in the corresponding section of `platform_sdk.md`. Summary:

| Interface | Decision | Core evidence | Consequence |
|---|---|---|---|
| **`Agent`** (§4.2) | **NARROW** to the dict-based `output_schema` + `execute(inputs) -> outputs` shape | 5 real agents on it (`agent.py:71-73`); `AgentStepExecutor` calls `execute(dict)` and validates against `output_schema` (`step_executor.py:165-172`); `SqlAgentRegistry` gates on `isinstance` (`registry.py:222`) | `AgentRequest`/`AgentResult` deferred past v1.0.0 — **step 3 shrinks** |
| **`Tool`** (§4.3) | **NARROW** the Protocol; **MIXED** on `ToolResult`; **DEFER** `ToolRequest` | `tool.py:65-68` — and the method is named `execute`, not the spec's `invoke`; `SqlToolRegistry` also cross-checks `trust_tier` against `catalog.tools` (`registry.py:277-284`) | `ToolResult`: `stdout_ref`/`stderr_ref` narrowed to inline strings (no `StorageService`), `artifacts` deferred, **and extended with `timed_out`/`truncated`** — two real outcomes the spec cannot express (`sandbox/models.py:63-68`) |
| **`LLMGateway`** (§5.1) | **NARROW** methods 5 → 2 (`complete`, `capabilities`); **EXTEND** `ProviderCapabilities` 10 → 13 fields | `DispatchingLLMGateway` has `complete()` (`gateway.py:358`) and `capabilities()` (`:337`) only; real `ProviderCapabilities` carries 13 fields and its own docstring names this document as the discrepancy it "implements past" (`capability_negotiator.py:98-109`) | `stream`/`embed`/`count_tokens` deferred (additive later = minor bump) |
| **`PromptRegistry`** (§5.2) | **KEEP the documented keyword call style** (reject the Kernel's request-object envelope); **NARROW** `version` to required; **DEFER** `get()` and 2 `RenderedPrompt` fields | Real `PromptEngine.render(request)` (`renderer.py:79`); `models.py:56-63` requires `version` and its docstring states *"nothing here silently picks a version on the caller's behalf"*; `SqlPromptCatalog` exposes only `render()` (`catalog.py:65`) | **The one deliberate "documented shape wins" decision** — a pack-facing Protocol is shaped for its caller, not its implementer (ADR-0004); adapter conversion is ~3 lines |
| **`ToolInvoker`** (§5.6) | **DESIGN** — signature KEPT, grounded in a platform-provided tool id `platform.sandbox.run_command` | The pack declares **zero** tools (`manifest.yaml:71-73`); `SandboxedCommandTool.execute(inputs)` **ignores `inputs`** entirely (`sandboxed_tool.py:110-118`), so agents construct a fresh tool per call (`build.py:324`, `verification.py:265`) | Moving the command into `inputs` makes `invoke(tool_id, inputs)` genuinely meaningful and **fixes** the ignored-inputs wart; step 6a's adapter is built directly over `SandboxExecutor`, not over the dict-based `Tool` |

**Net direction:** four narrowings, two targeted extensions where the real Kernel is richer than the specification, one deliberate reversal in the specification's favour (`PromptRegistry`'s call style), and one from-scratch design. **Every extension is a case where the spec could not express a real, materially-different outcome** — not scope creep.

**Effect on the remaining plan:** step 3 gets *smaller* (two Protocols, no models); step 6 gets one model (`ToolResult`) and one tool contract; steps 4, 5, 6a, 6b, 7, 8, 9–15 are unchanged in scope. **No new step is needed**, and nothing else blocks step 3.

---

## 6c. Step 3 — `Agent`/`Tool` Protocols built and proven (2026-07-29)

Two `Protocol` definitions in `platform_sdk/src/ai_os_sdk/contracts/`, plus the SDK's own `TrustTier`. Both are `@runtime_checkable`, matching the Kernel's own convention so a loader can reject a structurally unrelated entrypoint.

**The claim step 2a's narrowing rested on is now proven against real code, not a mock.** `tests/unit/platform_sdk/test_kernel_satisfies_sdk_contracts.py` imports the actual shipped classes and asserts `isinstance` against the new SDK Protocols — 13 assertions, all passing:

- **`Agent`:** all five Software Engineering pack agents (`ArchitectureAgentEntrypoint`, `BuildAgentEntrypoint`, `DocumentationAgentEntrypoint`, `RequirementsAnalystAgentEntrypoint`, `TestAgentEntrypoint`) plus the Kernel's `EchoAgent`. Each is constructed exactly as `EntrypointLoader` constructs it — `cls()`, zero arguments — so the test exercises the real construction path, and none performs I/O before first `execute`.
- **`Tool`:** the Kernel's `EchoTool` and the one real non-trivial tool, `SandboxedCommandTool`, constructed the way `agents/build.py` and `agents/verification.py` construct it.
- **Negative controls:** an object missing `output_schema` is not an `Agent`; a real agent is not a `Tool` (the tier is what separates them, and an agent passing as a tool could bypass ADR-0016's sandbox guard).

**Three decisions and findings worth recording:**

1. **The SDK defines its own `TrustTier` rather than importing the Kernel's**, because §2 rule 1 makes this SDK the dependency floor. Both enums independently mirror `manifest.schema.json`'s `tools[].trustTier` enum — verified to be exactly `["tier1_sandboxed", "tier2_trusted"]` — and a test asserts the two carry identical values, since nothing else would catch them drifting apart. **Consequence, recorded rather than hidden:** because they are distinct Python types, a Kernel-typed tool is *not statically assignable* to the SDK `Tool` Protocol even though it satisfies it at runtime. Bridging that is step 6a's adapter, exactly as §4.3's decision block anticipated.
2. **`mypy --strict` rejects enum-to-string and cross-enum equality as non-overlapping**, even where it is true at runtime for a `StrEnum`. Four such comparisons were written and then corrected to compare `.value` explicitly — which is also the more precise claim, since what must agree with the schema is the wire value, not member identity.
3. **The cross-boundary test lives in the root suite, not in `platform_sdk/tests/`.** A test importing `ai_os_kernel`, a pack, *and* `ai_os_sdk` is inherently a cross-boundary assertion, and `platform_sdk/tests/` deliberately imports nothing from the Kernel or any pack — holding the dependency-floor discipline in the SDK's own test suite, not only in its source.

**Also asserted: the limit of what the runtime check proves.** A test constructs an object whose `execute` is not async, takes no arguments, and whose `output_schema` is a plain string — and confirms it still passes `isinstance`. This makes §4.2's precision correction executable: a `runtime_checkable` Protocol verifies *member presence only*, never signatures, so the check converts "this is not remotely an agent" into a clear error but never certifies the contract.

---

## 6d. Step 4 — `LLMGateway` Protocol built and proven (2026-07-29)

One `Protocol` in `platform_sdk/src/ai_os_sdk/contracts/llm_gateway.py`, at the narrowed shape decided in 2a (`complete()` + `capabilities()` only), plus its full model set in `platform_sdk/src/ai_os_sdk/models/llm.py`: `Message`, `MessageRole`, `LLMRequest`, `StopReason`, `UsageRecord`, `LLMResponse` (field-for-field mirrors of the real, working Kernel models — narrowed exactly as far as the Kernel's own models are narrowed, no further), and `ProviderCapabilities` (extended to the real 13 fields, per 2a).

**Proven against the real `DispatchingLLMGateway`, not a mock.** Added to `tests/unit/platform_sdk/test_kernel_satisfies_sdk_contracts.py`: `DispatchingLLMGateway(router=StaticRouter(routes={}), gateways={})` — constructed with an empty routing table and no providers, so zero I/O and zero network occur — satisfies the new SDK `LLMGateway` Protocol via `isinstance`. **A negative control worth keeping:** `EchoLLMGateway`, which implements only `complete()`, does *not* satisfy the SDK Protocol. This is itself evidence the narrowing is exact rather than accidentally loose — the Kernel's own internal `LLMGateway` Protocol requires only `complete()`, so it is narrower than this SDK Protocol; only the real, production `DispatchingLLMGateway` (which also implements `capabilities()`) conforms.

**One thing worth recording:** `LLMRequest.metadata` uses the SDK's own canonical, seven-field `TraceContext` (`ai_os_sdk.models.common`, built in step 2), not the Kernel's own two-field reduced `TraceContext` (`llm_gateway/models.py:107`). The two are structurally different models serving the same field name in different packages — not a bug, since the SDK is the dependency floor and cannot reference the Kernel's type, but worth a future adapter author's attention when step 6a builds the real conversion between a pack-facing `LLMRequest` and whatever the Kernel's `DispatchingLLMGateway.complete()` actually consumes.

**Verified:** `platform_sdk/tests/` 109 → 124 passed (15 new: `test_llm_models.py`, `test_llm_gateway_contract.py`); cross-boundary proof 13 → 15 passed; `mypy --strict` clean across 304 files; `ruff check`/`format` clean; root suite 816 → 818 passed, 11 skipped (unchanged); SE pack suite unchanged at 46; zero Kernel/pack source modified (`git status -- kernel/ capability_packs/` reports nothing).

---

## 6e. Step 5 — `PromptRegistry` Protocol built and proven by conversion, not identity (2026-07-29)

One `Protocol` in `platform_sdk/src/ai_os_sdk/contracts/prompt_registry.py`, at the shape 2a kept from the specification: `async def render(prompt_id, variables, *, version) -> RenderedPrompt`, `version` keyword-only and required. `RenderedPrompt` in `platform_sdk/src/ai_os_sdk/models/prompt.py`, narrowed to `prompt_id`/`version`/`content` — the real `PromptRenderResponse`'s exact three fields.

**Why this step's proof is shaped differently from steps 3 and 4's.** `Agent`, `Tool`, and `LLMGateway` were each narrowed *to* a shape a real Kernel class already has, so a single `isinstance` call proved compatibility. `PromptRegistry` is the opposite case: 2a deliberately *kept* the documented call style specifically because it is a better pack-facing API than the Kernel's own `PromptRenderRequest` envelope. No real class implements this signature, and building one would be step 6a's job, not this one's. What this step proves instead — the claim the 2a decision block's "the adapter conversion in step 6a is three lines" rests on — is that converting between the two shapes loses nothing in either direction.

**The proof:** `tests/unit/platform_sdk/test_prompt_registry_adapter_conversion.py`, 7 assertions, all passing. Two small, deliberately test-local functions (not shipped in `ai_os_sdk` — building the real adapter is step 6a's) perform the conversion:

- `sdk call → PromptRenderRequest`: every field maps one to one with no transformation (`prompt_id=prompt_id, version=version, variables=variables`); an empty `variables` mapping survives as empty on both sides; a value as rich as a nested dict inside `variables` survives untouched; the Kernel's own blank-`prompt_id` validator still fires after conversion, proving the conversion doesn't bypass existing validation.
- `PromptRenderResponse → RenderedPrompt`: every field maps one to one; a full request→(simulated real engine)→response→`RenderedPrompt` round trip preserves `prompt_id`/`version` exactly; no field is fabricated, defaulted, or silently drawn from the request instead of the response.

**Also proven, at the Protocol level (`platform_sdk/tests/test_prompt_registry_contract.py`):** the documented keyword call style itself — `await registry.render("requirements.analyze", {"requirement": "x"}, version="0.1.0")`, the exact one-line call the decision block cites as the reason this shape was kept — and that `version` is genuinely keyword-only: calling with it positional raises `TypeError` before any implementation code runs.

**Verified:** `platform_sdk/tests/` 124 → 142 passed (18 new: `test_prompt_models.py`, `test_prompt_registry_contract.py`); cross-boundary suite 15 → 22 passed (7 new); `mypy --strict` clean across 309 files (two `type: ignore` mismatches found and corrected — one now-unneeded, one pointing at the wrong error code); `ruff check`/`format` clean; root suite 818 → 825 passed, 11 skipped (unchanged skips); SE pack suite unchanged at 46; zero Kernel/pack source modified.

---

## 6f. Step 6 — `ToolInvoker` Protocol built and proven against real execution (2026-07-29)

One `Protocol` (`ToolInvoker`) in `platform_sdk/src/ai_os_sdk/contracts/tool_invoker.py`, plus `ToolDescriptor`/`ToolStatus`/`ToolResult` in `platform_sdk/src/ai_os_sdk/models/tool.py`, and the `platform.sandbox.run_command` tool concept (id constant + input/output JSON Schemas + a real `ToolDescriptor` instance, `PLATFORM_SANDBOX_RUN_COMMAND_DESCRIPTOR`) grounding `available_tools()` in a genuine, non-empty answer — all at the from-scratch shape 2a designed.

**Why this step's proof runs real commands through a real sandbox, not just constructed models.** `ToolInvoker` is a from-scratch design like `PromptRegistry` — no Kernel class satisfies it, so there's no `isinstance` proof. But unlike `PromptRegistry`, the whole point of `ToolResult`'s extension (`timed_out`/`truncated`) is to preserve a distinction a **real, executing** sandbox produces — proving that with hand-constructed `SandboxResult` values would only show the model accepts three different inputs, not that the real execution path genuinely produces three different outcomes. `tests/unit/platform_sdk/test_tool_invoker_sandbox_conversion.py` therefore runs the real `LocalSubprocessSandbox` three times — a clean `print('hello')`, a `time.sleep(5)` against a 0.3s timeout, and a 100,000-byte print against a 16-byte cap — and converts each real `SandboxResult` through a test-local reference function into a `ToolResult`.

**This real execution found two genuine bugs before they could reach step 6a — exactly what running the design was for.**

1. **The `ToolResult` model's own validator was wrong.** It required `exit_code` to be set whenever `timed_out` was `False`, on the assumption that only a real timeout leaves a process without a confirmed exit code. Running the truncation case disproved this: `sandbox/executor.py`'s own cap-breach path (`cap_exceeded` branch, `_finished_result(wait_task, None)`) kills the process as soon as the output cap is hit, and on this platform that kill consistently outraces the process's own exit — confirmed by direct probing across `max_output_bytes` values from 16 down to 1 byte, every run producing `exit_code=None` with `timed_out=False`. The validator was corrected to one direction only: `exit_code` must be `None` when `timed_out` is `True`; the reverse is not enforced, because it is false.
2. **A real circular import between `ai_os_sdk.errors` and `ai_os_sdk.models`.** `ToolResult.error` needs `StructuredError`, which lived in `ai_os_sdk.errors` — but `ai_os_sdk.errors.taxonomy` itself imports `ai_os_sdk.models.common.TraceContext`. Importing `ai_os_sdk.errors` first (as the sandbox-conversion test originally did) triggered `ai_os_sdk.models` package init, which reached `models.tool`, which needed `ai_os_sdk.errors` again, mid-initialization — `ImportError`. Fixed by relocating `ErrorCategory`/`StructuredError` into a new `ai_os_sdk.models.error` module (both remain fully public via `ai_os_sdk.errors.ErrorCategory`/`ai_os_sdk.errors.StructuredError`, re-exported unchanged) — the same fix shape already applied to `TrustTier` one step earlier, for the identical reason: a lower layer cannot import a higher one.

**A related, smaller layering fix, found while adding `ToolDescriptor`:** `TrustTier` was relocated from `ai_os_sdk.contracts.tool` (where step 3 first defined it) into `ai_os_sdk.models.tool`, since `ToolDescriptor` — a model — needed it too, and a model depending on a contract would invert the package's own layering. `ai_os_sdk.contracts.tool` re-exports it unchanged.

**Verified after both fixes:** `platform_sdk/tests/` 142 → 170 total across the two directories (26 in the cross-boundary suite, including the corrected truncation test, which now correctly asserts `FAILURE`/`exit_code=None` for the truncated case — not the originally-assumed `SUCCESS`); `mypy --strict` clean across 315 files (two more `type: ignore` issues found and fixed, plus a genuine `attr-defined` error from the `taxonomy.py` re-export requiring explicit `as`-import syntax under `--strict`); `ruff check`/`format` clean; root suite 825 → 829 passed, 11 skipped (unchanged skips); SE pack suite unchanged at 46; zero Kernel/pack source modified.

**One documented, deliberately unresolved nuance:** `ToolInvoker.invoke`'s own `timeout_seconds` keyword and `platform.sandbox.run_command`'s required `inputs["timeout_seconds"]` are two different timeouts — an outer, tool-agnostic ceiling and the specific sandboxed command's own required timeout. §5.6's decision block documents both; how a step 6a adapter reconciles the two when both are supplied is explicitly left to that step, not decided here.

---

## 6g. Step 6a — real Kernel-side SDK adapters built and proven against real underlying objects (2026-07-29)

Three adapter classes in the new `kernel/src/ai_os_kernel/sdk_adapters/` package (explicitly **not** inside `platform_sdk/`, since the SDK cannot depend on the Kernel — this is the boundary where the Kernel implements the SDK, the reverse dependency direction): `LLMGatewayAdapter` wraps `DispatchingLLMGateway`, `PromptRegistryAdapter` wraps `PromptEngine`, `ToolInvokerAdapter` wraps `SandboxExecutor`. `kernel/pyproject.toml` gained its first and only dependency change this entire initiative — `ai-os-sdk` — since the Kernel now genuinely *implements* SDK Protocols rather than merely resembling them.

**Every field mapping was verified against real source before writing, not assumed** — continuing the discipline of steps 4–6. `UsageRecord` and `ProviderCapabilities` are field-identical on both sides (by step 4's own design); `MessageRole`/`StopReason` map by `.value` between independent enum classes, matching the `TrustTier` precedent from step 6. `TraceContext` is a real, one-directional, documented narrowing: the SDK's 7-field canonical shape reduces to the Kernel `llm_gateway.models.TraceContext`'s 2 fields (`workflow_id`/`step_id`) — flagged as a discrepancy to resolve exactly here in step 4's own record (§6d), now resolved honestly rather than silently.

**A new, real gap surfaced while building `ToolInvokerAdapter`, not assumed in advance:** v1.0.0's `ToolInvoker.invoke(tool_id, inputs, *, timeout_seconds)` Protocol carries no trace or correlation parameter at all, yet a failing `ToolResult.error: StructuredError` requires a `TraceContext` (§4.4, required). Resolved by generating a fresh, real, per-invocation `TraceContext` via the Kernel's existing `ai_os_kernel.observability.trace.generate_trace_id()` (called twice, once per field, since no dedicated span-id generator exists) — explicitly **not** correlated with whatever workflow or step actually invoked the tool, since the Protocol provides nothing to correlate against. Recorded here as a concrete, evidenced input for whichever future step revisits §5.6's signature.

**The timeout-precedence decision step 6 explicitly left open (§6f) is resolved: the more restrictive of the two timeouts always wins.** `invoke()`'s own `timeout_seconds` and the tool's own required `inputs["timeout_seconds"]` can both be supplied; the effective timeout is `min(timeout_seconds, inputs["timeout_seconds"])` when an outer bound is supplied, or `inputs["timeout_seconds"]` directly otherwise (`None` means "no outer ceiling," not "no timeout at all" — the tool's own timeout is always required and always real). Chosen over "one is authoritative, reject on conflict" because it never rejects a call over a disagreement with an obvious safe resolution, and because it composes correctly with the Protocol's own stated intent for the outer parameter as "a generic, tool-agnostic ceiling" (`ai_os_sdk.contracts.tool_invoker`) — a ceiling is a `min()`, never an equality requirement. Proven with four real-execution tests measuring genuine `time.monotonic()` elapsed wall-clock time around real `time.sleep(5)` subprocess commands through `LocalSubprocessSandbox`, not by inspecting internal variable state: a tighter outer bound governs, a tighter inputs bound governs, no outer bound falls through to the inputs bound, and two generous bounds do not falsely trigger a timeout.

**Proven against real underlying objects, not mocks**, matching this initiative's established convention: `LLMGatewayAdapter` against a real `DispatchingLLMGateway` (`StaticRouter` + `StaticCapabilityNegotiator` + `EchoLLMGateway`, 8 tests) — including discovering, by real test failure rather than assumption, that `EchoLLMGateway` hardcodes its own `provider`/`model_id` self-description (`"echo"`/`"echo-1"`) regardless of registration key; `PromptRegistryAdapter` against a real `InMemoryPromptEngine` (5 tests); `ToolInvokerAdapter` against a real `LocalSubprocessSandbox` running genuine subprocesses (10 tests, including the 4 timeout-precedence tests above). None of the three are wired into `bootstrap.py`, `EntrypointLoader`, or any pack — that is step 6b (injection path) and steps 9–14 (migration); this step proves the adapters exist and work in isolation.

**Verified:** 23 new tests, all passing (`tests/unit/kernel/sdk_adapters/`); root suite 829 → 852 passed, 11 skipped (unchanged skips); SE pack suite unchanged at 46; `mypy --strict` clean across 323 files; `ruff check`/`format` clean; `git status` confirms only `kernel/pyproject.toml` and `uv.lock` modified among pre-existing tracked files, everything else strictly additive.

---

### 6a.1 Additional observations recorded during Step 2 (not review findings)

Two things found while building step 2, recorded here because they are inputs to step 2a rather than problems with this plan:

- **Three `TraceContext` definitions will now coexist.** `ai_os_kernel.observability.trace.TraceContext` (`trace_id` required; no `span_id`, no `run_id`) and `ai_os_kernel.llm_gateway.models.TraceContext` (`workflow_id`/`step_id` only) already exist, and **both docstrings explicitly name `platform_sdk.md` §4.1 as the canonical shape they are reduced slices of**. Step 2 creates that canonical version. Consolidating the two Kernel partials onto it is Kernel-side work for step 2a/6a to schedule — it is not in scope for step 2, which adds a type and changes no existing code.
- **`StructuredError.trace` is required by §4.4 but no raise site can supply one yet.** §4.4 marks `retry_after_seconds` and `details` as `| None` but *not* `trace`, so required is the faithful reading. Step 2 therefore keeps `AiOsError.trace` optional (a raise site deep in a library genuinely may not know it) while `StructuredError.trace` is required, with the conversion demanding a trace explicitly at the boundary. Whether that boundary discipline is workable in practice is a step-2a question.

---

## 6h. Step 6b — `PackContext` construction and the `EntrypointLoader` injection mechanism, resolved and proven (2026-07-29)

**The blocker, restated precisely.** `EntrypointLoader.load()` constructs every entrypoint with zero arguments (`cls()`) and is not modified by this step — its own docstring's stated reason still holds ("passing manifest-declared configuration into an entrypoint's constructor is real Capability Manager design work"). Every real pack agent today works around this by being zero-arg-constructible itself and lazily building its own real dependencies on first `execute()`, reaching into Kernel internals directly — five separate, hand-rolled copies of the identical shape. This step generalizes that shape into one real, reusable SDK mechanism, rather than adding a sixth copy or changing `EntrypointLoader`'s own contract.

**The mechanism: a new SDK Protocol, not a change to the loader.** `ai_os_sdk.contracts.PackContextReceiver` (`platform_sdk/src/ai_os_sdk/contracts/entrypoint_context.py`) declares one method, `bind_pack_context(context: Any) -> None`, that a zero-arg-constructible entrypoint implements to receive its real context once, before its first genuine use — the entrypoint itself decides what "before first use" means for its own lazy-build lock, exactly as `ArchitectureAgentEntrypoint._ensure_agent` already does today. Whoever constructs the entrypoint (today: nothing production-real; in a future migration step: `SqlAgentRegistry`, right after its own existing `EntrypointLoader.load()` call) checks `isinstance(loaded, PackContextReceiver)` and calls `bind_pack_context()` if so — `EntrypointLoader.load()`'s own signature and behavior are untouched by this step, proven by calling it exactly as `SqlAgentRegistry.resolve_agent` calls it today in the test below.

**Why `context: Any`, not `context: PackContext` — a real, documented dependency-direction constraint, not a shortcut.** The SDK is the dependency floor (`platform_sdk.md` §2 rule 1): it depends on nothing else. The real, richly-typed `PackContext` (§6, carrying `llm`/`prompts`/`tools` as real SDK Protocol instances) still lives on the Kernel side (`ai_os_kernel.capability_manager.pack_contract.PackContext`, extended additively this step — see below) because formally relocating the whole entry-point contract into `ai_os_sdk` is step 7's own explicitly scoped job, not this one's. Typing `PackContextReceiver`'s one parameter as `Any` lets the mechanism work today without inverting that dependency, and needs no change when step 7 gives `PackContext` its real SDK home — only the type used at each call site narrows then.

**`PackContext` extended additively, exactly as its own docstring already anticipated.** `ai_os_kernel.capability_manager.pack_contract.PackContext` gains `llm: LLMGateway | None`, `prompts: PromptRegistry | None`, `tools: ToolInvoker | None` (all typed against the real SDK Protocols from step 6a) — the identical "reduced slice now, additive later" precedent already used throughout this codebase, not a redesign. A new function, `ai_os_kernel.sdk_adapters.pack_context.build_pack_context`, constructs one from a set of declared permissions plus whichever real Kernel objects (`DispatchingLLMGateway`, `PromptEngine`, `SandboxExecutor`) are available, enforcing §6's own rule — "attributes are present only if the manifest declared the corresponding capability and it was granted" — concretely: `llm`/`prompts` together are granted only when `llm:invoke` is declared (the closed permission vocabulary has no separate `prompts:*` permission, and rendering a prompt has no purpose without also being able to call an LLM — a real, load-bearing reading recorded here as an input for whichever future step writes a formal permission-to-capability table into `platform_sdk.md` §6 itself); `tools`, only when `sandbox:execute` is declared. A permission declared without the matching real object supplied raises `ValueError` rather than silently downgrading to `None` — a caller granting a permission it cannot back is a configuration error.

**A real, load-bearing design decision made and recorded, not left implicit: build one `PackContext` per entrypoint's own declared permissions, never the pack's aggregate.** The real manifest (`capability_packs/software-engineering/manifest.yaml`) declares `permissions: [llm:invoke, sandbox:execute]` at the pack level, but each agent declares its own, narrower subset — `qa-test` declares `sandbox:execute` only (it makes no LLM call at all); `requirements-analyst`/`architecture` declare `llm:invoke` only (no sandboxed side effect). Building every agent's context from the pack's aggregate list instead of each agent's own would hand every agent in the pack every capability *any* agent in it was granted — exactly the over-provisioning §6 prohibits. `build_pack_context`'s own docstring records this explicitly.

**"Declared" stands in for "granted," honestly, not silently.** There is no independent runtime permission-grant step yet — Security Manager enforcement of roles/permissions (ADR-0023) is still 0%-built — so this step treats manifest declaration as the current stand-in for grant, the identical simplification already implicit everywhere else permissions are mentioned in this codebase.

**Proven end to end against a real test entrypoint, not a real migrated agent — exactly this step's own approved scope.** `tests/unit/kernel/sdk_adapters/_pack_context_injection_fixtures.py`'s `EchoCapabilityTestEntrypoint` is zero-arg-constructible, implements `PackContextReceiver`, and dispatches `execute()` to whichever real capability its inputs name. The test (`test_pack_context_injection.py`) calls the real `EntrypointLoader().load(...)` with that class's own dotted module path — the identical mechanism a manifest's `agents[].entrypoint` string would resolve — checks `isinstance(loaded, PackContextReceiver)`, calls `bind_pack_context()` with a real `build_pack_context(...)` result, and then genuinely calls through it three times: an Echo-backed LLM completion, a real prompt render, and a real sandboxed command (`print(...)` through `LocalSubprocessSandbox`). A fourth test confirms calling `execute()` before `bind_pack_context()` fails with a clear, named error, not a bare `AttributeError`. `build_pack_context` itself is proven against the real manifest's own declared permissions for `qa-test`/`architecture`/`build` (loaded through the real `ManifestLoader`, not hand-typed), confirming the exact asymmetric provisioning §6's rule requires.

**No blocker found; no fork required stopping.** `EntrypointLoader`'s zero-argument constraint is resolved cleanly by a caller-side `isinstance` check plus one standard method call — no change to `EntrypointLoader` itself, and no new capability demanded of it. The one real design fork this step did surface — whether the real, adapter-carrying `PackContext` should be built now inside `ai_os_sdk` (doing a slice of step 7's own explicitly scoped job early) or extended in place on the Kernel side (deferring the SDK relocation to step 7 as planned) — was resolved in the Kernel's favor and recorded above, not decided silently: it costs nothing this step needs (only test code touches the type, and test code is not bound by "a pack may only import `ai_os_sdk`"), and step 7's own job — relocating a class, now three fields richer, into its final home — remains additive, not a redesign, exactly as `pack_contract.py`'s own docstring already promised before this step touched it.

**Verified:** 12 new tests, all passing (`tests/unit/kernel/sdk_adapters/test_pack_context.py`, 7; `test_pack_context_injection.py`, 5) — combined with step 6a's 23, 35 total in `tests/unit/kernel/sdk_adapters/`; root suite 852 → **864 passed, 11 skipped** (unchanged skips, confirmed via a fresh full run); SE pack suite unchanged at **46 passed**; `mypy --strict` clean across 328 files; `ruff check`/`format` clean; `git status` confirms only `pack_contract.py`, `sdk_adapters/__init__.py`, and `contracts/__init__.py` modified among pre-existing tracked files, everything else strictly additive — no pack, `bootstrap.py`, `EntrypointLoader`, or `SqlAgentRegistry` source modified.

---

## 6i. Step 7 — `pipeline.py` relocated, `ContextService` landed, the entry-point contract formally moved into `ai_os_sdk` (2026-07-29)

**`pipeline.py` relocated from `capability_packs/software-engineering/src/ai_os_pack_software_engineering/pipeline.py` to `tests/integration/_delivery_pipeline.py`, resolving P4 in full.** Re-verified before moving, not merely trusted from §4.1's own earlier finding: zero pack source modules import it; its only two importers are `tests/integration/workflow_engine/test_delivery_pipeline.py` and `tests/integration/sandbox/test_delivery_pipeline_docker.py`, both already inside `tests/`. `_DEFINITION_PATH`'s own relative-path computation was recomputed for the new location (two directory levels shallower than before) and re-verified to resolve to the real, unmoved `capability_packs/software-engineering/workflows/delivery_pipeline.yaml`. Named with a leading underscore, matching `tests/integration/_postgres_fixture.py`'s own convention, so pytest never collects it as a test module.

**Proven by a real wheel build, not by inspection alone.** `python -m uv build --package ai-os-pack-software-engineering` followed by inspecting the resulting `.whl`'s own file list confirms `pipeline.py` is genuinely absent from the shipped package — no `packages = [...]` exclusion pattern was needed, since the file simply no longer lives inside `src/ai_os_pack_software_engineering/`. A follow-up `grep` across the pack's entire `src/` tree confirms zero remaining `sqlalchemy` imports anywhere in pack source — `pipeline.py` was the only module that imported `sqlalchemy.ext.asyncio`, and it is gone. `pack_contract_suite` check 7 (forbidden imports, still unbuilt — step 8's own job) can now genuinely pass on this pack once built, rather than failing forever as `pipeline.py`'s continued presence in the shipped wheel would have guaranteed.

**Both relocated integration tests re-run from the new location and pass unchanged — including the real, live Docker-backed one.** `test_delivery_pipeline.py`'s deterministic tier (real Postgres, `LocalSubprocessSandbox`) passed; its opt-in live tier skipped as expected (no API key set). `test_delivery_pipeline_docker.py` — this session's Docker daemon was reachable — ran for real against `DockerSandbox` and passed, re-proving the network-isolation/filesystem-containment guarantee from inside a file the relocated pipeline itself generated and ran. No behavior change to the pipeline itself; only its module path and one internal path computation changed.

**`ContextService` boundary models landed, narrowed against the real, already-reduced Kernel shape — a genuine, dated reconciliation decision this step made, even though §5.3 had none recorded before it.** Unlike `PromptRegistry`/`ToolInvoker` (from-scratch designs with nothing to reconcile against), a real `ai_os_kernel.context_manager.models` already exists, already reduced from this document's own full documented shape, and already consumed by two real pack agents (`agents/documentation.py`, `agents/verification.py`) for type annotation. `ContextRequest`/`AssembledContext` are narrowed to match that real reduction (dropping `required_types`/`query`/`filters`/`index_generation` — each absent because nothing in this codebase backs it yet, not merely unused); `ContextItem` is kept at full parity (the Kernel already implements it completely); `SourceRef` is a from-scratch design matching the real Kernel's own (this document names no fields for it); `SourceType` is narrowed to the one real resolver. Landed in `ai_os_sdk.models.context` (models) and `ai_os_sdk.contracts.context_service` (the `ContextService` Protocol) — full reconciliation block recorded in `platform_sdk.md` §5.3. **Structural compatibility is proven against the real, already-working `DefaultContextManager`** via `isinstance`, the identical discipline already applied to `Agent`/`Tool`/`LLMGateway`, even though no Kernel-side adapter wraps it — there is no real caller for `.assemble()` through the SDK Protocol yet (`AgentStepExecutor` already assembles context itself), so building one now would have no consumer to justify it.

**The entry-point contract — `PackContext`/`CapabilityPack`/`PackRegistration`/`HealthReport` — relocated from the Kernel into `ai_os_sdk.contracts.capability_pack`, additively, exactly as step 6b's own record (§6h) already announced this step would do.** The Kernel's own `ai_os_kernel.capability_manager.pack_contract` is now a real re-export (`from ai_os_sdk.contracts.capability_pack import ...`), not a second, parallel definition — proven by identity (`kernel_pack_contract.PackContext is ai_os_sdk.contracts.PackContext`), not merely by import success. This is why real pack source (`ai_os_pack_software_engineering.pack`, still importing the Kernel path) needed **zero changes** this step: migrating that one import onto `ai_os_sdk` directly is step 14's own explicitly scoped job, not this one's, and the shim is what makes deferring it safe.

**A real, discovered layering correction, made here rather than silently — this document's own `models/__init__.py` had speculatively planned a `pack.py` under `ai_os_sdk.models` before this step was ever built, and that plan was wrong.** Every other real boundary model in this SDK is pure data; none types a field against another Protocol, so `models/` never needs to import `contracts/`, and every real `contracts/*.py` module imports *from* `models/`, never the reverse. `PackContext`/`PackRegistration` break that pattern by their very nature — `PackContext.llm: LLMGateway`, `PackRegistration.agents: dict[str, Agent]` — so placing them in `models/` would have forced `models/` to import `contracts/`, inverting every real dependency direction this package established before step 7 ever encountered the conflict. Landed in `contracts/capability_pack.py` instead, alongside the `CapabilityPack` Protocol that already needs them as its own method's parameter/return types; `models/__init__.py`'s own docstring is corrected in this same step, not left describing a file this package will never actually add. `PackRegistration.agents`/`tools` are also now typed against *this SDK's own* `Agent`/`Tool` Protocols rather than the Kernel's internal ones — proven, not merely assumed, that a real Kernel agent (`ArchitectureAgentEntrypoint`) still validates against the relocated model, and that the pack's own real `test_pack.py` suite (which exercises this exact substitution through a genuine `activate()` call) still passes unmodified.

**No blocker found.** Every relocation this step performs is additive or a pure re-export; nothing required a redesign, and nothing required touching `EntrypointLoader`, `bootstrap.py`, `SqlAgentRegistry`, or any real pack source.

**Verified:** the pack's wheel build confirms `pipeline.py` and its `sqlalchemy` import are both genuinely gone; both relocated integration tests pass from their new location (including the real Docker-backed one, this session's daemon being reachable); 5 new cross-boundary tests pass (`tests/unit/platform_sdk/test_context_service_and_capability_pack.py`); root suite 864 → **869 passed, 11 skipped** (unchanged skips — the pipeline tests moved, not multiplied); SE pack suite unchanged at **46 passed**; `mypy --strict` clean across 332 files; `ruff check`/`format` clean; `git status` shows a clean rename (`pipeline.py` → `tests/integration/_delivery_pipeline.py`) plus the expected modified/new files — nothing unexpected, no stray build artifacts.

---

## 6j. Step 8 — `pack_contract_suite` check 7 and its expiring waiver, built and proven both ways (2026-07-29)

**Check 7 built exactly as §4's own reasoning specified: a pure AST import-scanner, dependent on nothing else in this plan.** `ai_os_sdk.testing.forbidden_imports.scan_pack_source` walks every `*.py` file under a pack's own `src/` tree with `ast.walk`, classifying each `Import`/`ImportFrom` node's root module against five denylists: `ai_os_kernel` (always forbidden — the exact mechanism that would have caught this pack's own months-long, hand-recorded violation), `ai_os_services`, another pack's own `ai_os_pack_*` package (excluding the pack's own), a maintained provider-SDK denylist (`anthropic` — the one this codebase's own Kernel layer really depends on — plus `openai`/`google.generativeai`/`cohere`/`mistralai`/`boto3`), and a maintained database-driver denylist (`sqlalchemy`/`asyncpg` — this codebase's own real drivers — plus their common siblings).

**A sixth category, deliberately broader than §9 item 7's literal text, recorded as a real decision rather than silently narrowed.** "No HTTP client to a provider" cannot be verified by import scanning alone — a raw `httpx`/`requests` import says nothing about which host it eventually reaches, and that requires tracing call sites, not import statements. This step reads it as "no direct HTTP client import at all" (`httpx`/`requests`/`aiohttp`/`urllib3`), safely over-inclusive relative to the literal text and consistent with the surrounding rule's own intent: a compliant pack reaches external services only through the SDK's own Protocols, never a raw transport library, to any destination.

**A real, discovered portability bug, found and fixed before this step's own proof ever completed:** the first version of the report renderer used emoji markers (⚠️/❌/✅); running the real CLI on this Windows development environment crashed with `UnicodeEncodeError` on the very first real run, because the default console codepage (`cp1252`) cannot encode them. CI runs on Ubuntu with a UTF-8 locale, where the crash would never have shown up — exactly the "works in CI, breaks for a contributor running it locally" gap this project's own discipline exists to catch. Fixed by switching every marker to plain ASCII (`[PASS]`/`[FAIL]`/`[WAIVED]`/`[WAIVER ACTIVE]`), with a regression test (`render_report(...).encode("ascii")`) added specifically so it cannot silently regress.

**The waiver mechanism, built to the letter of this step's own constraint: loud, dated, step-bound, and exact, never a silent allowlist.** `ai_os_sdk.testing.waiver.load_waiver` reads a real YAML file — `pack_contract_waiver.yaml`, checked into the pack's own root directory, sibling to `manifest.yaml`, impossible to miss — naming the check it waives, a human-readable `reason`, a `plan_reference` back to this document, a `granted` date, an `expires_at_step` (an integer step number from this plan's own 18-step sequence, not a date, since a step-bound expiry is enforceable by this same tooling and cannot silently drift the way a date could if migration ran long or short), and the **exact** list of dotted module paths it covers. `apply_waiver` matches strictly by that module list — a genuinely new violation in an unlisted module, even inside an already-waived pack, still fails immediately (proven by `test_a_new_violation_in_an_unlisted_module_still_fails`). Every application is reported explicitly, listing each waived violation by file and line, even when the overall result is a pass — a CI run that succeeds because of an active waiver looks visibly different from one that succeeds because there was genuinely nothing to waive.

**The real waiver, for the real pack, built from the scanner's own real output — not hand-typed and hoped correct.** Running the scanner against `capability_packs/software-engineering/src/` found **44 real `ai_os_kernel` imports across exactly 6 modules** (the five agent entrypoints plus `pack.py`'s own `CapabilityPack` implementation) — confirmed `pipeline.py` contributes zero, per step 7's own relocation. `pack_contract_waiver.yaml` lists exactly those six modules, `expires_at_step: 14` (the step that migrates `pack.py`'s entry point and removes this waiver, per this plan's own step table), and a `reason` naming steps 9–14 as the real path to zero.

**Proven both ways — genuinely fails without the waiver, genuinely passes with it — by an automated regression test, not only a one-off manual command.** `tests/unit/platform_sdk/test_check_import_boundaries_cli.py` builds a synthetic pack under `tmp_path` with a real `ai_os_kernel` import: `main()` returns 1 with no waiver file present, and 0 once a matching waiver is added — the direct before/after proof, re-run on every future test run, never mutating the real repository's own waiver file to prove it (a live manual `mv`/restore of the real file was used once, by hand, to draft this record, then replaced by this durable, synthetic version). A second real test confirms `main()` returns 0 against the actual, checked-in pack and its actual, checked-in waiver today.

**Wired into CI for real, with zero workflow-file logic change.** `.github/workflows/ci.yml`'s own `contract` job already had a step gated on `hashFiles('scripts/check_import_boundaries.py') != ''` — written before this step existed, anticipating it exactly. Creating `scripts/check_import_boundaries.py` (which discovers every pack under `capability_packs/` with a real `pyproject.toml` + `src/` tree, reading each one's own top-level package name from its own `pyproject.toml` rather than a hardcoded name — a second real pack is checked automatically the day it exists) made that gate's condition true; the only workflow edit was replacing one stale comment ("the script does not exist yet"). **Removing the waiver at step 14 is a one-file deletion, not a redesign or a code change of any kind** — `pack_contract_waiver.yaml` simply stops existing, `load_waiver` returns `None`, and every remaining import in the (by-then-migrated) pack is enforced unconditionally.

**No blocker found.** The check needed no `PackContext`/`CapabilityPack` semantics to build, exactly as §4 predicted; the waiver mechanism is real, generic infrastructure (any future pack can use the identical file format), not a one-off hack scoped to this one pack.

**Verified:** 29 new tests, all passing (`test_forbidden_imports_check.py` 13, `test_waiver.py` 13, `test_check_import_boundaries_cli.py` 3); `mypy --strict` clean across 339 files (`scripts/` added to the configured scope, with `scripts/__init__.py` added so mypy and this file's own `scripts.check_import_boundaries` import path agree on one module identity); `ruff check`/`format` clean; the real scanner run against the real pack finds exactly 44 violations across exactly 6 modules, matching the waiver file exactly.

---

## 6k. Step 9 — `qa-test` migrated, the first real use of the step 6b injection mechanism, two real gaps found and resolved (2026-07-29)

**The migration itself, mechanically.** `agents/verification.py`'s `TestAgentEntrypoint` now implements `ai_os_sdk.contracts.Agent` and `ai_os_sdk.contracts.entrypoint_context.PackContextReceiver` only. The constructor's `sandbox: SandboxExecutor | None` parameter is gone entirely; `bind_pack_context(context)` stores the injected context, and `execute()` calls `self._context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, {...})` instead of constructing a real `SandboxedCommandTool` directly over a Kernel `SandboxExecutor`. Zero `ai_os_kernel` imports remain in this module — confirmed by the real scanner, not assumed (`test_qa_test_is_migrated_and_contributes_zero_violations`).

**Finding 1 — the real `inputs["context"]` object is not, and cannot yet be, an instance of the SDK's own `AssembledContext`.** `AgentStepExecutor` (unmigrated Kernel code, out of this step's scope) still constructs and passes through a real `ai_os_kernel.context_manager.models.AssembledContext` — a different, unrelated Python class from `ai_os_sdk.models.context.AssembledContext`, even though step 7 narrowed the two to identical fields. A migrated agent cannot import the Kernel class to `isinstance`-check against it, and `isinstance` against the SDK's own model would always be `False` against the real object the Workflow Engine actually sends — this is not a hypothetical edge case: it is the *only* real path this agent's own `context` fallback branch is ever exercised through in production, since `AgentStepExecutor._invocation_inputs()` has no per-step mechanism to hand named fields like `workingDirectory` directly (the same documented gap step 3's own record already named). **Resolution:** duck-type the context object (`getattr(context, "items", None)`, then read each item's own `.content`) instead of a nominal `isinstance` check — consistent with the SDK's own `Agent` Protocol docstring, which already documents `inputs` as carrying "rich objects, not only strings" without naming their concrete types. Not a workaround; the correct answer given what the SDK Protocol actually promises.

**Finding 2 — `SqlAgentRegistry` never calls `bind_pack_context()` after resolving an entrypoint, and one real, *unconditionally-run* test exercises exactly that gap.** `tests/integration/workflow_engine/test_verification_agent_pack.py` resolves `qa-test` through the real `SqlAgentRegistry`/`EntrypointLoader` path and calls `execute()` directly — no API-key gate, since this agent makes no LLM call, so this test runs in every normal suite pass, not only opt-in-live ones. Before this step's own fix, that would have meant `resolved.execute(...)` immediately raising `TestInstructionError` (execute-before-bind), a real, live regression, not a hypothetical one. **Resolution, recorded rather than silently patched:** the test itself now calls `bind_pack_context()` immediately after `resolve_agent()`, standing in for the production wiring that genuinely does not exist yet — `SqlAgentRegistry` itself is unmodified, exactly as this step's own constraints require. **This is the concrete, discovered scope of what step 6b's mechanism does not yet cover**: it proves the mechanism works when *something* calls it; nothing in this codebase's real production call path calls it yet. Recorded here as a real input for whichever future step wires it in for real (a natural candidate: `SqlAgentRegistry.resolve_agent()` itself, checking `isinstance(loaded, PackContextReceiver)` and injecting a `PackContext` built from the row's own declared `required_permissions`) — not scoped into this step, which migrates one agent's own code, not the registry that resolves it.

**A related, smaller capability loss, also discovered and resolved in the same test.** The pre-migration agent exposed its resolved `sandbox: SandboxExecutor` as a public attribute, letting a caller introspect `resolved.sandbox.python_command` to discover which real backend `AIOS_SANDBOX_BACKEND` selected (needed to build a correct, non-hardcoded `runCommand` for a test file). The migrated agent's only sandbox access is the injected `context.tools`, an opaque `ToolInvoker` with no such introspection — by design, the Protocol grants only `invoke()`/`available_tools()`. Resolved by having the test build its own `SandboxExecutor` first (via the identical `build_default_sandbox_executor()` the agent used to call internally), reading `python_command` from that reference directly, and injecting the same instance — the caller already holds the one reference it needs; the agent never needed to re-expose it.

**No blocker in the injection mechanism's own design.** Both findings are real, but neither is a defect in what step 6b built: zero-arg construction followed by a caller-side `bind_pack_context()` call, before first use, worked exactly as designed everywhere it was actually called. What was missing is a *caller* in one specific, real production path (`SqlAgentRegistry`) — a wiring gap step 6b's own record already flagged as deliberately out of its scope, now concretely confirmed rather than left theoretical.

**Proven: zero behavior change to the pipeline's real output.** Both real Delivery Pipeline end-to-end tests — the deterministic Postgres-backed one and the live Docker-backed containment proof — pass unchanged through the migrated agent, producing byte-identical `passed`/`exitCode`/`output` results to before this step. The pack's own local test suite (14 tests, 2 new) and the SqlAgentRegistry-backed integration test (2 tests, both unconditionally run) all pass.

**Verified:** `pack_contract_waiver.yaml` reduced from 6 modules/44 violations to 5 modules/40 violations — `agents.verification` removed entirely; check 7 re-run confirms it genuinely passes for that module, unwaived, and the remaining 5 stay correctly waived; root suite 898 → **899 passed, 11 skipped** (unchanged skips); SE pack suite 46 → **48 passed** (2 new tests); `tests/unit/platform_sdk/` 60 → **61 passed** (1 new test); `mypy --strict` clean across 339 files; `ruff check`/`format` clean; `git status` shows only the expected files modified, no stray artifacts.

---

## 6l. Step 9a — `SqlAgentRegistry`/`SqlToolRegistry` genuinely wired to `bind_pack_context()`, closing the repeatable gap step 9 found (2026-07-29)

**Why this step exists at all, inserted rather than deferred to step 10.** Step 9's own migration of `qa-test` found that `SqlAgentRegistry.resolve_agent()` never calls `bind_pack_context()` after resolving an entrypoint — a real, unconditionally-run integration test (`test_verification_agent_pack.py`, no API-key gate) proved it live, not hypothetically. Steps 10–13 each migrate one more real agent; each would have hit the identical gap and each would have needed the identical test-side workaround, four more times, before anyone fixed the actual cause. Fixing it once, here, before the next migration, is strictly less total work than fixing it once per remaining step — the product owner's own framing for inserting this step.

**The fix, mechanically — `SqlAgentRegistry.resolve_agent()` and `SqlToolRegistry.resolve_tool()`, `EntrypointLoader` itself untouched.** Both methods now select `required_permissions` (already a real, existing `catalog.agents`/`catalog.tools` column) and `packs.version` alongside what they already selected. After the existing `isinstance(loaded, Agent)`/`isinstance(loaded, Tool)` structural check succeeds, a new shared helper, `_bind_pack_context_if_receiver`, checks `isinstance(loaded, PackContextReceiver)` — a true no-op for every not-yet-migrated entrypoint today — and, if it implements the Protocol, calls `build_pack_context()` with *that row's own* `required_permissions` (never the pack's aggregate — the identical "no over-provisioning" rule `build_pack_context` itself already enforces) and injects the result via `bind_pack_context()`, before the entrypoint is ever returned to a caller. Both registry classes gained optional, keyword-only `llm_gateway`/`prompt_engine`/`sandbox` constructor parameters to supply the real Kernel objects `build_pack_context` needs.

**`sandbox` defaults to a real object (`build_default_sandbox_executor()`); `llm_gateway`/`prompt_engine` deliberately do not.** Unlike the sandbox, there is no existing "build the one true default" factory for either — every real agent that needs one composes it bespoke, reading `config/llm.yaml` and a real secret provider (see `architecture.py`'s own `_build_real_service`). Inventing one here, ahead of any real caller needing it, would be exactly the kind of speculative composition this project's own discipline avoids. Left `None` by default: `build_pack_context` raises a clear, real error the moment a resolved entrypoint's own permissions actually need one that is missing, rather than silently proceeding — and step 10, "the first agent needing real gateway injection," is precisely where a real caller will need to supply them for the first time.

**A real, discovered detail: `bootstrap.py` does not construct `SqlAgentRegistry` at all yet.** Confirmed by search, not assumed — this class is exercised only by tests today. This step's own constructor changes are therefore genuinely risk-free to every real, running system; the only real callers affected are the test files updated in this same step.

**Both of step 9's smaller findings reconfirmed, not left assumed.** The `AssembledContext` duck-typing gap is unrelated to this step's own fix — `AgentStepExecutor` (unmigrated, out of scope) still constructs a Kernel-typed context regardless of who resolves the agent, so duck-typing remains the correct, permanent answer, not a symptom of the registry gap this step closes. The `python_command` introspection question generalizes cleanly one layer up: `test_verification_agent_pack.py` now builds its own `SandboxExecutor` and hands it to `SqlAgentRegistry`'s own new `sandbox=` override, keeping the same reference for `python_command` — the identical "the caller already holds the one reference it needs" principle, now proven to compose across two layers of caller, not merely one.

**Test-side proof: the manual workaround from step 9 is gone entirely.** `test_verification_agent_pack.py` no longer imports `build_pack_context` or calls `bind_pack_context()` anywhere — both tests resolve `qa-test` through the real, unmodified `SqlAgentRegistry` and it is genuinely, immediately usable. A new unit test file (`test_registry_pack_context_injection.py`) proves the shared helper's own logic in isolation, with no database: a non-receiver is a true no-op; a receiver is genuinely bound with a real `PackContext`; a receiver's own declared permissions govern exactly what it gets (no over-provisioning even at this layer); a declared-but-unbacked permission raises the correct error family (`AgentRegistryError` for agents, `ToolRegistryError` for tools).

**Zero regression, confirmed across every already-existing `SqlAgentRegistry`/`SqlToolRegistry`-backed test, not only `qa-test`'s own.** All 8 files using either registry (`test_verification_agent_pack.py`, `test_delivery_pipeline.py`, `test_requirements_analyst_agent_pack.py`, `test_documentation_agent_pack.py`, `test_registry.py`, `test_build_agent_pack.py`, `test_architecture_agent_pack.py`, `test_pack_lifecycle.py`) were re-run together — the four still-unmigrated real agents (`architecture`, `build`, `requirements-analyst`, `documentation`) resolve and run exactly as before, since `_bind_pack_context_if_receiver` is a true no-op for each of them.

**No blocker found.** `EntrypointLoader` needed no change, exactly as required; the fix is entirely additive caller-side logic in the two registry classes.

**Verified:** 5 new unit tests (`test_registry_pack_context_injection.py`), all passing; the 8 files using either registry (33 tests total, 5 correctly skipped as opt-in-live) all pass unchanged; the real Delivery Pipeline end-to-end tests (deterministic + live Docker) still pass; root suite 899 → **904 passed, 11 skipped** (unchanged skips); SE pack + `tests/unit/platform_sdk/` combined unchanged at **109 passed**; `mypy --strict` clean across 340 files; `ruff check`/`format` clean; `git status` shows only the expected files modified.

---

## 6m. Step 10 — `requirements-analyst` migrated, the lazy-build lock found obsolete, a real circular import found and fixed (2026-07-29)

**The migration itself, mechanically.** `agents/requirements_analyst.py`'s `RequirementsAnalystAgentEntrypoint` now implements `ai_os_sdk.contracts.Agent` and `PackContextReceiver` only. It no longer builds a Kernel `PromptedAgent`/`PromptedCompletionService` at all — `execute()` now directly replicates `PromptedCompletionService.complete_from_prompt`'s own real logic (render via `self._context.prompts.render(...)`, then complete via `self._context.llm.complete(...)`) using only SDK models and Protocols. Zero `ai_os_kernel` imports remain — confirmed by the real scanner.

**The real tension this step was explicitly scoped to find: the lazy-build lock is not in conflict with `bind_pack_context()` — it is simply obsolete for a fully-migrated agent, and this module no longer carries it.** `ArchitectureAgentEntrypoint`'s own established pattern (`_ensure_agent()` under an `asyncio.Lock()`, building a real service lazily on first `execute()`) existed for one reason: `EntrypointLoader`'s zero-arg `cls()` construction gives a real, async-composed dependency nowhere honest to be built except lazily, guarded against a concurrent double-build race. The step 6b/9a mechanism removes the reason the guard existed at all: `bind_pack_context()` is a plain, synchronous method, called exactly once by the resolver (`SqlAgentRegistry`, as of step 9a) *before* the entrypoint is ever handed to anything that could call `execute()` — there is no concurrent-first-use race to guard against, because composition moved from "lazily, racing its own first use" to "eagerly, by the caller, strictly before use is possible." This is a real simplification, proven by test (`test_concurrent_execute_calls_all_succeed_independently` — five concurrent `execute()` calls, no lock, no shared build state, correct independent results every time), not merely asserted.

**A real, discovered, unavoidable capability loss, recorded rather than silently dropped: real Anthropic completions from this agent are no longer recorded to `evaluation.llm_calls`.** `build_anthropic_prompted_completion_service` always wired a real `SqlLLMCallRecorder`; Platform SDK v1.0.0 has no `Telemetry`/`TraceabilityService` surface at all (both explicitly deferred past v1.0.0, `platform_sdk.md` §2), so there is no SDK-sanctioned way to preserve this without reintroducing exactly the forbidden `ai_os_kernel` import this migration removes. This does not change the agent's own visible output — only a real, silent loss of observability data, recorded here as a concrete input for whichever future step adds a real Telemetry surface to the SDK.

**`workflow_id`/`step_id` still faithfully reach the real per-workflow budget ceiling, even though this agent can no longer build a Kernel `TraceContext` directly.** The SDK's own `TraceContext` requires `trace_id`/`span_id` (the SDK's canonical 7-field shape); this module generates fresh ones per call with the stdlib's own `uuid.uuid4().hex` — the identical scheme `ai_os_kernel.observability.trace.generate_trace_id()` uses, reimplemented with zero `ai_os_kernel` import, since `LLMGatewayAdapter`'s own already-proven conversion (step 6a) narrows the SDK's `TraceContext` down to the Kernel's `workflow_id`/`step_id`-only one before it ever reaches `DispatchingLLMGateway`'s real budget enforcement — `trace_id`/`span_id` are functionally inert once converted, but required by the SDK model regardless.

**A real, previously-latent circular import, introduced by step 9a and surfaced by this step's own new test file, found and fixed.** `ai_os_kernel.workflow_engine.registry`'s new (step 9a) module-level `from ai_os_kernel.sdk_adapters.pack_context import build_pack_context` transitively re-enters `ai_os_kernel.workflow_engine` itself: `pack_context` imports `capability_manager.pack_contract`, which triggers `capability_manager`'s own package init, which imports `capability_manager.models`, which imports `workflow_engine.pack_state` — and importing any submodule of `ai_os_kernel.workflow_engine` requires that package's own `__init__.py` to finish first, which re-imports `registry.py` while it is itself still mid-import. This went undetected in step 9a because every test run there happened to import `ai_os_kernel.workflow_engine.*` before `ai_os_kernel.sdk_adapters.pack_context`; this step's own new `test_requirements_analyst.py` imports `ai_os_kernel.sdk_adapters.pack_context` first, triggering the cycle for real. **Fixed by moving `build_pack_context`'s own import to function scope inside `_bind_pack_context_if_receiver`** — genuinely safe, since nothing can call `resolve_agent`/`resolve_tool` before the whole module system has already settled. Verified against four distinct import orderings, including `import ai_os_kernel.bootstrap` itself (the real composition root, which imports nearly everything).

**A real, discovered consequence of step 9a, proven here rather than assumed: `SqlAgentRegistry`'s own deterministic resolution test now genuinely requires a real `llm_gateway`/`prompt_engine`.** This agent's `catalog.agents` row declares `llm:invoke`; step 9a's own injection logic (§6l) raises `AgentRegistryError` if a resolved entrypoint's own declared permissions require a capability the registry has no real object to back — supplying none would now make even the *deterministic* resolution test fail, not silently succeed. `test_requirements_analyst_agent_pack.py`'s own deterministic tier now supplies a real, Echo-backed pair; its opt-in live tier builds the real Anthropic composition directly (moved out of the agent, into this test file's own composition root, mirroring the identical relocation `qa-test`'s own sandbox composition underwent in step 9).

**No blocker in the injection mechanism's own design.** Every finding above is either an obsolescence (the lock), an unavoidable, already-precedented capability loss (call recording), or a real bug this step's own testing surfaced and fixed (the circular import) — none is a defect in what steps 6a/6b/9a built.

**Verified:** the pack's own local test suite (`capability_packs/software-engineering/tests/test_requirements_analyst.py`, 7 tests, all passing, up from 4 — including 2 new: execute-before-bind and concurrent-execute-without-a-lock); `test_requirements_analyst_agent_pack.py` (deterministic passes, live correctly skipped: 1 passed, 1 skipped); zero regression across every `SqlAgentRegistry`/`SqlToolRegistry`-backed test re-run together (39 passed, 5 correctly skipped); the real Delivery Pipeline end-to-end tests (deterministic + live Docker) still pass unchanged (this agent is not yet chained into `se.delivery_pipeline`, so this is a regression check on the registry/import-graph change, not a direct exercise of this agent); root suite (`testpaths = ["tests"]`, which excludes `capability_packs/` entirely) **904 passed, 11 skipped** — numerically unchanged from step 9a, since the only root-visible edit this step is `test_forbidden_imports_check.py`'s two renamed-in-place tests (net zero); SE pack suite 48 → **51 passed** (the 3 net new tests in `test_requirements_analyst.py`); `tests/unit/platform_sdk/` unchanged at **61 passed**; `pack_contract_waiver.yaml` reduced from 5 modules/40 violations to 4 modules/32 — `agents.requirements_analyst` removed entirely; check 7 re-run confirms it genuinely passes for that module, unwaived; `mypy --strict` clean across 340 files; `ruff check`/`format` clean.

---

## 6n. Step 11 — `architecture` migrated, identical pattern to step 10, a real Delivery Pipeline test fix needed (2026-07-29)

**The migration itself, mechanically.** `agents/architecture.py`'s `ArchitectureAgentEntrypoint` now implements `ai_os_sdk.contracts.Agent` and `PackContextReceiver` only — a direct, deliberate structural copy of `requirements_analyst.py`'s own step-10 shape, differing only in output field name (`content`, not `analysis`) and error class name (`ArchitectureAgentInputError`). It no longer builds a Kernel `PromptedAgent`/`PromptedCompletionService`, no longer takes a `service_factory` constructor override, and no longer carries the `asyncio.Lock()`-guarded lazy build. Zero `ai_os_kernel` imports remain — confirmed by the real scanner.

**No new tension between the lazy-build lock and injection — this was already resolved at step 10, and this step only confirms it generalizes.** `architecture.py`'s pre-migration docstring was the original source of the "lazy-build under a lock" reasoning `requirements_analyst.py` used to cite back to; migrating the *second* agent that ever used this pattern, onto the *same* injection mechanism, produced the identical outcome for the identical, already-understood reason — not a fresh finding to re-derive. This step's own report is proof the step-10 finding was general, not agent-specific.

**The one real, new thing this step surfaced: `architecture` is chained into the real Delivery Pipeline, and neither prior migration was.** `qa-test` (step 9) and `requirements-analyst` (step 10) are both real, proven agents but neither is wired into `se.delivery_pipeline`'s own four-step chain (Architecture → Build → Test → Documentation) — so their own migrations never had to touch `test_delivery_pipeline.py`/`test_delivery_pipeline_docker.py`. `architecture` is the *first* step of that real chain. Migrating it broke both files' own `InMemoryAgentRegistry` construction (`ArchitectureAgentEntrypoint(service_factory=...)` — a constructor parameter that no longer exists) and the deterministic run failed loudly with `TypeError` on the first attempt — caught immediately by running these tests, not discovered later. **Fixed** by adding `_architecture_agent_with_prompt()` to both files (mirroring `_test_agent_with_sandbox()`'s own already-established zero-arg-construct-then-`bind_pack_context()` pattern) and, in `test_delivery_pipeline.py`'s own opt-in live tier, supplying real `llm_gateway`/`prompt_engine` objects to `SqlAgentRegistry(engine, ...)` — the same real, discovered consequence of step 9a's own enforcement logic step 10 already proved for `requirements-analyst`'s own pipeline test, now proven a second time for a genuinely different caller (the four-agent pipeline trigger, not a single-agent resolution test).

**The identical, already-recorded capability loss recurs, confirming it as structural rather than a one-off.** Real Anthropic completions from `architecture` are no longer recorded to `evaluation.llm_calls`, for the identical reason recorded at step 10 (`platform_sdk.md` §2's own Telemetry deferral). Because this is the *second* LLM-calling agent to lose this, and `build`/`documentation` will be the third and fourth once migrated (steps 12/13), this is no longer treated as a per-step footnote — it is now tracked in `feature_inventory.md`'s own new **§6a Known Regressions** section, specifically so it is visible as a real, compounding gap once all four LLM-calling agents are migrated, not re-discovered independently at each step.

**No blocker in the injection mechanism's own design.** The one real fix this step needed (the Delivery Pipeline tests) was a consequence of `architecture` being pipeline-chained, not a defect in `bind_pack_context()`/`build_pack_context()`/`SqlAgentRegistry`'s own step 6b/9a machinery, which needed zero changes.

**Verified:** the pack's own local test suite (`capability_packs/software-engineering/tests/test_architecture_agent.py`, 7 tests, all new — extracted from `test_pack.py`'s own prior, now-removed `service_factory`-based tests, mirroring `test_requirements_analyst.py`'s own step-10 split from this pack's shared `test_pack.py`); SE pack suite 51 → **54 passed** (net +3: 6 tests removed from `test_pack.py`, 7 tests added in the new file, plus `test_pack.py`'s own 2 pack-level tests unaffected); `test_architecture_agent_pack.py` (deterministic passes with an Echo-backed `llm_gateway`/`prompt_engine`, live correctly skipped: 1 passed, 1 skipped); both real Delivery Pipeline end-to-end tests (deterministic + live Docker) pass after the fix described above (2 passed, 1 skipped — the live tier); zero regression across every `SqlAgentRegistry`/`SqlToolRegistry`-backed test re-run together (29 passed, 5 correctly skipped); the real scanner confirms `agents.architecture` contributes zero violations and the remaining 3 modules/24 violations match the reduced `pack_contract_waiver.yaml` exactly; root suite (`testpaths = ["tests"]`) **903 passed, 1 failed, 11 skipped** — the 1 failure is `test_local_adapter.py::test_complete_maps_a_real_successful_response_end_to_end`, a pre-existing, already-documented local-HTTP-server flake (`feature_inventory.md` module 42's own "known flake family," now a 4th observed instance), confirmed unrelated by re-running the file in isolation (22 passed, 0 failed); total collected (915) is numerically unchanged from step 10, since this step added no new test functions to anything under the root suite's own `testpaths`; `mypy --strict` clean across 341 files (one new test module); `ruff check`/`format` clean.

---

## 6o. Step 12 — `build` migrated, the first agent needing gateway AND tool injection together, a real ToolInvoker tension found (2026-07-29)

**The migration itself, mechanically.** `agents/build.py`'s `BuildAgentEntrypoint` now implements `ai_os_sdk.contracts.Agent` and `PackContextReceiver` only. Its LLM call is a direct structural copy of `architecture.py`'s own render-then-complete replication (same `context.llm`/`context.prompts` usage, same `TraceContext` handling). Its write is now `self._context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, {...})` — the identical Tool injection path `verification.py` (step 9) already proved, now proven a second time for an agent that also makes an LLM call. Zero `ai_os_kernel` imports remain — confirmed by the real scanner. This is the first migration needing `context.llm`, `context.prompts`, and `context.tools` all three together in one agent.

**The lock is not fully obsolete here — a real, discovered nuance neither step 10 (`requirements-analyst`) nor step 11 (`architecture`) needed to consider, since neither ever held any state beyond the LLM composition the lock guarded.** `build.py`'s own pre-migration lock guarded two independent concerns at once: (1) lazily building a `PromptedAgent`/`PromptedCompletionService` (the concern steps 10/11 found fully obsolete — resolved by injection arriving once, synchronously, before any `execute()` is possible), and (2) lazily creating a private working directory when none is supplied — a concern that has nothing to do with `PackContext` injection at all. Two concurrent first `execute()` calls with no lock at all would each call `tempfile.mkdtemp()` independently, silently forking into two unrelated directories instead of sharing one — a real correctness bug, not a missed optimisation. **This module therefore keeps a narrower lock, guarding only the working-directory creation**, proven by `test_concurrent_execute_calls_share_one_working_directory` (five concurrent calls, one shared directory, all writes succeed to distinct filenames inside it).

**The real tension this step was explicitly scoped to find: using `ToolInvoker` instead of direct sandbox construction loses a real, backend-specific fact the old code depended on.** The pre-migration agent asked its own directly-injected `SandboxExecutor` for its `python_command` property (`sys.executable` for `LocalSubprocessSandbox`, `python3` for `DockerSandbox` — real, differing values, the reason this property exists in the Kernel's own sandbox module at all). A migrated agent holds only a generic `ai_os_sdk.contracts.ToolInvoker`, which has no `python_command` concept — the Protocol is deliberately backend-agnostic, by design (`platform_sdk.md` §5.6), so this fact does not, and should not, cross it. **Resolution:** `python_command` becomes a constructor-time default (`("python3",)`, matching `DockerSandbox`, the real, documented system-wide default backend per `ai_os_kernel.sandbox.default_executor`'s own `AIOS_SANDBOX_BACKEND` default of `"docker"`), overridable by a caller that knows better — the identical "optional constructor override, always the default in real `EntrypointLoader` dispatch" shape every agent in this pack already uses. **The real, narrow regression risk, recorded rather than silently accepted:** the old code's dynamic lookup was *always* correct, automatically, regardless of which backend was actually active; the new default is only correct for whichever backend is actually active *when this default was chosen* (`"docker"`, today). If a real deployment ever sets `AIOS_SANDBOX_BACKEND=local` without also updating this agent's own default, the write step fails with a real, clear `platform.sandbox.run_command` execution error (interpreter not found) rather than silently writing nothing — loud, not silent, but a real coupling that did not exist before. Tracked as a second, distinct entry in `feature_inventory.md`'s §6a Known Regressions, alongside the `evaluation.llm_calls` gap.

**The identical, already-recorded `evaluation.llm_calls` capability loss applies here too — a third occurrence, not a new finding.** See `requirements_analyst.py`'s own docstring for the full reasoning.

**No blocker in the injection mechanism's own design.** The one real tension this step found (python_command) is a genuine, documented limitation of the `ToolInvoker` Protocol's own deliberately backend-agnostic scope, not a defect in `bind_pack_context()`/`build_pack_context()`/`SqlAgentRegistry`'s own step 6b/9a machinery, which needed zero changes.

**Verified:** the pack's own local test suite (`capability_packs/software-engineering/tests/test_build_agent.py`, 15 tests, up from 11 — including 2 new: execute-before-bind and the concurrent-shared-working-directory proof); SE pack suite 54 → **58 passed** (net +4); `test_build_agent_pack.py` (deterministic passes with an Echo-backed `llm_gateway`/`prompt_engine`, live correctly skipped: 1 passed, 1 skipped); both real Delivery Pipeline end-to-end tests pass after the same construction fix step 11 already established (deterministic: 1 passed, 1 skipped; **live Docker: 1 passed** — a real file genuinely written inside a real container through the new `ToolInvoker` sandbox path, this pack's most important proof of this migration since Build is the pipeline's real file-writer); zero regression across every `SqlAgentRegistry`/`SqlToolRegistry`-backed test re-run together (29 passed, 5 correctly skipped); the real scanner confirms `agents.build` contributes zero violations and the remaining 2 modules/13 violations match the reduced `pack_contract_waiver.yaml` exactly; root suite (`testpaths = ["tests"]`) **904 passed, 11 skipped, 0 failed** — numerically identical to step 10/11's own count (this step added no new test functions under the root suite's own `testpaths`), and clean this run (step 11's own transient local-HTTP-server flake, `test_local_adapter.py::test_complete_maps_a_real_successful_response_end_to_end`, passed here); `mypy --strict` clean across 341 files (unchanged file count — no new source modules); `ruff check`/`format` clean.

---

## 6p. Step 12a (inserted) — close the ToolInvoker `python_command` regression, proven against both real backends (2026-07-29)

**The investigation: two real options, one chosen with the smaller surface area.** Step 12's own report named two candidate fixes: (a) expose the real backend's `python_command` through `available_tools()`/`ToolDescriptor`, or (b) have `platform.sandbox.run_command`'s own implementation resolve the interpreter internally so callers never guess it at all. Option (a) was rejected: `PLATFORM_SANDBOX_RUN_COMMAND_DESCRIPTOR` is a frozen, module-level SDK constant shared by every adapter instance, not something `available_tools()` currently builds per-call — making it instance-aware would mean adding a new field to `ToolDescriptor` (a real, already-shipped SDK model) *and* requiring every caller to synchronously query `available_tools()`, find the matching descriptor, and rebuild its own command with the extracted fact — real, repeated caller-side code for something that used to be automatic. Option (b), reinterpreted narrowly as "resolve one well-known placeholder token, not arbitrary templating," has none of these costs: no SDK model change, no caller-side lookup, and the "write it once, correct against every backend" property is restored exactly.

**The fix.** `ai_os_sdk.contracts.tool_invoker.PLATFORM_PYTHON_INTERPRETER` (`"{platform.python}"`) is a new, well-known token a caller may write as any element of `inputs["command"]`. `ToolInvokerAdapter.invoke()` — the one place that still holds the real `SandboxExecutor` — expands every occurrence into that instance's own real `sandbox.python_command` before dispatching to the real sandbox (`_resolve_python_interpreter`, `kernel/src/ai_os_kernel/sdk_adapters/tool_invoker_adapter.py`). Expansion, not 1:1 substitution, since `python_command` may be more than one token. Deliberately scoped to this one, named, documented need — not a general `{...}` templating mechanism, which would reopen the "agent reaching for a side effect by any means other than the declared Protocol" risk `ToolInvoker`'s own docstring already warns against.

**`build.py` simplified, not just fixed.** The `python_command` constructor parameter and `_DEFAULT_PYTHON_COMMAND` constant from step 12's own first attempt are removed entirely. `execute()` now writes `[PLATFORM_PYTHON_INTERPRETER, "-c", _WRITE_FILE_SCRIPT, str(relative_path)]` — zero backend knowledge, zero default to keep in sync with `AIOS_SANDBOX_BACKEND`, restoring the pre-migration behaviour's own automatic-correctness guarantee exactly, not merely documenting the gap better.

**Proven against both real backends this codebase ships — the explicit requirement this step was scoped to satisfy, not just the currently-active default.** `tests/unit/kernel/sdk_adapters/test_tool_invoker_adapter.py::TestPlatformPythonInterpreterSubstitution` (3 new tests): the placeholder resolves to the real, actually-executed `LocalSubprocessSandbox`'s own `sys.executable`, a multi-argument command survives the substitution with the trailing argument intact, and a command with no placeholder at all passes through unchanged. `tests/integration/sandbox/test_tool_invoker_adapter_docker.py` (new file, 2 tests, real live Docker daemon, ADR-0015-style clean skip if unreachable): the placeholder resolves to the real container's own `python3`, genuinely different from any host-side `sys.executable`, and a real file is genuinely written to disk through the substituted interpreter end to end.

**No blocker.** The fix is additive at the SDK contract level (`PLATFORM_PYTHON_INTERPRETER` is a new export, nothing existing changed shape) and confined to `ToolInvokerAdapter`'s own `invoke()` body plus `build.py`'s own command construction — no other migrated agent (`qa-test`, `requirements-analyst`, `architecture`) references `python_command` or this token at all, so none needed touching.

**Verified:** `tests/unit/kernel/sdk_adapters/test_tool_invoker_adapter.py` — **13 passed** (10 existing unchanged + 3 new); `tests/integration/sandbox/test_tool_invoker_adapter_docker.py` — **2 passed** (new file, real Docker); pack-local SE suite — **58 passed** (unchanged); zero regression across every `SqlAgentRegistry`/`SqlToolRegistry`-backed test plus both real Delivery Pipeline end-to-end tests re-run together — **44 passed, 5 correctly skipped**; root suite (`testpaths = ["tests"]`) **909 passed, 11 skipped, 0 failed** (904 → 909, the 5 new tests, all under `tests/`, unchanged skips); `mypy --strict` clean across 342 files (one new test module); `ruff check`/`format` clean; repo-wide link-checker: 0 broken links.

---

## 6q. Step 13 — `documentation` migrated, the fifth and final agent; a real correction to this step's own "empty waiver" framing (2026-07-29)

**The migration itself, mechanically.** `agents/documentation.py`'s `DocumentationAgentEntrypoint` now implements `ai_os_sdk.contracts.Agent` and `PackContextReceiver` only. Its LLM call is a direct structural copy of `build.py`'s/`architecture.py`'s own render-then-complete replication; its write is `self._context.tools.invoke(PLATFORM_SANDBOX_RUN_COMMAND, {..., "command": [PLATFORM_PYTHON_INTERPRETER, "-c", _WRITE_FILE_SCRIPT, str(doc_relative_path)], ...})` — the step 12a fix used from the start, exactly as that step's own report predicted, since this agent's pre-migration code used the identical `self.sandbox.python_command` pattern `build.py` used to. Zero `ai_os_kernel` imports remain — confirmed by the real scanner. `_extract_payload` is duck-typed against `context` (`getattr(context, "items", None)`) rather than `isinstance(context, AssembledContext)`, the same resolution `requirements_analyst.py`/`verification.py` already established.

**No new tension using `PLATFORM_PYTHON_INTERPRETER` — confirming step 12a's fix generalizes, not a fresh finding.** This is the second agent to use the token (after `build`), and it needed zero adaptation: write the token, get the real interpreter, done.

**A further, confirming data point on the lock-obsolescence finding — the clearest one yet.** This agent needs no lock at all, unlike `build` (which kept a narrower lock for lazy working-directory creation). `documentation.py` lazily builds and creates nothing: it reuses the caller-supplied `workingDirectory` directly and, post-migration, has no `PromptedAgent`/service to construct either. Across the four LLM-calling/tool-using agents this pack has, the lock finding now spans the full range: fully obsolete and removed (`requirements-analyst`, `architecture`), narrower and kept (`build`), and never needed at all (`documentation`) — a complete, evidenced picture, not a single data point generalized prematurely.

**A real, discovered correction to this step's own initial approved framing, surfaced and corrected rather than silently forced through.** The step's own task description stated migrating `documentation` "should leave the waiver empty, since all 5 agents will now be migrated." Checking this against the real waiver file and the real scanner found this to be imprecise: `pack.py` (this pack's own `CapabilityPack` entry point) was *never* one of the 5 agents — it has always been a separate, sixth entry in the waiver, and migrating it is step 14's own explicitly scoped job (per this very document's own step table, unchanged since the plan was first revised). Forcing the waiver to literally zero entries this step would have meant either falsely removing `pack.py`'s own still-real violation (which would make check 7 fail outright, not pass) or silently under-scoping step 13 to also migrate `pack.py` (out of this step's own approved boundary). **The correct, honest outcome, verified against the real scanner:** the waiver now covers exactly one module, `pack.py`, with exactly 1 real violation — down from 2 modules/13 violations before this step. Check 7 passes with a real, correctly-shrunk waiver, not a falsely-empty one — the accurate signal that step 14 (and only step 14) can now retire it entirely.

**The identical, already-recorded `evaluation.llm_calls` capability loss applies here too — the fourth and final occurrence, reaching the gap's full, permanent extent.** See `requirements_analyst.py`'s own docstring for the full reasoning; `feature_inventory.md`'s §6a now records this as expected-to-stay-open (not a step-13 regression) until a real SDK Telemetry surface exists.

**No blocker.** Every finding this step made was either a confirmation of an already-proven pattern (the token, the lock spectrum) or a scope correction caught before it could ship as a false claim — nothing here is a defect in `bind_pack_context()`/`build_pack_context()`/`ToolInvokerAdapter`'s own machinery.

**Verified:** the pack's own local test suite (`capability_packs/software-engineering/tests/test_documentation_agent.py`, 15 tests, up from 12 — including 3 new: satisfies-both-protocols, execute-before-bind, and missing-invocation-fields); SE pack suite 58 → **61 passed** (net +3); `test_documentation_agent_pack.py` (deterministic passes with an Echo-backed `llm_gateway`/`prompt_engine`, live correctly skipped: 1 passed, 1 skipped); both real Delivery Pipeline end-to-end tests pass with the full four-agent pipeline now migrated (deterministic: 1 passed, 1 skipped; **live Docker: 1 passed** — a real documentation artifact genuinely written inside a real container through the new `ToolInvoker` path); zero regression across every `SqlAgentRegistry`/`SqlToolRegistry`-backed test re-run together (29 passed, 5 correctly skipped); the real scanner confirms `agents.documentation` contributes zero violations and the waiver's one remaining module (`pack.py`) matches exactly; `scripts/check_import_boundaries.py` exits 0 with exactly 1 module/1 violation waived; root suite (`testpaths = ["tests"]`) **909 passed, 11 skipped, 0 failed** — numerically unchanged from step 12a, since this step added no new test functions under the root suite's own `testpaths` (all new tests are pack-local); `mypy --strict` clean across 342 files (unchanged — no new source modules); `ruff check`/`format` clean; repo-wide link-checker: 0 broken links.

---

## 7. Explicit non-goals for v1.0.0

Deferred, deliberately, so scope doesn't creep mid-build:

- **The 11 Protocol interfaces with no consumed usage today** (`RetrievalService`, `MemoryService`, `EventBus`, `ConfigService`, `StorageService`, `WorkspaceService`, `Telemetry`, `TraceabilityService`, `QualityGateRegistry`, `SpeechGateway`, and — added in v2.0 — **`SecretResolver`**). For the first ten, each sits on a 0%-built or docstring-only Kernel subsystem, so building the pack-facing contract first would ship an interface nothing implements. For `SecretResolver` and `ConfigService` specifically, the need disappears once step 6b injects a constructed gateway; and `SecretResolver` would additionally grant a capability the pack's manifest never declared (§2.3).
- **SDK semantic-versioning enforcement in the Manifest Loader** (§8 of `platform_sdk.md`) — requires a real SDK version to exist first; a small follow-on once v1.0.0 itself is versioned and shipped.
- **The full 9-check `pack_contract_suite`** in the SDK-build phase — only check 7 ships early (§4, §4.1); checks 2/3/4/9 need a migrated pack to test against meaningfully.
- **`platform_sdk/prompts/`.** Purpose undocumented anywhere in the source spec; left untouched pending a product-owner decision, not invented here.
- **Consolidating the Kernel's two partial `TraceContext` classes** onto the SDK's canonical one (§6a.1). A real Kernel change with its own regression surface; scheduled by step 2a/6a, not smuggled into a model-definition step.
- **Any 6th SE-pack agent, or any new Capability Pack.** Still blocked by the existing hard gate until step 15 completes — building the SDK does not itself lift the gate (see §6's gate semantics note).

**Two non-goals from v1.0 are now withdrawn**, because the architecture review showed they were load-bearing rather than deferrable:

- ~~"`pipeline.py`'s 11 composition-root imports"~~ — withdrawn. It is now **step 7**. v1.0 deferred this to a future Capability Manager; the review found a far cheaper resolution (relocate it into `tests/`, since nothing in the pack's own source imports it) which is also required for check 7 to ever pass. See §4.1.
- ~~"Migrating `kernel/bootstrap.py` or any other Kernel-internal composition"~~ — withdrawn. v1.0 justified this as *"the Kernel implements these Protocols; it does not consume them,"* which is true but beside the point: **something must construct the adapters and the `PackContext` and inject them, and `bootstrap.py` is the composition root** (ADR-0010, no DI container). That work is now **steps 6a and 6b**. It remains true that no Kernel code is migrated to *depend on* `ai_os_sdk` types for its own internal use.

---

## 8. Final Authority

Order of precedence:

1. Project Constitution
2. AI Governance Framework
3. `platform_sdk.md` (Platform SDK Specification)
4. `capability_pack_contract.md`
5. This document
6. Source Code

---

## 9. Related Documents

- [`platform_sdk.md`](platform_sdk.md) — the specification this document sequences the build of
- [`../capability_framework/capability_pack_contract.md`](../capability_framework/capability_pack_contract.md) — the growth gate this build closes, and the dated exception steps 9–14 retire
- [`../../process/standing_rules.md`](../../process/standing_rules.md) — the Platform SDK growth gate and scope-discipline rules this plan's step sequence is designed to satisfy
- [`../../19_roadmap/implementation_status.md`](../../19_roadmap/implementation_status.md) §6 — the handoff this document answers
- [`../../19_roadmap/feature_inventory.md`](../../19_roadmap/feature_inventory.md) — module 27 (Platform SDK), module 29 (SE Pack — Agents), module 8 (Capability Manager, the real owner of `pipeline.py`'s gap)
- [`../agents/agent_architecture.md`](../agents/agent_architecture.md) — the Invocation Lifecycle that already assembles context before an agent runs (§2.2's `ContextService` note)
- [`../../../capability_packs/software-engineering/`](../../../capability_packs/software-engineering/) — the one real pack this plan migrates
