"""Voice (Jarvis) — the Platform Integration Layer's first real
increment (`voice_architecture.md` §4.5, `P06-S06-M33-T01`).

See :mod:`ai_os_kernel.voice_jarvis.intent_router`'s own module
docstring for why this lives inside the Kernel rather than as a
separate Capability Pack.
"""
